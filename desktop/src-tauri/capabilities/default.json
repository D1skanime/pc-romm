{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only typed RomM desktop IPC and core event access for the main webview.",
  "windows": ["main"],
  "permissions": ["core:event:default", "core:window:default"]
}
