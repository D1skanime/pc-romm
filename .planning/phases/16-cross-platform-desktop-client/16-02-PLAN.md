---
phase: 16-cross-platform-desktop-client
plan: 02
type: tdd
wave: 2
depends_on: [16-01]
files_modified:
  - desktop/crates/romm-download-core/src/path.rs
  - desktop/crates/romm-download-core/src/job_store.rs
  - desktop/crates/romm-download-core/src/recovery.rs
  - desktop/crates/romm-download-core/src/lib.rs
  - desktop/crates/romm-download-core/tests/path_security.rs
  - desktop/crates/romm-download-core/tests/job_recovery.rs
autonomous: true
requirements: [CLNT-02, CLNT-04, CLNT-05]
must_haves:
  truths:
    - "The client writes only beneath the user-selected canonical destination root, regardless of hostile manifest text or existing symlinks."
    - "A part resumes only when its persistent identity is an exact match, while foreign finals are never silently overwritten."
  artifacts:
    - path: desktop/crates/romm-download-core/src/path.rs
      provides: "cross-platform safe local target resolver and opaque destination-root registry"
    - path: desktop/crates/romm-download-core/src/job_store.rs
      provides: "atomic per-member persisted recovery state"
    - path: desktop/crates/romm-download-core/src/recovery.rs
      provides: "safe final/part classification"
  key_links:
    - from: desktop/crates/romm-download-core/src/recovery.rs
      to: desktop/crates/romm-download-core/src/job_store.rs
      via: "exact manifest/member/size/hash/snapshot identity comparison"
      pattern: "manifest_id.*member_id.*sha256.*snapshot"
---

<objective>
Build the cross-platform local filesystem trust boundary and exact restart state.

Purpose: no hostile manifest, coincidental part file, symlink, or existing final can escape the destination root or cause unsafe resumption.
Output: pure path, job-store, and local-candidate APIs with adversarial tests.
</objective>

<context>
@.planning/phases/16-cross-platform-desktop-client/16-CONTEXT.md
@.planning/phases/16-cross-platform-desktop-client/16-PATTERNS.md
@docs/superpowers/specs/2026-09-15-cross-platform-pc-downloader-design.md
@.planning/phases/16-cross-platform-desktop-client/16-01-PLAN.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Canonical local path, persisted identity, and recovery classification</name>
  <files>desktop/crates/romm-download-core/src/path.rs, desktop/crates/romm-download-core/src/job_store.rs, desktop/crates/romm-download-core/src/recovery.rs, desktop/crates/romm-download-core/src/lib.rs, desktop/crates/romm-download-core/tests/path_security.rs, desktop/crates/romm-download-core/tests/job_recovery.rs</files>
  <behavior>
    - Reject `../foo`, `..\\foo`, `/foo`, `\\foo`, `C:\\foo`, `C:foo`, `\\\\server\\share`, `//server/share`, embedded traversal, colon, controls, `CON/NUL/PRN/AUX/COM1/LPT1`, trailing dot/space, case and Unicode-normalization collisions before creating anything.
    - Resolve each accepted relative destination from a canonical selected root, create parents safely, and re-prove canonical containment immediately before opening each part/final; reject an existing symlink component or target that escapes the root.
    - `DestinationRootRegistry` owns opaque, non-path-serializable `DestinationRootHandle` values. Its registration API accepts a selected path only from the native embedding boundary, canonicalizes and retains it in the core, and all queue/recovery APIs thereafter accept only a registry-issued handle.
    - Before materializing a member, reject any collision with an existing target tree entry under exact, portable case-fold, NFC, or NFD comparison, including a pre-existing sibling that differs only by case or normalization.
    - Use only a sibling `.romm-part-<manifest_member_id>` for incomplete output. Persist and atomically update exact manifest ID, member ID, expected `u64` size, SHA-256, quoted snapshot, safe destination, state and byte count in a client-private job store.
    - Classify matching final as completed, differing final as `LOCAL_CONFLICT`, a smaller part only with exact sidecar identity as resumable, coincidental/mismatched part as untrusted local state, and oversized part as corrupt local state. Never auto-overwrite a differing final.
  </behavior>
  <action>Keep parsing platform-independent rather than relying only on the host `Path` parser, so Windows unsafe forms are rejected on Linux tests too. Define `DestinationRootHandle` as an opaque generated core ID with no path serialization, and `DestinationRootRegistry::register_native_selection(path)` as the sole path-bearing registration seam. It canonicalizes and owns the selected root; queue, resume, conflict, and finalization APIs receive only a live registry-issued handle and reject forged, expired, or unknown handles before any I/O. Add temporary-root tests for exact/case-folded/NFC/NFD collisions already present in the target tree as well as manifest-wide collisions from Plan 16-01. Persist no token, source path, or raw server response. Write RED tests first using temporary isolated roots, synthetic metadata, and platform simulation hooks; then implement pure resolver/store/recovery APIs. Commit the red tests and implementation separately as `test(16-02): add local path and recovery regressions` and `feat(16-02): add safe local recovery boundary`.</action>
  <verify><automated>cd desktop && cargo test -p romm-download-core --test path_security --test job_recovery && cargo fmt --check && cargo clippy -p romm-download-core --all-targets -- -D warnings</automated></verify>
  <done>All specified Windows, POSIX, Unicode, collision, and symlink attacks fail before an out-of-root file is opened; restart classification never trusts a merely matching part filename.</done>
</task>
</tasks>

<verification>Run the task's isolated adversarial filesystem suite, formatter, and clippy gate.</verification>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                   |
| ---------------------------------------- | ----------------------------------------------------------------------------- |
| manifest destination to local filesystem | hostile cross-platform name controls a potential write location               |
| existing destination tree to downloader  | symlinks, colliding names, and stale parts are not downloader-owned authority |

## STRIDE Threat Register

| Threat ID       | Category               | Component                     | Disposition | Mitigation Plan                                                                                                  |
| --------------- | ---------------------- | ----------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-16-04         | Elevation of privilege | PathResolver                  | mitigate    | reject portable unsafe forms, canonicalize root/target, and prove containment before each write                  |
| T-16-05         | Tampering              | JobStore/resume               | mitigate    | resume only on exact five-field identity plus destination, atomically persist job records, distrust orphan parts |
| T-16-06         | Tampering              | symlink/collision handling    | mitigate    | adversarial symlink, Unicode normalization, case and reserved-name tests; no automatic overwrite                 |
| T-16-07         | Information disclosure | job records                   | mitigate    | persist only opaque manifest/member values and bounded local state, never token or source path                   |
| T-16-07a        | Elevation of privilege | destination-root registration | mitigate    | core-owned opaque handle registry is the only later path authority; reject unknown/expired handles before I/O    |
| </threat_model> |

<success_criteria>
All specified Windows, POSIX, Unicode, collision, and symlink attacks fail before an out-of-root file is opened; restart classification never trusts a merely matching part filename.
</success_criteria>

<output>
Create `.planning/phases/16-cross-platform-desktop-client/16-02-SUMMARY.md` when done.
</output>
