---
phase: 16-cross-platform-desktop-client
plan: 03
type: tdd
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - desktop/crates/romm-download-core/src/http.rs
  - desktop/crates/romm-download-core/src/transfer.rs
  - desktop/crates/romm-download-core/src/scheduler.rs
  - desktop/crates/romm-download-core/src/lib.rs
  - desktop/crates/romm-download-core/tests/transfer_protocol.rs
  - desktop/crates/romm-download-core/tests/scheduler.rs
autonomous: true
requirements: [CLNT-01, CLNT-02]
must_haves:
  truths:
    - "A fresh original file is accepted only as an exact 200 stream, and a restart appends only a strict exact 206 response."
    - "The core retrieves and validates the immutable manifest itself from a configured origin and opaque manifest ID, never from arbitrary shell JSON."
    - "Changed sources, lifecycle/auth failures, malformed range responses, and oversized local state never combine source versions."
    - "The client uses conservative bounded per-file concurrency with u64 accounting."
  artifacts:
    - path: desktop/crates/romm-download-core/src/http.rs
      provides: "origin-bound authenticated manifest and member client"
    - path: desktop/crates/romm-download-core/src/transfer.rs
      provides: "Phase-15 protocol client"
    - path: desktop/crates/romm-download-core/src/scheduler.rs
      provides: "2-4 bounded sequential per-file scheduler"
  key_links:
    - from: desktop/crates/romm-download-core/src/transfer.rs
      to: backend/endpoints/download_manifests.py
      via: "exact Range and If-Match request/response contract"
      pattern: "Range|If-Match|Content-Range"
---

<objective>
Implement the direct authenticated Phase-15 transfer protocol and bounded per-file scheduler.

Purpose: allow safe resume without ZIPs, byte mixing, multi-range striping, browser cookies, or unbounded native resource use.
Output: mock-transport tested transfer engine and scheduler.
</objective>

<context>
@.planning/phases/16-cross-platform-desktop-client/16-CONTEXT.md
@.planning/phases/16-cross-platform-desktop-client/16-PATTERNS.md
@.planning/phases/15-direct-resumable-transfer/15-VERIFICATION.md
@.planning/phases/15-direct-resumable-transfer/15-03-SUMMARY.md
@.planning/phases/16-cross-platform-desktop-client/16-02-PLAN.md
@backend/endpoints/download_manifests.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Strict direct transfer and bounded scheduling</name>
  <files>desktop/crates/romm-download-core/src/http.rs, desktop/crates/romm-download-core/src/transfer.rs, desktop/crates/romm-download-core/src/scheduler.rs, desktop/crates/romm-download-core/src/lib.rs, desktop/crates/romm-download-core/tests/transfer_protocol.rs, desktop/crates/romm-download-core/tests/scheduler.rs</files>
  <behavior>
    - Accept only a configured HTTP(S) origin and opaque manifest UUID as a core request. Fetch `GET /api/download-manifests/{manifest_id}` itself with `Authorization: Bearer <device-token>`, validate the returned JSON through Plan 16-01 before job/path work, and never accept arbitrary raw manifest JSON from the shell/webview.
    - Bind every manifest/member URL to the configured origin. Reject a different scheme/host/port, absolute URL, redirect that leaves the configured origin, malformed or invalid manifest body, and any origin-replacement attempt before credentials or bytes are accepted.
    - Map manifest/member 401 or 403 to `AUTH_REQUIRED`, 410 expired/revoked to `MANIFEST_STALE`, 409 `manifest_source_changed` to `SOURCE_CHANGED`, and malformed JSON/status to bounded failure states without a job or write.
    - Join a server-supplied relative member route only to that configured HTTP(S) origin; reject redirect credential forwarding to another origin. Send `Authorization: Bearer` device token for every manifest/member request, never cookie or CSRF state.
    - Fresh transfer sends no Range and accepts only `200` with exact Content-Length. Resume sends exactly `Range: bytes=<u64 offset>-` and the byte-for-byte persisted quoted strong `If-Match`; reject weak ETags and never append an unexpected 200.
    - Parse a resume `206` strictly: Content-Range start equals requested offset, total equals expected size, declared/streamed length cannot overflow or exceed expected. Map 412 snapshot/source codes to `SOURCE_CHANGED`, 409/410 lifecycle to `MANIFEST_STALE`, 401/403 to `AUTH_REQUIRED`, network interruption to `NETWORK_ERROR`; retain safe local state.
    - Treat 416 with part length exactly expected as a verification candidate, and an oversized part as corrupt local state. Use `u64` for all bytes and test 5, 80, and 120 GiB metadata without allocating those files.
    - Schedule only sequential whole-file streams, default configurable limit 2 through 4, release every permit on success, pause, cancellation, disk/I/O error, checksum failure, and network error.
  </behavior>
  <action>Introduce testable HTTP and file-sink traits so mock responses prove manifest retrieval and all protocol edges without a live server. `DownloadEngine::queue_manifest(origin, manifest_id, destination_root_handle)` must obtain its target only through the Plan-16-02 `DestinationRootRegistry`; it rejects an unknown/expired/forged opaque handle and exposes no arbitrary path parameter. The shell gets no raw-manifest submission API. Test 401/403, 410 lifecycle, 409 source changed, invalid JSON, invalid manifest members, cross-origin/redirect attempts, invalid root handles, and assert no job/file is made. Build a real native HTTP adapter only after protocol tests fail. Do not implement multiple byte ranges per file, zip/archive behavior, server changes, live-NAS calls, or a token in logs/URLs/jobs. Commit `test(16-03): add strict Phase-15 client protocol tests` before `feat(16-03): add bounded direct transfer engine`.</action>
  <verify><automated>cd desktop && cargo test -p romm-download-core --test transfer_protocol --test scheduler && cargo fmt --check && cargo clippy -p romm-download-core --all-targets -- -D warnings</automated></verify>
  <done>Mocked Phase-15 outcomes prove no invalid continuation can append data, every offset is u64 correct, and no more than the configured number of whole-file streams execute concurrently.</done>
</task>
</tasks>

<verification>Run the strict mock transport/scheduler suite, formatter, and clippy gate.</verification>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                  |
| -------------------------------- | ---------------------------------------------------------------------------- |
| configured origin to HTTP client | authenticated server bytes and headers are still untrusted until validated   |
| device token to HTTP request     | native credential must not become a redirect, log, or local-state disclosure |

## STRIDE Threat Register

| Threat ID       | Category               | Component       | Disposition | Mitigation Plan                                                                                |
| --------------- | ---------------------- | --------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-16-08         | Tampering              | Range response  | mitigate    | exact 200/206/Content-Range/length validation before append, with u64 overflow rejection       |
| T-16-09         | Spoofing               | resume snapshot | mitigate    | exact strong quoted If-Match only; reject weak/list/wildcard/missing validator                 |
| T-16-10         | Information disclosure | redirects/token | mitigate    | credentialed requests remain configured-origin-only; redact token from all errors/events/state |
| T-16-11         | Denial of service      | scheduler       | mitigate    | bounded 2-4 permits, cancellation-safe release tests, one sequential stream per member         |
| </threat_model> |

<success_criteria>
Mocked Phase-15 outcomes prove no invalid continuation can append data, every offset is `u64` correct, and no more than the configured number of whole-file streams execute concurrently.
</success_criteria>

<output>
Create `.planning/phases/16-cross-platform-desktop-client/16-03-SUMMARY.md` when done.
</output>
