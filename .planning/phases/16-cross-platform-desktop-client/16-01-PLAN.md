---
phase: 16-cross-platform-desktop-client
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - desktop/Cargo.toml
  - desktop/DEPENDENCY-AUDIT.md
  - desktop/crates/romm-download-core/Cargo.toml
  - desktop/crates/romm-download-core/src/lib.rs
  - desktop/crates/romm-download-core/src/error.rs
  - desktop/crates/romm-download-core/src/model.rs
  - desktop/crates/romm-download-core/src/manifest.rs
  - desktop/crates/romm-download-core/tests/manifest_contract.rs
autonomous: true
requirements: [CLNT-01, CLNT-04]
must_haves:
  truths:
    - "The desktop core accepts only a strict, path-free Phase-14 manifest with opaque member identities and strong snapshots."
    - "No manifest supplied by a server can select an arbitrary origin, source path, or unsafe destination."
  artifacts:
    - path: desktop/crates/romm-download-core/src/model.rs
      provides: "u64 manifest/job/state domain contracts"
    - path: desktop/crates/romm-download-core/src/manifest.rs
      provides: "strict Phase-14 JSON validation"
  key_links:
    - from: desktop/crates/romm-download-core/src/manifest.rs
      to: desktop/crates/romm-download-core/src/model.rs
      via: "validated deserialize boundary"
      pattern: "ValidatedManifest|ValidatedMember"
---

<objective>
Create the isolated Rust workspace and the strict manifest contract used by every later desktop operation.

Purpose: make the wire boundary path-free and deterministic before local files or HTTP streams exist.
Output: a testable `romm-download-core` crate with validated immutable manifest data.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/16-cross-platform-desktop-client/16-CONTEXT.md
@.planning/phases/16-cross-platform-desktop-client/16-PATTERNS.md
@docs/superpowers/specs/2026-09-15-cross-platform-pc-downloader-design.md
@backend/endpoints/responses/download_manifest.py

<interfaces>
The Phase-15 manifest response is schema version `1`; each member has UUID `file_id`, POSIX `destination`, non-negative `size`, lower-case 64-hex `sha256`, quoted `snapshot`, and relative `/api/download-manifests/{uuid}/files/{uuid}` `download`. The core preserves `snapshot` separately from SHA-256 and represents every byte quantity as `u64`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Workspace and strict immutable-manifest boundary</name>
  <files>desktop/Cargo.toml, desktop/DEPENDENCY-AUDIT.md, desktop/crates/romm-download-core/Cargo.toml, desktop/crates/romm-download-core/src/lib.rs, desktop/crates/romm-download-core/src/error.rs, desktop/crates/romm-download-core/src/model.rs, desktop/crates/romm-download-core/src/manifest.rs, desktop/crates/romm-download-core/tests/manifest_contract.rs</files>
  <behavior>
    - Before adding a non-std crate, record its exact version, official crates.io owner/source, license, advisory result, and direct purpose in `desktop/DEPENDENCY-AUDIT.md`; add no unaudited or [SUS]/[SLOP] dependency.
    - A valid version-1 manifest produces only canonical `ValidatedManifest` and `ValidatedMember` records containing manifest ID, opaque member ID, destination, `u64` size, lowercase SHA-256, exact quoted strong snapshot, and relative member URL.
    - Reject unknown schema versions, non-UUID IDs, duplicate member IDs/destinations, negative/overflow sizes, invalid hashes, empty/weak/unquoted/list snapshots, unknown fields, absolute URL/authority/query/fragment downloads, and member URLs not exactly rooted at the manifest's opaque Phase-15 route.
    - Before a job, directory, part, or final is created, reject any manifest-wide destination collision after exact comparison, case folding, NFC normalization, or NFD normalization. This remains portable even when the current host filesystem is case-sensitive.
    - Keep source/NAS path fields absent from Rust public types, serialized job data, errors, and diagnostics.
  </behavior>
  <action>Use a Cargo workspace rooted at `desktop/`, with `romm-download-core` as an ordinary Rust library, not Tauri code. Define a closed `DownloadState` enum containing every locked normal and recovery state, typed opaque IDs, `u64` accounting types, bounded public errors, and the strict serde validator. Make `ManifestValidator::validate` perform full manifest collision analysis before returning a validated collection, testing exact, case-folded, NFC, and NFD duplicate destination pairs. First add failing contract tests, then the smallest implementation that passes; commit `test(16-01): add failing manifest contract tests` and `feat(16-01): add validated download core contract` separately. Do not create any backend, source-root, v2, NAS, or Team4s integration.</action>
  <verify><automated>cd desktop && cargo test -p romm-download-core --test manifest_contract && cargo fmt --check && cargo clippy -p romm-download-core --all-targets -- -D warnings</automated></verify>
  <done>The core crate builds independently and malformed or path-bearing manifest data cannot become a validated member or persistent job input.</done>
</task>
</tasks>

<verification>Run the task's focused core contract suite, formatter, and clippy gate.</verification>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                |
| -------------------------------- | -------------------------------------------------------------------------- |
| server manifest to core          | authenticated but untrusted JSON controls names and future request targets |
| dependency registry to workspace | third-party native code enters the desktop binary                          |

## STRIDE Threat Register

| Threat ID       | Category               | Component          | Disposition | Mitigation Plan                                                                                                           |
| --------------- | ---------------------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-16-01         | Tampering              | manifest parser    | mitigate    | closed schema/version, UUID/hash/ETag/relative-route validation and duplicate rejection before persistence                |
| T-16-02         | Information disclosure | core models/errors | mitigate    | omit source paths by type design; expose bounded IDs and states only                                                      |
| T-16-03         | Elevation of privilege | manifest URL       | mitigate    | accept only a relative exact Phase-15 opaque member route, never origin replacement                                       |
| T-16-SC         | Tampering              | Cargo dependencies | mitigate    | audit each exact crate before Cargo install in `desktop/DEPENDENCY-AUDIT.md`; reject unaudited, [SUS], and [SLOP] entries |
| </threat_model> |

<success_criteria>
The core crate builds independently, and malformed or path-bearing manifest data cannot become a validated member or persistent job input.
</success_criteria>

<output>
Create `.planning/phases/16-cross-platform-desktop-client/16-01-SUMMARY.md` when done.
</output>
