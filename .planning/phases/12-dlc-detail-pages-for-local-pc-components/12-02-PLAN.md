---
phase: 12-dlc-detail-pages-for-local-pc-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/plugins/router.ts
  - frontend/src/v2/router/routes.ts
  - frontend/src/v2/router/routeInventory.ts
  - frontend/src/v2/router/routeInventory.test.ts
  - frontend/src/v2/views/PcDlcDetails.vue
  - frontend/src/v2/views/PcDlcDetails.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A direct DLC URL resolves only a component belonging to the fetched visible parent ROM."
    - "Malformed, absent, stale, or non-DLC IDs expose no component data and provide a bounded return."
  artifacts:
    - path: frontend/src/v2/views/PcDlcDetails.vue
      provides: "Strict route resolver and bounded state controller"
  key_links:
    - from: PcDlcDetails.vue
      to: romApi.getRom
      via: "authorized parent-ROM read before component resolution"
      pattern: "getRom"
---

<objective>
Register the canonical `rom/:rom/dlc/:component` route and safely resolve its child from the authorized parent response.
Purpose: Establish the D-01 parent-owned page boundary without a new endpoint or source operation.
Output: Route contract, strict resolver, and route regression tests.
</objective>
<execution_context>@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-CONTEXT.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-RESEARCH.md
<interfaces>`ROUTES.PC_DLC` is the exact name for `rom/:rom/dlc/:component`. The resolver accepts a scalar route parameter only, then returns a `PcComponentSchema` only when it is contained in the fetched `DetailedRomSchema.components` and `kind === "dlc"`.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Specify strict route parsing and containment rejection</name>
<files>frontend/src/v2/views/PcDlcDetails.test.ts, frontend/src/v2/router/routeInventory.test.ts</files>
<behavior>
 - Valid decimal-integer strings resolve only matching DLC members of the fetched parent.
 - Arrays, empty values, signs, whitespace, decimal values, suffixed values such as `1x`, unsafe integers, stale IDs, and non-DLC IDs render unavailable without child data.
 - Parent fetch errors render a localized library escape.
</behavior>
<action>Write failing tests for direct load and route updates. Treat `route.params.component` as valid only when it is a string matching the full ASCII decimal integer grammar `^(?:0|[1-9]\\d*)$`, then convert with `Number`, require `Number.isSafeInteger`, and finally perform the parent-contained id plus `kind === "dlc"` lookup. Explicitly test array rejection before conversion. Do not use parseInt, coercion, or a global component lookup.</action>
<verify><automated>cd frontend &amp;&amp; npm run test -- src/v2/views/PcDlcDetails.test.ts src/v2/router/routeInventory.test.ts</automated></verify>
<done>Tests reject every malformed route form before numeric conversion and prove parent ownership plus DLC type are mandatory.</done>
</task>
<task type="auto">
<name>Task 2: Register and implement the parent-owned DLC resolver</name>
<files>frontend/src/plugins/router.ts, frontend/src/v2/router/routes.ts, frontend/src/v2/router/routeInventory.ts, frontend/src/v2/router/routeInventory.test.ts, frontend/src/v2/views/PcDlcDetails.vue, frontend/src/v2/views/PcDlcDetails.test.ts</files>
<action>Implement the resolved architecture from RESEARCH.md: add `ROUTES.PC_DLC = "pc-dlc"`, lazy registry and inventory entries, and the nested route before player routes. In PcDlcDetails, follow GameDetails parent-fetch/update behavior, but apply the full-string scalar parser from Task 1 on every update and resolve only the fetched/current parent member with matching id and `kind === "dlc"`. Render localized loading, unavailable, and parent-load-error states with parent or library named-route escapes. Keep the valid state a typed, temporary route-level handoff that Plan 12-03 replaces with the composite. Do not create an endpoint, generated type, mutation, provider request, source-path URL, or synthetic ROM.</action>
<verify><automated>cd frontend &amp;&amp; npm run test -- src/v2/views/PcDlcDetails.test.ts src/v2/router/routeInventory.test.ts &amp;&amp; npm run typecheck</automated></verify>
<done>The registered route cannot render any malformed, sibling, or non-DLC component, and valid state has only authorized parent-derived data.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| URL to parent-ROM response | Untrusted router parameters must not select arbitrary component data. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-01 | Tampering / Elevation | PcDlcDetails | mitigate | Scalar full-match parsing, safe-integer check, fetched-parent containment, and DLC-kind gate. |
| T-12-SC | Tampering | npm installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused route tests and typecheck.</verification>
<success_criteria>The canonical route is fully registered and rejects arrays, decimals, suffixes, and every non-owned/non-DLC component before rendering data.</success_criteria>
<output>Create `.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-02-SUMMARY.md` when done.</output>
