---
phase: 12-dlc-detail-pages-for-local-pc-components
plan: 07
type: tdd
wave: 4
depends_on: [12-01, 12-03]
files_modified:
  - backend/models/rom.py
  - backend/alembic/versions/<revision>_component_owned_media_and_notes.py
  - backend/endpoints/responses/rom.py
  - backend/handler/database/roms_handler.py
  - backend/tests/handler/database/test_pc_component_resources.py
  - backend/tests/models/test_rom_component_resources.py
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-09, D-10, D-12]
must_haves:
  truths:
    - A DLC owns its imported/uploaded media and notes independently of its parent and siblings.
    - Component records retain source-manifest local media separately from RomM-owned uploads/imports.
    - A stale component version cannot replace metadata, media, or notes.
    - "D-03: no DLC save-data record is created."
    - "D-04: DLC-only overview/files data has isolated persistence."
    - "D-05: managed DLC media is RomM-owned."
    - "D-06: notes remain scoped to one DLC."
    - "D-09: DLC persistence cannot overwrite its parent."
    - "D-10: provider media requires explicit confirmation."
    - "D-12: technical component evidence remains component-scoped."
  artifacts:
    - path: backend/models/rom.py
      provides: Component-owned media and component-note ORM relationships.
    - path: backend/alembic/versions/<revision>_component_owned_media_and_notes.py
      provides: Portable owned-media and component-note persistence.
  key_links:
    - from: backend/handler/database/roms_handler.py
      to: backend/models/rom.py
      via: component-id, parent-id, and version guarded persistence.
---

<objective>
Create the durable, component-scoped ownership boundary used by the shared matcher, DLC media tab, and DLC notes tab.
Purpose: deliver D-03 through D-06 and D-09/D-10 without weakening existing source-evidence local media or parent note ownership.
Output: migration-backed ORM/schema/database contracts with RED-GREEN tests.

Decision traceability: D-03: parent-style DLC tabs require no DLC save data. D-04: DLC-only content needs isolated records. D-05: managed media is RomM-owned. D-06: notes are component-scoped. D-09: selected DLC metadata/media must not write to the parent. D-10: provider media is imported only after explicit confirmation. D-12: component technical evidence remains component-scoped.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-CONTEXT.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-RESEARCH.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-PATTERNS.md
<read_first>
backend/models/rom.py
backend/alembic/versions/0116_pc_component_local_media.py
backend/handler/database/roms_handler.py
backend/endpoints/responses/rom.py
</read_first>
</context>

<feature>
  <name>Component-owned resource persistence</name>
  <files>backend/models/rom.py, backend/alembic/versions/&lt;revision&gt;_component_owned_media_and_notes.py, backend/endpoints/responses/rom.py, backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_component_resources.py, backend/tests/models/test_rom_component_resources.py</files>
  <behavior>
    - RED: prove an owned-media row and a note have one DLC owner; parent and sibling queries never return them.
    - RED: prove existing RomComponentLocalMedia still requires source path and SHA-256 evidence and is not reused for uploads or provider imports.
    - RED: prove optimistic component version mismatch persists nothing and a valid mutation updates only the target component timestamp.
    - GREEN: add distinct owned-media role/category, MIME type, owned path, origin/provider identity and component-note records with parent/user/public/title constraints matching established RomNote behavior.
  </behavior>
  <implementation>Use separate RomComponentOwnedMedia and RomComponentNote relationships, portable timestamp/index/foreign-key migration conventions, response schemas with from_attributes, and database methods whose predicates require rom_id, component_id, DLC kind where caller requires it, and expected updated_at. Do not add a nullable component_id to RomNote, do not store source paths in owned media, and do not alter parent cover columns.</implementation>
</feature>

<tasks>
<task type="tdd">
<name>Task 1: Specify and implement component-owned ORM and migration contracts</name>
<files>backend/models/rom.py, backend/alembic/versions/&lt;revision&gt;_component_owned_media_and_notes.py, backend/tests/models/test_rom_component_resources.py</files>
<read_first>backend/models/rom.py; backend/alembic/versions/0116_pc_component_local_media.py; latest Alembic revision and dialect test conventions</read_first>
<action>Write failing model/migration tests first. Add distinct component-owned media and component-note tables/relationships per D-05/D-06, retaining immutable source-evidence RomComponentLocalMedia unchanged. Use explicit portable enum/check constraints, FK cascade, bounded owned path and MIME fields, origin/provider identity, timestamps, title uniqueness per component/user, and public/owner indexes. Generate the migration from the real current head, hand-review upgrade and symmetric downgrade for MariaDB and PostgreSQL. No source-root path, raw browser path, or parent ROM cover column may be stored.</action>
<acceptance_criteria>Migration creates and downgrades only component-owned tables/enums/indexes; records cannot cross parent or sibling component ownership; local-media source evidence remains mandatory.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/models/test_rom_component_resources.py -q && uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql</automated></verify>
<done>Portable persistence represents owned DLC media and DLC notes while preserving the existing local-media model.</done>
</task>
<task type="tdd">
<name>Task 2: Add version-guarded component resource database contracts and OpenAPI schemas</name>
<files>backend/endpoints/responses/rom.py, backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_component_resources.py</files>
<read_first>backend/handler/database/roms_handler.py apply_pc_component_metadata_candidate/apply_pc_local_media; backend/endpoints/responses/rom.py PcComponentLocalMediaSchema/UserNoteSchema</read_first>
<action>Write RED tests for create/list/update/delete/select operations that use parent id, component id, authenticated user and expected component version. Implement isolated database helpers and authoritative response/request schemas for owned media and component notes. A conflict must return no partial database mutation; replacement bookkeeping must expose only owned paths eligible for cleanup. Include owned media in PcComponentSchema only through explicit component relationships. Do not flatten parent media/notes into component responses.</action>
<acceptance_criteria>Database helpers reject stale, foreign, non-owned and sibling-targeted writes; response schemas expose only component records.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/handler/database/test_pc_component_resources.py tests/models/test_rom_component_resources.py -q && trunk fmt --no-fix backend/models/rom.py backend/endpoints/responses/rom.py backend/handler/database/roms_handler.py && trunk check --no-fix backend/models/rom.py backend/endpoints/responses/rom.py backend/handler/database/roms_handler.py</automated></verify>
<done>Later endpoints have typed, test-proven persistence operations without authority over the immutable library.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                          |
| ----------------------------------- | ------------------------------------------------------------------------------------ |
| endpoint to database                | Caller-controlled component id and version must not cross a parent/sibling boundary. |
| owned records to filesystem cleanup | Only newly-created or replaced owned paths may be removed.                           |

## STRIDE Threat Register

| Threat ID       | Category  | Component             | Disposition | Mitigation Plan                                                                          |
| --------------- | --------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-12-07-01      | E/T       | component persistence | mitigate    | Require parent/component/version predicates and user ownership in every mutation helper. |
| T-12-07-02      | I         | owned-media record    | mitigate    | Persist owned paths only, never source-root paths or provider credentials.               |
| T-12-07-03      | D         | migration             | mitigate    | Test upgrade/downgrade on supported dialects and use bounded/indexed fields.             |
| T-12-SC         | Tampering | package installs      | accept      | No package installation occurs.                                                          |
| </threat_model> |

<verification>
Run focused model/database tests, then the repository's isolated deterministic migration harness with `--dialects mariadb mysql postgresql`. That harness provisions fresh named containers, runs Alembic upgrade to head, its established downgrade cycle and re-upgrade to head on each dialect. Do not substitute a local default-database Alembic invocation for this three-dialect gate.
</verification>
<success_criteria>Owned component media and notes are isolated, durable, version-guarded, and cannot represent source-library writes.</success_criteria>
<output>Create `.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-07-SUMMARY.md` when done.</output>
