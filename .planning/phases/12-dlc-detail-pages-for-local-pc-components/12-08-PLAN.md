---
phase: 12-dlc-detail-pages-for-local-pc-components
plan: 08
type: tdd
wave: 5
depends_on: [12-07]
files_modified:
  - backend/endpoints/roms/pc_metadata.py
  - backend/handler/metadata/pc_match_handler.py
  - backend/handler/filesystem/resources_handler.py
  - backend/tests/endpoints/roms/test_pc_metadata.py
  - backend/tests/handler/metadata/test_pc_match_handler.py
autonomous: true
requirements: [D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - Parent games and every eligible recognized PC component, including DLCs, query the same review-first matching contract without sharing a write target.
    - Browsing, filtering, and cancelling candidates never changes metadata or media.
    - A confirmed DLC match stores only explicitly selected, verified provider media in owned storage.
    - "D-07: the polished shared matcher replaces bespoke PC matching."
    - "D-08: one shared flow serves every specified entry point."
    - "D-09: explicit DLC selection writes only to that DLC."
    - "D-10: provider images import only after explicit confirmation."
  artifacts:
    - path: backend/endpoints/roms/pc_metadata.py
      provides: Bounded parent/component candidate query and confirm routes.
    - path: backend/handler/metadata/pc_match_handler.py
      provides: Typed target-aware provider candidate collection.
  key_links:
    - from: backend/endpoints/roms/pc_metadata.py
      to: backend/handler/filesystem/resources_handler.py
      via: server-verified selected candidate-media references after confirmation.
---

<objective>
Make the PC metadata backend support the polished shared matcher safely for both parent and every eligible recognized PC component target.
Purpose: implement D-07 through D-10 with explicit query, candidate recomputation, optimistic confirmation, and owned provider-media import.
Output: tested target-aware metadata API with no implicit provider or source-library writes.

Decision traceability: D-07: one polished matcher replaces the bespoke PC view. D-08: parent, component list and DLC overflow use one flow. D-09: target-contained selection writes only to its target. D-10: provider media import requires explicit confirmation and owned storage.
</objective>
<execution_context>@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-CONTEXT.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-RESEARCH.md
<read_first>backend/endpoints/roms/pc_metadata.py; backend/handler/metadata/pc_match_handler.py; backend/handler/filesystem/resources_handler.py; backend/tests/endpoints/roms/test_pc_metadata.py</read_first>
</context>
<feature>
<name>Target-aware review-first PC matcher API</name>
<files>backend/endpoints/roms/pc_metadata.py, backend/handler/metadata/pc_match_handler.py, backend/handler/filesystem/resources_handler.py, backend/tests/endpoints/roms/test_pc_metadata.py, backend/tests/handler/metadata/test_pc_match_handler.py</files>
<behavior> - RED: explicit bounded search query yields provider results for a parent or a contained base, update, DLC, hotfix, language-pack or extra component and no request state changes. - RED: selection recomputes exactly the submitted target/query and rejects a candidate or media id not returned for that candidate. - RED: confirmed DLC media is written only under owned resources, after metadata confirmation, and failure removes only the newly-created owned file.
</behavior>
<implementation>Introduce a typed target adapter at the handler/endpoint boundary, preserve configured-provider availability/reasons, and retain existing parent routes as compatible adapter consumers. Component targets include base, update, DLC, hotfix, language_pack and extra. Unresolved is intentionally ineligible because its source layout has not established a trustworthy classified component identity, so manual query matching must not create an authoritative association; its candidate and selection API requests return a bounded 404. Add a bounded query/search-by contract. Extend selection with explicit selected media references. Validate HTTPS/allowlisted provider response, MIME, byte budget and media membership server-side before resource storage. Provider-media import is available only for a DLC confirmation per D-10; other eligible components confirm metadata through the same dialog without an owned-media import. Never fetch/import during GET, filter, preview, close or cancel.</implementation>
</feature>
<tasks>
<task type="tdd">
<name>Task 1: Add target-aware candidate query and server-side revalidation</name>
<files>backend/endpoints/roms/pc_metadata.py, backend/handler/metadata/pc_match_handler.py, backend/tests/endpoints/roms/test_pc_metadata.py, backend/tests/handler/metadata/test_pc_match_handler.py</files>
<read_first>Current parent and nested DLC metadata candidate/selection routes; PcMetadataCandidate fields; provider error conventions; RomComponentKind enum</read_first>
<action>Write failing query/selection tests before implementation. Make the parent route and one nested generic PC-component route accept the same bounded explicit query/search mode, preserve provider filters/results/reasons, and recompute the target's candidates on POST. Replace the DLC-only containment helper with a parent-contained `_pc_component` guard that permits only base, update, DLC, hotfix, language_pack and extra, asserts visibility before provider work, and returns 404 for unresolved on both candidate and selection routes. Test every accepted kind plus unresolved candidate/selection rejection. The unresolved exclusion is mandatory because an unclassified source layout has no trustworthy component identity and manual matching must not create an authoritative association. Reject unknown candidate, mismatched query, unavailable provider, foreign candidate-media reference and stale expected version with bounded 4xx responses. Preserve non-mutating behavior for all GET/query/filter paths.</action>
<acceptance_criteria>Parent and all classified component route contracts differ only in explicit target identity; unresolved receives 404/no selection, and no generic parent update path is reachable from a component request.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/endpoints/roms/test_pc_metadata.py tests/handler/metadata/test_pc_match_handler.py -q</automated></verify>
<done>All matcher candidates are reviewable, target-contained and server-revalidated at confirmation.</done>
</task>
<task type="tdd">
<name>Task 2: Import selected provider media only after confirmed component selection</name>
<files>backend/endpoints/roms/pc_metadata.py, backend/handler/filesystem/resources_handler.py, backend/tests/endpoints/roms/test_pc_metadata.py</files>
<read_first>resources_handler.py store_pc_component_image/remove_file; PC metadata conflict cleanup; storage policy capability APIs</read_first>
<action>Write RED tests that snapshot component, parent, sibling and source evidence across candidate browsing and cancellation, then verify only a valid explicit DLC confirmation imports selected provider media. Prove the same shared matcher confirmation for every other eligible kind rejects selected provider-media references instead of silently importing them. Implement owned-resource writer/import validation with bounded size, declared/validated media type, trusted URL scheme and candidate-media membership. Persist media origin/provider id through Plan 12-07 helpers, clean up solely newly-created owned paths on failed optimistic persistence, and never call source handlers, generic screenshot/soundtrack APIs, or parent artwork updates.</action>
<acceptance_criteria>Only selected candidate media becomes component-owned after an explicit successful confirmation; failure leaves source, parent and sibling records unchanged.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/endpoints/roms/test_pc_metadata.py -q && trunk fmt --no-fix backend/endpoints/roms/pc_metadata.py backend/handler/metadata/pc_match_handler.py backend/handler/filesystem/resources_handler.py && trunk check --no-fix backend/endpoints/roms/pc_metadata.py backend/handler/metadata/pc_match_handler.py backend/handler/filesystem/resources_handler.py</automated></verify>
<done>Confirmed provider data is safe, explicit and DLC-scoped.</done>
</task>
</tasks>
<threat_model>

## Trust Boundaries

| Boundary                           | Description                                             |
| ---------------------------------- | ------------------------------------------------------- |
| browser to matcher API             | Query, candidate and media ids are untrusted.           |
| provider response to owned storage | Remote bytes must not become executable/source content. |

## STRIDE Threat Register

| Threat ID                                                                                                                                                                            | Category  | Component             | Disposition | Mitigation Plan                                                                                          |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | --------------------- | ----------- | -------------------------------------------------------------------------------------------------------- |
| T-12-08-01                                                                                                                                                                           | T/E       | nested selection      | mitigate    | Resolve visibility/containment, recompute candidates and optimistic version before persistence.          |
| T-12-08-02                                                                                                                                                                           | T/D       | provider media import | mitigate    | Validate membership, scheme, MIME and byte cap, write only owned storage, clean up new files on failure. |
| T-12-08-03                                                                                                                                                                           | R         | metadata confirmation | mitigate    | Require explicit POST selection rather than implicit GET/filter side effects.                            |
| T-12-SC                                                                                                                                                                              | Tampering | package installs      | accept      | No package installation occurs.                                                                          |
| </threat_model>                                                                                                                                                                      |
| <verification>Run focused handler/endpoint tests and Trunk checks; defer OpenAPI regeneration to Plan 12-10 after all schema routes exist.</verification>                            |
| <success_criteria>The shared matcher can search and explicitly apply parent or any eligible PC-component metadata without cross-target writes or source mutation.</success_criteria> |
| <output>Create `.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-08-SUMMARY.md` when done.</output>                                                                   |
