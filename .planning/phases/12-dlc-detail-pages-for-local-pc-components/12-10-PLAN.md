---
phase: 12-dlc-detail-pages-for-local-pc-components
plan: 10
type: execute
wave: 7
depends_on: [12-08, 12-09]
files_modified:
  - frontend/src/__generated__/
  - frontend/src/services/api/rom.ts
  - frontend/src/v2/components/MatchRom/types.ts
  - frontend/src/v2/components/Dialogs/MatchRomDialog.vue
  - frontend/src/v2/components/Dialogs/GlobalDialogs.vue
  - frontend/src/v2/components/GameDetails/PcComponents.vue
  - frontend/src/v2/views/GameDetails.vue
  - frontend/src/v2/components/GameDetails/PcMetadataReview.vue
  - frontend/src/v2/components/Dialogs/MatchRomDialog.test.ts
  - frontend/src/v2/components/GameDetails/PcComponents.test.ts
autonomous: true
requirements: [D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - PC parent search and every classified PC component search, including DLC, open the same polished ROM matching dialog; unresolved components have no matcher launcher.
    - Provider filters, result cards/list, description preview and cover picker remain present.
    - A component confirmation calls only its nested component API and refreshes only its containing PC detail state.
    - "D-07: the existing polished matcher serves PC parent and components."
    - "D-08: PC Components and DLC overflow are shared-flow entry points."
    - "D-09: a DLC target is isolated from the parent target."
    - "D-10: confirmation gates selected provider-media import."
  artifacts:
    - path: frontend/src/v2/components/Dialogs/MatchRomDialog.vue
      provides: Typed target-neutral matcher shell.
    - path: frontend/src/services/api/rom.ts
      provides: Generated-contract-backed parent/component matcher adapters.
  key_links:
    - from: frontend/src/v2/components/GameDetails/PcComponents.vue
      to: frontend/src/v2/components/Dialogs/MatchRomDialog.vue
      via: explicit component target launch event.
    - from: frontend/src/v2/components/Dialogs/MatchRomDialog.vue
      to: frontend/src/services/api/rom.ts
      via: target-specific search and explicit confirm adapter.
---

<objective>
Reuse the polished ROM matching experience for PC parent and component metadata rather than retaining a second simplified UI.
Purpose: fulfil D-07 through D-10 and make PC Components' metadata action the same product-quality flow shown for normal ROM assignment.
Output: generated API types, target adapter, unified dialog launches and regression tests.

Decision traceability: D-07: use the established polished matcher for parent and classified components. D-08: PC Components and DLC detail overflow are shared-flow entry points. D-09: selected DLC writes stay DLC-scoped. D-10: explicit confirmation controls provider-media import.
</objective>
<execution_context>@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>
@CLAUDE.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-CONTEXT.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-RESEARCH.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-UI-SPEC.md
<read_first>frontend/src/v2/components/Dialogs/MatchRomDialog.vue; frontend/src/v2/components/MatchRom/types.ts; frontend/src/v2/components/GameDetails/PcMetadataReview.vue; frontend/src/v2/components/GameDetails/PcComponents.vue; frontend/src/v2/views/GameDetails.vue; frontend/src/services/api/rom.ts</read_first>
<interfaces>
PcMatchTarget is a discriminated union: `{ kind: "rom"; romId: number; label: string } | { kind: "component"; romId: number; componentId: number; componentKind: "base" | "update" | "dlc" | "hotfix" | "language_pack" | "extra"; label: string }`. `unresolved` is deliberately absent because its source layout lacks a trustworthy classified component identity, and manual matching must not create an authoritative association.
Its adapter contract exposes `search(query, searchBy)`, `confirm({ candidateId, selectedMedia, expectedVersion, query, searchBy })`, and `refresh()`; component adapters must never call generic `updateRom`.
</interfaces>
</context>
<tasks>
<task type="auto">
<name>Task 1: Regenerate and expose target-specific PC matcher service contracts</name>
<files>frontend/src/**generated**/, frontend/src/services/api/rom.ts</files>
<read_first>CLAUDE.md API contract rule; generated OpenAPI client conventions; backend schemas/routes from Plans 12-07 through 12-09</read_first>
<action>After backend migrations and routes are available, run the repository OpenAPI generator. Do not hand-edit generated files. Add narrow service adapter functions for parent and all classified component candidate query/confirmation, component owned media/notes/downloads, and normalized matcher result conversion. Keep explicit target fields, classified component kind, expected versions, bounded query/search mode and selected provider media refs in the API contract. Do not make an unresolved target type or client call: its API is a 404/no-selection safety fence because an unclassified source layout has no trustworthy identity for an authoritative manual association. If generation cannot run because the backend is unavailable or schema fails, stop this plan at a blocking human-verify gate with the exact command/error; do not invent client types.</action>
<acceptance_criteria>Generated types match backend OpenAPI and the frontend has no untyped route/body construction for PC matcher targets.</acceptance_criteria>
<verify><automated>cd frontend && npm run generate && npm run typecheck</automated></verify>
<done>Frontend API calls are generated-contract-backed and distinguish ROM from DLC component targets.</done>
</task>
<task type="auto" tdd="true">
<name>Task 2: Extract the shared matcher shell behind an explicit target adapter</name>
<files>frontend/src/v2/components/MatchRom/types.ts, frontend/src/v2/components/Dialogs/MatchRomDialog.vue, frontend/src/v2/components/Dialogs/GlobalDialogs.vue, frontend/src/v2/components/Dialogs/MatchRomDialog.test.ts</files>
<behavior>

- Parent and all eligible component launch events mount the same dialog shell and preserve grid/list, provider chips, query controls, descriptions and cover selection.
- A component confirm invokes only the component adapter and returns selected provider media only after explicit confirmation.
- Closing, filtering and previewing invoke no selection mutation.
</behavior>

<read_first>MatchRomDialog.vue current generic updateRom path; MatchRomBodyGrid/List props/events; global emitter registration conventions</read_first>
<action>Write focused tests first, then extract target-neutral state/body presentation from MatchRomDialog into an explicit typed adapter contract. Preserve existing RDialog mobile/full-height behavior and visual body components. Replace the parent-only persistence branch with adapter calls, retain generic non-PC users if they still launch the dialog, and add parent/component launch payloads without route-name inference. Component targets must carry a concrete classified kind and be structurally unable to include unresolved. This exclusion is intentional: unresolved source layout has no trustworthy classified component identity, so UI must offer no launcher and must never form an authoritative association. Ensure errors use the established snackbar path and refresh only after successful confirmation. Do not retain parallel simplified matching behavior.</action>
<acceptance_criteria>One dialog UI serves generic ROM matching and PC target adapters, with component selection structurally unable to call updateRom.</acceptance_criteria>
<verify><automated>cd frontend && npm run test -- src/v2/components/Dialogs/MatchRomDialog.test.ts && npm run typecheck</automated></verify>
<done>The screenshot-quality existing matcher is safely reusable for PC targets.</done>
</task>
<task type="auto" tdd="true">
<name>Task 3: Replace PC metadata-review entry points only after shared matcher parity</name>
<files>frontend/src/v2/components/GameDetails/PcComponents.vue, frontend/src/v2/views/GameDetails.vue, frontend/src/v2/components/GameDetails/PcMetadataReview.vue, frontend/src/v2/components/GameDetails/PcComponents.test.ts</files>
<behavior>

- Parent PC metadata search opens the shared matcher with a ROM target.
- Each base, update, DLC, hotfix, language-pack and extra row metadata search opens it with that exact component target and refreshes parent details after apply; unresolved gets no matcher launcher because its unclassified source layout has not established a trustworthy identity for authoritative association.
- PcMetadataReview no longer renders or remains reachable after equivalent entry paths pass tests.
</behavior>

<read_first>GameDetails.vue PC component refresh mechanism; PcComponents.vue events; PcMetadataReview tests and imports</read_first>
<action>Write entry-point tests, then wire the existing PC-parent metadata action and each classified PC-component row's `Metadaten suchen` action to the shared dialog target. Include base, update, DLC, hotfix, language_pack and extra fixtures. Assert unresolved renders no matcher launcher and its attempted API candidate/selection calls are not made from the UI; backend endpoint tests in Plan 12-08 prove 404/no selection. This is a safety fence because unresolved source layout has no trustworthy component identity, so manual matching cannot create an authoritative association. Reuse existing refreshPcDetails/current-detail refresh after a success. Remove PcMetadataReview and its tests/imports only after parent and all classified component paths assert same shell and target-specific API. Do not modify overview navigation or create a second candidate list.</action>
<acceptance_criteria>All classified PC metadata entry points use one matcher UI; unresolved has no launcher/API selection path; no bespoke PcMetadataReview remains, and applying any component candidate cannot refresh or overwrite another target.</acceptance_criteria>
<verify><automated>cd frontend && npm run test -- src/v2/components/Dialogs/MatchRomDialog.test.ts src/v2/components/GameDetails/PcComponents.test.ts && npm run typecheck</automated></verify>
<done>PC main-game and component metadata search are product-equivalent, shared-flow entry points.</done>
</task>
</tasks>
<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                       |
| -------------------------- | --------------------------------------------------------------------------------- |
| UI launch event to adapter | Target identity must remain typed, never inferred from URL or a loose numeric id. |
| client adapter to API      | The client must not substitute generic parent update for component selection.     |

## STRIDE Threat Register

| Threat ID                                                                                                                                                    | Category  | Component              | Disposition | Mitigation Plan                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------- | ---------------------- | ----------- | -------------------------------------------------------------------------------------------- |
| T-12-10-01                                                                                                                                                   | T/E       | matcher target         | mitigate    | Use a discriminated target union and separate API adapter methods tested by spies.           |
| T-12-10-02                                                                                                                                                   | R         | explicit confirmation  | mitigate    | Keep browse/filter/cancel outside confirm handler and use server version/candidate contract. |
| T-12-10-03                                                                                                                                                   | I         | client state           | mitigate    | Refresh only contained detail data and do not render parent/sibling component data.          |
| T-12-SC                                                                                                                                                      | Tampering | generated dependencies | accept      | No dependency install, generated code only from local OpenAPI source.                        |
| </threat_model>                                                                                                                                              |
| <verification>OpenAPI generation and typecheck are blocking gates. Then run matcher and PC Components focused tests.</verification>                          |
| <success_criteria>The polished matcher is a single shared UI for parent and every eligible PC component, while writes remain target-safe.</success_criteria> |
| <output>Create `.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-10-SUMMARY.md` when done.</output>                                           |
