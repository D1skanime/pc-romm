---
phase: 12-dlc-detail-pages-for-local-pc-components
plan: 03
type: execute
wave: 2
depends_on: [12-02, 12-04, 12-05]
files_modified:
  - frontend/src/v2/views/PcDlcDetails.vue
  - frontend/src/v2/views/PcDlcDetails.test.ts
  - frontend/src/v2/components/GameDetails/PcDlcDetail.vue
  - frontend/src/v2/components/GameDetails/PcDlcDetail.test.ts
  - frontend/src/v2/components/GameDetails/PcDlcFiles.vue
  - frontend/src/v2/components/GameDetails/PcDlcFiles.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "A valid page presents only the selected DLC's identity, local owned media, and immutable manifest."
    - "The user can return to the parent game and no source, provider, or selection action is available."
  artifacts:
    - path: frontend/src/v2/components/GameDetails/PcDlcDetail.vue
      provides: "Component-only identity and owned-media presentation"
    - path: frontend/src/v2/components/GameDetails/PcDlcFiles.vue
      provides: "Read-only exact DLC manifest"
  key_links:
    - from: PcDlcDetails.vue
      to: PcDlcDetail.vue
      via: "separate parent and prevalidated component props"
      pattern: "PcDlcDetail"
    - from: PcDlcFiles.vue
      to: component.manifest_members
      via: "direct immutable list rendering"
      pattern: "manifest_members"
---

<objective>
Render the resolved DLC as a feature-specific, read-only v2 detail page.
Purpose: Fulfil D-03 through D-05 while keeping the source library and review workflows untouched.
Output: Tested component-only composite, media isolation, and manifest evidence.
</objective>
<execution_context>@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-CONTEXT.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-UI-SPEC.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-RESEARCH.md
<interfaces>`PcDlcDetail` takes `parent: DetailedRomSchema` and `component: PcComponentSchema`. It derives title from `component.component_metadata?.name` or localized `DLC: {relativePath}`, and image URLs only from that component's `local_media[].owned_path` below `FRONTEND_RESOURCES_PATH`. `PcDlcFiles` takes the validated component only.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Specify isolated component identity, media, and manifest behavior</name>
<files>frontend/src/v2/components/GameDetails/PcDlcDetail.test.ts, frontend/src/v2/components/GameDetails/PcDlcFiles.test.ts, frontend/src/v2/views/PcDlcDetails.test.ts</files>
<behavior>
 - Selected metadata title/summary and owned cover/media render, otherwise the localized relative-path title and neutral cover placeholder render.
 - Parent and sibling artwork never render; no media section renders when the DLC has no selected media.
 - Files render exactly each manifest member's relative path, byte size, and SHA-256, with no parent-file matching, download, or mutation control.
</behavior>
<action>Write failing tests with parent and sibling decoy data. Assert the first action is named `Back to {game}`, facts include only populated relative path, version evidence, manifest count, and aggregate member size, and the semantic list uses only `manifest_members`. Include the valid PcDlcDetails handoff assertion. Do not assert a provider result, parent cover, aggregate artwork helper, source-relative image URL, or FilesTab query behavior.</action>
<verify><automated>cd frontend &amp;&amp; npm run test -- src/v2/components/GameDetails/PcDlcDetail.test.ts src/v2/components/GameDetails/PcDlcFiles.test.ts src/v2/views/PcDlcDetails.test.ts</automated></verify>
<done>Focused tests prove component-only presentation and read-only immutable file evidence.</done>
</task>
<task type="auto">
<name>Task 2: Implement the accessible read-only DLC composites and route handoff</name>
<files>frontend/src/v2/views/PcDlcDetails.vue, frontend/src/v2/views/PcDlcDetails.test.ts, frontend/src/v2/components/GameDetails/PcDlcDetail.vue, frontend/src/v2/components/GameDetails/PcDlcDetail.test.ts, frontend/src/v2/components/GameDetails/PcDlcFiles.vue, frontend/src/v2/components/GameDetails/PcDlcFiles.test.ts</files>
<action>Per D-03 through D-05, replace the valid route handoff with `PcDlcDetail` and pass separate parent/component props. Build both feature composites below GameDetails from existing RBtn, RTag/RChip, RImg, REmptyState/RAlert and detail patterns. Render selected component metadata, owned-path cover/background/gallery only, neutral placeholder when cover is absent, and omit an empty media surface. Implement dedicated `PcDlcFiles` as the resolved research decision: exact read-only `manifest_members`, `formatBytes`, SHA-256, semantic list/definition list, safely wrapping values, no `rom.files` prefix match or file action. Apply UI-SPEC token-only styles, `html[data-bp]`, natural DOM focus, and first-control parent back link. Do not mutate, fetch metadata, select media, call providers, or use source paths as URLs.</action>
<verify><automated>cd frontend &amp;&amp; npm run test -- src/v2/components/GameDetails/PcDlcDetail.test.ts src/v2/components/GameDetails/PcDlcFiles.test.ts src/v2/views/PcDlcDetails.test.ts &amp;&amp; npm run typecheck</automated></verify>
<done>The valid page is localized, accessible, responsive, parent-linked, component-only, and read-only.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Persisted component data to browser | Only selected component metadata, owned media, and immutable manifest become visible. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-12-02 | Information disclosure | PcDlcDetail/PcDlcFiles | mitigate | Filter to the resolved component's fields, never parent or sibling aggregates. |
| T-12-03 | Tampering | page mount | mitigate | Presentation code imports no review, provider, or mutation API. |
| T-12-SC | Tampering | npm installs | accept | No package installation is planned. |
</threat_model>
<verification>Run focused component/view tests and typecheck.</verification>
<success_criteria>The page shows only selected local DLC evidence and an exact immutable manifest, with a first-control parent return and no write-capable UI.</success_criteria>
<output>Create `.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-03-SUMMARY.md` when done.</output>
