---
phase: 15-direct-resumable-transfer
plan: 03
type: tdd
wave: 2
depends_on:
  - 15-01
  - 15-02
files_modified:
  - backend/endpoints/download_manifests.py
  - backend/tests/endpoints/test_download_manifests.py
autonomous: true
requirements:
  - XFER-01
  - XFER-02
  - XFER-03
  - XFER-04
must_haves:
  truths:
    - "The opaque manifest member URL sends the original member bytes directly, never a ZIP, archive, directory listing, or source path."
    - "Every request performs authentication, ROMS_READ, owner lookup, current ROM visibility, lifecycle/member checks, exact strong If-Match validation, and strong pre-transfer verification before bytes."
    - "No Range returns 200 with exact Content-Length; one valid byte range returns 206 with exact Content-Range; invalid, multiple, and unsatisfiable ranges return bounded 416 with bytes */total."
    - "A changed member returns 412 with the bounded JSON code `source_changed` before transfer opening or streaming, and a weak, missing, or mismatched resume validator cannot continue content."
  artifacts:
    - path: "backend/endpoints/download_manifests.py"
      provides: "Protected direct single-member range endpoint"
      contains: "/download-manifests/{manifest_id}/files/{public_id}"
    - path: "backend/tests/endpoints/test_download_manifests.py"
      provides: "Authenticated direct-transfer, range, validator, lifecycle, and disclosure regressions"
  key_links:
    - from: "backend/endpoints/download_manifests.py"
      to: "DBDownloadManifestsHandler.get_transfer_manifest_member"
      via: "owner- and public-ID-scoped candidate lookup before source I/O"
      pattern: "get_transfer_manifest_member"
    - from: "backend/endpoints/download_manifests.py"
      to: "FSRomsHandler.open_verified_download_manifest_member"
      via: "verified lease becomes the StreamingResponse iterator"
      pattern: "open_verified_download_manifest_member"
---

<objective>
Expose the final protected HTTP endpoint for original-file delivery and strict, snapshot-bound single-range resume.

Purpose: This is the client-visible contract Phase 16 will rely on. It must make a legitimate interrupted direct download distinguishable from a stale, unauthorized, malformed, or changed request before any content can be appended.
Output: A tested `/api/download-manifests/{manifest_id}/files/{public_id}` endpoint with no archive implementation and precise 200, 206, 412, and 416 semantics.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-direct-resumable-transfer/15-CONTEXT.md
@.planning/phases/15-direct-resumable-transfer/15-PATTERNS.md
@.planning/phases/14-immutable-download-manifests/14-VERIFICATION.md
@.planning/phases/15-direct-resumable-transfer/15-01-PLAN.md
@.planning/phases/15-direct-resumable-transfer/15-02-PLAN.md

<interfaces>
From Plan 15-01:
- `get_transfer_manifest_member(manifest_id, user_id, public_id)` resolves only a currently eligible persisted member relation. The endpoint must apply `assert_rom_visible(request, manifest.rom, not_found_detail=_NOT_FOUND)` before any validator, source I/O, or error that could disclose existence.

From Plan 15-02:

- The verified transfer lease either offers a read-only sequential iterator and trusted exact `size_bytes`, or reports SOURCE_CHANGED before bytes. It owns descriptor and concurrency-slot cleanup for the entire `StreamingResponse` lifecycle.

From `backend/endpoints/roms/files.py`:

- `_range_bounds` is the existing single-range precedent. Phase 15 must use an equally strict, bounded parser, but must not inherit its authentication disable switch or its path/file-name route contract.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement protected strict-range file delivery</name>
  <files>backend/endpoints/download_manifests.py, backend/tests/endpoints/test_download_manifests.py</files>
  <read_first>backend/endpoints/download_manifests.py, backend/endpoints/roms/files.py, backend/tests/endpoints/test_download_manifests.py, .planning/phases/15-direct-resumable-transfer/15-01-PLAN.md, .planning/phases/15-direct-resumable-transfer/15-02-PLAN.md</read_first>
  <behavior>
    - The opaque member URL serves exact original fixture bytes only after unconditional ROMS_READ, owner/member/lifecycle/visibility checks and fresh strong verification.
    - No Range is 200 with exact length, a valid one-range resume with exact stored quoted strong If-Match is 206 with exact metadata, and synthetic 5/80/120 GiB totals remain exact.
    - Weak, missing, wildcard/list, altered, or stale resume validators, changed source, and malformed/multi/unsatisfiable ranges produce bounded non-appendable failures, specifically a 412 JSON error with `code: source_changed` and no transfer-open/yield/source bytes, or 416 `bytes */total` as applicable.
  </behavior>
  <action>Per the Direct original-file delivery, Strong snapshot contract, Range protocol, and Resource and source safety decisions, first add the endpoint regressions and commit them as `test(15-03): specify direct range transfer`. Implement the protected GET route at the Phase-14 URL using Plan 15-01 lookup, existing `_NOT_FOUND` and `assert_rom_visible`, exact stored quoted strong If-Match validation, a bounded u64 single-range parser, and Plan 15-02's descriptor-backed lease iterator. Return no-range 200 or valid-range 206 with exact headers, map verified source change before transfer-lease opening to HTTP 412 with the project-style bounded JSON detail `{code: source_changed}`, and assert it neither opens/yields the transfer nor contains source-file bytes. Return bounded 416 plus `Content-Range: bytes */total` for invalid/multi/unsatisfiable ranges. Never use the legacy auth-disable switch or add ZIP, path, Tauri, UI, local-part, cache, or NAS behavior. Commit passing implementation as `feat(15-03): add direct manifest transfer`.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_download_manifests_handler.py tests/handler/filesystem/test_roms_handler.py tests/endpoints/test_download_manifests.py -k 'transfer or download_manifest' -q</automated></verify>
  <done>The client-visible URL has exact, authenticated direct-transfer and resume semantics with no archive/path disclosure and every invalid continuation failing safely before bytes.</done>
</task>

</tasks>

<feature>
  <name>Protected snapshot-bound single-range delivery endpoint</name>
  <files>backend/endpoints/download_manifests.py, backend/tests/endpoints/test_download_manifests.py</files>
  <behavior>
    - RED: An authorized owner with a visible ROM receives a small fixture's exact original bytes through the opaque public ID URL, with attachment-safe headers and no ZIP/archive creation, source path, internal ID, or directory response.
    - RED: No `Range` returns 200, `Accept-Ranges: bytes`, and exact decimal `Content-Length`. Valid `bytes=start-end`, open-ended, and safe suffix single ranges return 206 with exact `Content-Range: bytes start-end/total`, body bytes, and totals at synthetic 5 GiB, 80 GiB, and 120 GiB values.
    - RED: Malformed units, negative/non-decimal/overflow values, multiple ranges, start beyond EOF, and inverted ranges return 416 plus exactly `Content-Range: bytes */total`, never a partial or 200 fallback.
    - RED: A resume range requires `If-Match` equal to the persisted quoted strong snapshot byte-for-byte. Reject missing, weak `W/`, list/wildcard, altered, and stale validators before the transfer lease. A no-range fresh download may use the same validator if supplied but cannot accept a weak/mismatched validator.
    - RED: Foreign owner, hidden ROM, foreign member UUID, and expired/revoked/source-changed lifecycle retain their bounded existing errors. A Plan-15-02 strong mismatch maps before transfer-lease opening to HTTP 412 with project-style JSON `{"detail": {"code": "source_changed"}}`; the regression proves no transfer iterator was opened or yielded and no source-file bytes occur in the response.
    - GREEN: Add the route, strict parsing/response construction, and all regressions.
    - REFACTOR: Keep the existing Phase-14 JSON routes unchanged and reuse its `_NOT_FOUND`, `_STATE_ERRORS`, `@protected_route(..., [Scope.ROMS_READ])`, and visibility mask. Do not enable `DISABLE_DOWNLOAD_ENDPOINT_AUTH`, add HEAD/legacy download behavior, or send a response that a client could append after an unexpected 200.
  </behavior>
  <implementation>
Add the GET route at the prepublished Phase-14 relative URL in `backend/endpoints/download_manifests.py`. It must authenticate with `ROMS_READ` unconditionally, query the Plan-15-01 owner/public-ID contract, apply current ROM visibility, map lifecycle states using the existing bounded envelope, and validate the request's `If-Match` before acquiring the Plan-15-02 lease. Treat a Range request as a resume and require exactly one stored quoted strong validator; reject all weak `W/` forms and alternative/list values rather than applying generic HTTP wildcard semantics. Implement a strict single `bytes` range parser using arbitrary-size Python integer parsing with explicit non-negative/u64 bounds, then calculate only from the lease's trusted total. For invalid/multi/unsatisfiable input return 416 and `Content-Range: bytes */{total}`. On a verified success, use the lease's sequential iterator directly in `StreamingResponse`: no Range is 200 with exact `Content-Length`; a range is 206 with exact `Content-Range`, correct length, `Accept-Ranges: bytes`, and a safe attachment disposition derived only from trusted persisted filename data. Map a changed lease result before response streaming to the standard small JSON error `{\"detail\": {\"code\": \"source_changed\"}}` with HTTP 412; that branch must not open/yield source transfer data. Keep all source work read-only. Do not add browser UI, Tauri, part-file, local-path, disk-space, checksum-finalization, archive, cache, or NAS tests, per the phase-context decisions.
  </implementation>
</feature>

<verification>
  <automated>cd backend && uv run pytest tests/handler/database/test_download_manifests_handler.py tests/handler/filesystem/test_roms_handler.py tests/endpoints/test_download_manifests.py -k 'transfer or download_manifest' -q</automated>
</verification>

<success_criteria>

- The published opaque file URL serves precisely one original verified member and never invokes ZIP/archive/directory machinery.
- An authenticated valid resume has unambiguous 206 and exact byte metadata; every malformed or nonmatching resume fails before an appendable response.
- Source/manifest version changes yield a pre-stream 412 JSON `source_changed` response with no transfer opening, yielded source bytes, or source-data disclosure; all 5/80/120 GiB synthetic values preserve their exact headers and offsets under the configured bounded transfer gate.
  </success_criteria>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                              |
| ---------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Client HTTP headers and opaque URL | Untrusted IDs, Range, and If-Match determine whether a byte response is eligible.                        |
| Route to transfer lease            | The route translates authorized persisted identity into a read-only, freshly verified descriptor stream. |

## STRIDE Threat Register

| Threat ID       | Category                        | Component                  | Disposition | Mitigation Plan                                                                                                                                         |
| --------------- | ------------------------------- | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-15-10         | Spoofing/Elevation of privilege | Direct file route          | mitigate    | Require `ROMS_READ` without legacy auth bypass, owner-scoped lookup, member relation check, and current ROM visibility on every request.                |
| T-15-11         | Tampering                       | Resume protocol            | mitigate    | Require exact stored quoted strong `If-Match`, reject weak/list/wildcard validators, then run Plan-15-02 fresh HASH/snapshot verification before bytes. |
| T-15-12         | Information disclosure          | Error and response headers | mitigate    | Preserve 404 masking for ownership/visibility/member mismatch; expose only bounded lifecycle/source-changed codes and never paths or internal IDs.      |
| T-15-13         | Denial of service               | Range parser/stream        | mitigate    | Accept one bounded u64 single range only, return 416 deterministically, and rely on the lease's process-local stream/verification limit.                |
| T-15-14         | Tampering                       | Source library             | mitigate    | Stream one descriptor-backed original member directly, with no ZIP, extraction, cache, temporary output, or source mutation operation.                  |
| </threat_model> |

<source_audit>

| Source          | Item                                                                    | Coverage                                    |
| --------------- | ----------------------------------------------------------------------- | ------------------------------------------- |
| GOAL            | Direct original file, safe resumable range, no packaging                | Plan 15-03 route and Plan 15-02 lease       |
| REQ             | XFER-01 direct non-ZIP delivery                                         | Plans 15-02 and 15-03                       |
| REQ             | XFER-02 exact authenticated Range resume                                | Plan 15-03                                  |
| REQ             | XFER-03 exact snapshot and changed-source failure                       | Plans 15-01, 15-02, and 15-03               |
| REQ             | XFER-04 >4 GiB accounting and bounded concurrency                       | Plans 15-02 and 15-03                       |
| CONTEXT         | Opaque member only, per-request auth/visibility/lifecycle, no paths/ZIP | Plans 15-01 and 15-03                       |
| CONTEXT         | Exact strong If-Match and fresh HASH snapshot before bytes              | Plans 15-02 and 15-03                       |
| CONTEXT         | Strict 200/206/416 semantics and >4 GiB values                          | Plan 15-03                                  |
| CONTEXT         | Read-only source, bounded work, isolated synthetic tests                | Plan 15-02                                  |
| DEFERRED        | Client local state/path/part files, UI handoff                          | Explicitly excluded from all Phase-15 plans |
| </source_audit> |

<output>
Create `.planning/phases/15-direct-resumable-transfer/15-03-SUMMARY.md` when done.
</output>
