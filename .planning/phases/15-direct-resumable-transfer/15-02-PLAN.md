---
phase: 15-direct-resumable-transfer
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/config/__init__.py
  - backend/handler/filesystem/roms_handler.py
  - backend/tests/handler/filesystem/test_roms_handler.py
autonomous: true
requirements:
  - XFER-01
  - XFER-03
  - XFER-04
must_haves:
  truths:
    - "A delivery candidate is fully HASH-verified immediately before its bytes can be read, and its recomputed Phase-14 strong snapshot must equal the persisted quoted snapshot."
    - "A mismatch is classified as SOURCE_CHANGED before any bytes are yielded."
    - "A bounded per-process slot spans source HASH, descriptor ownership, and the complete response iterator, including disconnect/error cleanup."
    - "Transfer totals and seek offsets remain Python integers and are tested with isolated 5 GiB, 80 GiB, and 120 GiB metadata, without allocating those files."
  artifacts:
    - path: "backend/config/__init__.py"
      provides: "Validated direct-transfer process concurrency setting"
      contains: "DOWNLOAD_MANIFEST_TRANSFER_MAX_CONCURRENCY"
    - path: "backend/handler/filesystem/roms_handler.py"
      provides: "Read-only, strong-verified member transfer lease and bounded chunk iterator"
      contains: "open_verified_download_manifest_member"
    - path: "backend/tests/handler/filesystem/test_roms_handler.py"
      provides: "Strong snapshot, >4 GiB metadata, read-only, and cleanup/concurrency regressions"
  key_links:
    - from: "backend/handler/filesystem/roms_handler.py"
      to: "_download_manifest_snapshot"
      via: "recompute from fresh HASH result and persisted public ID/destination/size"
      pattern: "_download_manifest_snapshot"
    - from: "backend/handler/filesystem/roms_handler.py"
      to: "StorageOperation.HASH"
      via: "trusted member source opened only through HASH capability before stream"
      pattern: "StorageOperation.HASH"
---

<objective>
Build the read-only verified-transfer lease that makes a direct byte stream safe for a single immutable manifest member.

Purpose: A range response is only safe if the exact persisted snapshot has been freshly verified and the expensive work cannot grow without bound under concurrent requests.
Output: A tested filesystem transfer primitive and documented process-local bound, ready for the route to stream without re-opening arbitrary paths.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-direct-resumable-transfer/15-CONTEXT.md
@.planning/phases/15-direct-resumable-transfer/15-PATTERNS.md
@.planning/phases/14-immutable-download-manifests/14-VERIFICATION.md

<interfaces>
From `backend/handler/filesystem/roms_handler.py`:
- `FSRomsHandler.capture_download_manifest_member(rom, member, public_id)` already establishes the exact trusted HASH pattern: open a HASH capability, compare pre/post `fstat`, verify SHA-256 and stored size, then call `_download_manifest_snapshot(public_id, destination, size, sha256)`.
- `_download_manifest_snapshot` produces the quoted strong validator from NUL-separated `romm-manifest-v1`, opaque public ID, destination, decimal size, and verified SHA-256.
- `pc_component_member_path(rom, member)` derives the source only from persisted trusted relations; it must never be replaced with route/path input.

From `backend/endpoints/roms/files.py`:

- A descriptor-backed generator closes its access in `finally`; the Phase-15 lease must extend this pattern so its transfer slot is released on normal completion, disconnect, and generator error.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement a strongly verified bounded transfer lease</name>
  <files>backend/config/__init__.py, backend/handler/filesystem/roms_handler.py, backend/tests/handler/filesystem/test_roms_handler.py</files>
  <read_first>backend/config/__init__.py, backend/handler/filesystem/roms_handler.py, backend/tests/handler/filesystem/test_roms_handler.py, backend/endpoints/roms/files.py, .planning/phases/15-direct-resumable-transfer/15-CONTEXT.md</read_first>
  <behavior>
    - Fresh HASH capability evidence, regular-file pre/post indicators, expected size/SHA-256, and exact quoted Phase-14 snapshot must match before the first readable byte.
    - Synthetic 5 GiB, 80 GiB, and 120 GiB evidence preserves totals and offsets exactly without large allocation; changed evidence yields SOURCE_CHANGED with no iterator.
    - A finite process-local slot spans HASH through response iterator cleanup and is released once after normal completion, error, or early close; source fixtures remain unchanged.
  </behavior>
  <action>Per the Strong snapshot contract and Resource and source safety decisions, first add the failing isolated filesystem tests and commit them as `test(15-02): specify verified transfer lease`. Add validated `DOWNLOAD_MANIFEST_TRANSFER_MAX_CONCURRENCY` following `ROM_PATCHER_MAX_CONCURRENCY`, then implement the smallest typed `open_verified_download_manifest_member` lease in `FSRomsHandler`. It must accept only Phase-14 ORM evidence, derive source exclusively with `pc_component_member_path`, acquire the finite slot before HASH, validate pre/post `fstat`, recompute `_download_manifest_snapshot`, and retain the descriptor plus slot in a finally-cleaned sequential iterator. On any mismatch close/release and return bounded SOURCE_CHANGED without bytes. Do not create archives, cache/temp output, paths in public types, or client part-file logic. Commit passing code as `feat(15-02): add verified transfer lease`.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/filesystem/test_roms_handler.py -k 'verified_download_manifest_member or transfer_concurrency or download_manifest' -q</automated></verify>
  <done>A read-only descriptor stream exists only after exact current strong evidence and its bounded resource slot is proven to release on every iterator exit.</done>
</task>

</tasks>

<feature>
  <name>Strongly verified, bounded direct-transfer lease</name>
  <files>backend/config/__init__.py, backend/handler/filesystem/roms_handler.py, backend/tests/handler/filesystem/test_roms_handler.py</files>
  <behavior>
    - RED: A matching persisted member opens only after a fresh HASH-capability pass verifies regular-file identity stability, expected size, expected SHA-256, and the exact quoted Phase-14 snapshot. Changed content, size, identifiers, or snapshot yields a bounded SOURCE_CHANGED result with no readable iterator.
    - RED: A 5 GiB, 80 GiB, and 120 GiB synthetic stat/evidence case preserves exact non-negative integer totals and requested offsets without materializing a large file.
    - RED: With the configured transfer limit exhausted, another lease cannot begin; releasing or closing the first lease makes capacity available. The slot is held from before HASH until the iterator's `finally` closes the descriptor, including an iterator exception or early close.
    - RED: The primitive uses only read/HASH capabilities and leaves an isolated source fixture byte-for-byte and tree-for-tree unchanged.
    - GREEN: Implement the smallest typed lease/result and all tests.
    - REFACTOR: Keep Phase 14's capture and STAT-only revalidation semantics intact, sharing the exact snapshot helper rather than a second hash format.
  </behavior>
  <implementation>
Add a positive, environment-configurable `DOWNLOAD_MANIFEST_TRANSFER_MAX_CONCURRENCY` setting following the existing `ROM_PATCHER_MAX_CONCURRENCY` validation convention. In `FSRomsHandler`, add a narrow typed transfer lease returned by `open_verified_download_manifest_member(rom, persisted_manifest_member)` (or equivalently named method) that receives only Phase-14 trusted ORM evidence. Acquire one process-local bounded slot before opening/HASHing. Derive the path exclusively with `pc_component_member_path`, use `StorageOperation.HASH`, check pre/post regular-file indicators, calculate SHA-256, recompute the existing `romm-manifest-v1` strong snapshot, and compare every expected field exactly. On mismatch, close and release before reporting `SOURCE_CHANGED`, without yielding data or mutating the aggregate/source. On success retain the verified descriptor and expose a bounded sequential chunk iterator with validated `int` start/length; its `finally` must close the descriptor and release the same slot exactly once. Do not package, cache, extract, write temporary files, use source paths in a public type, or implement client `.romm-part` behavior, per D-01, D-02, and D-04.
  </implementation>
</feature>

<verification>
  <automated>cd backend && uv run pytest tests/handler/filesystem/test_roms_handler.py -k 'verified_download_manifest_member or transfer_concurrency or download_manifest' -q</automated>
</verification>

<success_criteria>

- A direct-transfer candidate has a freshly rehashed Phase-14 snapshot before its first byte and fails SOURCE_CHANGED with no stream when evidence differs.
- One configured slot remains occupied during both verification and streaming, and is reliably freed on all iterator exits.
- Synthetic 5/80/120 GiB evidence proves 64-bit-scale accounting without NAS access, real large allocations, ZIP production, or any source mutation.
  </success_criteria>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                 |
| -------------------------------------------------- | --------------------------------------------------------------------------- |
| Persisted member relation to filesystem capability | Database evidence authorizes one descriptor-derived source read.            |
| Stream iterator to HTTP response                   | A long-lived stream holds finite verification/read resources until cleanup. |

## STRIDE Threat Register

| Threat ID       | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                  |
| --------------- | ---------------------- | ------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-15-05         | Tampering              | Source between manifest and delivery | mitigate    | Fresh HASH plus pre/post file identity checks and exact Phase-14 snapshot recomputation before yielding bytes.                   |
| T-15-06         | Information disclosure | Filesystem addressing                | mitigate    | Accept no browser path and derive only via trusted `pc_component_member_path`; do not serialize the result.                      |
| T-15-07         | Denial of service      | Hash and response stream             | mitigate    | Positive process-local bounded concurrency holds one slot for HASH through iterator cleanup and tests release paths.             |
| T-15-08         | Tampering              | External library                     | mitigate    | Restrict the primitive to HASH/read capabilities, assert before/after fixture evidence, and create no temp/cache/archive output. |
| T-15-09         | Repudiation            | Changed source result                | mitigate    | Return a bounded SOURCE_CHANGED domain result for route mapping, never a partial/new-version stream.                             |
| </threat_model> |

<output>
Create `.planning/phases/15-direct-resumable-transfer/15-02-SUMMARY.md` when done.
</output>
