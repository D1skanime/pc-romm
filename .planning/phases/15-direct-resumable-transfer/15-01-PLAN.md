---
phase: 15-direct-resumable-transfer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/handler/database/download_manifests_handler.py
  - backend/tests/handler/database/test_download_manifests_handler.py
autonomous: true
requirements:
  - XFER-01
  - XFER-03
must_haves:
  truths:
    - "A transfer lookup resolves one persisted public member ID only within its owning user's manifest and its selected component relation."
    - "A transfer lookup preserves the distinct VALID, EXPIRED, REVOKED, and SOURCE_CHANGED lifecycle meanings without doing Phase 14's normal whole-manifest STAT revalidation."
    - "No transfer lookup exposes a raw source path or grants a historical URL independent of current ownership."
  artifacts:
    - path: "backend/handler/database/download_manifests_handler.py"
      provides: "Owner-scoped, member-scoped transfer lookup with lifecycle enforcement"
      contains: "get_transfer_manifest_member"
    - path: "backend/tests/handler/database/test_download_manifests_handler.py"
      provides: "Persistence and lifecycle regressions for direct member lookup"
  key_links:
    - from: "backend/handler/database/download_manifests_handler.py"
      to: "models.download_manifest.DownloadManifestMember.public_id"
      via: "owner-scoped eager query constrained by manifest ID and public member ID"
      pattern: "public_id"
---

<objective>
Define the server-side transfer lookup contract before any byte stream is added.

Purpose: The file route must be able to resolve exactly one Phase-14 manifest member, under current authorization and lifecycle state, without accepting paths or repeating normal manifest GET's full aggregate light revalidation.
Output: A tested database-handler transfer lookup that supplies only trusted ORM relations to the later filesystem and HTTP plans.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-direct-resumable-transfer/15-CONTEXT.md
@.planning/phases/15-direct-resumable-transfer/15-PATTERNS.md
@.planning/phases/14-immutable-download-manifests/14-VERIFICATION.md

<interfaces>
From `backend/handler/database/download_manifests_handler.py`:
- `DBDownloadManifestsHandler.get_manifest(manifest_id: str, user_id: int) -> DownloadManifest | None` is owner-scoped but does ordinary, aggregate STAT-only revalidation and is therefore not the transfer contract.
- `DownloadManifest` owns `rom`, `components`, and lifecycle `status`; each `DownloadManifestMember` has public opaque `public_id`, selected `component_id`, `manifest_member`, `size_bytes`, `sha256`, `snapshot`, and no persisted source path.

From Phase 14:

- Whole game already means selected eligible base, update, DLC, and extra components only. This phase must never discover or recursively enumerate arbitrary ROM-directory files.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement owner-scoped transfer-member lookup</name>
  <files>backend/handler/database/download_manifests_handler.py, backend/tests/handler/database/test_download_manifests_handler.py</files>
  <read_first>backend/handler/database/download_manifests_handler.py, backend/tests/handler/database/test_download_manifests_handler.py, backend/models/download_manifest.py, .planning/phases/15-direct-resumable-transfer/15-CONTEXT.md</read_first>
  <behavior>
    - A lookup succeeds only for the owning user's manifest ID and an attached opaque `public_id`; a foreign user, random UUID, internal ID, or member from another manifest returns no candidate.
    - It gives later layers only trusted aggregate/source relations and Phase-14 evidence, never a source path, and it preserves EXPIRED, REVOKED, and SOURCE_CHANGED separately.
    - It does not invoke `light_revalidate_download_manifest_member` or HASH, leaving normal manifest GET STAT-only and the strong transfer check to Plan 15-02.
  </behavior>
  <action>Per the Direct original-file delivery and Strong snapshot contract decisions, first add the failing database-handler tests, commit them as `test(15-01): specify transfer member lookup`, then implement `get_transfer_manifest_member(manifest_id, user_id, public_id, session=...)` with a single owner- and public-ID-scoped eager ORM query following the durable selected-component/source-member topology. Apply the existing expiry transition and preserve non-VALID lifecycle state, but do not call normal `get_manifest`, paths, source I/O, ZIP code, or a client token. Commit the passing implementation as `feat(15-01): add transfer member lookup`.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_download_manifests_handler.py -k 'transfer_manifest_member or download_manifest' -q</automated></verify>
  <done>One opaque member can be resolved only from its current owner's persisted valid manifest relation, with source paths and normal GET revalidation absent.</done>
</task>

</tasks>

<feature>
  <name>Owner-scoped transfer-member lookup</name>
  <files>backend/handler/database/download_manifests_handler.py, backend/tests/handler/database/test_download_manifests_handler.py</files>
  <behavior>
    - RED: A lookup succeeds only for its owner's manifest ID and a member `public_id` actually attached to that manifest; an internal member ID, foreign owner, random UUID, or public ID from another manifest resolves to `None`.
    - RED: It eagerly supplies the canonical ROM, selected component, source manifest-member relation, and persisted Phase-14 evidence required for delivery, but has no raw-path input or output contract.
    - RED: It expires a live elapsed manifest and preserves pre-existing REVOKED and SOURCE_CHANGED state; it does not invoke `light_revalidate_download_manifest_member` or HASH, because transfer performs the one strong per-member check in Plan 15-02.
    - GREEN: Implement the narrow method and make all tests pass.
    - REFACTOR: Keep `get_manifest` unchanged as Phase 14's normal GET path and share only lifecycle/query primitives that do not change its STAT-only semantics.
  </behavior>
  <implementation>
Create `get_transfer_manifest_member(manifest_id, user_id, public_id, session=...)` in `DBDownloadManifestsHandler`, using a single owner-scoped ORM query whose member predicate is the public UUID and whose eager loads follow the durable selected-component and source-member topology. Apply expiry transition exactly as the existing handler does; return a bounded `None`/aggregate-member result for absent or non-VALID lifecycle states so the route can retain its established 404 mask and explicit lifecycle envelope. Do not add migrations, paths, a client token, ZIP logic, or a transfer stream. Keep all persisted identities opaque and do not call normal `get_manifest` from this method, per D-01 and D-02.
  </implementation>
</feature>

<verification>
  <automated>cd backend && uv run pytest tests/handler/database/test_download_manifests_handler.py -k 'transfer_manifest_member or download_manifest' -q</automated>
</verification>

<success_criteria>

- The next plans can obtain exactly one authorized, manifest-attached source member via opaque IDs, with the source relation enforced by Phase 14's durable topology.
- Expired, revoked, and source-changed manifests cannot be treated as a valid transfer candidate.
- The normal JSON manifest GET retains its light STAT-only behavior and no source data is read or modified by this lookup.
  </success_criteria>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                        |
| ------------------------------------- | -------------------------------------------------------------------------------------------------- |
| HTTP route to database handler        | Untrusted manifest and file identifiers become owner-scoped ORM predicates.                        |
| Persisted manifest to source relation | Durable selected/source component relations authorize a later read without exposing a source path. |

## STRIDE Threat Register

| Threat ID       | Category               | Component              | Disposition | Mitigation Plan                                                                                                              |
| --------------- | ---------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-15-01         | Elevation of privilege | Transfer member lookup | mitigate    | Constrain by manifest ID, current user ID, and opaque public member ID, then retain router visibility masking in Plan 15-03. |
| T-15-02         | Information disclosure | ORM result             | mitigate    | Return trusted ORM relations only, never source paths, host roots, or internal member IDs in an API shape.                   |
| T-15-03         | Tampering              | Lifecycle transition   | mitigate    | Preserve persisted REVOKED/SOURCE_CHANGED and transition elapsed VALID manifests to EXPIRED before delivery eligibility.     |
| T-15-04         | Denial of service      | Normal manifest GET    | mitigate    | Keep transfer lookup separate so it does not turn routine Phase-14 GET into a HASH operation.                                |
| </threat_model> |

<output>
Create `.planning/phases/15-direct-resumable-transfer/15-01-SUMMARY.md` when done.
</output>
