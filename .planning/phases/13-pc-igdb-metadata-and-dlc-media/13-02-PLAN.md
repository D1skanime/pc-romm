---
phase: 13-pc-igdb-metadata-and-dlc-media
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - backend/endpoints/sockets/scan.py
  - backend/handler/database/roms_handler.py
  - backend/handler/metadata/pc_match_handler.py
  - backend/handler/filesystem/resources_handler.py
  - backend/tests/endpoints/sockets/test_scan.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
requirements: []
must_haves:
  truths:
    - Scan-time parent enrichment persists normalized PC IGDB data and owned media without source writes.
    - Only an unambiguous trusted DLC candidate receives component metadata and owned media.
    - Optional media failures do not discard valid metadata or hide a scanned ROM.
  artifacts:
    - path: backend/endpoints/sockets/scan.py
      provides: Scan-time PC parent and DLC enrichment orchestration.
  key_links:
    - from: backend/endpoints/sockets/scan.py
      to: backend/handler/database/roms_handler.py
      via: Versioned parent/component persistence helpers and owned media records.
---

<objective>Apply normalized IGDB parent and unambiguous DLC metadata during scans, with RomM-owned media and non-fatal optional download failures.</objective>
<context>@CLAUDE.md
@docs/superpowers/specs/2026-09-04-pc-igdb-metadata-and-dlc-media-design.md
@.planning/phases/12-dlc-detail-pages-for-local-pc-components/12-07-PLAN.md
@.planning/phases/13-pc-igdb-metadata-and-dlc-media/13-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Add authoritative PC enrichment persistence helpers</name>
<files>backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<read_first>backend/handler/database/roms_handler.py parent metadata writers and apply_pc_component_metadata_candidate; RomComponentOwnedMedia helper conventions; Phase 12 component ownership tests</read_first>
<action>Write failing database tests for applying normalized IGDB fields to a parent and contained DLC independently. Add explicit helpers that accept normalized values and expected version, update no other parent/sibling component, and return a conflict/no-op result on stale containment failure. Reuse the existing owned-media persistence helper with provider origin and provider media identifier rather than storing provider URLs.</action>
<acceptance_criteria>Parent and DLC persistence are target-contained; no helper accepts a filesystem path or writes component data through a parent record.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
</task>
<task type="auto" tdd="true">
<name>Task 2: Orchestrate scan-time parent and unambiguous DLC import</name>
<files>backend/endpoints/sockets/scan.py, backend/handler/metadata/pc_match_handler.py, backend/handler/filesystem/resources_handler.py, backend/tests/endpoints/sockets/test_scan.py</files>
<read_first>backend/endpoints/sockets/scan.py scan_rom and post-scan parent resource downloads; PcMetadataMatchHandler.find_unique_related_igdb_candidate; resources_handler owned resource writers and Phase 12 provider media validation</read_first>
<action>Write failing scan tests for a Windows parent, one uniquely linked DLC, ambiguous related DLCs, unresolved components and a provider media failure. After normal parent scan persistence, enrich the parent through the new normalized contract. For each DLC call `find_unique_related_igdb_candidate`; only exactly one trusted result may persist metadata and download cover, screenshots and artwork to component-owned storage. Keep all provider downloads behind existing trusted URL/MIME/size validation. Catch and log optional media failures with parent/component identity, while retaining valid metadata and completing scan events. Do not scan-match non-DLC components automatically, fetch media on a read route, or touch an external library path.</action>
<acceptance_criteria>Ambiguous/unresolved DLCs are unchanged, successful DLC media has component-owned provenance, and media failure leaves scanned parent visibility and valid metadata intact.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/endpoints/sockets/test_scan.py tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
</task>
</tasks>
<threat_model><threat><id>T-13-02-01</id><risk>Ambiguous relationship data could enrich the wrong DLC.</risk><mitigation>Require exactly one related IGDB candidate and parent containment before persistence.</mitigation></threat><threat><id>T-13-02-02</id><risk>Provider media could mutate or escape source storage.</risk><mitigation>Reuse owned-resource writers and validated remote media only; never construct a source path from provider data.</mitigation></threat></threat_model>
<verification>Run focused scan/database tests and inspect that optional-media exceptions are logged but do not abort the scan lifecycle.</verification>
