---
phase: 13-pc-igdb-metadata-and-dlc-media
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handler/metadata/igdb_handler.py
  - backend/models/rom.py
  - backend/endpoints/responses/rom.py
  - backend/alembic/versions/<revision>_pc_igdb_structured_metadata.py
  - backend/tests/handler/metadata/test_igdb_handler.py
  - backend/tests/models/test_pc_igdb_metadata.py
autonomous: true
requirements: []
must_haves:
  truths:
    - Parent ROMs and components expose first developer, publishers, themes and a Windows-only PC release date when IGDB supplies them.
    - Existing generic companies and first release date remain compatible.
  artifacts:
    - path: backend/handler/metadata/igdb_handler.py
      provides: Role-aware and Windows-only IGDB normalization.
    - path: backend/models/rom.py
      provides: Durable structured parent and component metadata fields.
  key_links:
    - from: backend/handler/metadata/igdb_handler.py
      to: backend/endpoints/responses/rom.py
      via: Normalized fields persisted and serialized by authoritative schemas.
---

<objective>Establish the durable, backward-compatible IGDB PC metadata contract for parent ROMs and components.</objective>
<context>@CLAUDE.md
@docs/superpowers/specs/2026-09-04-pc-igdb-metadata-and-dlc-media-design.md
@.planning/phases/13-pc-igdb-metadata-and-dlc-media/13-RESEARCH.md</context>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: Normalize role-aware IGDB PC metadata</name>
<files>backend/handler/metadata/igdb_handler.py, backend/tests/handler/metadata/test_igdb_handler.py</files>
<read_first>backend/handler/metadata/igdb_handler.py extract_metadata_from_igdb_rom, build_igdb_rom and GAMES_FIELDS; backend/adapters/services/igdb.py Windows platform registry; existing IGDB cassette tests</read_first>
<action>Write failing fixtures for multiple involved companies, themes, Windows and non-Windows release records, missing fields and empty names. Request company developer/publisher flags, theme names and release-date platform/date fields. Normalize `main_developer` to the first named developer, `publishers` and `themes` to de-duplicated non-empty ordered names, and `pc_release_date` only from the Windows platform record identified by the adapter registry. Preserve existing `companies` and `first_release_date` behavior unchanged.</action>
<acceptance_criteria>Tests prove non-Windows release records never populate `pc_release_date`, role flags control developer/publisher values, and legacy generic metadata remains present.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/handler/metadata/test_igdb_handler.py -q</automated></verify>
</task>
<task type="auto" tdd="true">
<name>Task 2: Persist and serialize equivalent parent/component fields</name>
<files>backend/models/rom.py, backend/endpoints/responses/rom.py, backend/alembic/versions/&lt;revision&gt;_pc_igdb_structured_metadata.py, backend/tests/models/test_pc_igdb_metadata.py</files>
<read_first>backend/models/rom.py RomMetadata and RomComponentMetadata; latest Alembic revisions; backend/endpoints/responses/rom.py RomMetadataSchema and PcComponentMetadataSchema; migration test conventions</read_first>
<action>Add nullable `main_developer`, JSON/list `publishers`, JSON/list `themes`, and nullable timestamp-compatible `pc_release_date` fields to both metadata records. Create one hand-reviewed, MariaDB/MySQL/PostgreSQL-safe migration with symmetric downgrade. Extend detailed schemas with stable list defaults and nullable scalar fields. Do not remove, rename, or repurpose `companies` or `first_release_date`.</action>
<acceptance_criteria>Migration upgrades and downgrades on supported dialects, and parent/component detailed schemas serialize the same structured field names without leaking provider URLs.</acceptance_criteria>
<verify><automated>cd backend && uv run pytest tests/models/test_pc_igdb_metadata.py -q && uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql</automated></verify>
</task>
</tasks>
<threat_model><threat><id>T-13-01-01</id><risk>Provider payload can contain malformed role or release data.</risk><mitigation>Type-check and filter names/dates during normalization; persist only normalized scalar/list values.</mitigation></threat><threat><id>T-13-01-02</id><risk>Migration can break a supported database dialect.</risk><mitigation>Use existing portable JSON/column patterns and test upgrade/downgrade across all required dialects.</mitigation></threat></threat_model>
<verification>Run focused normalization and migration tests, then Trunk formatting and checks on every touched backend file.</verification>
