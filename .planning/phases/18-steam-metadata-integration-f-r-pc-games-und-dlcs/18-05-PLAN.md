---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 05
type: tdd
wave: 5
depends_on: [18-04, 18-07]
files_modified:
  - backend/handler/metadata/pc_match_handler.py
  - backend/adapters/services/steam_types.py
  - backend/endpoints/sockets/scan.py
  - backend/handler/database/roms_handler.py
  - backend/tests/handler/metadata/test_pc_match_handler.py
  - backend/tests/adapters/services/test_steam.py
  - backend/tests/endpoints/sockets/test_scan.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
requirements: [STEAM-03, STEAM-05]
must_haves:
  truths:
    - "Existing IGDB DLC discovery remains the required identity authority for automatic DLC enrichment."
    - "Only one hydrated IGDB DLC identity can apply one high-confidence Steam DLC, and it updates the existing component rather than creating another component."
    - "Bundles, soundtracks, demos, editions, tools, unrelated products, ambiguous results, and mismatched Steam parents leave IGDB component metadata unchanged."
    - "A uniquely safe Steam DLC receives German-first metadata with same-App-ID English field fallback."
  artifacts:
    - path: backend/handler/metadata/pc_match_handler.py
      provides: IGDB-first Steam DLC candidate validation and parent/confidence gate
    - path: backend/endpoints/sockets/scan.py
      provides: Existing-component-only scan enrichment orchestration
    - path: backend/tests/endpoints/sockets/test_scan.py
      provides: Regression proof for safe application and rejection paths
  key_links:
    - from: backend/endpoints/sockets/scan.py
      to: backend/handler/metadata/pc_match_handler.py
      via: fetch_unique_related_igdb_candidate before Steam lookup
      pattern: fetch_unique_related_igdb_candidate
    - from: backend/handler/metadata/pc_match_handler.py
      to: backend/handler/metadata/steam_handler.py
      via: validated DLC lookup and App-ID detail resolution
      pattern: steam
    - from: backend/endpoints/sockets/scan.py
      to: backend/handler/database/roms_handler.py
      via: existing component metadata application
      pattern: apply_pc_component_metadata_candidate
---

<objective>
Add narrow, IGDB-first Steam DLC enrichment and run the final integration verification.

Purpose: improve safe DLC localization without ever allowing folder-name-only Steam matches, unrelated Store products, or a false parent relationship to damage existing component metadata.
Output: type/parent/confidence-gated DLC enrichment and regression tests.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-CONTEXT.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-RESEARCH.md
@backend/endpoints/sockets/scan.py
@backend/handler/metadata/pc_match_handler.py
@backend/tests/endpoints/sockets/test_scan.py

<interfaces>
From backend/handler/metadata/pc_match_handler.py: `fetch_unique_related_igdb_candidate(rom, component)` returns a hydrated candidate only after the current cached IGDB `expansions`/`dlcs` relation is exactly one match.

From backend/endpoints/sockets/scan.py: the current `_enrich_pc_dlc_from_igdb` seam uses an existing `RomComponent` and `apply_pc_component_metadata_candidate`; it must remain the sole automatic component update path.

From backend/handler/metadata/steam_handler.py: `get_rom_by_id` provides German-first normalized details for a known App ID. Search list results require Store-type, confidence, and parent validation before this method is used for application.

From `backend/adapters/services/steam_types.py`: a DLC parent is only `details.fullgame.appid`, accepted as a positive decimal JSON number or numeric string and normalized to `int`; `fullgame.name` is diagnostic only. Missing `fullgame` is parentless, while malformed `fullgame` is never evidence of a parent.
</interfaces>
</context>

<feature>
<name>IGDB-authoritative Steam DLC enrichment</name>
<files>backend/handler/metadata/pc_match_handler.py, backend/endpoints/sockets/scan.py, backend/handler/database/roms_handler.py, backend/tests/handler/metadata/test_pc_match_handler.py, backend/tests/endpoints/sockets/test_scan.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- With no unique hydrated IGDB relationship, no Steam name lookup or component write occurs.
- A Steam candidate must be a valid DLC product, not a bundle, soundtrack, demo, edition, tool, base game, or unrelated product. When details expose a parent, it equals the known parent Steam App ID.
- If no usable parent relation is exposed, automatic application requires exactly one high-confidence Steam result tied to the hydrated IGDB DLC title. Any ambiguity preserves IGDB fields unchanged.
- A valid Steam DLC is resolved by its App ID, merged through the same Steam normalizer as main games, and saved on the same component row. It never creates a component or changes IGDB relationships/structured fields.
</behavior>
<implementation>Write tests first, then extend the narrow PC matcher and socket scan seam rather than building a generic relationship resolver. Preserve the current IGDB `expansions`, `dlcs`, enrichment, and single-candidate guards. Introduce explicit Store-type allow/reject validation, parent-App-ID comparison when available, and a documented high-confidence uniqueness threshold for the parentless case. Reuse the Plan 18-04 shared normalizer and existing component persistence boundary. Treat Steam exceptions, empty results, region misses, malformed relations, and all ambiguity as no enrichment with bounded structured logs. Per D-05 and D-06, do not derive identity from local folder names alone and do not create or reparent components.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
<name>Task 1: Specify safe DLC identity and product rejection cases</name>
<files>backend/adapters/services/steam_types.py, backend/tests/adapters/services/test_steam.py, backend/tests/handler/metadata/test_pc_match_handler.py, backend/tests/endpoints/sockets/test_scan.py</files>
<behavior>
- Existing IGDB expansion/DLC detection and hydration succeeds unchanged before Steam is considered.
- A unique valid Steam DLC with matching parent enriches one existing component; a parentless unique high-confidence match is accepted only when tied to the hydrated IGDB identity.
- Ambiguous candidates, no Steam match, wrong parent, and each non-DLC category preserve prior IGDB component metadata and do not call persistence.
- A valid DLC uses same-App-ID German/English fallback and does not modify related-game relations.
</behavior>
<action>First correct the typed transport contract by replacing the loose `dict[str, int]` `fullgame` stub with `SteamFullGame(appid: int | str, name: NotRequired[str])`. Add fixture tests for valid numeric and numeric-string `fullgame.appid`, absent `fullgame`, and malformed objects/booleans/fractional/non-decimal values. Extend matcher/socket tests with mocked IGDB and Steam providers. Cover bundle, soundtrack, demo, edition, tool, base-game/unrelated type, malformed parent data, parent mismatch, missing parent with ambiguous candidates, and missing parent with exactly one high-confidence candidate. Assert no name lookup occurs before IGDB's unique hydrated candidate exists, and assert all rejection paths avoid component creation and persistence.</action>
<verify><automated>cd backend && uv run pytest tests/handler/metadata/test_pc_match_handler.py tests/endpoints/sockets/test_scan.py -q</automated></verify>
<done>The tests specify every required safe acceptance and refusal path for Steam DLCs while retaining the existing IGDB logic.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement the narrow Steam-after-IGDB DLC enrichment adapter</name>
<files>backend/handler/metadata/pc_match_handler.py, backend/endpoints/sockets/scan.py, backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- The automatic flow calls existing unique IGDB hydration first, validates search results, resolves only the accepted App ID to full Steam details, and applies the common normalizer to the same component.
- Existing IGDB developer/publisher/themes/release and relationship data remain when Steam has missing/non-authoritative values.
- Steam service failures are caught and leave the original IGDB-derived component usable.
</behavior>
<action>Implement small explicit helpers in `PcMetadataMatchHandler` for DLC Store-type classification, `fullgame.appid` extraction, parent validation, and confidence/uniqueness selection. Reject malformed parent objects rather than falling through as if they were a matching parent. Route accepted data through the existing Plan 18-04 normalizer and `apply_pc_component_metadata_candidate`; extend that boundary only for provider-scoped fields that plan already established. Keep IGDB's candidate relationship resolver first and intact. Do not add source-path parsing, a generic relationship system, or direct writes to artwork/relationship fields.</action>
<verify><automated>cd backend && uv run pytest tests/handler/metadata/test_pc_match_handler.py tests/endpoints/sockets/test_scan.py tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
<done>Only identity-safe Steam DLC details enrich existing components; all unsafe outcomes preserve IGDB state unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                 |
| ------------------------------------------ | ------------------------------------------------------------------------------------------- |
| IGDB relationship identity to Steam search | A local DLC name and external Storefront result must not become a false component identity. |
| Steam detail to existing component         | Validated remote content is merged into durable component metadata.                         |

## STRIDE Threat Register

| Threat ID       | Category          | Component            | Disposition | Mitigation Plan                                                                                                                   |
| --------------- | ----------------- | -------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-18-15         | Spoofing          | DLC search match     | mitigate    | Require a single hydrated IGDB identity, type allowlist, parent check where exposed, and unique high-confidence parentless match. |
| T-18-16         | Tampering         | component relations  | mitigate    | Preserve IGDB relationship fields and update only the existing component through the normalizer and optimistic handler.           |
| T-18-17         | Denial of service | DLC enrichment       | mitigate    | Handle Steam errors as empty enrichment and retain existing provider results.                                                     |
| T-18-18         | Repudiation       | automatic enrichment | mitigate    | Emit bounded logs with provider, component/parent App IDs, language, confidence, and refusal/fallback reason.                     |
| </threat_model> |

<verification>
Run focused DLC tests. Plan 18-08 owns complete cross-plan backend, migration, frontend, and upstream-compatibility verification.
</verification>

<success_criteria>
DLC Steam enrichment is IGDB-first, typed, parent-aware, unique, non-destructive, localized, and fully regression-tested.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-05-SUMMARY.md` when done.
</output>
