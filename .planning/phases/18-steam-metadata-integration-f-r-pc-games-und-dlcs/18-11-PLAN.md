---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 11
type: execute
wave: 8
depends_on: [18-02, 18-05, 18-08, 18-09, 18-10]
files_modified:
  - backend/alembic/versions/20260831_add_pc_rom_components.py
  - backend/tools/verify_phase18_backend_tests.sh
  - backend/tools/verify_phase18_postgres_migration.sh
  - .planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [STEAM-02, STEAM-03, STEAM-05]
must_haves:
  truths:
    - "The focused Phase 18 backend suite runs against a unique reachable MariaDB schema on the canonical Compose network, never host 127.0.0.1."
    - "A fresh disposable PostgreSQL upgrade reaches 0126, downgrades one revision, and re-upgrades after the 0115 enum baseline is corrected."
    - "The validation record contains final command, exit status, and output-path evidence for the backend and both migration dialects."
  artifacts:
    - path: backend/tools/verify_phase18_backend_tests.sh
      provides: Canonical-network isolated MariaDB Phase 18 pytest runner
      contains: "docker compose exec"
    - path: backend/alembic/versions/20260831_add_pc_rom_components.py
      provides: PostgreSQL-safe `romcomponentkind` enum creation before 0126
      contains: "romcomponentkind"
    - path: backend/tools/verify_phase18_postgres_migration.sh
      provides: Disposable PostgreSQL upgrade, downgrade, and re-upgrade verifier
  key_links:
    - from: backend/tools/verify_phase18_backend_tests.sh
      to: romm-db-dev
      via: Compose-network DB_HOST passed to pytest container
      pattern: "DB_HOST=romm-db-dev"
    - from: backend/tools/verify_phase18_postgres_migration.sh
      to: backend/alembic/versions/20260831_add_pc_rom_components.py
      via: fresh `alembic upgrade head` runs baseline before revision 0126
      pattern: "alembic upgrade head"
---

<objective>
Make the required Phase 18 backend and migration evidence reproducible in the canonical Linux Compose context.

Purpose: Phase 18 cannot close on host-only database defaults or a PostgreSQL baseline that fails before its Steam migration.
Output: one isolated MariaDB test runner, a PostgreSQL-safe 0115 enum baseline, refreshed disposable verifier, and recorded command evidence.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VERIFICATION.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VALIDATION.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-02-SUMMARY.md
@backend/tests/conftest.py
@backend/pytest.ini
@backend/alembic/versions/20260831_add_pc_rom_components.py
@backend/tools/verify_phase18_postgres_migration.sh

<interfaces>
From backend/tests/conftest.py: pytest creates the configured database and runs `alembic upgrade head` once per session. Its connection settings are read at import time from `ROMM_DB_DRIVER`, `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, and `DB_PASSWD`.

From docker-compose.yml: `romm-dev` and `romm-db-dev` share the canonical Compose network; the database host is `romm-db-dev` inside that network. The plan must not use host `127.0.0.1`, a host port, another database service, or a service restart.

From backend/alembic/versions/20260831_add_pc_rom_components.py: revision `0115_pc_rom_components` creates PostgreSQL enum `romcomponentkind` before creating `rom_components`; its enum lifecycle must not result in duplicate type creation during fresh PostgreSQL migration.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a canonical-network isolated MariaDB runner for the focused Phase 18 suite</name>
  <files>backend/tools/verify_phase18_backend_tests.sh</files>
  <action>Create an executable Bash runner using `set -Eeuo pipefail` and an EXIT trap that removes only its uniquely named Phase-18 MariaDB schema. Derive the canonical Compose project, database username/password, and root password from `docker compose config --environment` without printing credentials. Create the schema through the already-running `romm-db-dev` container, then run the exact selected suite inside the already-running `romm-dev` container with `ROMM_DB_DRIVER=mariadb`, `DB_HOST=romm-db-dev`, `DB_PORT=3306`, the derived credentials, a unique `DB_NAME`, a non-secret test auth key, and a unique `ROMM_BASE_PATH`. The suite is exactly: `tests/adapters/services/test_steam.py tests/handler/metadata/test_steam_handler.py tests/handler/metadata/test_steam_merge.py tests/handler/metadata/test_pc_match_handler.py tests/endpoints/roms/test_pc_metadata.py tests/endpoints/sockets/test_scan.py tests/models/test_pc_igdb_metadata.py tests/handler/database/test_pc_igdb_enrichment.py tests/handler/test_scan_handler.py -q`. Capture the command's exit status before cleanup, drop only the validated generated schema via the same existing MariaDB container, and return that original status. Do not edit `pytest.ini`, use host database settings, create another database service/container, access a NAS mount, or restart any Compose service.</action>
  <verify><automated>backend/tools/verify_phase18_backend_tests.sh</automated></verify>
  <done>The focused Phase 18 tests run through the canonical Linux Compose network against a disposable schema and finish with a final pytest exit status.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the 0115 PostgreSQL enum lifecycle before the Steam migration</name>
  <files>backend/alembic/versions/20260831_add_pc_rom_components.py</files>
  <action>Modify revision `0115_pc_rom_components` so PostgreSQL creates `romcomponentkind` exactly once before `rom_components` is created and does not let table creation attempt a second type creation. Retain the existing enum values, table columns, foreign keys, timestamp defaults, uniqueness constraints, revision ID, and downgrade behavior. Use the repository's SQLAlchemy/Alembic PostgreSQL enum convention, with an explicit type lifecycle that is safe on a fresh database. Do not alter revision ordering, skip baseline migrations, change the Phase 18 `0126_add_steam_metadata` migration, or add a second head.</action>
  <verify><automated>backend/tools/verify_phase18_postgres_migration.sh</automated></verify>
  <done>A fresh disposable PostgreSQL run passes 0115, reaches 0126, downgrades one revision, and re-upgrades to the sole head.</done>
</task>

<task type="auto">
  <name>Task 3: Execute both reproducible evidence paths and record their outputs</name>
  <files>backend/tools/verify_phase18_postgres_migration.sh, .planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VALIDATION.md</files>
  <action>Keep the PostgreSQL verifier's disposable canonical-network contract, but derive Compose values with defaults exactly as Compose resolves them so the verifier works when optional `POSTGRES_USER` is not explicitly present in `.env`. It must retain the bounded `pg_isready` loop, unique container/database names, first-failure-preserving EXIT cleanup, and the exact `heads`, `upgrade head`, `downgrade -1`, `upgrade head` sequence. From `/home/d1sk/romm`, execute `backend/tools/verify_phase18_backend_tests.sh` and `backend/tools/verify_phase18_postgres_migration.sh`; then execute the existing canonical MariaDB migration sequence in `romm-dev`: `docker compose exec -T romm-dev bash -lc 'cd /app/backend && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic heads && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic upgrade head && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic downgrade -1 && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic upgrade head'`. Update `18-VALIDATION.md` with each exact command, final status, and the output paths `backend/tools/verify_phase18_backend_tests.sh`, `backend/tools/verify_phase18_postgres_migration.sh`, and `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VALIDATION.md`. Do not claim a pass without its final zero exit status.</action>
  <verify><automated>backend/tools/verify_phase18_backend_tests.sh && backend/tools/verify_phase18_postgres_migration.sh && docker compose exec -T romm-dev bash -lc 'cd /app/backend && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic heads && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic upgrade head && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic downgrade -1 && ROMM_AUTH_SECRET_KEY=phase18-migration DB_HOST=romm-db-dev DB_PORT=3306 ROMM_DB_DRIVER=mariadb uv run alembic upgrade head'</automated></verify>
  <done>The Phase 18 validation record has reproducible passing backend, MariaDB, and disposable PostgreSQL command evidence, with no host database mismatch or service restart.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                       |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Host shell to Compose test environment  | Host pytest defaults must not choose an unreachable or noncanonical database endpoint.                            |
| Migration verifier to database services | Verification may create only labeled/disposable resources and must not touch shared PostgreSQL or Authentik data. |
| Alembic baseline to Phase 18 revision   | A pre-0126 enum failure can conceal whether the Steam migration is portable.                                      |

## STRIDE Threat Register

| Threat ID       | Category               | Component                | Disposition | Mitigation Plan                                                                                                                  |
| --------------- | ---------------------- | ------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-18-28         | Tampering              | MariaDB test runner      | mitigate    | Generate and validate one unique schema, use the existing Compose MariaDB container, then drop only that schema in EXIT cleanup. |
| T-18-29         | Denial of service      | Compose services         | mitigate    | Reuse running services, do not restart them, and bound PostgreSQL readiness polling.                                             |
| T-18-30         | Tampering              | PostgreSQL enum baseline | mitigate    | Create the named enum exactly once before table creation and prove it through fresh upgrade, downgrade, and re-upgrade.          |
| T-18-31         | Information disclosure | verifier environment     | mitigate    | Derive credentials without printing them and record only commands, statuses, and output paths.                                   |
| </threat_model> |

<verification>
Run the three-command sequence in Task 3 from `/home/d1sk/romm`. The passing evidence is recorded at `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-VALIDATION.md`; implementation summary is `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-11-SUMMARY.md`.
</verification>

<success_criteria>
The Phase 18 backend suite and both migration dialect paths have a reproducible, command-backed final result using only canonical running Compose resources and disposable schemas/containers.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-11-SUMMARY.md` when done.
</output>
