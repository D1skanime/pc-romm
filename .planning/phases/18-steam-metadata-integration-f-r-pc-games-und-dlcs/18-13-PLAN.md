---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 13
type: execute
wave: 1
depends_on: [18-11]
files_modified:
  - backend/endpoints/roms/pc_metadata.py
  - backend/handler/database/roms_handler.py
  - backend/tests/endpoints/roms/test_pc_metadata.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
gap_closure: true
requirements: [STEAM-02, STEAM-04, STEAM-05]
must_haves:
  truths:
    - "A valid parent Steam metadata selection returns 200 and persists only the server-resolved Steam identity and provenance under the submitted current version."
    - "An existing DLC component persists Steam provenance while preserving IGDB fields and allows another existing component to share the same Steam App ID."
    - "A genuinely stale ROM or component selection still returns the existing optimistic-lock conflict rather than bypassing the version guard."
  artifacts:
    - path: backend/endpoints/roms/pc_metadata.py
      provides: Parent Steam selection that retains the optimistic-lock contract after exact-ID re-resolution
    - path: backend/handler/database/roms_handler.py
      provides: Existing-component Steam persistence that preserves structured IGDB metadata
    - path: backend/tests/endpoints/roms/test_pc_metadata.py
      provides: 200-success and stale-409 parent Steam selection regression proof
    - path: backend/tests/handler/database/test_pc_igdb_enrichment.py
      provides: Existing-component Steam persistence regression proof
  key_links:
    - from: backend/endpoints/roms/pc_metadata.py
      to: backend/handler/database/roms_handler.py
      via: apply_pc_metadata_candidate with the request's expected_version
      pattern: "apply_pc_metadata_candidate"
    - from: backend/handler/database/roms_handler.py
      to: RomComponentMetadata
      via: existing selected component update with provider-scoped steam fields
      pattern: "steam_(id|metadata)"
---

<objective>
Resolve the two failing focused-suite product assertions without weakening the Steam safety or optimistic-concurrency contracts.

Purpose: a canonical Compose run currently has 184 passes but reports a 409 for a valid parent selection and no persisted component after an IGDB then Steam update.
Output: diagnosed, test-proven endpoint and component-persistence corrections.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-11-SUMMARY.md
@backend/tools/verify_phase18_backend_tests.sh
@backend/endpoints/roms/pc_metadata.py
@backend/handler/database/roms_handler.py
@backend/tests/endpoints/roms/test_pc_metadata.py
@backend/tests/handler/database/test_pc_igdb_enrichment.py

<interfaces>
`select_pc_metadata_candidate()` re-resolves a submitted Steam App ID and calls `db_rom_handler.apply_pc_metadata_candidate(id, selection.expected_version, candidate_fields)`; a `None` result deliberately maps to HTTP 409.

`apply_pc_component_metadata_candidate(rom_id, component_id, expected_updated_at, provider, data)` selects only an existing component at the supplied optimistic version. Per D-03, `steam_id` is nullable and may be shared. Per D-04, Steam cannot erase IGDB structured fields or become display authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize both observed failures before altering production code</name>
  <files>backend/tests/endpoints/roms/test_pc_metadata.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
  <behavior>
    - A valid current-version parent Steam selection yields HTTP 200, calls `get_rom_by_id` with the submitted App ID and platform, and persists the resolved ID and provenance.
    - Deliberately changing the ROM after candidate retrieval continues to yield HTTP 409.
    - Applying IGDB then Steam to an existing current-version DLC component returns that component, retains its IGDB structured fields, and permits a sibling component to share the Steam App ID.
  </behavior>
  <action>Run the two exact failing tests through `backend/tools/verify_phase18_backend_tests.sh` first and inspect the traceback plus the selected row/version state at each persistence boundary. Strengthen the existing tests so they distinguish a valid-current request from a deliberately stale request and expose the actual component selection/persistence failure. Do not change production code, relax the 200 or non-None assertions, replace the database-backed coverage with mocks, or record a guessed root cause. The RED output and the inspected version/row state determine the subsequent minimal fixes.</action>
  <verify><automated>bash backend/tools/verify_phase18_backend_tests.sh -k 'test_pc_parent_steam_selection_resolves_details_before_guarded_persistence or test_pc_component_steam_provenance_preserves_existing_provider_metadata'</automated></verify>
  <done>Both tests fail for their independently observed product conditions before implementation, while the explicit stale-version assertion still proves 409 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore valid parent Steam selection without weakening optimistic locking</name>
  <files>backend/endpoints/roms/pc_metadata.py, backend/tests/endpoints/roms/test_pc_metadata.py</files>
  <behavior>
    - A current parent ROM selection with a re-resolved valid Steam App ID returns 200 and stores only normalized Steam fields.
    - Invalid, unavailable, or mismatched re-resolution remains 422.
    - A stale expected version remains 409.
  </behavior>
  <action>Use Task 1's trace to correct only the parent selection path that causes a valid expected version to fail. Preserve server-side App-ID re-resolution, `normalize_steam`, candidate-media validation, manual-field protection, and the existing `None` to 409 mapping. Do not accept client-provided Steam detail fields, retry by silently substituting a newer version, or broaden provider access. Implement per D-02 and D-04.</action>
  <verify><automated>bash backend/tools/verify_phase18_backend_tests.sh -k 'test_pc_parent_steam_selection_resolves_details_before_guarded_persistence or test_select_pc_metadata_candidate_rejects_stale_version'</automated></verify>
  <done>A valid re-resolved Steam selection returns 200, while stale, malformed, unavailable, and mismatched requests retain their bounded rejection semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore existing-component Steam provenance persistence</name>
  <files>backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
  <behavior>
    - An existing current-version component persists `steam_id` and bounded `steam_metadata` after an IGDB update.
    - IGDB developer, publishers, themes, release date, name, and summary remain intact after the Steam write.
    - A stale component update returns None and no new component is allocated.
  </behavior>
  <action>Use the Task 1 database trace to correct the smallest existing-component selection or persistence defect. Retain the selected component's optimistic-lock boundary, provider metadata merge, and no-allocation behavior. Keep the provider field allowlist so Steam writes only observational Steam fields and cannot overwrite IGDB-only structured fields. Do not add a global unique constraint or index for component App IDs, per D-03 and D-04.</action>
  <verify><automated>bash backend/tools/verify_phase18_backend_tests.sh -k 'test_pc_component_steam_provenance_preserves_existing_provider_metadata or test_pc_igdb_enrichment_rejects_stale_parent_or_component'</automated></verify>
  <done>The original component test passes, two existing components can share an App ID, structured IGDB data remains present, and stale updates are rejected.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                                                                       |
| ----------------------------------- | ------------------------------------------------------------------------------------------------- |
| client to parent metadata selection | Candidate ID, requested media IDs, and optimistic version are untrusted request data.             |
| Steam provider to persistence       | Storefront details are remote data and must be re-resolved and normalized before a guarded write. |

## STRIDE Threat Register

| Threat ID       | Category  | Component                      | Disposition | Mitigation Plan                                                                                                         |
| --------------- | --------- | ------------------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------- |
| T-18-13-01      | Tampering | parent selection endpoint      | mitigate    | Re-resolve the App ID server-side, validate media IDs, and retain the expected-version write guard.                     |
| T-18-13-02      | Tampering | component metadata persistence | mitigate    | Update only the selected existing component and preserve the explicit Steam allowlist plus IGDB-only structured fields. |
| T-18-SC         | Tampering | package installs               | accept      | No package-manager installation is part of this plan.                                                                   |
| </threat_model> |

<verification>
Run the two diagnostic regressions, their stale-version companions, then `bash backend/tools/verify_phase18_backend_tests.sh`. The full runner must report 186 passed and zero failures before Plan 18-15 records closure evidence.
</verification>

<success_criteria>
The former HTTP 409 and `None` product failures are fixed by their identified causes, not by weakened assertions, skipped tests, or concurrency bypasses; the Compose-network focused suite is ready to pass in full.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-13-SUMMARY.md` when done
</output>
