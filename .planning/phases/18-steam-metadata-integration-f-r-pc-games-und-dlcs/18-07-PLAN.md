---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 07
type: tdd
wave: 4
depends_on: [18-04]
files_modified:
  - backend/handler/scan_handler.py
  - backend/tests/handler/test_scan_handler.py
  - backend/tests/handler/metadata/test_steam_merge.py
autonomous: true
requirements: [STEAM-02, STEAM-04, STEAM-05]
must_haves:
  truths:
    - "A persisted Steam ID refreshes by that same ID after a filename change and never silently rematches."
    - "Only win, linux, and mac can automatically search Steam; excluded PC platforms can use only an explicitly stored ID, and classic ROMs never invoke Steam."
    - "Steam failure is non-fatal and leaves successful IGDB, Moby, and LaunchBox output usable and populated."
  artifacts:
    - path: backend/handler/scan_handler.py
      provides: PC-scoped stored-ID-first Steam scan resolver
    - path: backend/tests/handler/test_scan_handler.py
      provides: Stored-ID, platform-gate, non-PC, and non-fatal provider regression tests
  key_links:
    - from: backend/handler/scan_handler.py
      to: backend/handler/metadata/steam_handler.py
      via: direct `get_rom_by_id` before eligible name lookup
      pattern: get_rom_by_id
    - from: backend/handler/scan_handler.py
      to: backend/handler/metadata/steam_merge.py
      via: shared normalizer before metadata application
      pattern: normalize_steam
---

<objective>
Apply the established Steam merge policy to automatic PC scans without expanding Steam into classic-ROM flows.

Purpose: make re-scrapes enrich an existing game safely while keeping stored App-ID identity stable and remote Storefront faults isolated.
Output: a stored-ID-first scan resolver and regression proof for every platform and failure boundary.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-01-PLAN.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-04-PLAN.md
@backend/handler/scan_handler.py
@backend/handler/metadata/steam_merge.py

<interfaces>
From Plan 18-01: `meta_steam_handler.get_rom_by_id(steam_id, platform_slug=None)` bypasses only automatic-search eligibility; name lookup remains eligible solely for `win`, `linux`, and `mac`.

From Plan 18-04: `steam_merge.normalize_steam(...)` is the only field-application policy for both automatic scan and manually selected candidates. It discards empty/malformed fields and preserves manual state and artwork authority.

From backend/handler/scan_handler.py: provider results are gathered and priority-merged. Steam must enter only the PC-specific branch and its exceptions must degrade to an empty result.
</interfaces>
</context>

<feature>
<name>Stored-ID-first automatic Steam scan</name>
<files>backend/handler/scan_handler.py, backend/tests/handler/test_scan_handler.py, backend/tests/handler/metadata/test_steam_merge.py</files>
<behavior>
- Existing `steam_id=1091500` refreshes details for App ID 1091500 after a filename change and makes no Steam name-search call.
- A Steam-enabled automatic scan name-searches only `win`, `linux`, or `mac`; `dos`, `win3x`, `win9x`, and every non-PC ROM make no Steam call without a stored ID.
- An explicit stored ID on dos/win3x/win9x uses direct detail resolution only, never name search.
- Timeout, malformed/empty Steam data, and no match cannot prevent a usable IGDB/Moby/LaunchBox result or clear prior populated metadata.
</behavior>
<implementation>Write scan mocks before production code. Add a focused PC resolver that reads the existing App ID first, calls `get_rom_by_id` for that exact ID, and only otherwise invokes Steam name matching under automatic platform eligibility. Catch Steam exceptions as an empty provider result, use Plan 18-04 normalization before generic scan state changes, and preserve the existing priority behavior for all non-Steam providers. Carry only `steam_id` and observational `steam_metadata` through existing metadata maps. Per D-02, D-04, and D-06, never replace a stored App ID with a searched one, never locale-research, never clear populated fields, and use bounded structured logs. Do not modify SGDB behavior or add Steam to global classic-provider dispatch.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Characterize stored-ID, platform eligibility, and provider-failure scan boundaries</name>
<files>backend/tests/handler/test_scan_handler.py, backend/tests/handler/metadata/test_steam_merge.py</files>
<behavior>
- Rename plus stored ID invokes only direct lookup for the original App ID.
- Eligible platforms search only when no stored ID exists; excluded and non-PC platforms make no automatic Steam requests.
- Explicit IDs on excluded PC platforms resolve directly without a search.
- Steam timeout leaves mock IGDB/Moby/LaunchBox metadata in the result and existing title, summary, release, and selected artwork intact.
</behavior>
<action>Add focused failing tests using existing handler mocks. Assert direct lookup/search call counts and IDs, not merely final values. Include empty/malformed normalized results and assert that Steam does not clear existing provider or manual state. Keep a non-PC regression test that fails if a Steam handler call is introduced.</action>
<verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/test_scan_handler.py tests/handler/metadata/test_steam_merge.py -q</automated></verify>
<done>Tests make stored identity, platform separation, non-PC isolation, and non-fatal degradation executable requirements.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement the narrow automatic Steam scan resolver</name>
<files>backend/handler/scan_handler.py, backend/tests/handler/test_scan_handler.py</files>
<behavior>
- Automatic Steam output reaches the same Plan 18-04 normalizer used for manual selection.
- Existing ID refresh, search gating, explicit-ID eligibility, and error isolation satisfy the characterization tests without changing SGDB or classic ROM behavior.
</behavior>
<action>Implement the smallest PC-only resolver that satisfies Task 1. Position it before generic provider priority application only where the existing record/platform makes Steam eligible. Use stored ID direct resolution before any name lookup; protect handler calls with the existing asynchronous/concurrent error boundary; feed a resolved mapping to `normalize_steam`; and leave generic values untouched if normalization has no updates. Do not introduce a second merge function, a new game/component creation path, or global Steam calls.</action>
<verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/test_scan_handler.py tests/handler/metadata/test_steam_merge.py -q</automated></verify>
<done>Automatic PC re-scrapes safely use the common Steam policy while excluded and classic flows remain unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                                      |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| Existing ROM identity to Storefront lookup  | An automatic scan can otherwise substitute a searched application for a persisted identity.      |
| Steam provider result to generic scan merge | Unavailable or malformed remote data must not downgrade successful providers or stored metadata. |

## STRIDE Threat Register

| Threat ID | Category          | Component              | Disposition | Mitigation Plan                                                                          |
| --------- | ----------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-18-21   | Spoofing          | automatic App-ID match | mitigate    | Resolve persisted IDs directly first and reject searched replacement of an existing ID.  |
| T-18-22   | Denial of service | Steam scan call        | mitigate    | Bound Steam to eligible PC calls and convert its exceptions to empty provider output.    |
| T-18-23   | Tampering         | scan metadata merge    | mitigate    | Use the single non-empty, manual-safe normalizer and preserve generic provider priority. |

</threat_model>

<verification>
Run the focused scan and normalizer suites. Inspect mock call assertions for zero Steam calls on non-PC and excluded no-ID inputs, plus direct-only calls for excluded explicit-ID inputs.
</verification>

<success_criteria>
Steam automatic scanning is stored-ID-stable, PC-scoped, non-fatal, and uses exactly the same manual-safe merge authority as manual candidate selection.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-07-SUMMARY.md` when done.
</output>
