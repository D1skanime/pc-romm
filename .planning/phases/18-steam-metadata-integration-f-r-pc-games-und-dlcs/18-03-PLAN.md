---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - backend/config/config_manager.py
  - backend/watcher.py
  - backend/endpoints/heartbeat.py
  - backend/endpoints/responses/heartbeat.py
  - frontend/src/v2/components/GameDetails/providers.ts
  - frontend/src/v2/views/Settings/MetadataSources.vue
  - backend/tests/config/test_config_loader.py
  - backend/tests/endpoints/test_heartbeat.py
  - frontend/src/v2/components/GameDetails/providers.test.ts
autonomous: true
requirements: [STEAM-01, STEAM-04, STEAM-05]
must_haves:
  truths:
    - "Steam can be included in metadata priority and availability reporting as Steam, while SteamGridDB continues to appear and behave as SteamGridDB."
    - "The v2 settings and game details surfaces show Steam enablement and App-ID links without a UI redesign."
    - "A forgotten enum, allowlist, heartbeat, or provider display entry causes a focused regression test failure."
  artifacts:
    - path: backend/config/config_manager.py
      provides: steam priority validation slug
    - path: backend/endpoints/heartbeat.py
      provides: Steam handler heartbeat routing and configuration output
    - path: frontend/src/v2/components/GameDetails/providers.ts
      provides: distinct Steam Storefront App-ID link registry entry
  key_links:
    - from: backend/endpoints/heartbeat.py
      to: backend/handler/metadata/__init__.py
      via: meta_steam_handler heartbeat dispatch
      pattern: meta_steam_handler\.heartbeat
    - from: frontend/src/v2/views/Settings/MetadataSources.vue
      to: backend/endpoints/responses/heartbeat.py
      via: STEAM_API_ENABLED configuration field
      pattern: STEAM_API_ENABLED
---

<objective>
Finish provider registration and minimal v2 presentation without conflating Steam Storefront with SteamGridDB.

Purpose: an enabled provider must be discoverable, priority-valid, health-checkable, and clearly labeled before scan paths use it.
Output: complete provider registry wiring and narrow UI/provider-link coverage.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-CONTEXT.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-PATTERNS.md

<interfaces>
From backend/handler/scan_handler.py: `MetadataSource.STEAM.value == "steam"` is the provider slug expected by priority validation and heartbeat routes.

From frontend/src/v2/components/GameDetails/providers.ts: each `Provider` maps one `DetailedRom` provider-ID field to a provider name, visual token, optional logo, and external URL builder.

From frontend/src/v2/views/Settings/MetadataSources.vue: `Source` supports `requiresKey: false`, backend heartbeat status, and a flag-only disabled state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Add central registration and heartbeat regression coverage</name>
<files>backend/tests/config/test_config_loader.py, backend/tests/endpoints/test_heartbeat.py</files>
<behavior>
- `steam` is accepted by scan metadata priority validation and every `MetadataSource` value is represented by the validation allowlist.
- Heartbeat config returns `STEAM_API_ENABLED` separately from `STEAMGRIDDB_API_ENABLED`, and `/heartbeat/metadata/steam` calls only the Steam handler.
- Existing SGDB behavior remains present in the same tests.
</behavior>
<action>Extend current config and heartbeat tests to assert exact enum/allowlist alignment, Steam enablement reporting, Steam handler dispatch, and independent SGDB registration. Do not test a live Storefront request. These tests must expose incomplete enum wiring rather than accepting a partial registry.</action>
<verify><automated>cd backend && uv run pytest tests/config/test_config_loader.py tests/endpoints/test_heartbeat.py -q</automated></verify>
<done>Registration tests fail if Steam is omitted from a central allowlist or if SGDB is renamed, redirected, or otherwise repurposed.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Wire Steam through priority, watcher, and health surfaces</name>
<files>backend/config/config_manager.py, backend/watcher.py, backend/endpoints/heartbeat.py, backend/endpoints/responses/heartbeat.py</files>
<behavior>
- `scan.priority.metadata` can contain `steam` without validation failure.
- The watcher’s availability map and metadata heartbeat support `MetadataSource.STEAM` using the singleton Steam handler.
- Heartbeat config emits a boolean `STEAM_API_ENABLED` without adding a key requirement or altering `STEAMGRIDDB_API_ENABLED`.
</behavior>
<action>Complete all central registration locations using `meta_steam_handler` and the existing `MetadataSource.STEAM` value. Retain the SGDB handler, slug, artwork role, and API-key behavior untouched. Ensure disabled Steam produces ordinary disabled availability rather than a scan exception. Implement D-01 and D-06 only as wiring, without adding a global non-PC lookup path.</action>
<verify><automated>cd backend && uv run pytest tests/config/test_config_loader.py tests/endpoints/test_heartbeat.py -q</automated></verify>
<done>Steam is independently configurable and observable through the same backend contract as other providers.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Add distinct v2 Steam provider display entries</name>
<files>frontend/src/v2/components/GameDetails/providers.ts, frontend/src/v2/views/Settings/MetadataSources.vue, frontend/src/v2/components/GameDetails/providers.test.ts</files>
<behavior>
- The provider registry renders `steam_id` as Steam with `https://store.steampowered.com/app/<id>`.
- The settings provider tile reads `STEAM_API_ENABLED`, has `requiresKey: false`, provides Steam Storefront links, and keeps the SteamGridDB tile unchanged.
</behavior>
<action>Add the smallest v2 registry and settings-tile entries following existing tokens, state handling, and accessibility patterns. Do not redesign the page, reuse SGDB labels/assets for Steam, or add user-visible text outside the established locale conventions. If a new visible string is necessary, load and follow the frontend-i18n skill before editing locale files. Add focused unit coverage for the provider URL and independent names; then regenerate OpenAPI types only if the already planned response schema change changes generated types.</action>
<verify><automated>cd frontend && npm run test -- --run src/v2/components/GameDetails/providers.test.ts && npm run typecheck</automated></verify>
<done>Users can see Steam as a no-key metadata provider and open persisted Steam App IDs, while SteamGridDB still visibly remains a separate artwork source.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                               |
| -------------------------------------- | ------------------------------------------------------------------------- |
| Backend heartbeat contract to v2       | Configuration and provider-state fields cross the generated API boundary. |
| Provider ID to external Storefront URL | Persisted App IDs are transformed into browser navigation targets.        |

## STRIDE Threat Register

| Threat ID       | Category               | Component           | Disposition | Mitigation Plan                                                                                     |
| --------------- | ---------------------- | ------------------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-18-08         | Spoofing               | provider UI         | mitigate    | Bind Steam URL construction only to numeric persisted `steam_id` and label it separately from SGDB. |
| T-18-09         | Tampering              | priority validation | mitigate    | Test enum/allowlist equality and accept only existing source slugs.                                 |
| T-18-10         | Information disclosure | heartbeat           | accept      | The endpoint already exposes provider enabled booleans; Steam adds no secret or credential value.   |
| </threat_model> |

<verification>
Run focused backend tests, then v2 test/typecheck. If schemas change generated types, start the backend in the normal development environment, run `npm run generate`, and repeat typecheck.
</verification>

<success_criteria>
Steam is consistently registered and visibly distinct from SteamGridDB throughout backend configuration/health and the minimal v2 provider surfaces.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-03-SUMMARY.md` when done.
</output>
