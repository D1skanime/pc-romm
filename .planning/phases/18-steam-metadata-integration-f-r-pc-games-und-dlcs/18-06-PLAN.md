---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 06
type: execute
wave: 2
depends_on: [18-02]
files_modified:
  - backend/endpoints/responses/rom.py
  - frontend/src/__generated__/models/PcComponentMetadataSchema.ts
  - frontend/src/__generated__/models/RomSchema.ts
  - frontend/src/__generated__/models/DetailedRomSchema.ts
  - backend/tests/endpoints/test_rom.py
autonomous: true
requirements: [STEAM-02, STEAM-04]
must_haves:
  truths:
    - "API consumers can observe persisted Steam App IDs and bounded provenance where the corresponding ROM or component metadata is already exposed."
    - "Generated TypeScript contracts are regenerated from the backend OpenAPI schema, never manually edited."
  artifacts:
    - path: backend/endpoints/responses/rom.py
      provides: Steam fields on existing ROM and PC component response schemas
    - path: frontend/src/__generated__/models/PcComponentMetadataSchema.ts
      provides: Generated component Steam contract
  key_links:
    - from: backend/endpoints/responses/rom.py
      to: frontend/src/__generated__/models/RomSchema.ts
      via: OpenAPI generation
      pattern: steam_(id|metadata)
---

<objective>
Expose the stable Steam persistence contract through the existing API and generated frontend types.

Purpose: keep API generation separate from model/migration repair, so database portability and generated-client changes can be reviewed and verified independently.
Output: response schemas, regenerated TypeScript models, and focused serialization coverage.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-02-PLAN.md
@backend/models/rom.py
@backend/endpoints/responses/rom.py

<interfaces>
From backend/models/rom.py: `Rom`, `RomFacets`, and `RomComponentMetadata` expose nullable `steam_id` and bounded `steam_metadata` only after Plan 18-02 migration/model work is complete.

From backend/endpoints/responses/rom.py: response schema declarations are the single backend source for the tracked OpenAPI models. Generated files under `frontend/src/__generated__` must be produced by `npm run generate`, not hand-maintained.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Serialize Steam identity and provenance only through existing metadata surfaces</name>
<files>backend/endpoints/responses/rom.py, backend/tests/endpoints/test_rom.py</files>
<behavior>
- Existing ROM and PC component response shapes include nullable `steam_id` and bounded `steam_metadata` only when their model metadata is already exposed.
- The response exposes no synthetic display field derived from `steam_metadata` and preserves existing provider IDs and manual/selected media state.
</behavior>
<action>Write response serialization tests first, then add Steam fields to the existing applicable response schemas. Retain optional/null conventions and omit any new endpoint or mutation path. Per D-03 and D-04, treat `steam_metadata` as observational provenance, never as an uncontrolled alternate display source.</action>
<verify><automated>cd backend &amp;&amp; uv run pytest tests/endpoints/test_rom.py -q</automated></verify>
<done>Existing API consumers can observe Steam identity/provenance without a new Steam-specific API or changed display-state authority.</done>
</task>

<task type="auto">
<name>Task 2: Regenerate and typecheck the frontend API contract</name>
<files>frontend/src/__generated__/models/PcComponentMetadataSchema.ts, frontend/src/__generated__/models/RomSchema.ts, frontend/src/__generated__/models/DetailedRomSchema.ts</files>
<action>Start the canonical backend only as required by the repository generator, run `cd frontend &amp;&amp; npm run generate`, and review the generated diff to confirm it contains the response-schema Steam fields and no hand edits or unrelated contract churn. Then run TypeScript typechecking. If generation cannot contact the canonical backend, report that precise service blocker and do not manually modify generated files.</action>
<verify><automated>cd frontend &amp;&amp; npm run generate &amp;&amp; npm run typecheck</automated></verify>
<done>Tracked generated models faithfully expose the backend Steam fields and the frontend typecheck passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                       |
| ----------------------------------------- | ----------------------------------------------------------------- |
| ORM metadata to API contract              | Persisted provider provenance becomes client-observable metadata. |
| OpenAPI generator to tracked client types | Generated code can drift from the authoritative response schemas. |

## STRIDE Threat Register

| Threat ID | Category    | Component        | Disposition | Mitigation Plan                                                                          |
| --------- | ----------- | ---------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-18-19   | Tampering   | response schema  | mitigate    | Serialize only declared nullable provider fields and test no synthetic display fallback. |
| T-18-20   | Repudiation | generated client | mitigate    | Regenerate from OpenAPI and review the tracked diff instead of hand editing.             |

</threat_model>

<verification>
Run focused response tests followed by `npm run generate` and `npm run typecheck`. Do not let generated contract work alter the migration graph or provider merge authority.
</verification>

<success_criteria>
The stable persistence contract is visible through existing API models and matching generated frontend types, with provenance remaining observational.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-06-SUMMARY.md` when done.
</output>
