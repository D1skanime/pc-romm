---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 09
type: tdd
wave: 4
depends_on: [18-02, 18-04]
files_modified:
  - backend/handler/database/roms_handler.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
requirements: [STEAM-02, STEAM-04]
must_haves:
  truths:
    - "An existing component can retain Steam identity/provenance without a new component or global App-ID uniqueness constraint."
    - "Steam provenance persistence cannot erase IGDB structured fields or become uncontrolled display state."
  artifacts:
    - path: backend/handler/database/roms_handler.py
      provides: Existing-component-only Steam field allowlist
    - path: backend/tests/handler/database/test_pc_igdb_enrichment.py
      provides: Component Steam persistence and IGDB preservation tests
  key_links:
    - from: backend/handler/database/roms_handler.py
      to: backend/models/rom.py
      via: Explicit existing-component metadata allowlist
      pattern: steam_id
---

<objective>
Admit Steam provenance through the existing PC component persistence boundary.

Purpose: a validated Steam match must enrich the selected component rather than create a duplicate or displace IGDB structures.
Output: tested allowlist persistence for Steam App IDs and observational metadata.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-02-PLAN.md
@backend/handler/database/roms_handler.py
@backend/tests/handler/database/test_pc_igdb_enrichment.py
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Characterize existing-component Steam persistence safeguards</name>
<files>backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- Two distinct existing component rows may use the same nullable Steam App ID.
- An existing component accepts `steam_id` and bounded `steam_metadata` while preserving IGDB main developer, publishers, themes, release, relationships, and existing provider fields.
- A Steam candidate does not create a component and cannot use `steam_metadata` as generic display state.
</behavior>
<action>Add database-backed failing tests at the existing candidate-application boundary. Use two component fixtures with the same App ID and assertions on the original structured IGDB values before and after the Steam candidate. Assert component count is unchanged and only explicit provider-scoped fields are accepted.</action>
<verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
<done>Tests establish no-duplicate, no-unique-constraint, and no-IGDB-erasure guarantees before persistence code changes.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Extend the existing component metadata allowlist</name>
<files>backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- The existing selected component receives Steam App ID/provenance and all Task 1 safeguards pass.
- Lazy metadata-row creation remains limited to an already selected component and optimistic `updated_at` behavior remains unchanged.
</behavior>
<action>Add `steam_id` and `steam_metadata` to the explicit component candidate allowlist, preserving non-empty provider-scoped state and existing persistence semantics. Per D-03 and D-04, do not add a global constraint/index, alternate component creation path, or display merge policy. Plan 18-04 remains the only owner of field-level display authority.</action>
<verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/database/test_pc_igdb_enrichment.py tests/models/test_pc_igdb_metadata.py -q</automated></verify>
<done>Existing component metadata safely persists Steam provenance with no uniqueness, duplication, or IGDB structured-field regression.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                                           |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Validated external candidate to durable component metadata | Provider data can otherwise create a duplicate or overwrite authoritative structures. |

## STRIDE Threat Register

| Threat ID | Category  | Component             | Disposition | Mitigation Plan                                                                             |
| --------- | --------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------- |
| T-18-26   | Tampering | component persistence | mitigate    | Keep an explicit allowlist, existing-row boundary, and structured-field preservation tests. |
| T-18-27   | Spoofing  | component identity    | mitigate    | Permit repeated nullable IDs and prohibit Steam-driven component allocation.                |

</threat_model>

<verification>
Run the persistence subset and inspect the test fixture count to prove a Steam update cannot create another component.
</verification>

<success_criteria>
Steam provenance is durable only on an existing component and cannot displace IGDB structures, manual state, or component identity.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-09-SUMMARY.md` when done.
</output>
