---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 14
type: execute
wave: 1
depends_on: [18-11]
files_modified:
  - backend/alembic/versions/0118_pc_igdb_structured_metadata.py
  - backend/tests/models/test_pc_igdb_metadata.py
  - backend/tests/tools/test_verify_phase18_evidence.py
autonomous: true
gap_closure: true
requirements: [STEAM-02, STEAM-05]
must_haves:
  truths:
    - "A fresh PostgreSQL database applies revision 0118 without changing the existing roms_metadata.player_count view-column type."
    - "The 0118 metadata view remains portable on MariaDB and includes its PC structured fields on PostgreSQL."
    - "The disposable PostgreSQL verifier can complete heads, upgrade, downgrade one revision, and re-upgrade through the Phase 18 migration."
  artifacts:
    - path: backend/alembic/versions/0118_pc_igdb_structured_metadata.py
      provides: Type-stable PostgreSQL roms_metadata view replacement
    - path: backend/tests/models/test_pc_igdb_metadata.py
      provides: Migration SQL contract for PostgreSQL player_count compatibility
    - path: backend/tests/tools/test_verify_phase18_evidence.py
      provides: Static verifier contract coverage for the whole migration cycle
  key_links:
    - from: backend/alembic/versions/0118_pc_igdb_structured_metadata.py
      to: roms_metadata.player_count
      via: PostgreSQL type-stable view projection during CREATE OR REPLACE VIEW
      pattern: "generated_player_count::text AS player_count"
    - from: backend/tools/verify_phase18_postgres_migration.sh
      to: backend/alembic/versions/0126_add_steam_metadata.py
      via: fresh upgrade, downgrade, and re-upgrade
      pattern: "run_alembic (upgrade head|downgrade -1)"
---

<objective>
Repair the historical 0118 PostgreSQL view replacement so the disposable Phase 18 migration verifier can complete its full cycle without sacrificing MariaDB portability.

Purpose: PostgreSQL rejects revision 0118 because `CREATE OR REPLACE VIEW` would change `roms_metadata.player_count` from legacy text to varchar(100), before the Steam migration executes.
Output: a characterized and type-stable 0118 migration plus verifier-contract coverage.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-11-SUMMARY.md
@backend/alembic/versions/0098_generated_metadata_columns.py
@backend/alembic/versions/0118_pc_igdb_structured_metadata.py
@backend/tools/verify_phase18_postgres_migration.sh
@backend/tests/models/test_pc_igdb_metadata.py
@backend/tests/tools/test_verify_phase18_evidence.py

<interfaces>
Revision 0098 already preserves PostgreSQL `roms_metadata.player_count` compatibility by projecting `generated_player_count::text AS player_count`. Revision 0118 replaces the same view and currently projects that column without the PostgreSQL-compatible cast.

`verify_phase18_postgres_migration.sh` uses a disposable PostgreSQL 16 container on the canonical Compose network and executes `heads`, `upgrade head`, `downgrade -1`, and `upgrade head`; it never connects to a host port or shared database.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize the 0118 view-type failure and its portable invariant</name>
  <files>backend/tests/models/test_pc_igdb_metadata.py, backend/tests/tools/test_verify_phase18_evidence.py</files>
  <behavior>
    - The PostgreSQL SQL emitted by 0118 keeps `player_count` as text while retaining the structured PC field projections.
    - The MariaDB SQL retains its existing JSON and numeric PC-field expressions and does not receive PostgreSQL syntax.
    - The verifier retains all four migration-cycle commands and isolated-container cleanup behavior.
  </behavior>
  <action>Before changing 0118, reproduce the first PostgreSQL failure with the disposable verifier and inspect the existing 0098 view projection and the actual PostgreSQL error. Add failing source-level tests that pin the required type-stable PostgreSQL projection and the existing MariaDB branch. Do not drop/recreate the view, use CASCADE, change unrelated prior migrations, or assume the cast before comparing the prior view definition and server error. Preserve the full-cycle verifier contract.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_pc_igdb_metadata.py tests/tools/test_verify_phase18_evidence.py -q</automated></verify>
  <done>The RED tests name the actual incompatible projection and the expected dialect-specific invariant, without weakening migration verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make revision 0118 type-stable on PostgreSQL and portable on MariaDB</name>
  <files>backend/alembic/versions/0118_pc_igdb_structured_metadata.py, backend/tests/models/test_pc_igdb_metadata.py, backend/tests/tools/test_verify_phase18_evidence.py</files>
  <behavior>
    - PostgreSQL `CREATE OR REPLACE VIEW roms_metadata` preserves legacy text type for `player_count` in both upgrade and downgrade view definitions.
    - PostgreSQL still exposes main_developer, publishers, themes, and numeric pc_release_date.
    - MariaDB keeps valid non-PostgreSQL SQL and the same selected field aliases.
  </behavior>
  <action>Implement the smallest correction justified by Task 1, following the established 0098 PostgreSQL type-stability convention for the same view column. Keep `CREATE OR REPLACE VIEW`, the current revision IDs, PC column add/drop ordering, JSON semantics, and dialect branch separation. Do not use destructive view drops, CASCADE, data rewriting, a new migration head, or a PostgreSQL-only workaround that breaks MariaDB. Implement the portable cross-dialect migration requirement from D-03 and preserve bounded failure evidence from D-06.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_pc_igdb_metadata.py tests/tools/test_verify_phase18_evidence.py -q</automated></verify>
  <done>The targeted migration contracts pass and 0118's PostgreSQL view replacement cannot change the legacy player_count type.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                    |
| --------------------------------------- | ---------------------------------------------------------------------------------------------- |
| historical migration to database schema | Revision SQL changes persistent database objects across PostgreSQL and MariaDB.                |
| verifier to disposable database         | The verifier must isolate credentials, network access, and cleanup to its generated container. |

## STRIDE Threat Register

| Threat ID       | Category          | Component             | Disposition | Mitigation Plan                                                                                                  |
| --------------- | ----------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-18-14-01      | Tampering         | 0118 view replacement | mitigate    | Preserve the existing view column type through a dialect-specific projection instead of destructive replacement. |
| T-18-14-02      | Denial of service | migration verifier    | mitigate    | Keep the unique disposable container, bounded readiness loop, first-failure capture, and cleanup path.           |
| T-18-SC         | Tampering         | package installs      | accept      | No package-manager installation is part of this plan.                                                            |
| </threat_model> |

<verification>
Run the model and verifier-contract tests, then let Plan 18-15 execute the isolated PostgreSQL verifier's complete heads, upgrade, downgrade, and re-upgrade cycle against PostgreSQL 16.
</verification>

<success_criteria>
Revision 0118 retains its schema intent and portable SQL while PostgreSQL can replace the existing view without a type-change error, unblocking Phase 18 migration evidence.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-14-SUMMARY.md` when done
</output>
