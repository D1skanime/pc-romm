---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models/rom.py
  - backend/alembic/versions/0119_add_steam_metadata.py
  - backend/alembic/versions/0126_add_steam_metadata.py
  - backend/tools/verify_phase18_postgres_migration.sh
  - backend/tests/models/test_pc_igdb_metadata.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
requirements: [STEAM-02, STEAM-04, STEAM-05]
must_haves:
  truths:
    - "A PC ROM and a PC component can retain a nullable Steam App ID and observational Steam metadata alongside all existing provider data."
    - "The database migration graph has one current head and Steam persistence is reversible."
  artifacts:
    - path: backend/alembic/versions/0126_add_steam_metadata.py
      provides: Non-conflicting portable Steam persistence migration
    - path: backend/models/rom.py
      provides: Rom, RomFacets, and RomComponentMetadata Steam fields and source mappings
    - path: backend/tools/verify_phase18_postgres_migration.sh
      provides: Disposable canonical-network PostgreSQL migration verifier with guaranteed cleanup
  key_links:
    - from: backend/alembic/versions/0126_add_steam_metadata.py
      to: backend/models/rom.py
      via: matching nullable columns on roms, roms_facets, and rom_component_metadata
      pattern: steam_(id|metadata)
---

<objective>
Make the Steam persistence contract portable and repair the incomplete migration graph before scan code relies on it.

Purpose: retain provider identity and provenance safely without duplicating games, imposing incorrect uniqueness, or leaving Alembic with competing heads.
Output: validated model/schema persistence and a single-head migration path.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-CONTEXT.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-PATTERNS.md
@backend/alembic/versions/0118_pc_igdb_structured_metadata.py
@backend/alembic/versions/0125_pc_component_manifest_members_missing_from_fs.py

<interfaces>
From backend/models/rom.py: `Rom`, `RomFacets`, and `RomComponentMetadata` already follow nullable provider-ID plus JSON metadata conventions. `METADATA_SOURCE_COLUMNS` and `METADATA_SOURCE_FACET_COLUMNS` bind provider slugs to model IDs.

From backend/handler/database/roms_handler.py: `apply_pc_component_metadata_candidate` is the explicit persistence boundary and currently uses an allowlist, then records provider metadata and IGDB structured fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Characterize Steam persistence and migration graph requirements</name>
<files>backend/tests/models/test_pc_igdb_metadata.py</files>
<behavior>
- Rom, facets, and component metadata retain nullable `steam_id`; Rom and component metadata retain JSON `steam_metadata`.
- The Steam migration has one downstream revision after the actual current head and its upgrade/downgrade adds/drops every required column.
</behavior>
<action>Extend existing PC metadata tests with model and migration-source assertions. Assert nullable Steam fields on ROM, facets, and component metadata and assert the plan's migration uses the current download-chain head, not the stale parallel `0118` head. Plan 18-09 owns candidate allowlist and existing-component preservation tests so this migration plan remains portable and independently executable.</action>
<verify><automated>cd backend && uv run pytest tests/models/test_pc_igdb_metadata.py -q</automated></verify>
<done>Tests fail against the conflicting scaffold and define the final portable persistence contract.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Replace the conflicting Steam migration with a single-head portable revision</name>
<files>backend/alembic/versions/0119_add_steam_metadata.py, backend/alembic/versions/0126_add_steam_metadata.py, backend/models/rom.py</files>
<behavior>
- Alembic discovers exactly one head after the obsolete uncommitted `0119_add_steam_metadata` collision is removed or renamed.
- The final revision descends from `0125_pc_component_manifest_members_missing_from_fs`, creates nullable columns on `roms`, `roms_facets`, and `rom_component_metadata`, and downgrades them in reverse order.
</behavior>
<action>Do not retain a duplicate revision ID. Replace the uncommitted `0119_add_steam_metadata.py` scaffold with `0126_add_steam_metadata.py`, downstream of the actual current head, and update its revision metadata accordingly. Use repository cross-dialect migration conventions and nullable `sa.JSON()` columns. Align model columns and facet source maps with the migration. Per D-03, do not introduce a component App-ID unique constraint, global index, generic display fields sourced from `steam_metadata`, or a second ROM/component creation path. API response schemas and generated frontend contracts are deliberately isolated in Plan 18-06 after this model/migration contract is stable.</action>
<verify><automated>cd backend &amp;&amp; uv run alembic heads &amp;&amp; uv run pytest tests/models/test_pc_igdb_metadata.py -q</automated></verify>
<done>Alembic reports one head and the reversible migration makes model fields physically representable without replacing existing metadata.</done>
</task>

<task type="auto">
<name>Task 3: Add an isolated PostgreSQL migration verifier on the canonical Compose network</name>
<files>backend/tools/verify_phase18_postgres_migration.sh</files>
<action>Create an executable Bash verifier for use from the canonical checkout. It must use `set -Eeuo pipefail`, derive `POSTGRES_USER` and `POSTGRES_PASSWORD` from `docker compose config --environment` (fail closed if either is absent), derive the canonical Compose network rather than assuming a host port, and start a uniquely named disposable `postgres:16-alpine` container on that network with an equally unique temporary database. Wait with a bounded loop for `pg_isready` before invoking Alembic from `docker compose exec -T romm-dev`; pass only the derived connection settings plus `ROMM_DB_DRIVER=postgresql` and a non-secret test auth key. Run `alembic heads`, upgrade, downgrade one revision, and re-upgrade as separately guarded commands. Install an EXIT trap before creating the container which records the first failing status, removes the disposable container, clears the trap, and exits with that original status. Do not use the shared `romm-postgres-dev`, Authentik database, fixed credentials, host ports, semicolon-chained commands, or cleanup that masks the first failed command.</action>
<verify><automated>backend/tools/verify_phase18_postgres_migration.sh</automated></verify>
<done>The PostgreSQL migration proof starts a ready disposable database on the canonical network, always removes it, and returns the first migration or readiness failure unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                     |
| ------------------------------------------- | ------------------------------------------------------------------------------- |
| Alembic revision graph to production schema | A malformed revision can strand deployments or apply incomplete schema changes. |

## STRIDE Threat Register

| Threat ID       | Category          | Component     | Disposition | Mitigation Plan                                                                                                  |
| --------------- | ----------------- | ------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-18-05         | Denial of service | Alembic graph | mitigate    | Remove the duplicate `0119` branch and require `alembic heads`, upgrade, downgrade, and re-upgrade verification. |
| </threat_model> |

<verification>
Run the persistence subset. For MariaDB, use the canonical `romm-dev` to `romm-db-dev` network path. Run the Task 3 verifier for PostgreSQL; it must create a fresh disposable server/container and database, wait for readiness, exercise upgrade/downgrade/re-upgrade, and clean up after success or failure without masking the original failure. The executor must record database-service failures separately from Alembic graph failures and must not rewrite unrelated migrations.
</verification>

<success_criteria>
Steam fields are represented consistently in model and database contracts, and the repository has a single valid Alembic head.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-02-SUMMARY.md` when done.
</output>
