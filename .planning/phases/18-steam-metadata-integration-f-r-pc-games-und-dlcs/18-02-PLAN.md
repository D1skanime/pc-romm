---
phase: 18-steam-metadata-integration-f-r-pc-games-und-dlcs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models/rom.py
  - backend/alembic/versions/0119_add_steam_metadata.py
  - backend/alembic/versions/0126_add_steam_metadata.py
  - backend/endpoints/responses/rom.py
  - backend/handler/database/roms_handler.py
  - backend/tests/models/test_pc_igdb_metadata.py
  - backend/tests/handler/database/test_pc_igdb_enrichment.py
autonomous: true
requirements: [STEAM-02, STEAM-04, STEAM-05]
must_haves:
  truths:
    - "A PC ROM and a PC component can retain a nullable Steam App ID and observational Steam metadata alongside all existing provider data."
    - "Two components can legitimately reference the same Steam App ID."
    - "The database migration graph has one current head and Steam persistence is reversible."
  artifacts:
    - path: backend/alembic/versions/0126_add_steam_metadata.py
      provides: Non-conflicting portable Steam persistence migration
    - path: backend/models/rom.py
      provides: Rom, RomFacets, and RomComponentMetadata Steam fields and source mappings
    - path: backend/endpoints/responses/rom.py
      provides: Serialized Steam ID and provenance fields where model metadata is exposed
  key_links:
    - from: backend/alembic/versions/0126_add_steam_metadata.py
      to: backend/models/rom.py
      via: matching nullable columns on roms, roms_facets, and rom_component_metadata
      pattern: steam_(id|metadata)
    - from: backend/handler/database/roms_handler.py
      to: backend/models/rom.py
      via: explicit component metadata allowlist
      pattern: steam_id
---

<objective>
Make the Steam persistence contract portable and repair the incomplete migration graph before scan code relies on it.

Purpose: retain provider identity and provenance safely without duplicating games, imposing incorrect uniqueness, or leaving Alembic with competing heads.
Output: validated model/schema persistence and a single-head migration path.
</objective>

<execution_context>
@/home/d1sk/.codex/get-shit-done/workflows/execute-plan.md
@/home/d1sk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-CONTEXT.md
@.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-PATTERNS.md
@backend/alembic/versions/0118_pc_igdb_structured_metadata.py
@backend/alembic/versions/0125_pc_component_manifest_members_missing_from_fs.py

<interfaces>
From backend/models/rom.py: `Rom`, `RomFacets`, and `RomComponentMetadata` already follow nullable provider-ID plus JSON metadata conventions. `METADATA_SOURCE_COLUMNS` and `METADATA_SOURCE_FACET_COLUMNS` bind provider slugs to model IDs.

From backend/handler/database/roms_handler.py: `apply_pc_component_metadata_candidate` is the explicit persistence boundary and currently uses an allowlist, then records provider metadata and IGDB structured fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Characterize Steam persistence and migration graph requirements</name>
<files>backend/tests/models/test_pc_igdb_metadata.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- Rom, facets, and component metadata retain nullable `steam_id`; Rom and component metadata retain JSON `steam_metadata`.
- Component IDs are not globally unique by Steam App ID.
- Component persistence accepts Steam fields without dropping IGDB `main_developer`, publishers, themes, release, or existing provider fields.
- The Steam migration has one downstream revision after the actual current head and its upgrade/downgrade adds/drops every required column.
</behavior>
<action>Extend existing PC metadata tests with database-backed and migration-source assertions. Include a direct test that two distinct component rows can share one Steam App ID, plus a preservation test that a Steam update cannot erase existing IGDB-only structures. Assert the plan's migration uses the current download-chain head, not the stale parallel `0118` head.</action>
<verify><automated>cd backend && uv run pytest tests/models/test_pc_igdb_metadata.py tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
<done>Tests fail against the conflicting scaffold and define the final portable persistence contract.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Replace the conflicting Steam migration with a single-head portable revision</name>
<files>backend/alembic/versions/0119_add_steam_metadata.py, backend/alembic/versions/0126_add_steam_metadata.py, backend/models/rom.py, backend/endpoints/responses/rom.py</files>
<behavior>
- Alembic discovers exactly one head after the obsolete uncommitted `0119_add_steam_metadata` collision is removed or renamed.
- The final revision descends from `0125_pc_component_manifest_members_missing_from_fs`, creates nullable columns on `roms`, `roms_facets`, and `rom_component_metadata`, and downgrades them in reverse order.
- API response schemas expose Steam fields only where the corresponding model is exposed.
</behavior>
<action>Do not retain a duplicate revision ID. Replace the uncommitted `0119_add_steam_metadata.py` scaffold with `0126_add_steam_metadata.py`, downstream of the actual current head, and update its revision metadata accordingly. Use repository cross-dialect migration conventions and nullable `sa.JSON()` columns. Align model columns, facet source maps, and response schemas with the migration. Per D-03, do not introduce a component App-ID unique constraint, global index, generic display fields sourced from `steam_metadata`, or a second ROM/component creation path.</action>
<verify><automated>cd backend && ROMM_AUTH_SECRET_KEY=planning-only-secret uv run alembic heads && ROMM_AUTH_SECRET_KEY=planning-only-secret uv run alembic upgrade head && ROMM_AUTH_SECRET_KEY=planning-only-secret uv run alembic downgrade -1 && ROMM_AUTH_SECRET_KEY=planning-only-secret uv run alembic upgrade head</automated></verify>
<done>Alembic reports one head and the reversible migration makes model and response fields physically representable without replacing existing metadata.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Admit Steam fields through the component persistence allowlist</name>
<files>backend/handler/database/roms_handler.py, backend/tests/handler/database/test_pc_igdb_enrichment.py</files>
<behavior>
- A component Steam candidate persists `steam_id` and observational `steam_metadata` on its existing component metadata row.
- Existing IGDB structured values remain intact when Steam lacks those values or has no authority over them.
- No metadata write allocates a new component solely because Steam returned a result.
</behavior>
<action>Add Steam fields to the explicit existing-component candidate allowlist and preserve existing non-empty provider-scoped state. Keep lazy metadata-row creation only for an already selected component, retain optimistic `updated_at` behavior, and do not treat observational `steam_metadata` as an alternate display-state source. Per D-03 and D-04, only later shared merge code may decide display values.</action>
<verify><automated>cd backend && uv run pytest tests/models/test_pc_igdb_metadata.py tests/handler/database/test_pc_igdb_enrichment.py -q</automated></verify>
<done>Existing components can persist Steam provenance safely, with no uniqueness constraint or IGDB structured-field regression.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                     |
| ------------------------------------------- | ------------------------------------------------------------------------------- |
| Alembic revision graph to production schema | A malformed revision can strand deployments or apply incomplete schema changes. |
| Provider candidate to component persistence | External-derived fields become durable data on a user-visible component.        |

## STRIDE Threat Register

| Threat ID       | Category               | Component             | Disposition | Mitigation Plan                                                                                                            |
| --------------- | ---------------------- | --------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------- |
| T-18-05         | Denial of service      | Alembic graph         | mitigate    | Remove the duplicate `0119` branch and require `alembic heads`, upgrade, downgrade, and re-upgrade verification.           |
| T-18-06         | Tampering              | component persistence | mitigate    | Retain explicit allowlists and optimistic component version checks.                                                        |
| T-18-07         | Elevation of privilege | metadata data model   | accept      | No new endpoint or authorization scope is introduced; existing protected candidate endpoints remain the mutation boundary. |
| </threat_model> |

<verification>
Run the persistence subset and the migration graph commands with an ephemeral local secret. The executor must record database-service failures separately from Alembic graph failures and must not rewrite unrelated migrations.
</verification>

<success_criteria>
Steam fields are represented consistently in model, database, and response contracts, and the repository has a single valid Alembic head.
</success_criteria>

<output>
Create `.planning/phases/18-steam-metadata-integration-f-r-pc-games-und-dlcs/18-02-SUMMARY.md` when done.
</output>
