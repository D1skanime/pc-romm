---
phase: 08-bounded-v1-removal
plan: 04
wave: 4
depends_on: [08-03]
requirements: [V2-03, V2-04, V2-05]
autonomous: true
---

# Plan 08-04: Targeted V2 Regression Gate

<objective>Lock the v2-only cutover with small automated regression coverage, without Phase 9 browser smoke work.</objective>
<tasks>
<task type="auto"><name>Finalize v2 route and boot regression coverage</name><action>Expand the route inventory test to assert real v2 components or declared redirects and prohibit deleted v1 imports. Add mocked boot/import coverage for auth, pairing, theme, router, shared stores, API services, generated types, and overlays.</action><verify>Run focused Vitest suites and typecheck.</verify><done>Targeted tests catch a removed v1 dependency, a missing v2 route target, or an invalid redirect.</done></task>
<task type="auto"><name>Run phase-scoped static verification</name><action>Run formatter/lint checks on changed frontend paths, typecheck with the proven Node heap setting, and collect the focused regression commands as phase evidence. Do not start browser smoke or live NAS validation.</action><verify>Focused checks exit successfully and git diff --check is clean.</verify><done>V1 removal is isolated from storage-domain changes and ready for Phase 9 live validation.</done></task>
</tasks>
