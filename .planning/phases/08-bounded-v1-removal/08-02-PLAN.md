---
phase: 08-bounded-v1-removal
plan: 02
wave: 2
depends_on: [08-01]
requirements: [V2-01, V2-03]
autonomous: true
---

# Plan 08-02: V2-only Boot and Preference Cleanup

<objective>Remove the dual-interface boot path and retain only neutral user preferences.</objective>
<tasks>
<task type="auto"><name>Make v2 the only application shell</name><action>Remove uiVersion switching, named-view dual rendering, v1 fallback selection, and NotReady handoff. Make the root layout, router, authentication, pairing, theme, overlays, and global initialization boot directly through v2.</action><verify>Run focused v2 boot/import suite and typecheck.</verify><done>The application has exactly one v2 boot path and no hidden v1 switch.</done></task>
<task type="auto"><name>Delete v1 UI-mode persistence safely</name><action>Remove frontend and backend uiVersion preference fields, APIs, migrations or serializers where applicable. Preserve theme, language, and other neutral preferences. Old stored v1 keys are ignored or removed silently.</action><verify>Run focused settings/contract tests and typecheck.</verify><done>No persisted or runtime uiVersion compatibility behavior remains.</done></task>
</tasks>
