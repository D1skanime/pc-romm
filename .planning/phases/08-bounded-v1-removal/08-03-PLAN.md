---
phase: 08-bounded-v1-removal
plan: 03
wave: 3
depends_on: [08-02]
requirements: [V2-02, V2-04]
autonomous: true
---

# Plan 08-03: Remove Frozen V1 Surface and Route Fallbacks

<objective>Physically remove frozen v1 frontend source and replace old links with deliberate v2 redirects or removals.</objective>
<tasks>
<task type="auto"><name>Install deliberate route outcomes</name><action>Implement the approved route inventory. Retain real v2 routes, redirect legacy console and special links to their nearest functional v2 destinations, and make intentionally removed routes fail explicitly. Do not leave placeholders or NotReady behavior.</action><verify>Run the route-inventory test and router-focused tests.</verify><done>Every supported route resolves to v2 or an explicit redirect/removal outcome.</done></task>
<task type="auto"><name>Delete v1 source and compatibility artifacts</name><action>Delete frozen v1 views, components, layouts, console surfaces, banners, fallback code, and obsolete v1-only tests after dependencies are moved. Remove dead imports, assets, styles, and exports exposed by deletion.</action><verify>Run git grep guards for removed paths, focused boot/import tests, typecheck, and git diff --check.</verify><done>No shipped v1 surface or v1 compatibility path remains.</done></task>
</tasks>
