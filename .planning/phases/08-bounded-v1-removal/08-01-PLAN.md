---
phase: 08-bounded-v1-removal
plan: 01
wave: 1
depends_on: []
requirements: [V2-02, V2-03]
autonomous: true
---

# Plan 08-01: Inventory and Shared Dependency Extraction

<objective>Produce the executable route and dependency inventory, then move every v2-required module out of v1-owned paths before deletion.</objective>
<tasks>
<task type="auto"><name>Inventory legacy frontend ownership</name><action>Trace router entries, named views, uiVersion gates, layouts, console routes, fallback components, imports from v1 directories, and persisted UI fields. Create a machine-readable test fixture or route inventory that classifies each legacy route as v2 view, redirect, or removed.</action><verify>Run focused inventory test and frontend typecheck.</verify><done>No planned deletion has an unclassified route or unknown v2 dependency.</done></task>
<task type="auto"><name>Extract v2 runtime dependencies</name><action>Move shared stores, services, types, overlays, and utilities still imported from v1-owned paths into neutral canonical paths. Update v2 imports without changing behavior or generated API contracts.</action><verify>Run focused boot/import suite and frontend typecheck.</verify><done>V2 has no runtime dependency on paths scheduled for v1 deletion.</done></task>
</tasks>
