---
phase: 09-operational-immutability-proof
plan: "05"
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04]
files_modified:
  - backend/tools/verify_operational_immutability.py
  - backend/tests/tools/test_verify_operational_immutability.py
  - backend/tests/integration/test_operational_immutability.py
  - frontend/e2e/fixtures/operational-proof.ts
  - frontend/e2e/operational-immutability.spec.ts
  - .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06, DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "Every workflow executes its named action against one fresh isolated Compose project before its after-manifest is captured."
    - "The gate accepts only browser, nginx, worker, restart, and cleanup evidence captured from the current run."
    - "The source fixture remains unchanged and cleanup affects only resources with the two exact Phase 9 labels."
  artifacts:
    - path: backend/tools/verify_operational_immutability.py
      provides: Live workflow orchestration, evidence capture, manifest comparison, and bounded cleanup
    - path: frontend/e2e/operational-immutability.spec.ts
      provides: Real v2 browser workflow matrix against the generated nginx origin
    - path: .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
      provides: Honest final-gate and re-verification commands
---

<objective>
Replace the synthetic Phase 9 evidence with a real, isolated proof.

Purpose: Prove each supported operation against a fresh read-only fixture through the actual nginx, worker, browser, and lifecycle paths.
Output: A full-gate implementation that either yields auditable live evidence or fails with diagnostic artifacts.
</objective>

<constraints>
- Use only a copied repository fixture and a unique Phase 9 Docker Compose project.
- Do not inspect, mount, restart, stop, configure, or write to Team4s, a real NAS, or an unrelated container.
- Keep `/romm/library/roms` read-only in every service and reject cleanup targets without `romm.phase=09` and `romm.phase.slug=operational-immutability-proof`.
- Missing, stale, literal, or fallback evidence is a failure, never a pass.
- Develop every new evidence contract test-first.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Execute and bracket real isolated workflows</name>
  <files>backend/tools/verify_operational_immutability.py, backend/tests/integration/test_operational_immutability.py, backend/tests/tools/test_verify_operational_immutability.py</files>
  <read_first>backend/tools/verify_operational_immutability.py, backend/docker-compose.immutability-test.yml, backend/tools/seed_phase9_database_platforms.py, backend/tests/integration/test_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VERIFICATION.md</read_first>
  <action>Write failing tests for an explicit live-run context containing the generated run id, Compose project, nginx URL, authenticated session, seeded IDs, and artifact root. Replace `execute_workflow()` so it builds `before.json`, calls the existing safe API or socket action for exactly one selected workflow, waits for the actual completion state, builds `after.json`, rejects changed source manifests, and writes the observed action evidence to `result.json`. Delivery workflows must make real authorised and direct-denial requests through nginx; scan and metadata workflows must record a real queue job id and matching worker log evidence; restart persistence must restart only the current Compose app service, re-query the mapping, and re-attest the read-only mount. Mapping, migration, rollback, catalog-removal, and mapping-removal actions must restore the synthetic baseline after their evidence is captured. Delete hardcoded `_workflow_http_probes()` values and literal service-witness fields.</action>
  <acceptance_criteria>
    - Every result records current run identity, action request/response, before/after manifests, and an unchanged diff.
    - nginx, worker, and restart evidence is observed from the live stack, not constructed in Python.
    - An action failure writes failed run and cleanup artifacts and cannot become a passing workflow.
  </acceptance_criteria>
  <verify><automated>cd backend && uv run pytest tests/integration/test_operational_immutability.py tests/tools/test_verify_operational_immutability.py -q</automated></verify>
  <done>Every workflow is a real action bracketed by source manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Bind browser and service evidence to the current run</name>
  <files>frontend/e2e/fixtures/operational-proof.ts, frontend/e2e/operational-immutability.spec.ts, backend/tools/verify_operational_immutability.py, backend/tests/tools/test_verify_operational_immutability.py</files>
  <read_first>frontend/e2e/fixtures/operational-proof.ts, frontend/e2e/operational-immutability.spec.ts, frontend/e2e/fixtures/auth.ts, backend/tools/verify_operational_immutability.py, backend/nginx.phase9.conf</read_first>
  <action>Write failing tests requiring each `browser.json` to contain `run_id`, the exact generated nginx origin, workflow slug, final status, route, and observed action details. Pass `PHASE9_BROWSER_RUN_ID` and the dynamic nginx URL from the harness to Playwright. Update each existing v2 workflow to perform the safe action it claims, including mapping create/change/preview/remove with restoration, scan and metadata start, catalog-only removal confirmation, and file download. Capture Docker Compose logs before teardown and fail closed if any required service log cannot be captured. Remove `_write_service_logs()` and its fallback. Extend browser validation to reject stale run ids, another origin, missing selected workflows, failed artifacts, and artifacts created without the harness run id.</action>
  <acceptance_criteria>
    - Chromium runs against the fresh nginx project, never a developer server or proof-only route.
    - Each selected non-restart workflow has a valid browser artifact tied to the live run.
    - App, database, nginx, Redis, and worker logs are Compose output from the current project.
  </acceptance_criteria>
  <verify><automated>cd frontend && npx playwright test e2e/operational-immutability.spec.ts --project=chromium --workers=1</automated></verify>
  <done>Browser and service artifacts prove the same run that performed the workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Make cleanup and the final gate fail closed, then re-verify</name>
  <files>backend/tools/verify_operational_immutability.py, backend/tests/tools/test_verify_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</files>
  <read_first>backend/tools/verify_operational_immutability.py, backend/tests/tools/test_verify_operational_immutability.py, backend/docker-compose.immutability-test.yml, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md, .planning/phases/09-operational-immutability-proof/09-VERIFICATION.md</read_first>
  <action>Write failing cleanup tests that discover the live Compose project resources, inspect every candidate, require both Phase 9 labels, and record concrete resource ids and teardown output in `cleanup.json`. Refuse cleanup discovery failures and unlabelled candidates. Make `--all` run preflight, start one fresh project, seed its database and users, execute every workflow, run Playwright, collect logs before teardown, validate all documents and artifacts, and only then remove the discovered labelled resources. Update `09-VALIDATION.md` to state these real commands and current status. Run the full gate in a fresh `/tmp/phase9-live-proof.*` directory and run the Phase 9 verifier again. Do not mark the phase complete unless the verifier says `passed`.</action>
  <acceptance_criteria>
    - `cleanup.json` proves that every removed resource was discovered live and carried both labels.
    - The full gate fails on missing artifacts, changed source manifests, unlabelled cleanup, worker/restart failure, or nginx/browser mismatch.
    - A successful gate contains all workflow slugs, all requirement ids, real service logs, browser records, and unchanged manifests.
  </acceptance_criteria>
  <verify><automated>cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py tests/integration/test_operational_immutability.py -q && phase9_artifacts=$(mktemp -d /tmp/phase9-live-proof.XXXXXX) && python3 backend/tools/verify_operational_immutability.py --all --artifacts "$phase9_artifacts" && git diff --check</automated></verify>
  <done>The final Phase 9 gate is a real isolated proof or it fails with trustworthy diagnostics.</done>
</task>

</tasks>

<verification>
Run focused RED/GREEN tests during each task, then the backend integration and tool tests, the Chromium workflow matrix through the generated nginx URL, the full isolated gate in a fresh `/tmp` directory, `git diff --check`, and re-run Phase 9 verification.
</verification>

<success_criteria>
The Phase 9 verifier can confirm real nginx, worker, browser, restart, workflow, source-manifest, and cleanup evidence without touching Team4s or a real NAS.
</success_criteria>
