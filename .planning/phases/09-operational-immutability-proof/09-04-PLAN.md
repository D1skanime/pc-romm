---
phase: 09-operational-immutability-proof
plan: "04"
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - docs/external-read-only-library-operations.md
  - backend/tools/verify_operational_immutability.py
  - backend/tests/tools/test_verify_operational_immutability.py
  - .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
autonomous: true
requirements: [TEST-04, TEST-05, TEST-06, DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "The operator guide is an acceptance artifact tied to the executable harness, not a narrative detached from the tested deployment."
    - "The final phase gate runs only on synthetic or repository-controlled fixtures and never touches Team4s, a real NAS mount, or host restarts."
    - "Documentation and aggregate evidence must explicitly distinguish content immutability from optional access-time invariance under documented noatime equivalents."
  artifacts:
    - path: docs/external-read-only-library-operations.md
      provides: One complete operator guide for read-only mounting, writable separation, workflows, troubleshooting, noatime, and maintenance-window safety
    - path: backend/tools/verify_operational_immutability.py
      provides: Docs contract checks, aggregate matrix, final full-run gate, and ownership-safe cleanup attestation
    - path: .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
      provides: Final task and wave verification map with no placeholder IDs
  key_links:
    - from: docs/external-read-only-library-operations.md
      to: .planning/phases/09-operational-immutability-proof/09-CONTEXT.md
      via: one-root `:ro`, writable separation, noatime, maintenance window, and Team4s safeguards
      pattern: "D-13|D-14|D-15|D-16|D-17|D-18"
    - from: backend/tools/verify_operational_immutability.py
      to: frontend/e2e/operational-immutability.spec.ts
      via: stable workflow slugs and aggregate coverage matrix
      pattern: "workflows/<slug>|aggregate.json|cleanup.json"
---

<objective>
Close Phase 9 with operator documentation and the final aggregate acceptance gate.

Purpose: Bind the executable proof to a safe operator guide, docs contract checks, and one exhaustive synthetic full-run.
Output: Operator guide, docs verifiers, aggregate coverage matrix, final Validation sign-off, and full phase gate commands.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-operational-immutability-proof/09-CONTEXT.md
@.planning/phases/09-operational-immutability-proof/09-RESEARCH.md
@.planning/phases/09-operational-immutability-proof/09-PATTERNS.md
@.planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
@.planning/phases/09-operational-immutability-proof/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the operator guide and executable docs contract</name>
  <files>docs/external-read-only-library-operations.md, backend/tools/verify_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/09-operational-immutability-proof/09-CONTEXT.md
    .planning/phases/09-operational-immutability-proof/09-RESEARCH.md
    .planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
    examples/docker-compose.example.yml
    docs/design/v2-storage-administration.md
    backend/tools/verify_operational_immutability.py
  </read_first>
  <action>Write `docs/external-read-only-library-operations.md` in English as the single operator guide for the one-root Docker `:ro` mount, separate writable RomM-owned storage, root registration, folder browsing, mapping changes, scan and preview behavior, streaming and downloading, restart behavior, migration and catalog-only removal, troubleshooting, defense in depth, traversal and symlink behavior, `noatime` or NAS-equivalent guidance, known limitations, and the explicit maintenance-window checklist. State that Phase 9 uses only synthetic or repository-controlled fixtures and does not authorize Team4s changes. Extend `backend/tools/verify_operational_immutability.py` with `--check-docs DOC-01|DOC-02|DOC-03` section-presence and forbidden-guidance checks. Update `09-VALIDATION.md` so every Wave 0 and final gate row references the actual plan and task IDs created across Plans `09-01` to `09-04`.</action>
  <acceptance_criteria>
    - `docs/external-read-only-library-operations.md` contains headings or equivalent sections for read-only mount topology, writable separation, mapping/browse flow, migration/removal, troubleshooting, `noatime` guidance, maintenance window checklist, and Team4s safeguards.
    - The guide does not instruct the reader to restart Team4s, touch a real NAS mount during Phase 9 proof, or expose absolute host paths from the UI.
    - `python3 backend/tools/verify_operational_immutability.py --check-docs DOC-01 && python3 backend/tools/verify_operational_immutability.py --check-docs DOC-02 && python3 backend/tools/verify_operational_immutability.py --check-docs DOC-03` exits 0.
    - `.planning/phases/09-operational-immutability-proof/09-VALIDATION.md` contains no `TBD` placeholders.
  </acceptance_criteria>
  <verify><automated>python3 backend/tools/verify_operational_immutability.py --check-docs DOC-01 && python3 backend/tools/verify_operational_immutability.py --check-docs DOC-02 && python3 backend/tools/verify_operational_immutability.py --check-docs DOC-03 && ! rg -n "TBD" .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</automated></verify>
  <done>The operator guide and docs contract encode every DOC-01 to DOC-03 obligation in executable, reviewable form.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize aggregate coverage, full synthetic phase gate, and cleanup attestation</name>
  <files>backend/tools/verify_operational_immutability.py, backend/tests/tools/test_verify_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</files>
  <read_first>
    CLAUDE.md
    backend/tools/verify_operational_immutability.py
    backend/tests/tools/test_verify_operational_immutability.py
    frontend/e2e/operational-immutability.spec.ts
    .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
    .planning/phases/09-operational-immutability-proof/09-RESEARCH.md
  </read_first>
  <action>Finish the harness aggregate matrix so it records every TEST-04, TEST-05, TEST-06, DOC-01, DOC-02, and DOC-03 obligation, every D-07 workflow slug, fixture-dimension coverage, and cleanup results under one run identity. Add or extend tool tests to assert aggregate completeness and that cleanup evidence is emitted even on workflow failure. Implement the final `--all` phase gate so it runs preflight, backend workflow proofs, docs checks, and the nginx-backed browser matrix against only synthetic fixtures, then validates artifact schema and safe cleanup. Update `09-VALIDATION.md` sign-off and per-task rows with the exact quick, wave, and full-phase commands the executor must run.</action>
  <acceptance_criteria>
    - `python3 backend/tools/verify_operational_immutability.py --all --artifacts /tmp/phase9-artifacts` produces `run.json`, `aggregate.json`, `cleanup.json`, service logs, and one directory per required workflow slug.
    - `aggregate.json` records all Phase 9 requirement IDs and every required D-07 workflow slug with pass/fail status.
    - `backend/tests/tools/test_verify_operational_immutability.py` asserts aggregate completeness and cleanup emission on failure paths.
    - `cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q` exits 0.
    - `git diff --check` exits 0 after the final artifact and validation updates.
  </acceptance_criteria>
  <verify><automated>cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q && python3 backend/tools/verify_operational_immutability.py --all --artifacts /tmp/phase9-artifacts && git diff --check</automated></verify>
  <done>Phase 9 has one exhaustive synthetic acceptance gate with aggregate coverage and ownership-safe cleanup evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                       |
| --------------------------------------------- | --------------------------------------------------------------------------------- |
| Executable evidence to operator documentation | Human guidance must match the tested deployment and safety rules exactly.         |
| Synthetic proof gate to real operations       | Phase 9 acceptance must not imply permission to touch Team4s or a real NAS mount. |
| Failure handling to cleanup                   | Even failed proof runs must emit evidence and bounded cleanup results.            |

## STRIDE Threat Register

| Threat ID       | Category               | Component             | Disposition | Mitigation Plan                                                                                         |
| --------------- | ---------------------- | --------------------- | ----------- | ------------------------------------------------------------------------------------------------------- |
| T-09-10         | Repudiation            | Operator guidance     | mitigate    | Tie each documented workflow and safeguard to executable docs checks and aggregate requirement mapping. |
| T-09-11         | Information Disclosure | Operational procedure | mitigate    | Forbid Team4s actions, raw host paths, and unsafe bypass guidance in docs and validation.               |
| T-09-12         | Denial of Service      | Cleanup and full gate | mitigate    | Always emit cleanup evidence, keep resource ownership labels bounded, and fail before broad deletion.   |
| </threat_model> |

<verification>
Run the docs checks, the backend tool tests, the full `--all` synthetic gate, and `git diff --check`.
</verification>

<success_criteria>
Phase 9 ships one complete operator guide and one exhaustive synthetic acceptance harness that together satisfy TEST-04, TEST-05, TEST-06, DOC-01, DOC-02, and DOC-03.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-immutability-proof/09-04-SUMMARY.md`.
</output>
