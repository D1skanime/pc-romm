---
phase: 09-operational-immutability-proof
plan: "03"
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - frontend/e2e/fixtures/operational-proof.ts
  - frontend/e2e/operational-immutability.spec.ts
  - frontend/playwright.config.ts
  - frontend/e2e/fixtures/auth.ts
autonomous: true
requirements: [TEST-04, TEST-06]
must_haves:
  truths:
    - "Browser proof runs against the real nginx-backed RomM stack and existing v2 surfaces, never a proof-only dashboard or v1 fallback."
    - "Phase 8 deferred smoke coverage for authentication, hydrated boot, and supported v2 navigation lands inside the same proof run."
    - "Playwright selectors stay semantic and proof state remains local to existing route shells, per the approved UI-SPEC."
  artifacts:
    - path: frontend/e2e/operational-immutability.spec.ts
      provides: Browser workflow matrix for storage admin, browsing, scan, stream/play, download, migration, and removal flows
    - path: frontend/e2e/fixtures/operational-proof.ts
      provides: Authenticated proof helpers, workflow checkpoints, and artifact finalization bridge
    - path: frontend/playwright.config.ts
      provides: nginx-backed E2E base URL, deterministic worker count, trace, and screenshot behavior
  key_links:
    - from: frontend/e2e/operational-immutability.spec.ts
      to: .planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
      via: existing v2 surfaces, local status regions, semantic selectors, and no proof-only route
      pattern: "storage root and mapping administration flows|authentication and boot flows|stream, play, and download surfaces"
    - from: frontend/e2e/fixtures/operational-proof.ts
      to: frontend/e2e/fixtures/auth.ts
      via: authenticated storage state and hydrated-shell waits
      pattern: 'gotoHydrated|STORAGE_STATE|serviceWorkers: "block"'
---

<objective>
Prove the Phase 9 workflows through the real browser-visible product surface.

Purpose: Drive existing v2 routes through nginx, worker-backed workflows, and deferred Phase 8 smoke coverage without inventing test-only UI.
Output: Proof fixture helpers, nginx-backed browser matrix, and deterministic Playwright configuration.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-operational-immutability-proof/09-CONTEXT.md
@.planning/phases/09-operational-immutability-proof/09-RESEARCH.md
@.planning/phases/09-operational-immutability-proof/09-PATTERNS.md
@.planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
@.planning/phases/08-bounded-v1-removal/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Playwright proof helpers on top of the existing auth fixture</name>
  <files>frontend/e2e/fixtures/operational-proof.ts, frontend/e2e/fixtures/auth.ts, frontend/playwright.config.ts</files>
  <read_first>
    CLAUDE.md
    .planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
    .planning/phases/09-operational-immutability-proof/09-PATTERNS.md
    frontend/playwright.config.ts
    frontend/e2e/fixtures/auth.ts
    frontend/src/services/api/storage.ts
    frontend/src/v2/router/routes.ts
  </read_first>
  <action>Create `frontend/e2e/fixtures/operational-proof.ts` as a thin extension of the existing auth and hydration helpers. Add helpers to point tests at an nginx-backed `E2E_BASE_URL`, seed the approved theme and v2 state if still needed by the runtime, bootstrap proof fixtures or checkpoints through bounded API calls, and finalize or poll Phase 9 artifact state without replacing visible browser actions. Keep traces, screenshots, and `serviceWorkers: "block"` in the proof config. Update `frontend/playwright.config.ts` only if required to support a dedicated proof project or stricter `workers=1` for shared stack stability.</action>
  <acceptance_criteria>
    - `frontend/e2e/fixtures/operational-proof.ts` reuses `login`, `gotoHydrated`, or `STORAGE_STATE` from `frontend/e2e/fixtures/auth.ts` instead of duplicating auth flow logic.
    - Any `frontend/playwright.config.ts` change preserves `trace: "retain-on-failure"`, `screenshot: "only-on-failure"`, and `serviceWorkers: "block"`.
    - Proof helpers do not introduce selectors for a new proof-only route, dashboard, or v1 fallback path.
    - `cd frontend && npm run test:e2e -- --list` includes `operational-immutability.spec.ts` without config errors.
  </acceptance_criteria>
  <verify><automated>cd frontend && npm run test:e2e -- --list</automated></verify>
  <done>The browser harness can reuse existing authenticated E2E patterns while targeting the isolated nginx-backed stack.</done>
</task>

<task type="auto">
  <name>Task 2: Add the nginx-backed v2 operational immutability workflow matrix</name>
  <files>frontend/e2e/operational-immutability.spec.ts, frontend/e2e/fixtures/operational-proof.ts</files>
  <read_first>
    CLAUDE.md
    .planning/phases/09-operational-immutability-proof/09-UI-SPEC.md
    .planning/phases/09-operational-immutability-proof/09-RESEARCH.md
    frontend/e2e/fixtures/operational-proof.ts
    frontend/e2e/fixtures/auth.ts
    frontend/src/services/api/storage.ts
    frontend/src/v2/router/routes.ts
    frontend/src/v2/views/Storage/PlatformStorageMapping.vue
    frontend/src/v2/views/Player/EmulatorJS.vue
  </read_first>
  <action>Implement `frontend/e2e/operational-immutability.spec.ts` to drive the real v2 storage administration, browse, mapping test, mapping save/change/remove, preview, scan initiation and completion, stream or browser play, single and multi-file download, legacy migration and supported rollback, game catalog removal, platform mapping removal, and Phase 8 deferred smoke for login, hydrated boot, and supported routes. Bracket each workflow with proof checkpoints from `frontend/e2e/fixtures/operational-proof.ts` so the backend harness can finalize `before.json`, `after.json`, `diff.json`, and `result.json` for the matching slug. Use semantic selectors, visible headings, button names, and region labels; keep status assertions local to the affected page region per `09-UI-SPEC.md`.</action>
  <acceptance_criteria>
    - `frontend/e2e/operational-immutability.spec.ts` contains at least one test or grouped workflow for storage browse/mapping, scan, stream or play, download, migration, and removal coverage.
    - The browser suite references existing v2 surfaces and semantic selectors, with no route or selector pointing at `NotReady`, v1 views, or a proof-only page.
    - Running `cd frontend && E2E_BASE_URL=http://127.0.0.1:3000 npx playwright test e2e/operational-immutability.spec.ts --project=chromium --workers=1 --list` exits 0.
    - The proof helper contract maps each browser workflow to a stable artifact slug that matches the backend harness result directories.
  </acceptance_criteria>
  <verify><automated>cd frontend && E2E_BASE_URL=http://127.0.0.1:3000 npx playwright test e2e/operational-immutability.spec.ts --project=chromium --workers=1 --list && git diff --check</automated></verify>
  <done>Phase 9 has a real browser-visible proof matrix that exercises v2 through nginx and the isolated stack without alternate UI paths.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                  |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Browser actions to authorized storage workflows | User-visible actions must stay on existing v2 routes and authorized backend contracts.       |
| Playwright fixture helpers to proof harness     | Helpers may coordinate checkpoints, but they must not replace the visible action under test. |
| Browser client to nginx-backed delivery         | The suite must prove delivery through production-like routing, not a dev-only shortcut.      |

## STRIDE Threat Register

| Threat ID       | Category          | Component           | Disposition | Mitigation Plan                                                                                                                 |
| --------------- | ----------------- | ------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-09-07         | Spoofing          | Browser proof flow  | mitigate    | Require semantic assertions on real page headings, actions, and route context instead of hidden helper-only success paths.      |
| T-09-08         | Tampering         | Proof-only UI drift | mitigate    | Keep all work inside existing v2 surfaces and reject new routes or v1 fallbacks in review and verification.                     |
| T-09-09         | Denial of Service | Shared proof stack  | mitigate    | Run proof workflows serially with `--workers=1` when using the shared isolated stack and preserve trace artifacts for failures. |
| </threat_model> |

<verification>
Run Playwright listing and the scoped nginx-backed proof listing command, then `git diff --check`. Full browser execution waits for the backend harness from Plan `09-02`.
</verification>

<success_criteria>
Phase 9 can prove the required operator and user workflows through the real v2 browser surface, nginx routing, and deferred Phase 8 smoke scope.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-immutability-proof/09-03-SUMMARY.md`.
</output>
