---
phase: 09-operational-immutability-proof
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tools/verify_operational_immutability.py
  - backend/tests/tools/test_verify_operational_immutability.py
  - backend/docker-compose.immutability-test.yml
  - tests/fixtures/operational-immutability/
  - .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
autonomous: true
requirements: [TEST-04, TEST-05]
must_haves:
  truths:
    - "Every Phase 9 workflow proof starts from one canonical manifest format that captures names, structure, type, size, hash, and non-access timestamps without mutating the source fixture."
    - "The isolated stack models a read-only external library and separate RomM-owned writable paths before any browser or backend workflow runs."
    - "Cleanup and artifact ownership stay bounded to the Phase 9 harness, never Team4s, a real NAS mount, or unrelated containers."
  artifacts:
    - path: backend/tools/verify_operational_immutability.py
      provides: Bounded proof harness, manifest contract, compose lifecycle, and artifact schema
    - path: backend/tests/tools/test_verify_operational_immutability.py
      provides: RED/GREEN coverage for manifest completeness, topology attestation, and cleanup safety
    - path: backend/docker-compose.immutability-test.yml
      provides: Production-like isolated stack with explicit `:ro` source mount and separate writable owned storage
  key_links:
    - from: backend/tools/verify_operational_immutability.py
      to: backend/tests/integration/test_scan_source_immutability.py
      via: Stable manifest ordering, relative identity, and sha256 evidence
      pattern: "sorted(root.rglob)|sha256|relative_to"
    - from: backend/docker-compose.immutability-test.yml
      to: backend/docker-compose.policy-test.yml
      via: Explicit read-only source bind and separate owned output bind
      pattern: "read_only: true|target: /policy/external|target: /policy/owned"
---

<objective>
Establish the bounded proof harness foundation for Phase 9.

Purpose: Create the canonical manifest, deterministic fixture, topology attestation, and fast verification loop that every later workflow proof depends on.
Output: Harness CLI skeleton, RED/GREEN tool tests, isolated compose stack, fixture contract, and Validation task ID backfill.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-operational-immutability-proof/09-CONTEXT.md
@.planning/phases/09-operational-immutability-proof/09-RESEARCH.md
@.planning/phases/09-operational-immutability-proof/09-PATTERNS.md
@.planning/phases/09-operational-immutability-proof/09-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED canonical manifest and topology contracts</name>
  <files>backend/tests/tools/test_verify_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/09-operational-immutability-proof/09-RESEARCH.md
    .planning/phases/09-operational-immutability-proof/09-PATTERNS.md
    .planning/phases/09-operational-immutability-proof/09-VALIDATION.md
    backend/tests/integration/test_scan_source_immutability.py
    backend/tools/verify_read_only_policy.py
    backend/docker-compose.policy-test.yml
  </read_first>
  <action>Write failing pytest coverage for a new `backend/tools/verify_operational_immutability.py` contract before implementing it. Define a manifest schema version that includes root identity, sorted entries, encoded relative path, entry type, mode, size, sha256 for files, symlink target, mtime_ns, ctime_ns, optional birthtime_ns, separate atime_ns reporting, completeness counters, and aggregate digest. Add RED tests that reject missing root records, missing empty directories, duplicate or unsorted entries, reordered output, field omissions, unsafe absolute paths in artifacts, and cleanup requests for unlabeled resources. Update `09-VALIDATION.md` so `09-W0-01` and `09-W0-02` point at Plan `09-01` and concrete task IDs instead of `TBD`.</action>
  <acceptance_criteria>
    - `backend/tests/tools/test_verify_operational_immutability.py` contains explicit failing cases for incomplete manifest fields, unsorted entries, duplicate entries, missing empty directories, unsafe artifact paths, and unowned cleanup targets.
    - `.planning/phases/09-operational-immutability-proof/09-VALIDATION.md` no longer contains `TBD` for `09-W0-01` or `09-W0-02`.
    - `cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q` fails only because `backend/tools/verify_operational_immutability.py` and its compose contract are not implemented yet.
  </acceptance_criteria>
  <verify><automated>cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q</automated></verify>
  <done>The RED tests define the exact manifest, topology, and cleanup contract that the harness must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN harness preflight, fixture builder, and isolated compose stack</name>
  <files>backend/tools/verify_operational_immutability.py, backend/docker-compose.immutability-test.yml, tests/fixtures/operational-immutability/, backend/tests/tools/test_verify_operational_immutability.py, .planning/phases/09-operational-immutability-proof/09-VALIDATION.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/09-operational-immutability-proof/09-PATTERNS.md
    backend/tools/verify_read_only_policy.py
    backend/docker-compose.policy-test.yml
    backend/tests/integration/test_scan_source_immutability.py
    docker/nginx/templates/default.conf.template
    examples/docker-compose.example.yml
  </read_first>
  <action>Implement the Phase 9 harness as a bounded Python CLI under `backend/tools/verify_operational_immutability.py` with `--preflight-only`, `--workflow`, `--requirement`, `--all`, and `--artifacts` entrypoints. Add deterministic fixture creation under `tests/fixtures/operational-immutability/` or task-owned synthesized output that covers at least two mappings, Unicode paths, nested directories, empty directories, zero-byte files, multi-file content, and symlink/traversal rejection cases. Create `backend/docker-compose.immutability-test.yml` with app, worker, nginx, database, and queue services, explicit `read_only: true` source mounting, and separate writable owned mounts for cache, resources, config, temp data, database, and artifacts. Make preflight reject missing `:ro`, writable overlaps, fixed container names, host network mode, and unlabeled cleanup targets. Update `09-VALIDATION.md` so the quick commands and Wave 0 rows reference the exact Phase 9 files and Plan `09-01` task IDs.</action>
  <acceptance_criteria>
    - `backend/tools/verify_operational_immutability.py` exposes `--preflight-only`, `--workflow`, `--requirement`, `--all`, and `--artifacts` in its CLI help text.
    - `backend/docker-compose.immutability-test.yml` declares explicit `read_only: true` on the external fixture bind and separate writable mounts for owned storage paths with no shared target.
    - `cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q` exits 0.
    - `python3 backend/tools/verify_operational_immutability.py --preflight-only` exits 0 on the healthy stack and fails on seeded writable-source mutants covered by the tests.
    - `.planning/phases/09-operational-immutability-proof/09-VALIDATION.md` references Plan `09-01` instead of `TBD` for the foundational Wave 0 rows.
  </acceptance_criteria>
  <verify><automated>cd backend && uv run pytest tests/tools/test_verify_operational_immutability.py -x -q && python3 backend/tools/verify_operational_immutability.py --preflight-only && git diff --check</automated></verify>
  <done>The harness can deterministically build fixtures, attest topology, and fail closed before any higher-level workflow proof starts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                   |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Host fixture to container mounts          | Synthetic external library content crosses into a production-like stack and must remain read-only everywhere. |
| Harness orchestration to Docker resources | The CLI is allowed to create and remove only the exact stack resources it owns.                               |
| Artifact serialization to reviewers       | Evidence must stay deterministic, complete, and free of unsafe host paths or secrets.                         |

## STRIDE Threat Register

| Threat ID       | Category               | Component        | Disposition | Mitigation Plan                                                                                                              |
| --------------- | ---------------------- | ---------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-09-01         | Tampering              | Manifest capture | mitigate    | Define a versioned exact manifest schema, reject unsorted or partial entries, and test seeded mutants before implementation. |
| T-09-02         | Elevation of Privilege | Compose topology | mitigate    | Preflight rejects writable source mounts, mount overlaps, host networking, and unlabeled resources before startup.           |
| T-09-03         | Information Disclosure | Artifacts        | mitigate    | Forbid absolute host paths, cookies, and unrelated service identifiers in emitted JSON and cleanup logs.                     |
| </threat_model> |

<verification>
Run the tool-test quick command, the preflight command, and `git diff --check`. Record the healthy preflight output and at least one seeded failure case in the summary.
</verification>

<success_criteria>
Phase 9 has one approved canonical manifest and one safe isolated stack contract that every later workflow and doc proof reuses.
</success_criteria>

<output>
After completion, create `.planning/phases/09-operational-immutability-proof/09-01-SUMMARY.md`.
</output>
