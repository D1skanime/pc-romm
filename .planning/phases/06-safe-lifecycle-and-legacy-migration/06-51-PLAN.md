---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 51
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/endpoints/screenshots.py
  - backend/tests/endpoints/test_screenshots.py
autonomous: true
requirements: [CAT-02]
gap_closure: true
must_haves:
  truths:
    - "Owner PUT and DELETE for a screenshot whose ROM or platform is hidden return the same static Screenshot not found 404."
    - "Visibility masking occurs before public-state update, storage authorization, owned file removal, logging, or database deletion."
    - "Visible owner update/delete and existing upload/download visibility behavior remain unchanged."
  artifacts:
    - path: "backend/endpoints/screenshots.py"
      provides: "Visibility-before-effects guard for all screenshot mutations"
    - path: "backend/tests/endpoints/test_screenshots.py"
      provides: "Hidden ROM/platform PUT/DELETE response and zero-effect proof"
  key_links:
    - from: "owner screenshot lookup"
      to: "assert_rom_visible"
      via: "static Screenshot not found detail before mutation"
      pattern: "assert_rom_visible"
    - from: "visibility guard"
      to: "DB update/delete and ASSETS removal"
      via: "guard dominates every effect"
      pattern: "update_screenshot|remove_file|delete_screenshot"
---

<objective>
Mask hidden ROM and platform targets before screenshot update or delete.
Purpose: CR-04 and failed must-have 30 allow retained screenshot IDs to mutate hidden entities.
Output: Two early guards and adversarial zero-effect regressions with no schema or UI change.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-34-SUMMARY.md
@backend/handler/auth/dependencies.py
<interfaces>
assert_rom_visible(request, rom, not_found_detail="Screenshot not found") enforces both ROM and platform visibility. download_screenshot already applies it after owner/public lookup. update_screenshot and delete_screenshot currently perform owner lookup but omit the guard.
</interfaces>
</context>
<test_topology>
Use verified Linux /home/d1sk/romm and one nonce-owned disposable backend runner/DB/principal/basetemp. Keep romm-dev stopped, run no service restart/deploy/public port, compare exact ASSETS manifest and screenshot row state, prove cleanup and normal DB SELECT 1, and preserve baseline hash 4d4264efbb75049e71230f2417667c867656f6dd5054640e7aa6b8af391c81e1.
</test_topology>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED prove hidden PUT and DELETE have zero effects</name>
  <files>backend/tests/endpoints/test_screenshots.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/endpoints/screenshots.py, backend/handler/auth/dependencies.py, backend/tests/endpoints/test_screenshots.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-34-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Hidden ROM PUT and hidden platform PUT return identical 404 and preserve is_public.
    - Hidden ROM DELETE and hidden platform DELETE return identical 404 and preserve file and DB row.
    - No update/delete handler, storage authorization, path removal, or effectful log executes.
    - Response text leaks no ROM, platform, screenshot, user, permission, or path detail.
  </behavior>
  <action>Add a shared helper and four tests before endpoint changes. Use an owner with ASSETS_WRITE whose ROM or platform is hidden. Snapshot exact DB fields and the existing complete asset manifest, spy every downstream effect, call PUT and DELETE, and require {"detail":"Screenshot not found"} with identical zero effects. Keep visible owner and other-user cases as controls. RED must fail only because the visibility guard is absent.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, StageFailure, _execute_docker_module; name=\"tests/endpoints/test_screenshots.py::test_hidden_rom_screenshot_update_is_masked_before_effects\"; exec(\"try:\\n _execute_docker_module(Path.cwd(), name, ResourceIdentity.create(5101), 'romm-romm-dev', 'romm_default', 'romm-db-dev')\\nexcept StageFailure as exc:\\n text=str(exc); assert name in text and 'failed' in text.lower() and 'ERROR collecting' not in text\\nelse:\\n raise SystemExit('RED unexpectedly passed')\")"'</automated>
  </verify>
  <acceptance_criteria>
    - The named RED fails at 200 versus 404 or effect assertion, not infrastructure.
    - All four hidden target/method combinations prove indistinguishable response and exact zero effects.
  </acceptance_criteria>
  <done>Missing update/delete visibility is captured before production edits.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN enforce screenshot visibility before mutation</name>
  <files>backend/endpoints/screenshots.py, backend/tests/endpoints/test_screenshots.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/endpoints/screenshots.py, backend/handler/auth/dependencies.py, backend/tests/endpoints/test_screenshots.py</read_first>
  <behavior>
    - Owner check remains first, then assert_rom_visible with static screenshot detail.
    - Update/delete effects execute only after visibility succeeds.
    - Visible behavior and response schemas do not change.
  </behavior>
  <action>Immediately after successful owner lookup in update_screenshot and delete_screenshot, call assert_rom_visible(request, screenshot.rom, not_found_detail="Screenshot not found"). Place it before update_screenshot, authorize_api_storage_operation, remove_file, delete_screenshot, and effectful logging. Reuse the existing dependency exactly; do not inline visibility queries, reveal target identity, change scopes, alter ownership/public behavior, or touch generated contracts/UI.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, _execute_docker_module; _execute_docker_module(Path.cwd(), \"tests/endpoints/test_screenshots.py\", ResourceIdentity.create(5110), \"romm-romm-dev\", \"romm_default\", \"romm-db-dev\")"'</automated>
  </verify>
  <acceptance_criteria>
    - Full screenshot endpoint module passes with hidden upload/download/update/delete and visible controls.
    - Hidden update/delete leave exact DB and ASSETS manifests unchanged.
    - No API schema or frontend generation change is introduced.
  </acceptance_criteria>
  <done>Every screenshot mutation respects ROM/platform visibility before effects.</done>
</task>
</tasks>
<source_coverage>
GOAL-06 criterion 1, CAT-02, REVIEW CR-04, and VERIFICATION MH-30 are covered. CAT-04 external-root mutation removal, migration, and mapping requirements are not claimed.
</source_coverage>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| screenshot ID to hidden parent | Ownership does not override ROM/platform visibility. |
| guard to owned ASSETS | Hidden targets must fail before file or DB mutation. |
| error response to caller | Masking cannot leak which parent is hidden. |
| runner to live topology | Tests remain disposable and isolated. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-51-01 | Elevation of privilege | owner PUT/DELETE | mitigate | Apply assert_rom_visible after owner lookup and before effects. |
| T-06-51-02 | Information disclosure | 404 response | mitigate | Static Screenshot not found for ROM/platform hidden cases. |
| T-06-51-03 | Tampering | screenshot row/file | mitigate | Tests spy and compare exact DB and ASSETS state. |
| T-06-51-04 | Repudiation | ordering | mitigate | Assert every effect helper is uncalled on hidden targets. |
| T-06-51-05 | Denial of service | test resources | mitigate | One nonce lifecycle with exact cleanup and no restart. |
</threat_model>
<verification>
Run full screenshot module in a disposable lifecycle, scoped Trunk, git diff --check, exact DB/assets equality, cleanup, normal SELECT 1, stopped romm-dev, and original baseline.
</verification>
<success_criteria>
Hidden ROM/platform screenshot PUT/DELETE return identical static 404 before effects; visible owner behavior and all existing visibility routes remain green.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-51-SUMMARY.md.
</output>
