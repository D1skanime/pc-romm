---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 44
type: execute
wave: 3
depends_on: [06-35, 06-42]
files_modified:
  - backend/handler/database/legacy_migration_handler.py
  - backend/handler/database/roms_handler.py
  - backend/tests/handler/storage/test_legacy_migration.py
  - backend/tests/integration/test_legacy_migration.py
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, MIG-05]
gap_closure: true

must_haves:
  truths:
    - "Impact preview counts only unique catalog ROM identities whose exact mapping-relative source identity was observed."
    - "Migration reconnects only those ROMs and only child RomFile identities independently observed; absent ROMs and sidecars remain unreachable."
    - "Confirmation, source fingerprint, catalog fingerprint, identity set, transaction rollback, restart, privacy, and no-fallback invariants remain exact."
  artifacts:
    - path: "backend/handler/database/legacy_migration_handler.py"
      provides: "Persisted source/catalog intersection and atomic migration selection"
    - path: "backend/handler/database/roms_handler.py"
      provides: "Explicit ROM and RomFile ID reconnection updates"
    - path: "backend/tests/integration/test_legacy_migration.py"
      provides: "Present ROM, absent ROM, absent sidecar, restart, failure, and privacy regressions"
  key_links:
    - from: "LegacyDetectionOutcome private digests"
      to: "LegacyDetectionSourceIdentity rows"
      via: "save_detection_result atomic persistence"
      pattern: "identity_digest"
    - from: "persisted digest set"
      to: "preview counts"
      via: "exact unique mapping-relative catalog intersection"
      pattern: "reconnectable"
    - from: "locked preview selection"
      to: "reconnect_legacy_catalog"
      via: "explicit ROM and RomFile ID sets"
      pattern: "rom_file_ids"
    - from: "fresh source observation"
      to: "confirmation/migration"
      via: "fingerprint plus exact identity-set equality"
      pattern: "source_fingerprint"
---

<objective>
Bind legacy preview and migration reconnection to the exact private source-observed identity set.

Purpose: CR-01 currently substitutes catalog-only uniqueness for source existence and clears every child sidecar, violating D-19.
Output: Atomic digest persistence, one shared selection algorithm, explicit row updates, and restart/adversarial integration evidence.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@backend/handler/database/legacy_migration_handler.py
@backend/handler/database/roms_handler.py
@backend/handler/storage/legacy_migration.py
@backend/models/storage.py
@backend/tests/handler/storage/test_legacy_migration.py
@backend/tests/integration/test_legacy_migration.py
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-12-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-13-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-37-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-42-SUMMARY.md

<interfaces>
save_detection_result must require a selectable exact outcome to have a nonempty sorted unique digest tuple whose length equals observed_files, then persist it in the same transaction as LegacyDetectionResult; nonselectable/lower-bound results persist none. Define one private selection helper that receives persisted Platform.fs_slug, locked Rom/RomFile rows, and the persisted digest set. A catalog identity is safe only when its first exact path segment equals fs_slug; strip exactly that segment to obtain mapping-relative identity, normalize without case folding, hash through Plan 37's domain-separated helper, and reject unsafe/empty/alias paths. A ROM reconnects only when its logical identity is unique among ROM rows and digest-present. Each RomFile reconnects only if its parent ROM reconnects and its own exact file_path/file_name digest is present; never clear all children by parent alone. Preview and migrate use this same selection. Migration locks catalog and identity rows in deterministic ID/digest order, reobserves source, requires both aggregate fingerprint and exact digest tuple equality, recomputes catalog fingerprint, records rollback changes only for rows it will change, passes explicit ROM/RomFile ID sets to DBRomsHandler, and commits mapping/catalog/audit/detection-version/rollback metadata together. Public confirmation/results continue to expose counts/fingerprints already approved but never digests, paths, filenames, rows, or host details. Preserve D-01 through D-03, D-06 through D-23, especially D-12/D-14/D-19/D-20/D-21/D-23.
</interfaces>
</context>

<test_topology>
Plan 06-44 backend pytest gates never use a fixed or shared database. The exact t1, t2, and pv commands use a 128-bit lowercase-hex nonce: the database is the literal plan/task prefix plus 32 hex characters, while the MariaDB principal is exactly the same 32-character nonce so it remains within the username limit. Before any cleanup trap is registered or any mutation occurs, each command proves normal application SELECT 1, queries authoritative schema and mysql.user metadata, requires exact database and user@% absence, and requires exact basetemp absence; collision or lookup failure exits without cleanup or mutation. Only then does it install EXIT/INT/TERM cleanup with independent db_owned=0 and user_owned=0 flags, create database and principal separately, prove each exact identity present before setting its flag, and grant only after both flags equal 1. Cleanup revokes/drops the principal only when user_owned=1, drops the database only when db_owned=1, removes only the regex-guarded exact basetemp, preserves the primary status while reporting cleanup failures, and proves final exact DB/user/temp absence plus normal application SELECT 1. Every pytest process receives explicit DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWD, Redis, ROMM_BASE_PATH, and auth variables plus -p no:env, -p no:cacheprovider, and the exact basetemp. Plan 06-46 Task 2 additionally delegates three-dialect disposable authority to the current checked-in backend/tools/verify_storage_migrations.py contract. Never clean an identity whose owned flag was not established, print credentials, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED prove source-absent ROMs and sidecars remain unreachable</name>
  <files>backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/handler/database/legacy_migration_handler.py, backend/handler/database/roms_handler.py, backend/handler/storage/legacy_migration.py, backend/models/storage.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-37-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-42-SUMMARY.md</read_first>
  <behavior>
    - Absent ROM remains missing.
    - Absent sidecar remains missing even when parent exists.
    - Only exact observed identities enter preview and change sets.
  </behavior>
  <action>Per D-01, D-03, D-06, D-12, D-14, D-19, D-20, D-21, and D-23, reconnect only exact observed unambiguous identities inside one retry-safe transaction and preserve every absent or ambiguous identity as unreachable. Add exact integration test test_migration_reconnects_only_source_observed_rom_and_sidecar. The canonical source contains present.gb and a present child file, while the catalog also contains unique absent.gb, an absent sidecar under present.gb, an ambiguous duplicate, unsafe identity, Unicode/case-distinct controls, and a cross-platform row. Run detection, save, preview, confirmation, migration, restart/reload, and rollback. Assert preview ROM counts use only exact observed unique ROMs; migration clears only present ROM and present child; absent ROM/sidecar/ambiguous/unsafe rows remain missing and visible; rollback changes include only changed rows; no source or public response exposes identity digests/paths. Add drift cases where the aggregate source fingerprint or exact digest membership changes at the same file count and require stale rollback of the whole transaction. Record the exact named case as RED. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0644_t1_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0644-t1-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0644_t1_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0644-t1-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0644-t1-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; set +e; output=$(docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/integration/test_legacy_migration.py::test_migration_reconnects_only_source_observed_rom_and_sidecar -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0 items|usage: pytest|unrecognized arguments|INTERNALERROR|OperationalError|Access denied|Can.t connect|Unknown database|docker: Error"; printf "%s\n" "$output" | grep -Eq "FAILED .*test_migration_reconnects_only_source_observed_rom_and_sidecar"'</automated>
  </verify>
  <acceptance_criteria>
    - RED observes absent rows becoming reachable under current catalog-only logic.
    - The fixture distinguishes ROM and child RomFile membership.
    - Restart, fingerprint/set drift, transaction rollback, and response privacy are direct assertions.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>The exact CR-01 source-binding failure is reproduced end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN persist, lock, intersect, and update exact identities atomically</name>
  <files>backend/handler/database/legacy_migration_handler.py, backend/handler/database/roms_handler.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/handler/database/legacy_migration_handler.py, backend/handler/database/roms_handler.py, backend/handler/storage/legacy_migration.py, backend/models/storage.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-12-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-21-SUMMARY.md</read_first>
  <behavior>
    - CR-01 root cause is closed.
    - Transaction and confirmation invariants remain exact.
    - No source path or filename is exposed or guessed.
  </behavior>
  <action>Per D-01, D-03, D-06, D-12, D-14, D-19, D-20, D-21, and D-23, reconnect only exact observed unambiguous identities inside one retry-safe transaction and preserve every absent or ambiguous identity as unreachable. Implement atomic digest persistence and the single shared selection helper. Make preview query the private set without source I/O and compute truthful bounded ROM counts/problems. In migrate, lock private evidence and catalog deterministically, reobserve exact source under the existing 240-second deadline, require fingerprint and digest-set equality, and compute explicit ROM/RomFile sets once. Change reconnect_legacy_catalog to accept those explicit sets, validate every requested row belongs to the platform/parent selection under locks, update only those IDs, and return counts used for confirmation validation. Build rollback lineage from exactly those missing rows. Keep the whole mapping/reconnection/audit/result-version/migration record transaction and every injected flush seam. Remove no source content, add no fallback, and serialize no identity evidence publicly. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0644_t2_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0644-t2-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0644_t2_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0644-t2-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0644-t2-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/handler/storage/test_legacy_migration.py tests/integration/test_legacy_migration.py tests/handler/database/test_storage_lifecycle.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - Preview and migration share one selection result and agree on counts.
    - Only present exact ROM and child file IDs change and enter rollback lineage.
    - Fresh source fingerprint and exact set, catalog fingerprint, confirmation, expiry, locks, failures, retries, rollback, restart, and three-dialect-neutral SQL remain intact.
    - No raw source identity crosses responses or logs.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>D-19 is enforced by private source evidence through preview, migration, restart, rollback, and failure.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                    |
| -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| pytest runner to MariaDB/root authority            | Plan 06-44 tests may create only validated task-nonce resources while normal application storage remains live. |
| external source observation to private DB evidence | Only bounded digests cross and must bind exact membership.                                                     |
| private evidence to catalog rows                   | Incorrect normalization could make absent rows reachable.                                                      |
| locked selection to transaction                    | Preview, confirmation, updates, audit, and rollback must agree.                                                |
| internal evidence to public API/log                | Digests, source inventories, paths, and filenames must remain private.                                         |

## STRIDE Threat Register

| Threat ID       | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-44-01      | Spoofing               | catalog identity          | mitigate    | Exact fs_slug prefix, normalization, domain-separated digest, and uniqueness.                                                                                                                                                                                    |
| T-06-44-02      | Tampering              | source/catalog drift      | mitigate    | Fresh fingerprint plus exact digest-set and catalog-fingerprint equality under locks.                                                                                                                                                                            |
| T-06-44-03      | Information disclosure | private identity set      | mitigate    | Never serialize/log evidence; public contracts retain bounded counts only.                                                                                                                                                                                       |
| T-06-44-04      | Elevation of privilege | reconnection update       | mitigate    | Explicit validated ROM/RomFile ID sets under deterministic locks.                                                                                                                                                                                                |
| T-06-44-05      | Denial of service      | identity volume           | mitigate    | Existing entry/time/byte budgets and ordered indexed lookup.                                                                                                                                                                                                     |
| T-06-44-06      | Repudiation            | rollback lineage          | mitigate    | Record exactly changed immutable row incarnations in the same transaction.                                                                                                                                                                                       |
| T-06-44-99      | Elevation of privilege | isolated pytest lifecycle | mitigate    | Authoritative collision lookup precedes trap/mutation; 128-bit exact identities, separate post-presence ownership flags, grant-after-both-owned, ownership-gated exact cleanup, final absence checks, and normal SELECT 1 prevent deletion of unowned resources. |
| </threat_model> |

<verification>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0644_pv_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0644-pv-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0644_pv_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0644-pv-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0644-pv-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/handler/storage/test_legacy_migration.py tests/integration/test_legacy_migration.py tests/handler/database/test_storage_lifecycle.py -x'</automated>
This is an independent p0644_pv 128-bit lifecycle. Its pre-mutation authoritative absence proof, separate ownership flags, ownership-gated cleanup, final absence proof, and before/after application SELECT 1 must complete before the remaining plan gates.
Run the full legacy handler/integration/storage-lifecycle modules in the isolated plan DB, complete writable/read-only source manifests, scoped Trunk, git diff --check, exact cleanup, and normal DB continuity. Plan 46 supplies real MariaDB/MySQL/PostgreSQL schema authority. No generated/public contract or UI change.
</verification>

<success_criteria>

- Every p0644 pytest lifecycle uses a fresh validated nonce DB/principal/basetemp and proves exact cleanup plus normal DB continuity.
- CR-01 and must-haves 14/19 are closed.
- All migration confirmation, transaction, restart, rollback, privacy, and no-fallback invariants remain green.
- The implementation is ready for three-dialect lifecycle verification.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-44-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
