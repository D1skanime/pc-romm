---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tools/verify_phase6_contracts.py
  - backend/tests/tools/test_verify_phase6_contracts.py
  - backend/endpoints/responses/assets.py
  - backend/tests/endpoints/test_screenshots.py
  - backend/tests/endpoints/test_saves.py
  - backend/tests/endpoints/test_states.py
  - frontend/src/__generated__/models/ScreenshotSchema.ts
  - frontend/src/__generated__/models/UserScreenshotSchema.ts
autonomous: true
requirements: [CAT-01, CAT-02]
gap_closure: true
must_haves:
  truths:
    - "A cleanup failure leaves each unremoved harness-owned resource eligible for a later exact retry."
    - "Cleanup never retries an already removed resource and declares completion only when container, volume, and environment file are all absent."
    - "Screenshot API and generated models require live rom_id and do not advertise retained_catalog_id, while Save and State remain detachable."
  artifacts:
    - path: "backend/tools/verify_phase6_contracts.py"
      provides: "Per-resource durable retry-safe exact cleanup state"
    - path: "backend/endpoints/responses/assets.py"
      provides: "Separate live-ROM screenshot and detachable save/state contracts"
    - path: "frontend/src/__generated__/models/ScreenshotSchema.ts"
      provides: "Generated screenshot lifecycle that runtime persistence can produce"
  key_links:
    - from: "backend/tools/verify_phase6_contracts.py cleanup"
      to: "runner container, Node volume, environment file"
      via: "clear ownership state only after confirmed absence"
      pattern: "_cleaned|runner_container_id|_volume_created|_env_file"
    - from: "backend/endpoints/responses/assets.py"
      to: "frontend/src/__generated__/models/ScreenshotSchema.ts"
      via: "controlled OpenAPI regeneration"
      pattern: "rom_id|retained_catalog_id"
---

<objective>
Close both confirmed warnings: retry-safe controlled-harness cleanup and a coherent screenshot lifecycle contract.

Purpose: Ensure verification resources remain exactly recoverable after failure and generated clients cannot represent impossible detached screenshots.
Output: Per-resource cleanup retries, specialized asset schemas, regenerated screenshot models, and contract tests.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-10-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-14-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-15-SUMMARY.md
</context>

<interfaces>
From backend/tools/verify_phase6_contracts.py:
- runner_container_id, _volume_created, and _env_file are exact resource ownership state.
- cleanup currently sets _cleaned before removal and therefore disables all later retry after one failed remove.

From backend/models/assets.py:

- Screenshot inherits required RomAsset.rom_id and has no retained_catalog_id column.
- Save and State override rom_id as nullable and add retained_catalog_id.

From backend/endpoints/responses/assets.py:

- BaseAsset currently declares nullable rom_id plus retained_catalog_id, so ScreenshotSchema inherits an impossible detached state.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make exact harness cleanup retry-safe per resource</name>
  <files>backend/tools/verify_phase6_contracts.py, backend/tests/tools/test_verify_phase6_contracts.py</files>
  <read_first>backend/tools/verify_phase6_contracts.py, backend/tests/tools/test_verify_phase6_contracts.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-14-SUMMARY.md</read_first>
  <behavior>
    - If runner removal fails, runner_container_id remains and the next cleanup retries that same full ID.
    - Successful runner removal clears only runner state; volume/env cleanup continues and each successful resource is not removed twice.
    - Failed volume or env removal retains only that resource's ownership state for retry.
    - _cleaned becomes true only after every owned resource is confirmed absent; name, label, PID, glob, and unrelated-resource lookup are never used.
  </behavior>
  <action>RED: extend FakeCommand with first-call failures for runner removal, volume removal, and env unlink, followed by success. Assert exact call sequences across two cleanup calls, partial-success state, no duplicate success removals, secret redaction, and final completion. Commit with hooks as test(06-22): specify cleanup retry semantics. GREEN: replace the early global completion flag with per-resource state transitions. Clear runner_container_id only after docker rm -f returns success or an explicitly classified already-absent result; clear _volume_created only after successful/absent removal; clear _env_file only after unlink confirms absence. Attempt every owned resource on each pass, aggregate bounded errors, and set _cleaned only when all three states are clear. Keep exact full-ID removal and never discover targets by name/label/PID. Commit with hooks as fix(06-22): retain failed cleanup ownership. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0622 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/tools/test_verify_phase6_contracts.py -x</automated>
  </verify>
  <acceptance_criteria>Real failure-then-retry tests pass for all three owned resource types and exact removal never targets or repeats unrelated resources.</acceptance_criteria>
  <done>A transient cleanup failure cannot disable a later exact cleanup retry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specialize live screenshot and detachable asset schemas</name>
  <files>backend/endpoints/responses/assets.py, backend/tests/endpoints/test_screenshots.py, backend/tests/endpoints/test_saves.py, backend/tests/endpoints/test_states.py, frontend/src/__generated__/models/ScreenshotSchema.ts, frontend/src/__generated__/models/UserScreenshotSchema.ts</files>
  <read_first>backend/models/assets.py, backend/endpoints/responses/assets.py, backend/endpoints/responses/rom.py, backend/endpoints/screenshots.py, backend/tests/endpoints/test_screenshots.py, backend/tests/endpoints/test_saves.py, backend/tests/endpoints/test_states.py, frontend/src/__generated__/models/ScreenshotSchema.ts, frontend/src/__generated__/models/UserScreenshotSchema.ts, frontend/src/__generated__/models/SaveSchema.ts, frontend/src/__generated__/models/StateSchema.ts</read_first>
  <behavior>
    - ScreenshotSchema and UserScreenshotSchema require integer rom_id and expose no retained_catalog_id in OpenAPI or generated TypeScript.
    - SaveSchema/StateSchema and user variants continue to allow rom_id null with retained_catalog_id.
    - Screenshot serialization rejects a missing/null ROM owner, matching the non-null persistence contract.
    - Save/state nested screenshot fields retain the specialized live screenshot schema.
  </behavior>
  <action>RED: add endpoint/schema tests that inspect Pydantic JSON schema and runtime validation for screenshot required ownership, plus save/state detached compatibility. Commit with hooks as test(06-22): specify asset ownership contracts. GREEN: split common file metadata from ownership fields. Make ScreenshotSchema add rom_id: int and omit retained_catalog_id; make detachable SaveSchema and StateSchema add rom_id: int | None and retained_catalog_id: int | None. Preserve existing model validators, user-enriched variants, nested screenshot fields, endpoint behavior, and authorization. Run the controlled harness to regenerate only backend-authoritative models; do not hand-edit generated content beyond accepting the exact generated diff. Typecheck after regeneration. This preserves D-22 retained user value while prohibiting an impossible screenshot lifecycle. Commit with hooks as fix(06-22): align screenshot contract with persistence. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0622 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_screenshots.py tests/endpoints/test_saves.py tests/endpoints/test_states.py -k "schema or ownership or detached" -x</automated>
  </verify>
  <acceptance_criteria>Backend runtime/schema tests and generated frontend typecheck agree: screenshots are live-ROM-only, saves/states remain detachable, and no impossible screenshot state is advertised.</acceptance_criteria>
  <done>The OpenAPI and generated screenshot lifecycle exactly matches database persistence.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Risk                                                                       | Disposition | Mitigation                                                              |
| --------------------------------- | -------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------- |
| harness to Docker daemon          | Cleanup failure loses exact ownership and leaks credentials/resources      | mitigate    | Retain per-resource identity until confirmed absent; redact diagnostics |
| backend ORM to public schema      | Client is told a screenshot may be detached when runtime cannot produce it | mitigate    | Separate live and detachable ownership schemas                          |
| OpenAPI to generated frontend     | Hand edits drift from backend authority                                    | mitigate    | Controlled regeneration and typecheck                                   |
| asset identifier to authorization | Schema change weakens owner/platform checks                                | mitigate    | Preserve endpoint authorization and rerun screenshot/save/state tests   |

## STRIDE Threat Register

| Threat ID       | Category               | Component                   | Disposition | Mitigation Plan                                                                  |
| --------------- | ---------------------- | --------------------------- | ----------- | -------------------------------------------------------------------------------- |
| T-06-22-01      | Tampering              | cleanup resource state      | mitigate    | Clear identity only after successful or already-absent removal                   |
| T-06-22-02      | Repudiation            | cleanup retry               | mitigate    | Exact failure/retry call sequence tests                                          |
| T-06-22-03      | Information disclosure | env cleanup diagnostics     | mitigate    | Never print env values; retain bounded error text                                |
| T-06-22-04      | Denial of service      | repeated cleanup            | accept      | Idempotent per-resource state prevents repeated successful removals              |
| T-06-22-05      | Spoofing               | impossible screenshot owner | mitigate    | Required integer ROM ownership in runtime and generated types                    |
| T-06-22-06      | Elevation of privilege | detached asset access       | mitigate    | Preserve live-or-retained effective-platform authorization for saves/states only |
| </threat_model> |

<verification>
Use the focused romm_test_0622 task commands as immediate under-30-second feedback and remove the DB afterward. Then run cd backend &amp;&amp; /home/d1sk/.local/bin/uv run python tools/verify_phase6_contracts.py --source-container romm-dev --checkout /home/d1sk/romm --expected-image romm-romm-dev --network romm_default --user 1000:1000 --entrypoint /bin/sleep --command infinity --env-allowlist DB_HOST,DB_NAME,DB_PASSWD,DB_PORT,DB_USER,REDIS_DB,REDIS_HOST,REDIS_PORT,REDIS_SSL,ROMM_BASE_PATH --port 39006, followed by frontend typecheck. Assert runner container, env file, and Node volume are absent after normal and injected cleanup. Run scoped Trunk and git diff --check. Do not deploy or restart services.
</verification>

<success_criteria>

- Both 06-REVIEW warnings are deterministically closed, not deferred.
- Cleanup failure remains retryable without broad discovery or secret disclosure.
- Generated screenshot models express only states backed by persistence, while detached saves/states remain supported.
  </success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-22-SUMMARY.md.
</output>
