---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 09
type: execute
wave: 8
depends_on: [06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08]
files_modified:
  - backend/tests/integration/test_legacy_migration.py
  - backend/tests/handler/filesystem/test_storage_inventory.py
  - backend/tests/endpoints/test_storage_policy_denials.py
  - backend/tests/endpoints/test_storage.py
  - backend/tools/verify_phase6_contracts.py
  - backend/tests/tools/test_verify_phase6_contracts.py
  - frontend/src/__generated__
  - .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
autonomous: true
requirements:
  [CAT-01, CAT-02, CAT-03, CAT-04, MIG-01, MIG-02, MIG-03, MIG-04, MIG-05]
must_haves:
  truths:
    - "All Phase 6 regressions pass on writable and read-only fixtures."
    - "Closed inventory proves all external mutations absent or denied."
    - "Controlled OpenAPI generation and frontend typecheck pass."
  artifacts:
    - path: "backend/tests/integration/test_legacy_migration.py"
      provides: "Integration immutability and contract closure"
    - path: ".planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/tools/verify_phase6_contracts.py"
      to: "frontend/src/__generated__"
      via: "controlled OpenAPI generation"
      pattern: "39006|openapi"
---

<objective>
Integration immutability and contract closure
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/tests/integration/test_legacy_migration.py, backend/tests/handler/filesystem/test_storage_inventory.py, backend/tests/endpoints/test_storage_policy_denials.py, backend/tests/endpoints/test_storage.py, backend/tools/verify_phase6_contracts.py, backend/tests/tools/test_verify_phase6_contracts.py, frontend/src/__generated__, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/tests/integration/test_mapped_scan.py
@backend/tests/handler/filesystem/test_storage_inventory.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify controlled contract-closure harness</name>
  <files>backend/tests/tools/test_verify_phase6_contracts.py</files>
  <behavior>
    - Harness rejects a runner not bind-mounted from this exact checkout and rejects an occupied loopback port.
    - Harness records one Uvicorn PID, uses bounded readiness, a uniquely named Node volume, exact OpenAPI flags, and exact cleanup.
    - Failure paths never use host networking, published ports, port 3000, pkill, killall, arbitrary services, or Team4s.
  </behavior>
  <action>Read backend/tests/integration/test_mapped_scan.py, backend/tests/handler/filesystem/test_storage_inventory.py, and the controlled generation contract in Phase 3 Plan 06. Create only backend/tests/tools/test_verify_phase6_contracts.py with subprocess fakes and RED expectations for the not-yet-created harness. Do not invoke the missing harness in this task.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/tools/test_verify_phase6_contracts.py -x</automated></verify>
  <done>The focused test fails because backend/tools/verify_phase6_contracts.py and its controlled lifecycle are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Integration immutability and contract closure</name>
  <files>backend/tests/integration/test_legacy_migration.py, backend/tests/handler/filesystem/test_storage_inventory.py, backend/tests/endpoints/test_storage_policy_denials.py, backend/tests/endpoints/test_storage.py, backend/tools/verify_phase6_contracts.py, backend/tests/tools/test_verify_phase6_contracts.py, frontend/src/__generated__, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Run Nyquist samples for catalog/mapping removal, cancellation, reconnection, two grammars, impact, atomic migration, conflicts, first-use rollback, expiry/no-fallback and restart persistence. Manifests include path/type/mode/size/SHA-256/symlink identity on writable/read-only fixtures; exclude atime. Extend closed storage inventory so source delete/rename/move/upload/extract/patch/create/overwrite remain absent or denied before I/O. Create backend/tools/verify_phase6_contracts.py plus unit tests as the controlled generation harness. It must preflight that loopback port 39006 is unused inside the verified romm-dev runner, verify docker inspect maps this exact checkout to /app, launch Uvicorn from /app/backend on 127.0.0.1:39006 with a recorded PID and bounded readiness polling, run Node 24 in --network container:romm-dev with this checkout frontend bind-mounted and a uniquely named task-owned node_modules volume, invoke the installed openapi binary using the exact package.json flags and port 39006, run npm typecheck, and trap cleanup of only the recorded PID, PID/log files, and owned volume. Never use host networking, published ports, pkill/killall, port 3000, Team4s, or an arbitrary server. Regenerate types because schemas/routes changed; never hand-edit generated files or add Phase 7 UI. Update VALIDATION with actual evidence and run isolated MariaDB/MySQL/PostgreSQL cycles. Do not deploy, mutate source, touch v1 or Team4s services.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/integration/test_legacy_migration.py tests/handler/filesystem/test_storage_inventory.py tests/endpoints/test_storage_policy_denials.py tests/endpoints/test_storage.py tests/tools/test_verify_phase6_contracts.py -x &amp;&amp; cd .. &amp;&amp; test "$(docker inspect -f '{{range .Mounts}}{{if eq .Destination \"/app\"}}{{.Source}}{{end}}{{end}}' romm-dev)" = "$PWD" &amp;&amp; cd backend &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --runner-container romm-dev --handler-tests --handler-test-repetitions 1 &amp;&amp; uv run python tools/verify_phase6_contracts.py --runner-container romm-dev --port 39006</automated></verify>
  <done>Integration immutability and contract closure passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-09B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-09C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
| T-06-09D | D | work | mitigate | Fixed budgets, safe boundaries and retryable durable state. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-09-SUMMARY.md
</output>
