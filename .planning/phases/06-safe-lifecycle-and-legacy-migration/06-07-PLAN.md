---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 07
type: tdd
wave: 6
depends_on: [06-06]
files_modified:
  - backend/handler/database/legacy_migration_handler.py
  - backend/handler/storage/read_context.py
  - backend/handler/scan_handler.py
  - backend/endpoints/roms/__init__.py
  - backend/endpoints/roms/files.py
  - backend/tests/integration/test_legacy_migration.py
autonomous: true
requirements: [MIG-04]
must_haves:
  truths:
    - "Every productive consumer marks first use before source open."
    - "CAS is followed by exact revision revalidation."
    - "Concurrent first-use attempts produce one durable marker and every consumer revalidates the same mapping revision."
  artifacts:
    - path: "backend/handler/database/legacy_migration_handler.py"
      provides: "Productive first-use CAS integration"
    - path: "backend/tests/integration/test_legacy_migration.py"
      provides: "First-use ordering, concurrency, and immutable-source evidence"
  key_links:
    - from: "backend/handler/storage/read_context.py"
      to: "backend/handler/database/legacy_migration_handler.py"
      via: "source-open boundary to first-use CAS"
      pattern: "first_used_at|mark.*use"
---

<objective>
Productive first-use CAS integration
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/database/legacy_migration_handler.py, backend/handler/storage/read_context.py, backend/handler/scan_handler.py, backend/endpoints/roms/__init__.py, backend/endpoints/roms/files.py, backend/tests/integration/test_legacy_migration.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/storage/read_context.py
@backend/endpoints/roms/files.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify productive first-use ordering</name>
  <files>backend/tests/integration/test_legacy_migration.py</files>
  <behavior>
    - Scan, hash, stream, play, single-file download, and each multi-file member mark first use before the first source open.
    - The marker stores only migration identity, bounded operation, and timestamp, then exact mapping ID/revision is revalidated.
    - Concurrent consumer attempts are idempotent and source manifests remain identical.
  </behavior>
  <action>Read backend/handler/storage/read_context.py and backend/endpoints/roms/files.py. Write RED barrier tests for every productive consumer, descriptor-open ordering, idempotent concurrent CAS, post-marker revision revalidation, and writable/read-only source manifests. This plan defines only first-use consumer wiring; lifecycle reversal is owned by Plan 08.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/integration/test_legacy_migration.py -k "first_use or source_open or consumer_cas" -x</automated></verify>
  <done>Tests fail only because productive consumers do not yet mark and revalidate first use.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire first-use CAS into every productive consumer</name>
  <files>backend/handler/database/legacy_migration_handler.py, backend/handler/storage/read_context.py, backend/handler/scan_handler.py, backend/endpoints/roms/__init__.py, backend/endpoints/roms/files.py, backend/tests/integration/test_legacy_migration.py</files>
  <behavior>
    - MappingReadContext invokes one durable idempotent first-use CAS before capability issuance and source open.
    - Scan/hash batch boundaries and all delivery paths use the same marker and exact revision revalidation.
    - No marker contains a source path and no consumer can bypass the shared boundary.
  </behavior>
  <action>Follow MappingReadContext.validate/boundary/open and the mapped delivery analog. Add a legacy_migration_handler first-use CAS keyed by migration and mapping identity with bounded operation. Invoke it before first external open for scan, hash, stream, play, download, and each multi-file member, then revalidate exact mapping ID/revision. Centralize the call at the shared source-open boundary where possible and prove all entry points reach it. Do not add status routes, response schemas, lifecycle reversal, restart orchestration, or dialect-verifier work in this plan.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/integration/test_legacy_migration.py -k "first_use or source_open or consumer_cas" -x</automated></verify>
  <done>Every productive source consumer durably marks first use before open and revalidates exact mapping identity without changing source.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-07B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-07C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
| T-06-07D | D | work | mitigate | Fixed budgets, safe boundaries and retryable durable state. |
</threat_model>
<verification>
Run the focused first-use integration tests with descriptor-open and database-write instrumentation on writable and read-only fixtures. Confirm every files_modified path appears in a task and no endpoint schema or dialect verifier is touched.
</verification>
<success_criteria>
All productive consumers share one durable pre-open first-use CAS, exact mapping revalidation, idempotent concurrency behavior, and immutable source evidence.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-07-SUMMARY.md
</output>
