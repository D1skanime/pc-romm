---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 54
type: execute
wave: 4
depends_on: [06-53]
files_modified:
  - .planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json
  - .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
  - .planning/phases/06-safe-lifecycle-and-legacy-migration/06-SOURCE-AUDIT.md
autonomous: true
requirements:
  [CAT-01, CAT-02, CAT-03, CAT-04, MIG-01, MIG-02, MIG-03, MIG-04, MIG-05]
gap_closure: true
must_haves:
  truths:
    - "One fresh complete acceptance run matches every exact post-Plan-52 prior-module collected/outcome tuple and passes all current Phase 6 backend modules, three dialects, generated contracts, focused/full frontend, typecheck, build, locales, static checks, source manifests, DB continuity, and exact cleanup."
    - "Fresh evidence closes folder/multi-file reconnection, token-manual lifecycle and races, hidden screenshot PUT/DELETE, and descriptor reuse without regressing the other 27 must-haves."
    - "The acceptance, validation, and source-binding records contain all 33 exact REQUIRED_ROUND4_TEST_IDS with passing outcomes; omission, rename, shortening, or a different test in the same module/spec cannot satisfy closure."
    - "The final manual_ui stage consumes Plan 53's fresh Chromium record, names the browser-validated commit, and proves no relevant UI path drift; Plan 45 evidence cannot be relabeled as current."
    - "Validation and source audit bind to the exact new run_id/digest and are written only after every gate and cleanup succeeds."
  artifacts:
    - path: ".planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json"
      provides: "Fresh structured Round 4 acceptance record"
    - path: ".planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md"
      provides: "Fresh 31-of-31 verification narrative bound to the acceptance digest"
    - path: ".planning/phases/06-safe-lifecycle-and-legacy-migration/06-SOURCE-AUDIT.md"
      provides: "Exact current no-gap multi-source coverage audit"
  key_links:
    - from: "verify_phase6_acceptance.py --run-complete"
      to: "06-47-ACCEPTANCE.json"
      via: "atomic write after all stages and cleanup"
      pattern: "run_id|run_digest"
    - from: "acceptance JSON"
      to: "VALIDATION and SOURCE-AUDIT"
      via: "--verify-evidence digest, exact per-module results, exact 33-test result/binding set, exact source-row parser, and fresh UI binding"
      pattern: "verify-evidence|EXPECTED_PRIOR_MODULE_RESULTS|REQUIRED_ROUND4_TEST_IDS|fresh_browser_validated|COVERED"
    - from: "round4_regressions results"
      to: "current MH/CR/WR/UI source rows"
      via: "REQUIRED_ROUND4_SOURCE_BINDINGS with exact unshortened test IDs"
      pattern: "round4_regressions|REQUIRED_ROUND4_SOURCE_BINDINGS"
---

<objective>
Publish fresh complete Phase 6 evidence only after the Round 4 closure passes.
Purpose: Existing Plan 47 evidence does not exercise the verified gaps and must not remain the final authority after product changes.
Output: One new acceptance record plus digest-bound validation and exact source audit.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-48-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-51-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-52-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json
@backend/tools/verify_phase6_acceptance.py
@backend/tests/tools/test_verify_phase6_acceptance.py
<interfaces>
--run-complete performs fail-closed checkout/branch/origin/project/stopped-service preflight, exact baseline check, exact EXPECTED_PRIOR_MODULE_RESULTS equality for all 29 named prior modules, isolated Phase 6 tests, dialect/contracts/frontend/static/manifests/cleanup, verified fresh manual UI evidence, then atomically writes the evidence JSON. It has no --prior-outcomes-min escape hatch. The JSON must contain stages.round4_regressions.results as an ordered list of {"test_id": exact checked-in ID, "outcome": "passed", "source_bindings": exact checked-in source IDs}; its test_id sequence must equal REQUIRED_ROUND4_TEST_IDS exactly and its bindings must equal REQUIRED_ROUND4_SOURCE_BINDINGS exactly. --verify-ui-evidence requires Plan 53's fresh_browser_validated record, UI_EVIDENCE_COMMIT, exact UI/spec digests, all 16 exact Playwright IDs/outcomes, and no drift. --verify-evidence recomputes run_digest; binds VALIDATION run_id/digest, every exact prior tuple, fresh UI metadata, and a 33-row exact Round 4 result table; parses SOURCE-AUDIT source type, semantic label, and exact unshortened test IDs for all REQUIRED_ROUND4_SOURCE_BINDINGS; and requires exact COVERED equality from REQUIRED_SOURCE_ROWS. It rejects a missing, renamed, shortened, reordered, duplicate, extra, non-passing, or same-module/spec substitute test even when module counts and source IDs still match.
</interfaces>
</context>
<test_topology>
Run only on Linux /home/d1sk/romm. romm-dev must be stopped and remain stopped; romm-db-dev and romm-valkey-dev are observed dependencies, not restarted. Backend modules use per-module nonce DB/principal/basetemp/runner. Dialect, contract, Node, and Plan 53 browser resources are exact-ID disposable resources. Before and after the run, audit containers, volumes, databases, and principals for every Round 4 prefix (p0647, p0650/romm-p0650, p0652, p0653/romm-p0653) and io.romm.phase6.round4 ownership label; discovery is assertion-only and must never supply deletion targets. No public port, host network, deploy, service restart, NAS access, broad cleanup, environment dump, or STATE/ROADMAP/REQUIREMENTS edit. Preserve the original 28-entry status baseline hash after evidence commits.
</test_topology>
<tasks>
<task type="auto">
  <name>Task 1: Run the fresh complete acceptance gate</name>
  <files>.planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <action>First repeat fail-closed Linux, root, branch, origin, project, image, stopped-service, normal DB, and exact baseline checks. Run --audit-only and --verify-ui-evidence before the expensive gate. Require the UI record to be fresh_browser_validated with fresh_run=true, a full UI_EVIDENCE_COMMIT other than 364bb2764, exact UI/spec digests, all 16 exact Playwright REQUIRED_ROUND4_TEST_IDS with passed outcomes, zero cleanup, and git no-drift through current HEAD. Run --run-complete with canonical arguments and no removed/compatibility --prior-outcomes-min option. For every PRIOR_PHASE_MODULES record require exact equality with EXPECTED_PRIOR_MODULE_RESULTS for module order, collected, passed, skipped, xfailed, and xpassed; failed/errors/exit must be zero. Do not compare only an aggregate, and do not permit extra tests in another module to offset a deletion. Require stages.round4_regressions.results to contain the exact ordered 33-entry REQUIRED_ROUND4_TEST_IDS sequence, every outcome passed, and exact REQUIRED_ROUND4_SOURCE_BINDINGS. Results must come from the pytest JUnit, focused Vitest JSON, and verified Playwright JSON reporters; reject absent reporter data, a base function in place of its required parameterized nodes, an abbreviated title, a renamed ID, or a real different test from the same module/spec. Require exact source-row semantics, manifests, cleanup, dialects, frontend, and evidence paths. Treat any test-ID/result/binding, module-field, UI drift, source-row, stage, cleanup, service-state, baseline, manifest, generated, or DB-continuity failure as terminal and leave the previous JSON untouched. Record exact fresh per-module results, all exact Round 4 test results, and current browser metadata from structured output; never guess or copy Plan 47 counts/evidence.</action>
  <verify>
    <automated>bash -lc 'python3 backend/tools/verify_phase6_acceptance.py --audit-only --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev; python3 backend/tools/verify_phase6_acceptance.py --verify-ui-evidence --checkout /home/d1sk/romm --ui-evidence .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json; python3 backend/tools/verify_phase6_acceptance.py --run-complete --checkout /home/d1sk/romm --branch codex/pc-module-analysis --origin https://github.com/rommapp/romm.git --project "RomM PC Library" --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev --network romm_default --node-image node:24-bookworm --contract-port 39006 --baseline-untracked-count 28'</automated>
  </verify>
  <acceptance_criteria>
    - Every backend module and all dialect/contract/frontend/static/manifest stages exit zero.
    - Every prior module's collected/pass/skip/xfail/xpass fields equal the exact post-Plan-52 contract; no aggregate-minimum field or CLI override exists.
    - manual_ui embeds the fresh Plan 53 Chromium run/commit/digests and proves no relevant UI drift after validation.
    - stages.round4_regressions contains all 33 exact checked-in test IDs in order with passed outcomes and exact source bindings; omission, rename, shortening, reordering, or same-module/spec substitution fails.
    - Current gap tests execute from reporter-derived structured records, not only module membership, counts, or narrative.
    - Source and generated before/after digests match, normal DB returns 1, and every p0647/p0650/p0652/p0653 prefix/label owned resource count is zero.
    - JSON changes atomically only after complete success.
  </acceptance_criteria>
  <done>A fresh structured run proves the product tree and cleanup state.</done>
</task>
<task type="auto">
  <name>Task 2: Bind validation and exact source coverage to the fresh run</name>
  <files>.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-SOURCE-AUDIT.md</files>
  <read_first>CLAUDE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-SOURCE-AUDIT.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-48-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-51-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-52-SUMMARY.md</read_first>
  <action>Rewrite validation from the fresh JSON only: exact run_id/digest, canonical command without aggregate-minimum option, all 29 exact per-module collected/outcome tuples, commands, exits, current 31 must-haves, current CR-01..04 and WR-01, manifests, dialects, generated/frontend/UI/static stages, fresh browser commit/digests/matrix, DB continuity, stopped service, all-prefix/label cleanup, and baseline. Add one machine-parseable Round 4 result row for each entry of REQUIRED_ROUND4_TEST_IDS, in exact order, containing the full unshortened identifier, outcome passed, and exact source bindings copied from stages.round4_regressions; aliases, basenames, base-function IDs, labels, and ellipses are forbidden. Mark MH-14, MH-29, MH-30, and MH-31 verified only when their exact bound IDs and passed outcomes are present. Rewrite source audit with one unique row matching REQUIRED_SOURCE_ROWS for GOAL-06; CAT-01..04; MIG-01..05; R-01..09; D-01..23; MH-01..31; current CR-01..04; current WR-01; and UI-01..08. Use source type REVIEW and the exact current semantic labels: CR-01 "Folder and multi-file ROM selection from exact observed child evidence"; CR-02 "Token-path primary manual discovery and exact deletion"; CR-03 "Primary manual redownload staged CAS and active-v2 pending serialization"; CR-04 "Screenshot visibility before update and delete effects"; WR-01 "Single-consumption staged-publication descriptor close". In the evidence/test-ID column, render the full exact REQUIRED_ROUND4_SOURCE_BINDINGS set for every affected MIG/MH/CR/WR/UI row; the parser must compare parsed IDs to the checked-in mapping exactly, so a missing, shortened, renamed, reordered, duplicate, extra, or same-module/spec substitute ID fails even when the row remains COVERED. Map Plans 48-52 precisely, retain prior plan coverage for unaffected items, include no stale WR-02/WR-03 current rows or old Plan 47 meanings, and do not claim Phase 7-9 items. Run deterministic verification after formatting and reject any stale source type/description, missing/duplicate/unknown/deferred row, prior-tuple mismatch, Round 4 exact-result/binding mismatch, or UI binding mismatch.</action>
  <verify>
    <automated>python3 backend/tools/verify_phase6_acceptance.py --verify-evidence --checkout /home/d1sk/romm --evidence-json .planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-ACCEPTANCE.json --validation .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md --source-audit .planning/phases/06-safe-lifecycle-and-legacy-migration/06-SOURCE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Validation binds exact current run identity/digest and actual structured results.
    - Validation binds every exact prior-module result and the fresh browser-validated commit/digests, not aggregate or relabeled historical evidence.
    - Validation contains exactly 33 full reporter-derived Round 4 IDs, passed outcomes, and exact source bindings; --verify-evidence rejects omission, rename, shortening, order change, duplicate, extra, failure, or same-module/spec substitution.
    - Source audit covers all mandatory source types and current review/verification rows exactly once with exact current semantic labels, and affected rows bind the exact unshortened REQUIRED_ROUND4_SOURCE_BINDINGS sets with no gaps.
    - Evidence parser, git diff --check, all-prefix/label audit-only cleanup, stopped romm-dev, normal DB, and original baseline all pass after hooks.
  </acceptance_criteria>
  <done>Fresh evidence, validation, and source coverage form one deterministic no-gap authority.</done>
</task>
</tasks>
<source_coverage>
Mandatory audit covers GOAL-06, all nine Phase 6 REQs, all R-01..09 research constraints, all D-01..23 locked decisions, current MH-01..31, CR-01..04, WR-01, and UI-SPEC UI-01..08. Deferred Phase 7-9 work remains excluded.
</source_coverage>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| harness to runners/DBs | Tests must not affect normal services or persistent resources. |
| source/generated trees to evidence | Any drift invalidates acceptance. |
| JSON to Markdown evidence | Claims must bind exact run identity and digest. |
| test reporters to JSON and Markdown | Exact test identity, parameter/title path, outcome, and source binding cannot be replaced by module membership or aliases. |
| source artifacts to audit rows | Missing or stale items cannot count as coverage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-54-01 | Spoofing | run identity | mitigate | Canonical command, cryptographic run_id/digest, verified checkout. |
| T-06-54-02 | Tampering | source/generated tree | mitigate | Complete before/after SHA-256 manifests. |
| T-06-54-03 | Repudiation | validation claims | mitigate | Structured stages and deterministic evidence parser. |
| T-06-54-04 | Information disclosure | logs/evidence | mitigate | Existing redaction and bounded counts only. |
| T-06-54-05 | Denial of service | disposable resources | mitigate | Per-module limits, no restarts, finally cleanup. |
| T-06-54-07 | Tampering | Round 4 evidence | mitigate | --run-complete and --verify-evidence require all 33 exact ordered IDs, passed outcomes, and exact source bindings in JSON, validation, and affected source rows. |
| T-06-54-06 | Elevation of privilege | cleanup | mitigate | Exact created IDs only; discovery is assertion-only. |
</threat_model>
<verification>
After Task 2, rerun --verify-evidence, --verify-ui-evidence, and --audit-only; require the exact 33-ID ordered passed result set and exact REQUIRED_ROUND4_SOURCE_BINDINGS in acceptance JSON, validation, and affected source rows; then run git diff --check, stopped-service/image identity, normal DB SELECT 1, exact generated/source/UI digests, zero resources for every Round 4 prefix/label, and exact 28-entry baseline hash. Do not edit tracking files or commit with bypass.
</verification>
<success_criteria>
All 31 must-haves and nine requirements have fresh structured proof; exact per-module regression counts, all 33 exact Round 4 IDs with passed outcomes and source bindings, and fresh browser evidence are bound; no shortened, renamed, omitted, or same-module/spec substitute test is accepted; current gaps/warning are closed; all regression/dialect/frontend/static/manifest/all-prefix cleanup gates pass; evidence is digest-bound and exact-row complete.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-54-SUMMARY.md with exact run identity, counts, commands, exits, cleanup, and baseline evidence.
</output>
