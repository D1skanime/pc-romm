---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/models/catalog_lifecycle.py
  - backend/models/storage.py
  - backend/models/assets.py
  - backend/models/play_session.py
  - backend/alembic/versions/0111_safe_lifecycle_legacy_migration.py
  - backend/tests/models/test_safe_lifecycle.py
  - backend/tests/tools/test_verify_storage_migrations.py
  - backend/tools/verify_storage_migrations.py
autonomous: true
requirements: [CAT-02, MIG-02, MIG-04]
must_haves:
  truths:
    - "Catalog detachment preserves saves, states, play sessions, and reconnectable identity."
    - "Lifecycle state survives restart."
    - "Alembic 0111 round-trips three dialects."
  artifacts:
    - path: "backend/models/catalog_lifecycle.py"
      provides: "Durable ownership and lifecycle schema"
    - path: "backend/tools/verify_storage_migrations.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/models/assets.py"
      to: "backend/models/catalog_lifecycle.py"
      via: "retained ownership"
      pattern: "retained_catalog"
---

<objective>
Durable ownership and lifecycle schema
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/models/catalog_lifecycle.py, backend/models/storage.py, backend/models/assets.py, backend/models/play_session.py, backend/alembic/versions/0111_safe_lifecycle_legacy_migration.py, backend/tests/models/test_safe_lifecycle.py, backend/tests/tools/test_verify_storage_migrations.py, backend/tools/verify_storage_migrations.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/models/storage.py
@backend/alembic/versions/0109_mapping_administration_contracts.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Durable ownership and lifecycle schema</name>
  <files>backend/tests/models/test_safe_lifecycle.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart where applicable, and source-manifest equality.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/models/storage.py, backend/alembic/versions/0109_mapping_administration_contracts.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_safe_lifecycle.py tests/tools/test_verify_storage_migrations.py -x &amp;&amp; cd .. &amp;&amp; test "$(docker inspect -f '{{range .Mounts}}{{if eq .Destination \"/app\"}}{{.Source}}{{end}}{{end}}' romm-dev)" = "$PWD" &amp;&amp; cd backend &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --runner-container romm-dev</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Durable ownership and lifecycle schema</name>
  <files>backend/models/catalog_lifecycle.py, backend/models/storage.py, backend/models/assets.py, backend/models/play_session.py, backend/alembic/versions/0111_safe_lifecycle_legacy_migration.py, backend/tests/models/test_safe_lifecycle.py, backend/tests/tools/test_verify_storage_migrations.py, backend/tools/verify_storage_migrations.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Create failing tests then implement retained catalog identity, typed owned-cleanup intent, detection result, migration/rollback/version/expiry and nullable first-use state. Per D-09, D-14 through D-16 and D-22, Save/State cannot cascade away with Rom and PlaySession stays attributable. Store bounded IDs, relative paths, actor/timestamps, exact logical identity and optional complete hashes, never host paths or row dumps. Alembic 0111 uses named portable constraints/indexes and guarded downgrade preflight. Extend pristine and seeded-0110 upgrade/downgrade/re-upgrade verification for MariaDB, MySQL and PostgreSQL; state survives restart.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_safe_lifecycle.py tests/tools/test_verify_storage_migrations.py -x &amp;&amp; cd .. &amp;&amp; test "$(docker inspect -f '{{range .Mounts}}{{if eq .Destination \"/app\"}}{{.Source}}{{end}}{{end}}' romm-dev)" = "$PWD" &amp;&amp; cd backend &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --runner-container romm-dev</automated></verify>
  <done>Durable ownership and lifecycle schema passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-01B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-01C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
The mandatory real-dialect gate runs from repository root after verifying with docker inspect that romm-dev destination /app is sourced from exactly $PWD: cd backend && uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --runner-container romm-dev. It must use uniquely named disposable databases for pristine and seeded-0110 upgrade/downgrade/re-upgrade and remove only them; never use an existing database service.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-01-SUMMARY.md
</output>
