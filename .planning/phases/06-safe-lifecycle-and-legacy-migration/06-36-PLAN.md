---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 36
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/v2/sourceMutationInventory.test.ts
autonomous: true
requirements: [CAT-04]
gap_closure: true

must_haves:
  truths:
    - "The production inventory discovers global/member fetch, callable Axios clients, request(config), method members, aliases, wrappers, and reachable services."
    - "A recognized transport with any non-reducible receiver, method, URL, config, spread, computed key, alias, or wrapper fails closed with bounded redacted diagnostics."
    - "The live play-session keepalive POST appears as a safe database/control-plane operation and no runtime UI is introduced."
  artifacts:
    - path: "frontend/src/v2/sourceMutationInventory.test.ts"
      provides: "Complete fail-closed active-v2 HTTP transport inventory"
  key_links:
    - from: "production modules and reachable services"
      to: "extractRawCalls"
      via: "AST transport/config resolution"
      pattern: "fetch|request|CallExpression"
    - from: "extractRawCalls"
      to: "finalInventorySource"
      via: "same path for live and negative fixtures"
      pattern: "finalInventorySource"
    - from: "play-session.ts fetch POST"
      to: "CONTROL_PLANE database authority"
      via: "live service discovery"
      pattern: "play-sessions"
---

<objective>
Close every remaining transport-form blind spot in the active-v2 semantic mutation inventory.

Purpose: WR-02 proves the claimed exhaustive CAT-04 guard can silently omit a live direct POST and other recognized client forms.
Output: One build-time TypeScript AST guardrail with no runtime screen, setting, copy, or dependency.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@frontend/src/v2/sourceMutationInventory.test.ts
@frontend/src/services/api/index.ts
@frontend/src/services/api/play-session.ts
@frontend/src/services/api/rom.ts
@frontend/src/v2/sourceMutationControls.test.ts
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-29-SUMMARY.md

<interfaces>
Preserve the current Authority and RawCall vocabulary, route family classification, safeImporter basename redaction, active module graph, and zero-forbidden assertion. Extend extraction, not runtime code. Diagnostics may name only a redacted importer basename, transport kind, bounded function label, and UNKNOWN fields; never include arguments, payloads, URLs containing dynamic values, tokens, absolute paths, or source snippets.
</interfaces>
</context>

<test_topology>
Plan 06-36 frontend tasks run only in node:24-bookworm with one exact task-owned node_modules volume whose name contains a 128-bit lowercase-hex nonce. Each command proves Docker authority and exact volume-name absence before registering cleanup or creating the volume, installs EXIT/INT/TERM handling before creation, records ownership only after docker volume create returns the exact requested name, and validates that exact name by inspect. Cleanup preserves the primary exit status, emits a bounded cleanup failure when needed, removes only the exact owned result artifact and volume without force or discovery, requires exact volume inspect to fail with not-found status, and makes cleanup failure fail an otherwise successful task. Task 1 runs the entire selected Vitest file with JSON reporter output inside the owned volume; a deterministic Node parser requires valid JSON, process status 1, exactly one failed assertion with the exact fully qualified name and file, every other selected assertion passed, exact counters, and zero pending/todo/runtime/unhandled/config/transform/worker/timeout/OOM/infrastructure conditions. npm command echo and console-name grep are never evidence. Task 2 uses the same ownership-safe cleanup contract. Run npm as UID/GID 1000 with HOME=/tmp and NODE_OPTIONS=--max-old-space-size=4096. Never use broad discovery deletion, retain a result artifact, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED route every missing transport form through finalInventorySource</name>
  <files>frontend/src/v2/sourceMutationInventory.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/services/api/index.ts, frontend/src/services/api/play-session.ts, frontend/src/services/api/rom.ts, frontend/src/v2/sourceMutationControls.test.ts, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Global/member fetch is represented.
    - Callable/request(config) forms are represented.
    - Dynamic recognized transports cannot disappear silently.
  </behavior>
  <action>Per D-05 and D-11, keep the inventory's authority distinction between RomM control-plane mutation and forbidden source-library mutation. Add table-driven fixtures that enter only through finalInventorySource for fetch(url, init), window.fetch, imported callable api(config), axios(config), api.request(config), static member methods, default/named/namespace aliases, immutable const configs, reducible templates/concatenation, a one-layer recognized wrapper, spread/computed/dynamic/mutable configs, conditional clients, and unresolved wrappers. Add the exact named test live_play_session_keepalive_is_inventoried from the real play-session service graph. Expected safe cases classify; every recognized unresolved case throws the bounded diagnostic. Record the live keepalive test as behavioral RED because current extraction returns no call. Use the existing describe block exactly as describe("final active v2 semantic mutation closure") and the exact test title "live_play_session_keepalive_is_inventoried" in src/v2/sourceMutationInventory.test.ts; Vitest 4.1.8 must emit ancestorTitles ["final active v2 semantic mutation closure"] and fullName "final active v2 semantic mutation closure live_play_session_keepalive_is_inventoried" with normal spacing. Run the entire file with JSON reporter output and apply the deterministic record, counter-invariant, file, ancestry, title, fullName, failure-kind, and infrastructure parser from verify; console output and requested-name echo cannot satisfy RED.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0636-t1-node-$nonce; result=/app/node_modules/p0636-t1-vitest-$nonce.json; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0636-t1-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; set +e; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/v2/sourceMutationInventory.test.ts --reporter=json --outputFile=\"$result\""; status=$?; set -e; test "$status" -eq 1; docker run --rm --user 1000:1000 -e RESULT="$result" -e EXPECTED_SUITE="final active v2 semantic mutation closure" -e EXPECTED_TEST="live_play_session_keepalive_is_inventoried" -e EXPECTED_FILE="src/v2/sourceMutationInventory.test.ts" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm node -e "const fs=require(\"fs\");let d;try{d=JSON.parse(fs.readFileSync(process.env.RESULT,\"utf8\"));}catch(e){process.exit(41);}const suites=Array.isArray(d.testResults)?d.testResults:[];const assertions=suites.flatMap(r=&gt;Array.isArray(r.assertionResults)?r.assertionResults:[]);const failed=assertions.filter(a=&gt;a.status===\"failed\");const passed=assertions.filter(a=&gt;a.status===\"passed\");const owner=suites.find(r=&gt;(r.assertionResults||[]).includes(failed[0]));const file=String(owner&amp;&amp;owner.name||\"\").replaceAll(\"\\\\\",\"/\");const failureText=failed.flatMap(a=&gt;Array.isArray(a.failureMessages)?a.failureMessages:[]).join(\"\\n\");const infraText=JSON.stringify({messages:suites.map(r=&gt;r&amp;&amp;r.message||\"\"),failures:failureText,errors:Array.isArray(d.errors)?d.errors:[],unhandled:Array.isArray(d.unhandledErrors)?d.unhandledErrors:[]});const forbidden=/(collection|collecting|transform|config|worker|test timed out|timeout|out of memory|\\boom\\b|enomem|no tests?|no test suite|failed to load|cannot find module|module not found|infrastructure)/i;const unhandled=Array.isArray(d.unhandledErrors)?d.unhandledErrors.length:0;const errors=Array.isArray(d.errors)?d.errors.length:0;const expectedFull=process.env.EXPECTED_SUITE+\" \"+process.env.EXPECTED_TEST;const exactAncestors=failed.length===1&amp;&amp;Array.isArray(failed[0].ancestorTitles)&amp;&amp;failed[0].ancestorTitles.length===1&amp;&amp;failed[0].ancestorTitles[0]===process.env.EXPECTED_SUITE;const counters=d.numTotalTests===assertions.length&amp;&amp;d.numFailedTests===failed.length&amp;&amp;d.numPassedTests===passed.length&amp;&amp;(d.numPendingTests||0)===0&amp;&amp;(d.numTodoTests||0)===0;const ok=d.success===false&amp;&amp;suites.length===1&amp;&amp;owner===suites[0]&amp;&amp;assertions.length&gt;0&amp;&amp;assertions.every(a=&gt;a.status===\"passed\"||a.status===\"failed\")&amp;&amp;counters&amp;&amp;failed.length===1&amp;&amp;passed.length+1===assertions.length&amp;&amp;(d.numRuntimeErrorTestSuites||0)===0&amp;&amp;unhandled===0&amp;&amp;errors===0&amp;&amp;exactAncestors&amp;&amp;failed[0].status===\"failed\"&amp;&amp;failed[0].title===process.env.EXPECTED_TEST&amp;&amp;failed[0].fullName===expectedFull&amp;&amp;Array.isArray(failed[0].failureMessages)&amp;&amp;failed[0].failureMessages.length&gt;0&amp;&amp;!forbidden.test(infraText)&amp;&amp;file.endsWith(\"/\"+process.env.EXPECTED_FILE);if(!ok){process.exit(42);}"'</automated>
  </verify>
  <acceptance_criteria>
    - Fixtures use production extraction, never hand-built RawCall values.
    - The live service test fails because the current direct fetch disappears.
    - Diagnostics are asserted to omit payload fragments, secrets, absolute paths, and raw dynamic routes.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
    - RED requires valid Vitest 4.1.8 JSON, status 1, exactly one testResults record for src/v2/sourceMutationInventory.test.ts, exactly one failed assertion with ancestorTitles ["final active v2 semantic mutation closure"], title "live_play_session_keepalive_is_inventoried", fullName "final active v2 semantic mutation closure live_play_session_keepalive_is_inventoried", non-empty failureMessages, every other assertion passed, and zero collection/transform/config/worker/runtime/unhandled/timeout/OOM/no-tests/infrastructure errors; suite-count fields are not hard-coded.
  </acceptance_criteria>
  <done>Every current blind spot has a production-path RED or fail-closed expectation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN resolve supported calls and reject all unknown recognized transports</name>
  <files>frontend/src/v2/sourceMutationInventory.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/services/api/index.ts, frontend/src/services/api/play-session.ts, frontend/src/services/api/rom.ts, frontend/tsconfig.json</read_first>
  <behavior>
    - No direct/callable/config transport blind spot remains.
    - Unknown recognized syntax fails closed.
    - No runtime UI or new dependency exists.
  </behavior>
  <action>Per D-05 and D-11, keep the inventory's authority distinction between RomM control-plane mutation and forbidden source-library mutation. Teach the AST extractor to recognize global and window fetch, known imported callable clients, and request(config) in addition to current method members. Resolve static object literals and immutable aliases for method and url, default absent method to GET, follow safe import/const aliases and one-layer wrappers without evaluating code, and normalize routes through existing bounded parameter-marker rules. Treat spreads, computed unknown keys, mutable values, conditional receivers, and non-reducible wrapper flows rooted in a recognized transport as unclassifiable. Remove source or argument snippets from diagnostics and keep only bounded redacted identifiers. Ensure the real ingestPlaySessionsKeepalive POST is classified as safe CONTROL_PLANE/database and sourceMutationControls remains unchanged. Add no runtime error, toast, setting, or UI. Execute the exact task-owned volume lifecycle in verify; removal and not-found inspection are mandatory, cleanup preserves a failing primary status, and cleanup failure is both reported and fatal when the primary command succeeded.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0636-t2-node-$nonce; result=; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0636-t2-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/v2/sourceMutationInventory.test.ts src/v2/sourceMutationControls.test.ts"'</automated>
  </verify>
  <acceptance_criteria>
    - All supported transport forms enter final classification.
    - All rooted unknown forms fail with bounded redacted diagnostics.
    - The live keepalive POST is present and safe; zero forbidden live authorities remains true.
    - No runtime frontend or locale file changes.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
  </acceptance_criteria>
  <done>The CAT-04 guardrail is exhaustive for every supported HTTP transport syntax in the live graph.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                                     |
| ----------------------------- | --------------------------------------------------------------- |
| production source to test AST | Static analysis observes potentially sensitive code and routes. |
| AST call to authority         | Omission or misclassification can hide an external mutation.    |
| diagnostic to CI log          | Failure text must remain bounded and redacted.                  |

## STRIDE Threat Register

| Threat ID       | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-36-01      | Tampering              | authority inventory          | mitigate    | All recognized transport forms classify or fail closed.                                                                                                                                                                         |
| T-06-36-02      | Information disclosure | diagnostics                  | mitigate    | Basename/function/transport only; no args, tokens, payloads, raw routes, or host paths.                                                                                                                                         |
| T-06-36-03      | Elevation of privilege | unknown call                 | mitigate    | No CONTROL_PLANE default for unresolved recognized clients.                                                                                                                                                                     |
| T-06-36-04      | Repudiation            | live service coverage        | mitigate    | Real play-session call passes through the production finalInventorySource path.                                                                                                                                                 |
| T-06-36-99      | Elevation of privilege | disposable Node verification | mitigate    | Pre-create exact-name absence, post-create ownership proof, primary-status-preserving exact result/volume cleanup, mandatory volume removal, and not-found inspect prevent deletion or retention outside the task-owned volume. |
| </threat_model> |

<verification>
Use only the disposable Node 24 topology. Run focused inventory/control tests, then full frontend test/typecheck and repository-topology ESLint for the one file. Assert no runtime UI diff, remove the exact Node volume, and run git diff --check.
</verification>

<success_criteria>

- Both p0636 Node lifecycles remove and prove absence of their exact owned volume; Task 1 RED is accepted only by the deterministic Vitest JSON contract.
- WR-02 and CAT-04 warning are closed.
- The live play-session direct POST is explicitly classified.
- Every diagnostic is bounded and path/payload private.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-36-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
