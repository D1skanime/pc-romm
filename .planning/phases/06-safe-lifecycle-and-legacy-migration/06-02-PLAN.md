---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/handler/database/catalog_lifecycle_handler.py
  - backend/endpoints/roms/__init__.py
  - backend/endpoints/responses/rom.py
  - backend/tasks/manual/cleanup_catalog_assets.py
  - backend/tests/endpoints/roms/test_catalog_removal.py
autonomous: true
requirements: [CAT-01, CAT-02, CAT-04]
must_haves:
  truths:
    - "Remove from catalog accepts IDs only."
    - "Removal retains user value and source bytes."
    - "Cleanup targets only RomM-owned assets."
  artifacts:
    - path: "backend/handler/database/catalog_lifecycle_handler.py"
      provides: "Catalog-only game removal"
    - path: "backend/tests/endpoints/roms/test_catalog_removal.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/endpoints/roms/__init__.py"
      to: "backend/handler/database/catalog_lifecycle_handler.py"
      via: "IDs-only removal"
      pattern: "remove.*catalog"
---

<objective>
Catalog-only game removal
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/database/catalog_lifecycle_handler.py, backend/endpoints/roms/__init__.py, backend/endpoints/responses/rom.py, backend/tasks/manual/cleanup_catalog_assets.py, backend/tests/endpoints/roms/test_catalog_removal.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/endpoints/roms/__init__.py
@backend/handler/filesystem/resources_handler.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Catalog-only game removal</name>
  <files>backend/tests/endpoints/roms/test_catalog_removal.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart where applicable, and source-manifest equality.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/endpoints/roms/__init__.py, backend/handler/filesystem/resources_handler.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend && uv run pytest tests/endpoints/roms/test_catalog_removal.py tests/endpoints/test_storage_policy_denials.py -x</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Catalog-only game removal</name>
  <files>backend/handler/database/catalog_lifecycle_handler.py, backend/endpoints/roms/__init__.py, backend/endpoints/responses/rom.py, backend/tasks/manual/cleanup_catalog_assets.py, backend/tests/endpoints/roms/test_catalog_removal.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Tests seed every dependency and writable/read-only manifests. Per D-22, atomically create/reuse retained identity, preserve Save, State, PlaySession and reconnectable metadata/hash identity, remove active visibility plus explicit disposable RomFile, metadata/facets, notes, RomUser, collection/sibling links, and persist idempotent owned-cleanup intents. Replace mixed delete with Remove from catalog accepting IDs only, no delete_from_fs/path in OpenAPI/runtime. Return D-04 facts without typed-name friction. Cleanup only typed RomM-owned assets/resources; never broad cascade or source mutation. Test auth, rollback, retry and CAT-04 denials.</action>
  <verify><automated>cd backend && uv run pytest tests/endpoints/roms/test_catalog_removal.py tests/endpoints/test_storage_policy_denials.py -x</automated></verify>
  <done>Catalog-only game removal passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-02B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-02C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-02-SUMMARY.md
</output>
