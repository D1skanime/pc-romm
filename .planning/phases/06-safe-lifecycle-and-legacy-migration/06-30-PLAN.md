---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 30
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handler/storage/legacy_migration.py
  - backend/tests/handler/storage/test_legacy_migration.py
autonomous: true
requirements: [MIG-03, MIG-05]
gap_closure: true
must_haves:
  truths:
    - "Every entry, wall-clock, per-file byte, aggregate byte, descriptor byte, and descriptor deadline budget exit marks observed file and byte counts as lower bounds."
    - "Exact completed, empty, missing, unsafe, and non-budget error outcomes retain their truthful lower_bound value and remain subject to the existing selectability rules."
    - "A budget-limited result remains unselectable, has no source fingerprint, persists lower_bound=true, and serializes the same value through the public status schema without paths or file names."
  artifacts:
    - path: "backend/handler/storage/legacy_migration.py"
      provides: "Truthful lower-bound metadata for every bounded observation exit"
    - path: "backend/tests/handler/storage/test_legacy_migration.py"
      provides: "Budget matrix plus persistence and API serialization regression"
  key_links:
    - from: "backend/handler/storage/legacy_migration.py _inspect_candidate"
      to: "LegacyDetectionOutcome.lower_bound"
      via: "all partial budget returns propagate true through detect_legacy_storage"
      pattern: "lower_bound"
    - from: "LegacyDetectionOutcome"
      to: "LegacyDetectionResultSchema"
      via: "DBLegacyMigrationHandler.save_detection_result and endpoint schema conversion"
      pattern: "result.lower_bound"
---

<objective>
Correct bounded legacy-observation counts so every partial prefix is explicitly labelled as a lower bound.

Purpose: Make D-09 and MIG-03/MIG-05 status truthful without changing detection authority, selectability, or source access.
Output: Correct budget classification and end-to-end lower-bound tests.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-20-SUMMARY.md
</context>

<interfaces>
From backend/handler/storage/legacy_migration.py:
- _CandidateObservation defaults lower_bound to false and positional budget exits currently pass false.
- budget_reason returns entry_budget or time_budget before directory listing and before each entry.
- file_byte_budget, aggregate_byte_budget, DescriptorHashBudgetError, returned-byte overage, and DescriptorHashDeadlineError terminate observation before the candidate is complete.
- detect_legacy_storage copies observation.lower_bound into LegacyDetectionOutcome and never selects a result without source_fingerprint.

From backend/handler/database/legacy_migration_handler.py and backend/endpoints/storage.py:

- save_detection_result persists final.lower_bound.
- _legacy_detection_result_schema copies result.lower_bound into the public response.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify lower-bound semantics for every budget reason</name>
  <files>backend/tests/handler/storage/test_legacy_migration.py</files>
  <read_first>backend/tests/handler/storage/test_legacy_migration.py, backend/handler/storage/legacy_migration.py, backend/handler/storage/preview.py, backend/handler/filesystem/storage_access.py, backend/exceptions/storage_exceptions.py, backend/handler/database/legacy_migration_handler.py, backend/endpoints/storage.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md</read_first>
  <behavior>
    - Entry budget before the second entry reports the observed prefix with lower_bound=true.
    - Wall-clock budget before listing, between entries, and DescriptorHashDeadlineError report lower_bound=true and time_budget.
    - Per-file, aggregate, descriptor byte-budget exception, and dishonest returned-byte overage report lower_bound=true with zero or prior-prefix counts preserved.
    - Fully observed detected and empty candidates report lower_bound=false; non-budget unsafe/unreadable/concurrent-change outcomes keep their established state and do not become selectable.
    - Saving and serializing one budget outcome preserves lower_bound=true, selectable=false, null fingerprint, bounded reason, and path-free output.
  </behavior>
  <action>RED: replace the existing assertion in the exact named test_entry_budget_reports_observed_lower_bound that entry-budget lower_bound is false with true, then add a parametrized matrix for outer time, between-entry time, DescriptorHashDeadlineError, file-byte, aggregate-byte, DescriptorHashBudgetError under each limiting cap, and returned-byte overages. Assert counts are the observed prefix, never an upper/exact claim. Add exact terminal negative controls and one real save_detection_result plus _legacy_detection_result_schema round trip using the existing fixtures. Assert no absolute path, file name, raw error, or fingerprint is serialized for partial outcomes. Follow the lower-bound and bounded-problem contract mapped in 06-PATTERNS.md to backend/handler/storage/preview.py while preserving the exact two-grammar detector. Commit with hooks as test(06-30): require truthful detection lower bounds.</action>
  <verify>
    <automated>bash -lc 'set +e; output=$(docker exec -e DB_NAME=romm_test_0630 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/handler/storage/test_legacy_migration.py::test_entry_budget_reports_observed_lower_bound -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1 &amp;&amp; printf "%s\n" "$output" | grep -Eq "FAILED .*test_legacy_migration.py::test_entry_budget_reports_observed_lower_bound"'</automated>
  </verify>
  <acceptance_criteria>The RED gate accepts only pytest behavioral-failure status 1 with the exact lower-bound regression signature, rejects status 0 and collection, usage, interrupt, or infrastructure statuses, and the full RED matrix fails on current false values while exact terminal controls remain green.</acceptance_criteria>
  <done>All budget and exact-outcome semantics are specified through persistence and public serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mark every partial observation as a lower bound</name>
  <files>backend/handler/storage/legacy_migration.py, backend/tests/handler/storage/test_legacy_migration.py</files>
  <read_first>backend/handler/storage/legacy_migration.py, backend/tests/handler/storage/test_legacy_migration.py, backend/handler/storage/preview.py, backend/exceptions/storage_exceptions.py, backend/handler/filesystem/storage_access.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md</read_first>
  <action>GREEN: make every early return caused by entry_budget, time_budget, file_byte_budget, aggregate_byte_budget, DescriptorHashBudgetError, descriptor deadline, or returned byte overage construct _CandidateObservation with lower_bound=true. Catch DescriptorHashDeadlineError explicitly before the generic DescriptorHashError branch and classify it as time_budget. Follow the mapped backend/handler/storage/preview.py lower-bound and bounded-problem pattern: partial observations stay explicitly partial with capped problem categories, while exact completions stay exact. Leave complete fingerprinted observations, empty/missing layouts, unsafe/unreadable errors, and non-budget hash failures with their truthful existing semantics. Preserve observed prefix counts, null fingerprint, unselectable status, bounded reason vocabulary, exact two-grammar detection, and source immutability per D-07 through D-10 and D-23. Prefer keyword arguments for state/count/lower_bound/problem on early returns so a positional boolean cannot silently regress. Commit with hooks as fix(06-30): label partial legacy counts as lower bounds. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0630 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/handler/storage/test_legacy_migration.py -k "lower_bound or budget or deadline" -x</automated>
  </verify>
  <acceptance_criteria>Every partial budget case exposes lower_bound=true through domain, persistence, and public schema; exact cases remain false and no partial case is selectable.</acceptance_criteria>
  <done>Operators can distinguish incomplete prefix counts from exact observations for every enforced resource budget.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Risk                                                 | Disposition | Mitigation                                                  |
| ------------------------------------------ | ---------------------------------------------------- | ----------- | ----------------------------------------------------------- |
| bounded source observation to admin result | Prefix count is misrepresented as exact              | mitigate    | Mark every budget termination lower_bound=true              |
| descriptor hash to detection state         | Deadline is collapsed into generic incomplete status | mitigate    | Explicit deadline classification before generic hash errors |
| persisted result to public API             | Internal correction is lost during serialization     | mitigate    | Round-trip regression through handler and endpoint schema   |

## STRIDE Threat Register

| Threat ID  | Category               | Component                | Disposition | Mitigation Plan                                        |
| ---------- | ---------------------- | ------------------------ | ----------- | ------------------------------------------------------ |
| T-06-30-01 | Tampering              | observed count metadata  | mitigate    | Budget-reason matrix with lower-bound assertions       |
| T-06-30-02 | Repudiation            | persisted status         | mitigate    | Domain, DB, and response values must agree             |
| T-06-30-03 | Information disclosure | partial diagnostics      | mitigate    | Bounded code and counters only                         |
| T-06-30-04 | Denial of service      | large legacy tree        | mitigate    | Existing entry/time/byte budgets remain unchanged      |
| T-06-30-05 | Elevation of privilege | partial result selection | mitigate    | Null fingerprint and selectable=false remain mandatory |

</threat_model>

<verification>
Use only task-owned romm_test_0630 resources if database-backed round-trip tests require them, then drop/revoke/assert exact absence. Run the focused matrix followed by the complete test_legacy_migration.py suite and the affected detection task/endpoint storage tests, scoped Trunk on the two changed files, Python AST compilation, and git diff --check. Confirm the external source manifest is unchanged, normal application SELECT 1 remains available, and no service is restarted or deployed.
</verification>

<success_criteria>

- D-09 summaries truthfully label every incomplete budget observation as a lower bound.
- MIG-03/MIG-05 keep every partial result visible, bounded, unselectable, expiring, and fallback-free.
- Exact and non-budget outcomes do not acquire misleading lower-bound metadata.

</success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-30-SUMMARY.md.
</output>
