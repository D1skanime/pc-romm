---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 40
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/api/rom.ts
  - frontend/src/stores/upload.ts
  - frontend/src/services/api/rom.test.ts
  - frontend/src/stores/upload.test.ts
  - frontend/src/services/api/screenshot.test.ts
autonomous: true
requirements: [CAT-01, CAT-02]
gap_closure: true

must_haves:
  truths:
    - "A singular primary-manual client call sends exactly one file and one POST for one ROM."
    - "One operation identity owns one progress record; retry cannot update a stale failed record."
    - "The singular API rejects normally and never converts failure into PromiseSettled fan-out semantics."
    - "Existing upload-store start/update/fail behavior and screenshot progress labels remain byte-for-byte compatible in meaning."
  artifacts:
    - path: "frontend/src/services/api/rom.ts"
      provides: "Singular primary-manual upload service"
    - path: "frontend/src/stores/upload.ts"
      provides: "Retry-safe operation-keyed upload tracking"
    - path: "frontend/src/services/api/rom.test.ts"
      provides: "One-file, one-POST and retry regressions"
    - path: "frontend/src/stores/upload.test.ts"
      provides: "Legacy API compatibility and additive operation-keyed progress regressions"
    - path: "frontend/src/services/api/screenshot.test.ts"
      provides: "Screenshot progress identity, labels, failure, and allSettled compatibility regressions"
  key_links:
    - from: "uploadManual"
      to: "POST /roms/{id}/manuals"
      via: "one FormData and one Axios promise"
      pattern: 'api\.post'
    - from: "onUploadProgress"
      to: "upload store record"
      via: "ROM-scoped operation identity"
      pattern: "primary-manual"
    - from: "retry"
      to: "same operation key"
      via: "additive startOperation/updateOperation/failOperation methods replace stale same-operation state"
      pattern: "startOperation|failOperation"
    - from: "screenshot upload service"
      to: "legacy upload-store start/update/fail"
      via: "unchanged filename-keyed progress and labels"
      pattern: 'uploadStore\.(start|update|fail)'
---

<objective>
Create the singular request and progress contract consumed by the safe manual UI.

Purpose: CR-02 includes client-side Promise.allSettled fan-out and filename-keyed stale progress races.
Output: A tested singular service plus operation-keyed upload tracking; current consumer removal is owned by Plan 45.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@frontend/src/services/api/rom.ts
@frontend/src/stores/upload.ts
@frontend/src/services/api/rom.test.ts
@frontend/src/stores/upload.test.ts
@frontend/src/services/api/screenshot.ts
@frontend/src/services/api/screenshot.test.ts
@frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue
@frontend/src/v2/components/Notifications/UploadProgressToast.vue
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md

<interfaces>
Add uploadManual({romId,file}) as the new active-v2 contract. Its operation ID is derived from stable ROM identity and purpose, not the raw filename; the user-visible filename remains display data only. Add startOperation(operationId, displayFilename), updateOperation(operationId, progressEvent), and failOperation(operationId, reason) to the upload store without changing the signatures or behavior of start(filename), update(filename,event), updateChunkProgress(filename,...), fail(filename,reason), clearFinished(), or reset(). Operation-keyed records may add a private operationKey field, while legacy records and UploadProgressToast labels remain unchanged. screenshot.ts continues to call the legacy filename-keyed methods and preserves its Promise.allSettled contract. Keep API error propagation intact. The shared uploadManuals export and emitter-driven frozen v1 consumers remain intact through Phase 8; Plan 45 removes only the active-v2 coordinator.
</interfaces>
</context>

<test_topology>
Plan 06-40 frontend tasks run only in node:24-bookworm with one exact task-owned node_modules volume whose name contains a 128-bit lowercase-hex nonce. Each command proves Docker authority and exact volume-name absence before registering cleanup or creating the volume, installs EXIT/INT/TERM handling before creation, records ownership only after docker volume create returns the exact requested name, and validates that exact name by inspect. Cleanup preserves the primary exit status, emits a bounded cleanup failure when needed, removes only the exact owned result artifact and volume without force or discovery, requires exact volume inspect to fail with not-found status, and makes cleanup failure fail an otherwise successful task. Task 1 runs the entire selected Vitest file with JSON reporter output inside the owned volume; a deterministic Node parser requires valid JSON, process status 1, exactly one failed assertion with the exact fully qualified name and file, every other selected assertion passed, exact counters, and zero pending/todo/runtime/unhandled/config/transform/worker/timeout/OOM/infrastructure conditions. npm command echo and console-name grep are never evidence. Task 2 uses the same ownership-safe cleanup contract. Run npm as UID/GID 1000 with HOME=/tmp and NODE_OPTIONS=--max-old-space-size=4096. Never use broad discovery deletion, retain a result artifact, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify singular request and retry-safe progress</name>
  <files>frontend/src/services/api/rom.test.ts, frontend/src/stores/upload.test.ts, frontend/src/services/api/screenshot.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, frontend/src/services/api/rom.ts, frontend/src/stores/upload.ts, frontend/src/stores/upload.test.ts, frontend/src/services/api/screenshot.ts, frontend/src/services/api/screenshot.test.ts, frontend/src/v2/components/Notifications/UploadProgressToast.vue, frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md, frontend/src/services/api/rom.test.ts</read_first>
  <behavior>
    - One file yields one POST.
    - Failure rejects to the caller.
    - Retry cannot mutate a stale record.
    - Legacy screenshot progress, display labels, failure reasons, and allSettled behavior do not change.
  </behavior>
  <action>Per D-05 and D-11, the client operation may mutate only RomM-owned manual state and must never imply source-library mutation. Create rom.test.ts using the repository Vitest conventions and mocked Axios/store boundaries. Add exact test uploadManual_sends_one_file_in_one_request plus cases for progress, reject, same-filename retry, different-filename same-ROM retry, and different ROM independence. Add upload.test coverage proving every existing start/update/updateChunkProgress/fail/clearFinished/reset signature and filename-keyed transition is unchanged, while new operation-keyed methods replace only the stale same-operation record. Add screenshot.test coverage proving uploadGalleryScreenshots still calls legacy start/update/fail with file.name, preserves visible labels/failure reasons and Promise.allSettled ordering, and remains independent for duplicate/different filenames. Assert one FormData member, one POST, the existing dynamic field/header agreement, no Promise.allSettled, no array argument, and a stable per-ROM primary-manual operation record that replaces stale failure state. Run the exact named test and accept only its executed assertion that uploadManual is a callable singular export as RED. Access the module namespace without a static named import of the absent export so import/transform failure cannot qualify. Use the planned describe block exactly as describe("rom service") and the exact test title "uploadManual_sends_one_file_in_one_request" in src/services/api/rom.test.ts; Vitest 4.1.8 must emit ancestorTitles ["rom service"] and fullName "rom service uploadManual_sends_one_file_in_one_request" with normal spacing. Run the entire file with JSON reporter output and apply the deterministic record, counter-invariant, file, ancestry, title, fullName, failure-kind, and infrastructure parser from verify; console output and requested-name echo cannot satisfy RED.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0640-t1-node-$nonce; result=/app/node_modules/p0640-t1-vitest-$nonce.json; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0640-t1-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; set +e; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/services/api/rom.test.ts --reporter=json --outputFile=\"$result\""; status=$?; set -e; test "$status" -eq 1; docker run --rm --user 1000:1000 -e RESULT="$result" -e EXPECTED_SUITE="rom service" -e EXPECTED_TEST="uploadManual_sends_one_file_in_one_request" -e EXPECTED_FILE="src/services/api/rom.test.ts" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm node -e "const fs=require(\"fs\");let d;try{d=JSON.parse(fs.readFileSync(process.env.RESULT,\"utf8\"));}catch(e){process.exit(41);}const suites=Array.isArray(d.testResults)?d.testResults:[];const assertions=suites.flatMap(r=&gt;Array.isArray(r.assertionResults)?r.assertionResults:[]);const failed=assertions.filter(a=&gt;a.status===\"failed\");const passed=assertions.filter(a=&gt;a.status===\"passed\");const owner=suites.find(r=&gt;(r.assertionResults||[]).includes(failed[0]));const file=String(owner&amp;&amp;owner.name||\"\").replaceAll(\"\\\\\",\"/\");const failureText=failed.flatMap(a=&gt;Array.isArray(a.failureMessages)?a.failureMessages:[]).join(\"\\n\");const infraText=JSON.stringify({messages:suites.map(r=&gt;r&amp;&amp;r.message||\"\"),failures:failureText,errors:Array.isArray(d.errors)?d.errors:[],unhandled:Array.isArray(d.unhandledErrors)?d.unhandledErrors:[]});const forbidden=/(collection|collecting|transform|config|worker|test timed out|timeout|out of memory|\\boom\\b|enomem|no tests?|no test suite|failed to load|cannot find module|module not found|infrastructure)/i;const unhandled=Array.isArray(d.unhandledErrors)?d.unhandledErrors.length:0;const errors=Array.isArray(d.errors)?d.errors.length:0;const expectedFull=process.env.EXPECTED_SUITE+\" \"+process.env.EXPECTED_TEST;const exactAncestors=failed.length===1&amp;&amp;Array.isArray(failed[0].ancestorTitles)&amp;&amp;failed[0].ancestorTitles.length===1&amp;&amp;failed[0].ancestorTitles[0]===process.env.EXPECTED_SUITE;const counters=d.numTotalTests===assertions.length&amp;&amp;d.numFailedTests===failed.length&amp;&amp;d.numPassedTests===passed.length&amp;&amp;(d.numPendingTests||0)===0&amp;&amp;(d.numTodoTests||0)===0;const ok=d.success===false&amp;&amp;suites.length===1&amp;&amp;owner===suites[0]&amp;&amp;assertions.length&gt;0&amp;&amp;assertions.every(a=&gt;a.status===\"passed\"||a.status===\"failed\")&amp;&amp;counters&amp;&amp;failed.length===1&amp;&amp;passed.length+1===assertions.length&amp;&amp;(d.numRuntimeErrorTestSuites||0)===0&amp;&amp;unhandled===0&amp;&amp;errors===0&amp;&amp;exactAncestors&amp;&amp;failed[0].status===\"failed\"&amp;&amp;failed[0].title===process.env.EXPECTED_TEST&amp;&amp;failed[0].fullName===expectedFull&amp;&amp;Array.isArray(failed[0].failureMessages)&amp;&amp;failed[0].failureMessages.length&gt;0&amp;&amp;!forbidden.test(infraText)&amp;&amp;file.endsWith(\"/\"+process.env.EXPECTED_FILE);if(!ok){process.exit(42);}"'</automated>
  </verify>
  <acceptance_criteria>
    - The exact service behavior fails because uploadManual is absent.
    - Tests inspect real FormData/Axios inputs and store transitions.
    - No component or manually constructed result substitutes for the service boundary.
    - Store and screenshot baselines lock existing shared behavior before additive methods are implemented.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
    - RED requires valid Vitest 4.1.8 JSON, status 1, exactly one testResults record for src/services/api/rom.test.ts, exactly one failed assertion with ancestorTitles ["rom service"], title "uploadManual_sends_one_file_in_one_request", fullName "rom service uploadManual_sends_one_file_in_one_request", non-empty failureMessages, every other assertion passed, and zero collection/transform/config/worker/runtime/unhandled/timeout/OOM/no-tests/infrastructure errors; suite-count fields are not hard-coded.
  </acceptance_criteria>
  <done>The intended singular service and operation-record behavior are RED-specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN add singular upload and operation-keyed tracking</name>
  <files>frontend/src/services/api/rom.ts, frontend/src/stores/upload.ts, frontend/src/services/api/rom.test.ts, frontend/src/stores/upload.test.ts, frontend/src/services/api/screenshot.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/services/api/rom.ts, frontend/src/stores/upload.ts, frontend/src/services/api/rom.test.ts, frontend/src/stores/upload.test.ts, frontend/src/services/api/screenshot.ts, frontend/src/services/api/screenshot.test.ts, frontend/src/v2/components/Notifications/UploadProgressToast.vue</read_first>
  <behavior>
    - No singular fan-out exists.
    - Progress identity is stable and retry-safe.
    - Axios errors remain available for bounded UI classification.
    - Legacy store consumers, especially screenshot.ts, retain their signatures and transitions.
  </behavior>
  <action>Per D-05 and D-11, the client operation may mutate only RomM-owned manual state and must never imply source-library mutation. Implement uploadManual with a singular File, one FormData, one POST, the current safe upload filename header/field contract, and direct Axios resolution/rejection. Leave upload-store start/update/updateChunkProgress/fail APIs untouched. Add operation-keyed methods with stable operation ID plus display filename; startOperation replaces a stale record only for that same operation, and updateOperation/failOperation resolve by operationKey while keeping records for other operations. Update the singular service and all three focused test modules; retain shared uploadManuals and the emitter event for frozen v1 through Phase 8, and retain screenshot.ts unchanged. Add no global promise wrapper, queue, fan-out, raw backend detail, or new dependency. Execute the exact task-owned volume lifecycle in verify; removal and not-found inspection are mandatory, cleanup preserves a failing primary status, and cleanup failure is both reported and fatal when the primary command succeeded.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0640-t2-node-$nonce; result=; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0640-t2-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/services/api/rom.test.ts src/stores/upload.test.ts src/services/api/screenshot.test.ts &amp;&amp; npm run typecheck"'</automated>
  </verify>
  <acceptance_criteria>
    - Singular tests pass with exactly one POST.
    - Retry starts a fresh record under the same operation ID.
    - Existing start/update/updateChunkProgress/fail consumers retain exact signatures and transitions.
    - Screenshot progress labels, duplicate-file behavior, failures, and Promise.allSettled semantics remain unchanged.
    - Shared uploadManuals and emitter contracts remain callable for frozen v1 through Phase 8.
    - No runtime UI or locale change.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
  </acceptance_criteria>
  <done>The safe visible feature has a strict singular client/progress contract to consume.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                         |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| File object to HTTP request                      | Only one selected file may cross per operation.                                                     |
| Axios progress to global tracker                 | Raw filenames remain legacy display identities; new manual authority uses a separate operation key. |
| additive store API to legacy screenshot consumer | New operation-keyed methods must not change existing filename-keyed transitions or labels.          |
| API failure to caller                            | The visible feature must distinguish failure and conflict.                                          |

## STRIDE Threat Register

| Threat ID       | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-40-01      | Tampering              | request fan-out              | mitigate    | Singular signature and one POST test.                                                                                                                                                                                           |
| T-06-40-02      | Repudiation            | progress record              | mitigate    | Additive ROM-purpose operation methods replace stale same-operation state without mutating legacy APIs.                                                                                                                         |
| T-06-40-03      | Information disclosure | failure tracking             | mitigate    | Store only bounded display reason; UI does not expose raw server/path data.                                                                                                                                                     |
| T-06-40-04      | Denial of service      | repeated selection           | mitigate    | Manual service creates no queue; screenshot Promise.allSettled behavior remains intentionally unchanged.                                                                                                                        |
| T-06-40-05      | Tampering              | shared upload store          | mitigate    | Legacy method signatures/transitions and screenshot labels are locked by focused regressions.                                                                                                                                   |
| T-06-40-99      | Elevation of privilege | disposable Node verification | mitigate    | Pre-create exact-name absence, post-create ownership proof, primary-status-preserving exact result/volume cleanup, mandatory volume removal, and not-found inspect prevent deletion or retention outside the task-owned volume. |
| </threat_model> |

<verification>
Run the disposable Node 24 ROM, upload-store, and screenshot focused tests plus typecheck, scoped ESLint on all five modified files, git diff --check, and exact volume cleanup. Shared plural/emitter v1 contracts remain through Phase 8; Plan 45 removes only the active-v2 coordinator. No Phase 7 UI is added.
</verification>

<success_criteria>

- Both p0640 Node lifecycles remove and prove absence of their exact owned volume; Task 1 RED is accepted only by the deterministic Vitest JSON contract.
- The singular transport boundary is complete and tested.
- Progress state is keyed by operation identity.
- The repository remains type-correct for the dependency wave.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-40-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
