---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 48
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handler/database/legacy_migration_handler.py
  - backend/tests/handler/storage/test_legacy_migration.py
  - backend/tests/integration/test_legacy_migration.py
autonomous: true
requirements: [MIG-01, MIG-04]
gap_closure: true

must_haves:
  truths:
    - "Impact preview counts a unique directory-backed ROM when exact observed child-file evidence proves its folder boundary."
    - "Each RomFile reconnects only when its own exact file digest is observed; absent children stay unreachable."
    - "Folder selection survives restart, migration, and rollback without aliases, weak matching, or source mutation."
  artifacts:
    - path: "backend/handler/database/legacy_migration_handler.py"
      provides: "Exact parent-from-child selection shared by preview and migration"
    - path: "backend/tests/handler/storage/test_legacy_migration.py"
      provides: "Folder and multi-file presence, absence, ambiguity, and boundary regressions"
    - path: "backend/tests/integration/test_legacy_migration.py"
      provides: "Preview, migrate, restart, and rollback proof for nested folder ROMs"
  key_links:
    - from: "persisted LegacyDetectionSourceIdentity digests"
      to: "unique directory-backed Rom selection"
      via: "exact canonical RomFile identities below the Rom folder boundary"
      pattern: "_select_catalog|_catalog_source_identity"
    - from: "selected parent ROM"
      to: "selected child RomFiles"
      via: "independent digest membership for every child"
      pattern: "selected_rom_ids|selected_file_ids"
---

<objective>
Reconnect folder and multi-file ROM parents from the exact file-only evidence already persisted by detection.

Purpose: CR-01 and failed must-have 14 show that requiring a parent directory digest makes every directory-backed ROM permanently unmatched.
Output: A source-bound selection rule and restart/rollback regressions with no schema, public-contract, or source-authority expansion.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-44-SUMMARY.md
@backend/handler/storage/legacy_migration.py
@backend/models/storage.py

<interfaces>
- LegacyMigrationDatabaseHandler._catalog_source_identity(fs_slug: str, path: str, name: str) returns one canonical mapping-relative identity and private digest.
- LegacyMigrationDatabaseHandler._select_catalog(fs_slug, roms, rom_files, persisted_identity_digests) returns the selection shared by preview and migration.
- Detection intentionally persists only exact regular-file identity digests. Directory entries remain aggregate-fingerprint input, not new durable authority.
</interfaces>
</context>

<test_topology>
Run backend tests only on verified Linux /home/d1sk/romm through backend/tools/verify_phase6_acceptance.py disposable runner primitives. Each invocation owns one nonce database, principal, basetemp, and container from romm-romm-dev on romm_default; finally cleanup proves exact absence and normal DB SELECT 1. Keep romm-dev stopped. Do not restart services, deploy, publish ports, touch the NAS, or delete by prefix/discovery. Preserve the exact baseline hash 4d4264efbb75049e71230f2417667c867656f6dd5054640e7aa6b8af391c81e1 after task commits.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED prove folder-parent and independent child selection</name>
  <files>backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/handler/storage/legacy_migration.py, backend/handler/database/legacy_migration_handler.py, backend/models/rom.py, backend/models/storage.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-44-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - A unique ROM at platform_slug/folder with observed folder/disc1.bin and disc2.bin reconnects as a parent.
    - Only child rows whose own exact file digests are present reconnect; an absent sidecar remains missing.
    - A folder with no observed child, a child outside its exact boundary, duplicate identity, unsafe text, or basename similarity does not reconnect.
    - Preview count, migration, restart, and rollback use the exact changed ROM and RomFile set.
  </behavior>
  <action>Per D-03, D-12, D-14, D-19, and D-21, add unit and integration regressions before production changes. Seed a canonical folder ROM plus multiple RomFiles, observe real nested files through the detector, and assert the current preview failure first. Include an absent child, sibling-prefix control, duplicate catalog identity, fresh handler/session restart, migration, and rollback lineage. Assert source manifests are identical and private digests never enter public responses, repr, or logs. Do not add directory digest persistence, infer from display/provider names, or select every child merely because its parent qualifies.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, StageFailure, _execute_docker_module; name=\"tests/integration/test_legacy_migration.py::test_folder_rom_parent_reconnects_from_exact_observed_children\"; exec(\"try:\\n _execute_docker_module(Path.cwd(), name, ResourceIdentity.create(4801), 'romm-romm-dev', 'romm_default', 'romm-db-dev')\\nexcept StageFailure as exc:\\n text=str(exc); assert name in text and 'failed' in text.lower() and 'ERROR collecting' not in text\\nelse:\\n raise SystemExit('RED unexpectedly passed')\")"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on reconnectable count or missing_from_fs, not collection, Docker, DB, timeout, or cleanup.
    - Tests prove exact parent derivation and independent child selection, including absent and sibling-prefix controls.
    - Restart and rollback bind durable rows and exact lineage rather than memory state.
  </acceptance_criteria>
  <done>The parent-digest gate is disproven by deterministic source-bound tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN derive the unique parent boundary without weakening child evidence</name>
  <files>backend/handler/database/legacy_migration_handler.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/handler/database/legacy_migration_handler.py, backend/handler/storage/legacy_migration.py, backend/models/rom.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/integration/test_legacy_migration.py</read_first>
  <behavior>
    - Flat single-file ROM selection remains unchanged.
    - A unique directory parent qualifies only from an independently observed child within parent plus slash boundary.
    - selected_file_ids still require each child's own digest.
    - Preview, confirmation, migration, restart, and rollback share the deterministic selection.
  </behavior>
  <action>Refactor _select_catalog in the locked transaction. Canonicalize each ROM and RomFile through _catalog_source_identity once. Compute observed child rows by exact digest membership. Keep direct membership for flat ROMs. For a unique ROM whose own digest is absent, select it only when at least one of its own observed RomFiles is strictly below relative_identity + "/". Preserve exact case/Unicode semantics and ambiguity accounting. Never reconstruct names from digests, prefix-match without the slash boundary, bulk-select children, or change schema, public responses, locks, fingerprints, or source access.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, _execute_docker_module; modules=(\"tests/handler/storage/test_legacy_migration.py\",\"tests/integration/test_legacy_migration.py\"); [_execute_docker_module(Path.cwd(), m, ResourceIdentity.create(4810+i), \"romm-romm-dev\", \"romm_default\", \"romm-db-dev\") for i,m in enumerate(modules)]"'</automated>
  </verify>
  <acceptance_criteria>
    - New tests and all existing legacy handler/integration tests pass in cleaned disposable runners.
    - Flat ROM, absent child, ambiguity, confirmation, restart, rollback, and privacy do not regress.
    - No schema, generated contract, alias grammar, public identity list, or external mutation authority is added.
  </acceptance_criteria>
  <done>Folder parents reconnect from exact file-only evidence while absent children stay unreachable.</done>
</task>

</tasks>

<source_coverage>
GOAL-06 criterion 3, MIG-01, MIG-04, D-03, D-12, D-14, D-19, D-21, REVIEW CR-01, and VERIFICATION MH-14 are covered. Nothing is deferred.
</source_coverage>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                       |
| ---------------------------------- | ----------------------------------------------------------------- |
| private digests to catalog rows    | Opaque file evidence authorizes only canonical identities.        |
| child evidence to parent selection | A file match must not become loose prefix or cross-ROM authority. |
| preview to migration               | Counts and IDs remain identical under locks and confirmation.     |
| runner to services                 | Disposable resources do not start or alter romm-dev.              |

## STRIDE Threat Register

| Threat ID       | Category               | Component        | Disposition | Mitigation Plan                                                                 |
| --------------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------------------------- |
| T-06-48-01      | Spoofing               | parent selection | mitigate    | Require unique canonical ROM identity and one exact observed child owned by it. |
| T-06-48-02      | Tampering              | child set        | mitigate    | Require independent digest membership for every RomFile.                        |
| T-06-48-03      | Information disclosure | digests          | mitigate    | Keep digests out of responses, repr, logs, and evidence.                        |
| T-06-48-04      | Elevation of privilege | ancestry         | mitigate    | Use exact normalized identity plus slash boundary and reject sibling prefixes.  |
| T-06-48-05      | Denial of service      | runners          | mitigate    | Bounded nonce resources, finally cleanup, no restart.                           |
| </threat_model> |

<verification>
Run both changed backend modules in separate disposable lifecycles, scoped Trunk on touched files, git diff --check, source-manifest equality, exact cleanup, normal DB SELECT 1, stopped romm-dev, and original baseline after commits.
</verification>

<success_criteria>

- Folder parents reconnect from exact observed children.
- Present children reconnect independently and absent children remain missing.
- Preview, migration, restart, and rollback agree.
- Flat behavior, ambiguity, privacy, immutability, and no fallback remain intact.
  </success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-48-SUMMARY.md.
</output>
