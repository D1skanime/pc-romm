---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/handler/database/storage_handler.py
  - backend/handler/database/roms_handler.py
  - backend/handler/storage/read_context.py
  - backend/handler/scan_handler.py
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/endpoints/test_storage.py
  - backend/tests/handler/database/test_storage_lifecycle.py
autonomous: true
requirements: [CAT-03, CAT-04]
must_haves:
  truths:
    - "Removal consequences report retained unreachable catalog and cancellation."
    - "Confirmed removal revises mapping, reachability, and audit atomically."
    - "Old work stops and later mapping reconnects only unique identity."
  artifacts:
    - path: "backend/handler/database/storage_handler.py"
      provides: "Mapping removal, cancellation and reconnection"
    - path: "backend/tests/handler/database/test_storage_lifecycle.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/endpoints/storage.py"
      to: "backend/handler/database/storage_handler.py"
      via: "consequence confirmation to removal transaction"
      pattern: "remove.*mapping"
---

<objective>
Mapping removal, cancellation and reconnection
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/database/storage_handler.py, backend/handler/database/roms_handler.py, backend/handler/storage/read_context.py, backend/handler/scan_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/endpoints/test_storage.py, backend/tests/handler/database/test_storage_lifecycle.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/database/storage_handler.py
@backend/handler/storage/read_context.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Mapping removal, cancellation and reconnection</name>
  <files>backend/tests/handler/database/test_storage_lifecycle.py, backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart where applicable, and source-manifest equality.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/handler/database/storage_handler.py, backend/handler/storage/read_context.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_storage_lifecycle.py tests/handler/storage/test_read_context.py tests/endpoints/test_storage.py -x</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Mapping removal, cancellation and reconnection</name>
  <files>backend/handler/database/storage_handler.py, backend/handler/database/roms_handler.py, backend/handler/storage/read_context.py, backend/handler/scan_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/endpoints/test_storage.py, backend/tests/handler/database/test_storage_lifecycle.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Per D-01 through D-05, extend versioned remove with deterministic platform/root/mapping/catalog locks; deactivate, increment revision, audit and mark catalog/files visible-unreachable atomically while retaining games, metadata, saves, states, history, identity and source. Per D-03/D-19 reconnect only one-to-one normalized logical identity or unique complete CRC32+MD5+SHA1, never basename/display/provider/alias/partial/first match. Per D-02 validate before opens, between batches, before reconciliation/owned writes/response commitment and each multi-file member. Barrier-test races; old identity never redirects. Do not claim revocation of already-open descriptors. Per D-04, add a typed administrator mapping-removal consequence and confirmation contract distinct from game catalog removal. It reports retained visible-unreachable catalog count, preserved metadata/saves/states/history, source immutability, mapping-revision invalidation, and queued/running work cancellation at safe boundaries. Require expected_version and explicit ordinary confirmation, never a typed platform name. Authenticate before preview or confirmation, revalidate consequences in the removal transaction, return bounded errors, and test OpenAPI plus unauthorized, stale, changed-consequence, and confirmed cases.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_storage_lifecycle.py tests/handler/storage/test_read_context.py tests/endpoints/test_storage.py -x</automated></verify>
  <done>Mapping removal, cancellation and reconnection passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-03B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-03C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-03-SUMMARY.md
</output>
