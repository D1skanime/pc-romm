---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 27
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/endpoints/roms/__init__.py
  - backend/tests/endpoints/test_storage_policy_denials.py
autonomous: true
requirements: [CAT-04]
gap_closure: true
must_haves:
  truths:
    - "Any submitted fs_name that differs from the visible ROM's source name is authorized as external RENAME immediately after visibility and before unmatch, provider, database, cache, collection, or RomM-owned resource effects."
    - "Changed fs_name combined with unmatch_metadata or provider screenshot metadata returns the bounded 403 denial with zero effects and unchanged source and owned manifests."
    - "Omitted or unchanged fs_name preserves metadata-only and typed-owned-resource updates without requesting external RENAME authority."
  artifacts:
    - path: "backend/endpoints/roms/__init__.py"
      provides: "Single pre-effect changed-filename authorization gate"
    - path: "backend/tests/endpoints/test_storage_policy_denials.py"
      provides: "Early-return, provider, manifest, and same-name regressions"
  key_links:
    - from: "backend/endpoints/roms/__init__.py update_rom"
      to: "authorize_api_storage_operation"
      via: "sanitize, exact compare, then StorageOperation.RENAME immediately after assert_rom_visible"
      pattern: "assert_rom_visible[\\s\\S]*sanitize_filename[\\s\\S]*StorageOperation\\.RENAME"
---

<objective>
Move the crafted source-rename denial ahead of every mixed-authority side effect.

Purpose: Close the CAT-04 blocker where a denied external rename request can currently commit unmatch state or trigger owned screenshot work, preserving D-05 and the Phase 2 pre-I/O policy boundary.
Output: A single early rename preflight and adversarial endpoint regressions.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-26-SUMMARY.md
</context>

<interfaces>
From backend/endpoints/roms/__init__.py:
- update_rom loads Rom, raises RomNotFoundInDatabaseException when absent, then calls assert_rom_visible(request, rom).
- The unmatch_metadata branch currently mutates database, cache, and smart collections and returns before new_fs_name is computed.
- Normal metadata resolution can call fs_resource_handler.get_rom_screenshots before the current StorageOperation.RENAME authorization.
- authorize_api_storage_operation(StorageOperation.RENAME, legacy_external_storage) is the established bounded 403 boundary.

From backend/tests/endpoints/test_storage_policy_denials.py:

- _rom_update_fixture and _install_update_rom_tripwires provide a visible ROM plus database, resource, and external rename tripwires.
- _source_manifest records source and owned path, kind, mode, size, digest, and symlink identity.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify pre-effect rename denial across early and provider branches</name>
  <files>backend/tests/endpoints/test_storage_policy_denials.py</files>
  <read_first>backend/tests/endpoints/test_storage_policy_denials.py, backend/endpoints/roms/__init__.py, backend/endpoints/storage_policy.py, backend/handler/filesystem/storage_policy.py</read_first>
  <behavior>
    - Changed fs_name plus unmatch_metadata=true returns 403 before db_rom_handler.update_rom, cache invalidation, collection refresh, or response construction.
    - Changed fs_name plus a metadata-provider result containing url_screenshots returns 403 before the provider call and before fs_resource_handler.get_rom_screenshots.
    - Each denial makes exactly one RENAME authorization attempt, exposes no path, and leaves database/source/owned effect logs empty.
    - Omitted and unchanged fs_name continue through the existing metadata and typed-owned-resource path without RENAME authorization.
  </behavior>
  <action>RED: extend the existing update_rom tripwire suite with the exact named regression test_update_rom_changed_fs_name_denied_before_unmatch_effects and a changed fs_name plus provider screenshot result case. Record provider, database, cache, collection, response, owned-resource, and external-filesystem calls in one ordered effect log; require the first and only effect to be authorize:rename and require the existing bounded 403 detail. Preserve writable source and owned manifests before and after both requests. Keep the same-name positive test. This specifies CAT-04 and D-05 without changing production code. Commit with hooks as test(06-27): expose late rename authorization.</action>
  <verify>
    <automated>bash -lc 'set +e; output=$(docker exec -e DB_NAME=romm_test_0627 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_storage_policy_denials.py::test_update_rom_changed_fs_name_denied_before_unmatch_effects -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1 &amp;&amp; printf "%s\n" "$output" | grep -Eq "FAILED .*test_storage_policy_denials.py::test_update_rom_changed_fs_name_denied_before_unmatch_effects"'</automated>
  </verify>
  <acceptance_criteria>The RED gate accepts only pytest behavioral-failure status 1 with the exact named failing regression, rejects status 0 and collection, usage, interrupt, or infrastructure statuses, reproduces the unmatch bypass before the production edit, and leaves the positive unchanged-name assertion green.</acceptance_criteria>
  <done>The two live late-authorization paths are represented by deterministic zero-effect regression tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authorize changed filename immediately after visibility</name>
  <files>backend/endpoints/roms/__init__.py, backend/tests/endpoints/test_storage_policy_denials.py</files>
  <read_first>backend/endpoints/roms/__init__.py, backend/tests/endpoints/test_storage_policy_denials.py, backend/utils/validation.py, backend/endpoints/storage_policy.py</read_first>
  <action>GREEN: immediately after assert_rom_visible, compute the effective submitted filename from form_data.fs_name or rom.fs_name, sanitize it once with sanitize_filename, compare it exactly with rom.fs_name, and when changed call authorize_api_storage_operation(StorageOperation.RENAME, legacy_external_storage). Reuse that precomputed new_fs_name and should_update_fs later in the function. Remove the late duplicate computation and authorization. Do not query providers, mutate database/cache/collections, open owned resources, resolve a source path, or catch the denial before this preflight. Preserve unchanged and omitted filename behavior. This is the fail-closed ordering required by CAT-04 and D-05. Commit with hooks as fix(06-27): authorize rename before all effects. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0627 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_storage_policy_denials.py -k "rename or update_rom" -x</automated>
  </verify>
  <acceptance_criteria>All changed-name branches deny before any effect, both manifests remain exact, and same-name metadata/owned-resource behavior passes without RENAME authorization.</acceptance_criteria>
  <done>Changed fs_name has one authorization point immediately after ROM visibility and no request can reach an earlier side effect.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Risk                                                                        | Disposition | Mitigation                                                      |
| ----------------------------------------- | --------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------- |
| multipart update to mixed-authority route | Forbidden source rename is combined with permitted metadata or owned writes | mitigate    | Sanitize, compare, and authorize before every branch and effect |
| visible ROM to provider/cache/database    | Early return or provider work bypasses storage policy                       | mitigate    | One preflight immediately after visibility                      |
| denial to client                          | Host or source path leaks                                                   | mitigate    | Existing bounded operation/storage detail only                  |

## STRIDE Threat Register

| Threat ID  | Category               | Component                | Disposition | Mitigation Plan                                                             |
| ---------- | ---------------------- | ------------------------ | ----------- | --------------------------------------------------------------------------- |
| T-06-27-01 | Tampering              | update_rom fs_name       | mitigate    | External RENAME denial before unmatch/provider/database/resource work       |
| T-06-27-02 | Elevation of privilege | mixed metadata payload   | mitigate    | Treat any changed filename as external authority regardless of other fields |
| T-06-27-03 | Repudiation            | repeated crafted request | mitigate    | Deterministic one-attempt effect-log regression                             |
| T-06-27-04 | Information disclosure | denial response          | mitigate    | Stable redacted 403 schema                                                  |
| T-06-27-05 | Denial of service      | sanitizer work           | accept      | One bounded filename normalization after visibility                         |

</threat_model>

<verification>
Provision only the task-owned romm_test_0627 database/user if the existing test harness requires it, run the focused command after RED and GREEN, then run the complete tests/endpoints/test_storage_policy_denials.py suite, scoped Trunk on the two changed files, Python AST compilation, and git diff --check. Revoke the task grant, drop the exact task database/user, assert their absence, preserve the 28 pre-existing untracked entries, and do not restart or deploy any service.
</verification>

<success_criteria>

- CAT-04 no longer permits any database, cache, collection, provider, owned-resource, or source effect before changed-filename denial.
- The bounded 403 and exact source/owned manifests are proven for unmatch, provider screenshot, replay, traversal, symlink, and collision inputs.
- Unchanged and omitted filename updates remain functional.

</success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-27-SUMMARY.md.
</output>
