---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 49
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/endpoints/roms/manual.py
  - backend/handler/database/manual_handler.py
  - backend/handler/filesystem/resources_handler.py
  - backend/tests/endpoints/roms/test_manual.py
  - backend/tests/handler/filesystem/test_resources_handler.py
autonomous: true
requirements: [CAT-02]
gap_closure: true
must_haves:
  truths:
    - "A token-named PDF or Markdown in validated rom.path_manual is discoverable after restart and delete removes exactly that owned file and clears the authoritative reference."
    - "Upload and redownload publish unique staged owned files, compare-and-swap the same expected path, preserve the old path on every failure, and remove losing or superseded files only."
    - "Both upload-redownload race orderings produce one winner, one bounded conflict, no orphan, and no external source effect."
  artifacts:
    - path: "backend/endpoints/roms/manual.py"
      provides: "Shared staged primary-manual commit contract for upload, redownload, and exact deletion"
    - path: "backend/handler/database/manual_handler.py"
      provides: "Locked expected-reference clear of path_manual and url_manual"
    - path: "backend/handler/filesystem/resources_handler.py"
      provides: "Validated path_manual-first discovery with bounded fixed-name compatibility"
    - path: "backend/tests/endpoints/roms/test_manual.py"
      provides: "Delete, restart, failure, and upload/redownload race regressions"
    - path: "backend/tests/handler/filesystem/test_resources_handler.py"
      provides: "Token-path validation and legacy fixed-name compatibility tests"
  key_links:
    - from: "rom.path_manual"
      to: "owned RESOURCES manual file"
      via: "validated exact path under rom.fs_resources_path/manual"
      pattern: "path_manual|manual_exists|_get_manual_path"
    - from: "upload and redownload"
      to: "DBPrimaryManualHandler.compare_and_swap_path"
      via: "same expected-path CAS after OwnedCreate publication"
      pattern: "compare_and_swap_path"
    - from: "CAS outcome"
      to: "owned cleanup"
      via: "loser new path or winner superseded old path only"
      pattern: "_delete_owned_manual"
---

<objective>
Unify token-named primary-manual discovery, deletion, upload, and redownload under one failure-atomic CAS authority.
Purpose: CR-02/CR-03 and failed must-have 29 leave uploaded manuals unmanageable and let redownload overwrite a winning upload.
Output: Path-authoritative owned lifecycle plus restart, failure, race, and orphan regressions.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-43-SUMMARY.md
@backend/handler/database/manual_handler.py
@backend/handler/filesystem/storage_access.py
<interfaces>
DBPrimaryManualHandler.compare_and_swap_path(rom_id, expected_path, new_path) locks Rom and changes path_manual only on exact expected value. OwnedCreate.binary_file publishes complete durable bytes to a unique final name or no acknowledged final. FSResourcesHandler currently recognizes only fixed rom.id filenames and must treat a validated path_manual as primary authority while retaining only bounded fixed-name compatibility for old rows.
</interfaces>
</context>
<test_topology>
Use verified Linux /home/d1sk/romm and disposable acceptance-runner primitives with one nonce DB/principal/basetemp/container per module. Keep romm-dev stopped and verify exact cleanup, normal DB SELECT 1, source/resource manifests, and baseline hash 4d4264efbb75049e71230f2417667c867656f6dd5054640e7aa6b8af391c81e1. No restart, deploy, public port, NAS access, broad cleanup, or printed secret/path inventory.
</test_topology>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED specify token-path deletion and upload-redownload serialization</name>
  <files>backend/tests/endpoints/roms/test_manual.py, backend/tests/handler/filesystem/test_resources_handler.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/filesystem/resources_handler.py, backend/handler/filesystem/storage_access.py, backend/tests/endpoints/roms/test_manual.py, backend/tests/handler/filesystem/test_resources_handler.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-43-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Upload token PDF and Markdown paths are found after a fresh handler/client and DELETE removes the exact file and clears path_manual.
    - DELETE validates a stored owned path independently of file existence, removes that exact file when present, and atomically clears path_manual plus url_manual when the valid referenced file is already absent.
    - Invalid, escaped, cross-ROM, wrong-directory, or disallowed-extension path_manual values are never opened or deleted.
    - Redownload transport/write/CAS failure preserves old path and bytes and leaves no token or staging orphan.
    - file:// and launchbox-file:// redownloads read but never mutate the local metadata-provider source, publish through a unique owned stage, CAS the expected prior path, and preserve the prior path on failure.
    - Barrier-controlled upload-first and redownload-first races each have one success, one 409, and one referenced file.
  </behavior>
  <action>Write failing tests before production changes. Extend resource-handler tests for authoritative validated token paths and legacy fixed rom.id PDF/Markdown fallback only when path_manual is empty or validly points there. Extend endpoint tests for upload-delete-restart and a valid path_manual whose exact owned file has already disappeared: DELETE must not stop at manual_exists, must return success, and must clear path_manual and url_manual together without touching a sibling file. Add redownload failure and both CAS race orderings with source/resource manifests and exact orphan inventory. Add parameterized LOCAL_FILE_SCHEMES coverage for file:// and launchbox-file:// using a read-only source fixture and before/after source manifest; prove local input is copied into a unique staged owned path, CAS uses the expected prior path, the old manual survives every pre-CAS/CAS failure, and neither the local source nor its metadata changes. Mock HTTP only at the existing context client boundary, use barriers around compare_and_swap_path, and reject infrastructure failures as RED.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, StageFailure, _execute_docker_module; name=\"tests/endpoints/roms/test_manual.py::test_uploaded_token_manual_survives_restart_and_deletes_exactly\"; exec(\"try:\\n _execute_docker_module(Path.cwd(), name, ResourceIdentity.create(4901), 'romm-romm-dev', 'romm_default', 'romm-db-dev')\\nexcept StageFailure as exc:\\n text=str(exc); assert name in text and 'failed' in text.lower() and 'ERROR collecting' not in text\\nelse:\\n raise SystemExit('RED unexpectedly passed')\")"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails because fixed-name discovery misses the token path, not setup or cleanup.
    - Tests cover PDF/Markdown restart-delete, already-absent valid owned-file clearing, invalid authority, HTTP and local-file failure preservation, both race orderings, source immutability, and zero orphans.
  </acceptance_criteria>
  <done>The incompatible discovery and redownload authorities are locked as failing behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN share staged publication and expected-path CAS</name>
  <files>backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/filesystem/resources_handler.py, backend/tests/endpoints/roms/test_manual.py, backend/tests/handler/filesystem/test_resources_handler.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/filesystem/resources_handler.py, backend/handler/filesystem/storage_access.py, backend/tests/endpoints/roms/test_manual.py, backend/tests/handler/filesystem/test_resources_handler.py</read_first>
  <behavior>
    - Validated path_manual is authoritative for primary discovery and exact owned deletion.
    - DELETE validates path_manual without requiring the file to exist, deletes only that exact owned file when present, and clears path_manual plus url_manual atomically when the valid referenced file is absent.
    - Upload and redownload use unique names, OwnedCreate, expected prior path, CAS, loser cleanup, then superseded cleanup.
    - HTTP and LOCAL_FILE_SCHEMES redownload inputs share the same staged owned publication and CAS contract while local sources remain read-only.
    - Any pre-CAS failure leaves the prior DB reference and bytes unchanged.
    - Fixed-name legacy files remain discoverable without competing with a valid token path.
  </behavior>
  <action>Add one strict helper that validates path_manual as a relative allowed-extension path contained below the exact ROM resources manual directory without testing existence. Make manual_exists and _get_manual_path prefer that path; fixed rom.id candidates are compatibility only and never override a valid authoritative path. In DELETE, validate first, authorize RESOURCES deletion, remove only the exact referenced file when it exists, and use one locked database operation to clear path_manual and url_manual when the expected valid reference still matches, including FileNotFoundError/already-absent input; do not return the current precheck 404 for this case and do not clear a concurrently replaced path. Refactor redownload away from FSResourcesHandler.get_manual: generate a fresh token .pdf, stream HTTP bytes or copy LOCAL_FILE_SCHEMES bytes from _resolve_local_file_uri into OwnedCreate.binary_file, then use the same expected-path CAS/cleanup ordering as upload. Treat local source paths as read-only inputs, never unlink/rename/chmod them, return bounded 409 on a CAS loser, keep old path on download/content/write/CAS failure, and never clear it from a failed download. Delete only the authoritative owned file, not the manual directory, and do not touch external ROM files, legacy sidecar routes, schema, or generated types.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 -c "import sys; from pathlib import Path; sys.path.insert(0, \"backend\"); from tools.verify_phase6_acceptance import ResourceIdentity, _execute_docker_module; modules=(\"tests/endpoints/roms/test_manual.py\",\"tests/handler/filesystem/test_resources_handler.py\"); [_execute_docker_module(Path.cwd(), m, ResourceIdentity.create(4910+i), \"romm-romm-dev\", \"romm_default\", \"romm-db-dev\") for i,m in enumerate(modules)]"'</automated>
  </verify>
  <acceptance_criteria>
    - Token PDF/Markdown discovery-delete, restart, exact already-absent clearing, HTTP/local-file failures, and both races pass.
    - Exactly one final file matches path_manual after every successful race; no loser, superseded, partial, or staging orphan remains.
    - file:// and launchbox-file:// sources are unchanged and read-only; external source manifests and frozen legacy source-side manual routes remain unchanged.
  </acceptance_criteria>
  <done>Primary manuals have one validated path and CAS authority across upload, redownload, discovery, and deletion.</done>
</task>
</tasks>
<source_coverage>
GOAL-06 criterion 1, CAT-02, D-05, D-11, D-22, REVIEW CR-02/CR-03, and VERIFICATION MH-29 are covered. CAT-01, CAT-03, and migration requirements are not falsely claimed.
</source_coverage>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| DB path to owned RESOURCES | Stored text must not escape the exact ROM manual directory. |
| remote manual bytes to final file | Untrusted transport cannot publish partial content. |
| concurrent requests to path_manual | Only exact expected-path CAS chooses a winner. |
| cleanup to namespace | Cleanup may remove only request-owned loser or superseded path. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-49-01 | Tampering | path_manual | mitigate | Normalize and contain under exact owned manual directory with extension allowlist. |
| T-06-49-02 | Spoofing | CAS winner | mitigate | Lock Rom and compare exact expected path. |
| T-06-49-03 | Denial of service | redownload | mitigate | Stream through existing bounded client and staged publication. |
| T-06-49-04 | Elevation of privilege | delete | mitigate | Typed RESOURCES delete of one validated path only. |
| T-06-49-05 | Information disclosure | errors | mitigate | Static bounded 400/409/500 details without paths or URLs. |
</threat_model>
<verification>
Run both modules in independent disposable lifecycles, scoped Trunk, git diff --check, exact resource/source manifests, runner/DB cleanup, normal SELECT 1, stopped romm-dev, and baseline restoration.
</verification>
<success_criteria>
Token manuals are restart-discoverable and exactly deletable; upload/redownload share staged CAS behavior; failures preserve prior state; both races leave one winner and no orphan.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md.
</output>
