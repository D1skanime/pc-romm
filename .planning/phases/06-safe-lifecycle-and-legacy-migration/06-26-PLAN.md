---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 26
type: execute
wave: 4
depends_on: [06-25]
files_modified:
  - frontend/src/v2/components/Dialogs/EditRomDialog.vue
  - frontend/src/v2/components/Dialogs/MatchRomDialog.vue
  - frontend/src/v2/components/MatchRom/MatchRomBodyGrid.vue
  - frontend/src/v2/components/MatchRom/MatchRomBodyList.vue
  - frontend/src/v2/components/MatchRom/types.ts
  - frontend/src/services/api/rom.ts
  - frontend/src/v2/sourceMutationInventory.test.ts
  - frontend/src/v2/sourceMutationControls.test.ts
  - backend/endpoints/roms/__init__.py
  - backend/tests/endpoints/test_storage_policy_denials.py
autonomous: true
requirements: [CAT-01, CAT-04]
gap_closure: true
must_haves:
  truths:
    - "Active v2 cannot request a source filename change or any other external-root mutation, including through aliases, raw clients, mixed-authority routes, or conditional payload flags."
    - "ROM metadata/cover/manual resource updates remain available, but the shared service never serializes fs_name and match/edit controls cannot choose rename."
    - "A crafted changed fs_name request authorizes RENAME against legacy_external_storage before database, RESOURCES, or filesystem mutation and fails closed."
    - "The final semantic inventory allows ordinary reads/auth/token/log/stats, database-only control-plane changes, and proven typed ASSETS/RESOURCES/TEMP mutations."
  artifacts:
    - path: "frontend/src/services/api/rom.ts"
      provides: "Metadata-only updateRom contract with no fs_name serialization"
    - path: "backend/endpoints/roms/__init__.py"
      provides: "Pre-mutation external RENAME authorization"
    - path: "frontend/src/v2/sourceMutationInventory.test.ts"
      provides: "All-active-v2 method-route-payload-authority inventory"
    - path: "backend/tests/endpoints/test_storage_policy_denials.py"
      provides: "Complete external mutation-family direct-request proof"
  key_links:
    - from: "frontend/src/v2/sourceMutationInventory.test.ts"
      to: "all production v2 modules, reachable services, backend route registrations, StorageOperation, and descriptors"
      via: "semantic method/route/payload classification"
      pattern: "CREATE|UPLOAD|WRITE|OVERWRITE|RENAME|MOVE|COPY|DELETE|EXTRACT|MKDIR|SIDECAR_WRITE|COVER_WRITE"
    - from: "backend/endpoints/roms/__init__.py"
      to: "legacy_external_storage"
      via: "changed fs_name preflight before any update/resource I/O"
      pattern: "StorageOperation.RENAME"
---

<objective>
Close source rename and install the final active-v2 external mutation inventory.

Purpose: Complete CAT-04 across every active-v2 module without banning harmless generic clients or typed RomM-owned mutations.
Output: Metadata-only update controls/service, pre-I/O rename denial, and full semantic/static/behavioral negative coverage.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-18-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-24-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-25-SUMMARY.md
</context>

<interfaces>
PUT /roms/{id} is mixed authority: metadata and artwork/manual/media resources target database/RESOURCES, but a changed RomUpdateForm.fs_name calls fs_rom_handler.rename_fs_rom on legacy_external_storage and rewrites RomFile paths. The live route has no RENAME guard. EditRomDialog edits fs_name and both MatchRom bodies expose renameFromSource. The service serializes fs_name today. StorageOperation defines CREATE, UPLOAD, WRITE, OVERWRITE, RENAME, MOVE, COPY, DELETE, EXTRACT, PATCH, MKDIR, SIDECAR_WRITE, and COVER_WRITE; legacy external permits only EXTERNAL_READ_OPERATIONS. Typed owned kinds include RESOURCES, ASSETS, CONFIG, CACHE, HASHES, SCAN_STATE, TEMP, SYNC, AUDIT, and DATABASE.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove rename intent and close all-v2 semantic mutation inventory</name>
  <files>frontend/src/v2/components/Dialogs/EditRomDialog.vue, frontend/src/v2/components/Dialogs/MatchRomDialog.vue, frontend/src/v2/components/MatchRom/MatchRomBodyGrid.vue, frontend/src/v2/components/MatchRom/MatchRomBodyList.vue, frontend/src/v2/components/MatchRom/types.ts, frontend/src/services/api/rom.ts, frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/v2/sourceMutationControls.test.ts</files>
  <read_first>frontend/src/v2/components/Dialogs/EditRomDialog.vue, frontend/src/v2/components/Dialogs/MatchRomDialog.vue, frontend/src/v2/components/MatchRom/MatchRomBodyGrid.vue, frontend/src/v2/components/MatchRom/MatchRomBodyList.vue, frontend/src/v2/components/MatchRom/types.ts, frontend/src/services/api/rom.ts, frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/v2/sourceMutationControls.test.ts, frontend/src/v2/components/GameDetails/PatcherTab.vue, frontend/src/services/api/screenshot.ts, frontend/src/services/api/save.ts, frontend/src/services/api/state.ts, frontend/src/services/api/firmware.ts, backend/endpoints/roms/__init__.py, backend/endpoints/roms/patch.py, backend/endpoints/storage_policy.py, backend/handler/filesystem/storage_policy.py, frontend/vitest.config.ts, frontend/package.json</read_first>
  <behavior>
    - Recursively inspect every production .vue/.ts under frontend/src/v2 and every reachable shared API service; extract service mutation symbols and raw POST/PUT/PATCH/DELETE method/route/payload combinations.
    - Do not ban generic Axios or raw *Api imports. Allow GET/HEAD, auth/token/log/stat and database-only control-plane operations, plus calls proven by live guards/descriptors to target typed owned storage.
    - Fail unknown combinations and every legacy_external_storage operation outside the read set, including conditional flags/fields, with importer, call, method, route, payload capability, operation, and storage class.
    - Explicitly allow raw POST /roms/{file_id}/patch only as external READ plus TEMP PATCH; allow RESOURCES manual/cover/media, ASSETS screenshots/saves/states, catalog-only ROM/firmware/platform deletion, and database/config control-plane operations.
    - Edit displays source filename read-only; match bodies/types have no renameFromSource; updateRom cannot accept/serialize fs_name while metadata and typed-resource updates pass.
  </behavior>
  <action>RED: broaden the Plan 18 inventory to all production v2 and reachable service modules. Use a closed reviewed manifest derived from live backend route registrations, guards, descriptor construction, and payload-sensitive branches. Demonstrate failures for EditRom fs_name, both match rename controls, raw PUT with fs_name, aliases, conditional delete_from_fs, and fixtures for every external operation family. Demonstrate positives for Patcher raw POST, unrelated generic-client consumers such as PatcherTab, LibraryStatsWidget, ServerStats, ClientApiTokens, and Logs, ordinary reads/auth/token/stats, database-only routes, and typed RESOURCES/ASSETS/TEMP. Extend rendered tests for edit/match. Record RED and commit with hooks as test(06-26): inventory all v2 external mutation. GREEN: make EditRom source filename read-only; remove renameFromSource state, checkbox, emit property, and fs_name rewrite from MatchRom grid/list/dialog/types. Narrow updateRom so its public input and multipart serialization omit fs_name regardless of caller object shape; retain metadata and typed resource fields. Do not reject raw/generic clients merely for existing or create a local rename call. Commit with hooks as fix(06-26): remove v2 rename authority. Never use --no-verify.</action>
  <verify>
    <automated>docker exec --workdir /app/frontend romm-dev npm run test -- src/v2/sourceMutationInventory.test.ts src/v2/sourceMutationControls.test.ts</automated>
  </verify>
  <acceptance_criteria>The final inventory covers every active-v2 production module/service, fails forbidden/unknown external mutation combinations, passes named ordinary generic-client and typed-owned positives, and rendered edit/match controls cannot express rename.</acceptance_criteria>
  <done>No external mutation authority is expressible from active v2; source filename is immutable while metadata/owned resources remain editable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deny crafted source rename before any partial update</name>
  <files>backend/endpoints/roms/__init__.py, backend/tests/endpoints/test_storage_policy_denials.py</files>
  <read_first>backend/endpoints/roms/__init__.py, backend/tests/endpoints/test_storage_policy_denials.py, backend/endpoints/storage_policy.py, backend/handler/filesystem/__init__.py, backend/handler/filesystem/storage_policy.py, backend/handler/filesystem/rom_handler.py, backend/handler/filesystem/resources_handler.py</read_first>
  <behavior>
    - Unchanged/omitted fs_name follows metadata/typed-resource update behavior without external RENAME authorization.
    - Changed fs_name authorizes RENAME/external immediately after sanitize/compare and before database update, cover/manual/media resource I/O, or rename_fs_rom.
    - Crafted traversal, symlink/path replacement, stale/replayed update, and valid colliding filename all fail with zero partial mutation and identical source/owned/database manifests.
    - The complete server matrix covers CREATE, UPLOAD, WRITE, OVERWRITE, RENAME, MOVE, COPY, DELETE, EXTRACT, PATCH-to-external, MKDIR, SIDECAR_WRITE, and COVER_WRITE; external READ and PATCH-to-TEMP remain allowed.
  </behavior>
  <action>RED: add update route tests that combine changed fs_name with artwork/remove_cover/manual/media metadata so a late guard would expose partial owned or database mutation. Include traversal, symlink replacement, replay, and collision cases, immutable manifests, call ordering, and path-redacted denial. Ensure same-name metadata update remains green. Record RED and commit with hooks as test(06-26): deny source rename atomically. GREEN: after sanitizing/comparing new_fs_name, authorize RENAME against legacy_external_storage before any durable/resource mutation when it differs; Phase 6 denies. Preserve same-name metadata/RESOURCES behavior. Do not mutate DB first, catch denial, or fall back. Commit with hooks as fix(06-26): guard source rename before mutation. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0626 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_storage_policy_denials.py -k "rename or update_rom" -x</automated>
  </verify>
  <acceptance_criteria>Focused tests prove changed fs_name denies before every database/owned/external effect under adversarial inputs, while same-name metadata and typed-resource updates pass.</acceptance_criteria>
  <done>Crafted clients cannot rename source or trigger a partial mixed-authority update.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Risk                                           | Disposition | Mitigation                                          |
| ----------------------------- | ---------------------------------------------- | ----------- | --------------------------------------------------- |
| v2 graph to API               | Alias/raw/mixed route hides external authority | mitigate    | Method-route-payload-operation-descriptor inventory |
| update payload to mixed route | fs_name rename accompanies owned writes        | mitigate    | External preflight before all durable/resource work |
| path resolution to rename     | Traversal/symlink/TOCTOU replaces target       | mitigate    | Deny before path lookup/I/O                         |
| stale/replayed update         | Old confirmation renames current source        | mitigate    | Every changed request denied                        |
| inventory/error output        | Sensitive paths leak                           | mitigate    | Normalized route and stable storage metadata only   |

## STRIDE Threat Register

| Threat ID       | Category               | Component             | Disposition | Mitigation Plan                            |
| --------------- | ---------------------- | --------------------- | ----------- | ------------------------------------------ |
| T-06-26-01      | Tampering              | fs_name               | mitigate    | Remove client field and deny server RENAME |
| T-06-26-02      | Elevation of privilege | raw/mixed calls       | mitigate    | Payload-sensitive closed inventory         |
| T-06-26-03      | Spoofing               | typed versus external | mitigate    | Live descriptor identity                   |
| T-06-26-04      | Repudiation            | stale/replay          | mitigate    | Direct adversarial tests                   |
| T-06-26-05      | Information disclosure | failures              | mitigate    | Path-redacted output                       |
| T-06-26-06      | Denial of service      | recursive graph       | accept      | Bound checked-in traversal                 |
| </threat_model> |

<verification>
Run both fast focused commands first. Then full frontend typecheck/tests/build, complete backend denial matrix, scoped Trunk, git diff --check, and drop/assert absence of romm_test_0626. Final Plan 23 repeats inventory and authoritative gates. No restart/deploy.
</verification>

<success_criteria>

- CAT-04 covers every external create/upload/write/overwrite/rename/move/copy/delete/extract/patch/mkdir/sidecar/cover family.
- Generic/raw clients are judged by semantic call authority, not mere import.
- Typed owned and database/control-plane behavior remains usable.
- External source remains byte- and identity-exact across crafted requests.
  </success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-26-SUMMARY.md.
</output>
