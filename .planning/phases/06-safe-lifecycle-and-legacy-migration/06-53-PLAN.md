---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 53
type: execute
wave: 3
depends_on: [06-48, 06-49, 06-50, 06-51, 06-52]
files_modified:
  - backend/tools/verify_phase6_acceptance.py
  - backend/tests/tools/test_verify_phase6_acceptance.py
  - frontend/e2e/manual-mutation-pending.spec.ts
  - .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json
autonomous: true
requirements:
  [CAT-01, CAT-02, CAT-03, CAT-04, MIG-01, MIG-02, MIG-03, MIG-04, MIG-05]
gap_closure: true
must_haves:
  truths:
    - "The checked-in acceptance inventory executes every changed backend test module, including test_resources_handler.py, and retains focused ManualSubtab coverage."
    - "A checked-in ordered REQUIRED_ROUND4_TEST_IDS inventory names every required Round 4 pytest, Vitest, and Playwright test exactly; same-module deletion, rename, or substitution is a hard failure."
    - "Acceptance JSON records every exact required test identifier and its passing outcome from pytest JUnit, Vitest JSON, or Playwright JSON, with exact ordered-set equality and no aggregate or module-membership escape hatch."
    - "Every prior module has an exact post-Plan-52 collected/pass/skip/xfail/xpass contract; no aggregate minimum can hide a deleted regression behind new storage-access tests."
    - "The evidence parser requires the exact source type and current semantic label for CR-01..04 and WR-01, rejecting reused IDs with stale meanings and stale WR-02/WR-03 rows."
    - "A fresh disposable Chromium run validates the changed ManualSubtab at a committed UI revision while romm-dev stays stopped, and later acceptance rejects any drift from that revision."
    - "Harness self-test and audits prove exact inventory, source-row semantics, redaction, stopped-service identity, and absence of every Round 4 resource family."
  artifacts:
    - path: "backend/tools/verify_phase6_acceptance.py"
      provides: "Current Phase 6 module and source-coverage authority"
    - path: "backend/tests/tools/test_verify_phase6_acceptance.py"
      provides: "RED/GREEN contract for exact per-module outcomes, source-row semantics, UI evidence, and cleanup universes"
    - path: "frontend/e2e/manual-mutation-pending.spec.ts"
      provides: "Real-browser pending, stable-viewer, recovery, theme, viewport, and modality checks"
    - path: ".planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json"
      provides: "Fresh Playwright run metadata bound to the validated UI commit and path digest"
  key_links:
    - from: "Plans 48-52 changed tests"
      to: "PHASE6_MODULES and focused UI tests"
      via: "checked-in exact inventory"
      pattern: "PHASE6_MODULES|FOCUSED_UI_TESTS"
    - from: "06-REVIEW and 06-VERIFICATION"
      to: "REQUIRED_SOURCE_ROWS"
      via: "one unique exact source-type/semantic-label/COVERED row per current source item"
      pattern: "REQUIRED_SOURCE_ROWS|parse_source_audit"
    - from: "post-Plan-52 prior modules"
      to: "acceptance prior_backend records"
      via: "exact per-module collected and outcome tuple equality"
      pattern: "EXPECTED_PRIOR_MODULE_RESULTS"
    - from: "REQUIRED_ROUND4_TEST_IDS"
      to: "acceptance round4_regressions results"
      via: "pytest JUnit, Vitest JSON, and Playwright JSON exact identifier normalization plus passing outcome"
      pattern: "REQUIRED_ROUND4_TEST_IDS|round4_regressions"
    - from: "fresh disposable Playwright result"
      to: "final manual_ui stage"
      via: "UI_EVIDENCE_COMMIT, UI path digest, spec digest, and no-drift check"
      pattern: "UI_EVIDENCE_COMMIT|fresh_browser_validated"
---

<objective>
Rebind the deterministic Phase 6 acceptance harness to the Round 4 closure.
Purpose: Plan 47 evidence is internally valid but its inventory and exact-set parser predate the current three blockers and descriptor warning.
Output: A tested exact-count/exact-source-row harness plus fresh commit-bound browser evidence ready for Plan 54.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-45-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-48-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-51-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-52-SUMMARY.md
@frontend/src/v2/components/GameDetails/ManualSubtab.test.ts
@frontend/src/v2/components/GameDetails/ManualSubtab.vue
@frontend/e2e/README.md
@frontend/playwright.config.ts
<interfaces>
verify_phase6_acceptance.py owns PRIOR_PHASE_MODULES, PHASE6_MODULES, FOCUSED_UI_TESTS, exact EXPECTED_PRIOR_MODULE_RESULTS, ordered REQUIRED_ROUND4_TEST_IDS, REQUIRED_ROUND4_SOURCE_BINDINGS, exact REQUIRED_SOURCE_ROWS, deduplicated_phase6_modules(), --self-test, --verify-prior-contract, --run-ui-browser, --verify-ui-evidence, --run-complete, --audit-only, and --verify-evidence. EXPECTED_PRIOR_MODULE_RESULTS must be calibrated once after Plans 48-52 and then be immutable final authority: one exact tuple of collected, passed, skipped, xfailed, and xpassed for each of the 29 named prior modules.

REQUIRED_ROUND4_TEST_IDS is this exact checked-in ordered tuple. Prefixes are part of each identifier and cannot be stripped or rewritten:

- pytest::tests/integration/test_legacy_migration.py::test_folder_rom_parent_reconnects_from_exact_observed_children
- pytest::tests/endpoints/roms/test_manual.py::test_uploaded_token_manual_survives_restart_and_deletes_exactly
- pytest::tests/handler/filesystem/test_resources_handler.py::test_validated_token_manual_path_is_authoritative_after_restart
- pytest::tests/endpoints/roms/test_manual.py::test_missing_owned_token_manual_clears_exact_authoritative_reference
- pytest::tests/endpoints/roms/test_manual.py::test_primary_manual_cas_failure_preserves_prior_path_and_cleans_candidate
- pytest::tests/endpoints/roms/test_manual.py::test_upload_first_redownload_race_has_one_winner_and_no_orphan
- pytest::tests/endpoints/roms/test_manual.py::test_redownload_first_upload_race_has_one_winner_and_no_orphan
- pytest::tests/endpoints/roms/test_manual.py::test_local_uri_redownload_uses_staged_cas_and_preserves_source[file]
- pytest::tests/endpoints/roms/test_manual.py::test_local_uri_redownload_uses_staged_cas_and_preserves_source[launchbox-file]
- pytest::tests/endpoints/test_screenshots.py::test_hidden_rom_screenshot_update_is_masked_before_effects
- pytest::tests/endpoints/test_screenshots.py::test_hidden_platform_screenshot_update_is_masked_before_effects
- pytest::tests/endpoints/test_screenshots.py::test_hidden_rom_screenshot_delete_is_masked_before_effects
- pytest::tests/endpoints/test_screenshots.py::test_hidden_platform_screenshot_delete_is_masked_before_effects
- pytest::tests/handler/filesystem/test_storage_access.py::test_owned_create_close_error_never_closes_reused_descriptor[binary_file]
- pytest::tests/handler/filesystem/test_storage_access.py::test_owned_create_close_error_never_closes_reused_descriptor[subprocess_file]
- vitest::src/v2/components/GameDetails/ManualSubtab.test.ts::ManualSubtab > redownload_blocks_every_competing_manual_gesture
- vitest::src/v2/components/GameDetails/ManualViewerControls.test.ts::ManualViewerControls > pdf_controls_stay_visible_and_disabled_while_pending
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > mobile-320 > mouse
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > mobile-320 > touch
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > mobile-320 > keyboard
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > mobile-320 > gamepad
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > desktop > mouse
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > desktop > touch
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > desktop > keyboard
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > light > desktop > gamepad
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > mobile-320 > mouse
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > mobile-320 > touch
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > mobile-320 > keyboard
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > mobile-320 > gamepad
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > desktop > mouse
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > desktop > touch
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > desktop > keyboard
- playwright::chromium::e2e/manual-mutation-pending.spec.ts::manual mutation pending matrix > dark > desktop > gamepad

The pytest parameter IDs file, launchbox-file, binary_file, and subprocess_file are explicit stable test IDs, not inferred labels. Vitest IDs use repository-relative file plus full suite/test name. Playwright IDs use projectName, repository-relative spec, and full titlePath. REQUIRED_ROUND4_SOURCE_BINDINGS binds the folder ID to MIG-01/MIG-04/MH-14/CR-01; all token-manual pytest IDs to CAT-02/MH-29/CR-02/CR-03; both Vitest IDs and all 16 Playwright IDs to MH-29/CR-03/UI-02/UI-03/UI-04/UI-05/UI-06; all four screenshot IDs to CAT-02/MH-30/CR-04; and both descriptor IDs to CAT-02/MH-31/WR-01. REQUIRED_SOURCE_ROWS binds every ID to its source type, with exact current REVIEW labels: CR-01 "Folder and multi-file ROM selection from exact observed child evidence"; CR-02 "Token-path primary manual discovery and exact deletion"; CR-03 "Primary manual redownload staged CAS and active-v2 pending serialization"; CR-04 "Screenshot visibility before update and delete effects"; WR-01 "Single-consumption staged-publication descriptor close". Current verification retains MH-01..31 with failures at MH-14, MH-29, MH-30 and warning at MH-31. UI_EVIDENCE_COMMIT and 06-53-UI-EVIDENCE.json must identify the new browser-validated commit, never Plan 45 commit 364bb2764.
</interfaces>
</context>
<test_topology>
Harness contract tests use --self-test through one disposable backend module lifecycle. Exact-count calibration and --verify-prior-contract use the existing per-module isolated database/principal/basetemp/container contract and write no acceptance evidence. Fresh UI evidence uses a nonce-owned disposable application, database/principal, RESOURCES volume, Node/Playwright volume, and loopback-only ephemeral port, all labeled io.romm.phase6.round4=06-53-ui; romm-dev remains stopped. Register cleanup before every create, capture and validate exact Docker IDs, preserve the primary exit, remove only exact owned IDs, assert each exact ID absent, and fail a successful stage on cleanup failure. Final audit queries prefixes p0647, p0650/romm-p0650, p0652, and p0653/romm-p0653 plus io.romm.phase6.round4 labels only as assertions, never deletion targets. No complete acceptance JSON write occurs in this plan. No restart, deploy, non-loopback/public port, NAS access, broad cleanup, STATE/ROADMAP edit, or baseline change.
</test_topology>
<tasks>
<task type="auto" tdd="true">
<name>Task 1: RED require the Round 4 module and source universes</name>
<files>backend/tests/tools/test_verify_phase6_acceptance.py</files>
<read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-47-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-48-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-51-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-52-SUMMARY.md</read_first>
<behavior> - PHASE6_MODULES contains every changed backend module exactly once, adding tests/handler/filesystem/test_resources_handler.py. - Focused UI retains ManualSubtab and viewer coverage. - EXPECTED_PRIOR_MODULE_RESULTS names all 29 prior modules and exact per-module collected/pass/skip/xfail/xpass values after Plan 52; any field mismatch fails even when the aggregate rises. - REQUIRED_SOURCE_ROWS contains GOAL-06, CAT-01..04, MIG-01..05, R-01..09, D-01..23, MH-01..31, current CR-01..04, current WR-01, and UI-01..08 exactly once with correct source types. - Parser rejects stale WR-02/WR-03, each old Plan 47 review description reused under CR-01..04/WR-01, missing/duplicate/unknown/non-COVERED/deferred rows, and stale validation/UI binding. - Audit-only rejects every Round 4 prefix/label but never feeds discovered names to a remove command. - REQUIRED_ROUND4_TEST_IDS equals the exact 33-entry tuple in interfaces in order, with no duplicate, missing, shortened, renamed, or same-module substitute identifier. - A valid exact-results fixture records all 33 identifiers as passed; removing one, renaming one, substituting a different test from the same module/spec, duplicating one, adding one, reordering the list, or changing any outcome fails.
</behavior>
<action>Add pure tests first. Pin the exact module set after Plans 48-52 and assert no changed test is omitted. Check in REQUIRED_ROUND4_TEST_IDS exactly as listed in interfaces and REQUIRED_ROUND4_SOURCE_BINDINGS exactly as specified there. Add test_required_round4_test_ids_reject_omission_rename_and_same_module_substitution and fixture helpers that require ordered exact equality and outcome passed for all 33 identifiers. Exercise removal of each ID in turn, spelling/title rename, replacement by a real different test from the same backend module or frontend file, duplicate, unexpected extra, reordered list, and passed-to-skipped/failed/error change. None may be accepted through module membership, count equality, substring, suffix, normalized-label, or aggregate logic. Replace the 843 aggregate fixture with a 29-entry per-module fixture whose storage-access row is explicitly post-Plan-52; mutate each module's collected, passed, skipped, xfailed, and xpassed field independently and prove validation fails without aggregate-offset logic. Define exact source-row fixtures with source type and semantic label, then add one stale-description rejection for each CR-01..04 and WR-01 using the old Plan 47 meanings while retaining the current ID and COVERED status. Assert WR-02/WR-03 are unknown. Add parser fixtures for pytest JUnit, Vitest JSON, and Playwright JSON proving exact parameter IDs, fullName/titlePath, and projectName survive normalization without aliasing. Add command-graph tests proving --prior-outcomes-min is absent/rejected; fresh browser evidence must have status fresh_browser_validated, fresh_run true, a full validated commit different from 364bb2764, exact UI/spec digests, all 16 exact Playwright matrix IDs with passed outcomes, successful cleanup fields, and no UI path drift. Add audit tests for every p0647/p0650/p0652/p0653 prefix and io.romm.phase6.round4 label, plus a spy proving discovery output is never passed to docker rm/volume rm or SQL DROP. Keep all mandatory source types and UI-SPEC rows covered. Tests use temporary evidence and injected runners only.</action>
<verify>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); name=p0653-red-$nonce; id=; owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$owned" -eq 1 ]; then docker rm -f "$id" >/dev/null || cleanup_rc=1; if docker inspect "$id" >/dev/null 2>&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; docker info >/dev/null; if docker container inspect "$name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; id=$(docker create --name "$name" --label io.romm.phase6.round4=06-53 --network none --read-only --tmpfs /tmp:rw,nosuid,nodev --user 1000:1000 --mount type=bind,src=/home/d1sk/romm/backend,dst=/app/backend,readonly -w /app/backend --entrypoint /app/.venv/bin/python romm-romm-dev -m pytest -p no:env -p no:cacheprovider tests/tools/test_verify_phase6_acceptance.py::test_required_round4_test_ids_reject_omission_rename_and_same_module_substitution -x); [[ "$id" =~ ^[0-9a-f]{64}$ ]]; owned=1; test "$(docker inspect --format "{{.Name}}" "$id")" = "/$name"; set +e; output=$(docker start --attach "$id" 2>&amp;1); rc=$?; set -e; printf "%s\n" "$output"; test "$rc" -eq 1; printf "%s\n" "$output" | grep -q "test_required_round4_test_ids_reject_omission_rename_and_same_module_substitution"; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0|ModuleNotFound|permission denied"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on the missing exact 33-ID/outcome authority, including omission, rename, or same-module substitution rejection, not infrastructure.
    - All 29 prior modules and all mandatory GOAL/REQ/RESEARCH/CONTEXT rows stay asserted without an aggregate minimum escape hatch.
    - All 33 exact Round 4 IDs have omission, rename, same-module substitution, duplicate, extra, order, and non-passing outcome negative tests.
    - The exact p0653 RED container ID is absent after the check and cleanup failure cannot be suppressed.
  </acceptance_criteria>
  <done>The final harness cannot silently reuse stale Plan 47 coverage.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN bind the harness to current closure</name>
  <files>backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Inventory executes every current changed module without duplicate prior execution.
    - Prior validation accepts only exact equality with the calibrated per-module collected/outcome map.
    - Parser accepts only the current exact coverage universe, source types, and current review semantics.
    - UI evidence validation accepts only a fresh Playwright record bound to UI_EVIDENCE_COMMIT and zero drift.
    - Self-test and audit-only leave zero resources for every Round 4 prefix/label and normal DB continuity.
    - Backend pytest JUnit and focused Vitest JSON results normalize to exact REQUIRED_ROUND4_TEST_IDS entries, and UI evidence supplies the exact Playwright subset; acceptance validation requires the ordered union with outcome passed.
  </behavior>
  <action>Add test_resources_handler.py to PHASE6_MODULES and keep deduplication exact. Add a capture-only prior-contract mode that runs every named prior module under existing isolated resources, writes a temporary structured record but no acceptance evidence, and reports collected/pass/skip/xfail/xpass per module. Run it once after Plans 48-52, transcribe its 29 exact records into EXPECTED_PRIOR_MODULE_RESULTS, then run --verify-prior-contract to execute all 29 again and require field-for-field equality. Remove PRIOR_OUTCOMES_MIN, --prior-outcomes-min, aggregate comparison, and the argument from CANONICAL_COMMAND_IDENTITY; validation must compare module order and exact fields only. Check in the exact 33-entry REQUIRED_ROUND4_TEST_IDS and binding map from interfaces. Extend backend module execution with task-owned pytest JUnit XML using stable legacy file/name fields, parse every testcase outcome, and normalize it to pytest::repository-relative-node-id without dropping parameter IDs. Extend the focused Vitest command with its JSON reporter/output file and normalize repository-relative file plus full suite/test name to the two exact vitest:: IDs. Merge those results with the exact Playwright records from verified Plan 53 UI evidence into stages.round4_regressions.results in REQUIRED_ROUND4_TEST_IDS order; validate exact equality and outcome passed before atomic acceptance writing. Reject base-function matching when required parameter IDs exist, basename-only matching, suffix matching, title slugging, and any substitute from an already accepted module/file. Replace REQUIRED_SOURCE_IDS-only authority with REQUIRED_SOURCE_ROWS: validate exact source type for every row and exact semantic label for CR-01..04/WR-01 as declared in interfaces, while removing stale WR-02/WR-03. Implement fresh UI evidence modes and schema: record full validated commit, current UI-path SHA-256 manifest, browser-spec SHA-256, run_id, Chromium version, exact Playwright IDs/results, fresh_run=true, and zero cleanup; require UI_EVIDENCE_COMMIT equality, ancestry, and git diff --quiet plus digest equality for UI_EVIDENCE_PATHS after that commit. Old Plan 45 metadata/status and commit 364bb2764 must fail. Extend _audit_absence with assertion-only prefix and ownership-label checks for every Round 4 container, volume, database, and principal family; never delete discovery results. Bump schema/canonical command identity so Plan 47 JSON is stale. Preserve redaction, stopped-source-service preflight, generated/full frontend/dialect/source-manifest gates, and atomic evidence writing.</action>
  <verify>
    <automated>bash -lc 'test "$(uname -s)" = Linux; test "$(git rev-parse --show-toplevel)" = /home/d1sk/romm; test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 backend/tools/verify_phase6_acceptance.py --self-test --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev; python3 backend/tools/verify_phase6_acceptance.py --verify-prior-contract --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev; python3 backend/tools/verify_phase6_acceptance.py --audit-only --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev'</automated>
</verify>
<acceptance_criteria> - Pure harness tests and disposable self-test pass. - All 29 prior modules match exact post-Plan-52 collected/outcome records; deleting a prior storage-access test cannot be offset by any new test. - Old Plan 47 evidence and old Plan 45 UI metadata are rejected as stale by command, source-row, and UI binding. - Exact pytest/Vitest/Playwright test-result extraction is checked in and the 33-entry ordered result set cannot be satisfied by a different test in the same module or spec. - Exact cleanup across all Round 4 prefixes/labels, redaction, DB continuity, and stopped service checks remain enforced.
</acceptance_criteria>
<done>The checked-in harness is ready to prove the current closure once, completely, in Plan 54.</done>
</task>
<task type="auto">
<name>Task 3: Generate fresh commit-bound browser evidence for the changed manual UI</name>
<files>frontend/e2e/manual-mutation-pending.spec.ts, backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json</files>
<read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/e2e/README.md, frontend/playwright.config.ts, frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts, backend/tools/verify_phase6_acceptance.py, backend/tests/tools/test_verify_phase6_acceptance.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-45-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md</read_first>
<action>Create a dedicated Playwright Chromium spec for the assembled v2 manual subtab. At Task start record full git rev-parse HEAD as the product commit, require Plan 50's commit to be its ancestor, require no tracked UI-path edits, and explicitly reject 364bb2764. The harness must provision one exact nonce application container from romm-romm-dev, one isolated DB/principal, owned RESOURCES and Node/Playwright volumes, seeded e2e admin plus one ROM/manual fixture, and a loopback-only ephemeral port while romm-dev remains exited; bind canonical backend/frontend read-only and label every resource io.romm.phase6.round4=06-53-ui. Generate exactly the 16 Playwright test title paths listed in REQUIRED_ROUND4_TEST_IDS, one for each light/dark, mobile-320/desktop, and mouse/touch/keyboard/gamepad cell under the exact "manual mutation pending matrix" suite. Each cell holds redownload and same-ROM refresh requests deterministically and proves aria-busy, Replace/drop/delete/redownload disablement, no second mutation, stable PDF/Markdown viewer, refresh-before-success, and recovery after 409 and transport failure. Use Playwright JSON results, not console text, and require the exact chromium/project/spec/titlePath identifiers with passed outcomes, zero retries/failures/unhandled errors, and no extra substitute identifier. Cleanup must preserve the browser result, remove only captured exact IDs, assert each absent, prove normal DB SELECT 1 and romm-dev still exited, then atomically write 06-53-UI-EVIDENCE.json with the recorded product commit, exact UI-path/spec digests, Chromium version, the 16 exact IDs/outcomes, fresh_run=true, and zero cleanup. Set UI_EVIDENCE_COMMIT to that full recorded commit after the successful run; the later evidence-only commit may change harness/evidence files but no UI_EVIDENCE_PATHS file. Do not relabel Plan 45 component tests or create browser evidence without launching Chromium.</action>
<verify>
<automated>bash -lc 'test "$(docker inspect --format "{{.Config.Image}}:{{.State.Status}}" romm-dev)" = "romm-romm-dev:exited"; python3 backend/tools/verify_phase6_acceptance.py --run-ui-browser --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev --network romm_default --node-image node:24-bookworm --ui-evidence .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json; python3 backend/tools/verify_phase6_acceptance.py --verify-ui-evidence --checkout /home/d1sk/romm --ui-evidence .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-UI-EVIDENCE.json; python3 backend/tools/verify_phase6_acceptance.py --audit-only --checkout /home/d1sk/romm --source-container romm-dev --db-container romm-db-dev --expected-source-image romm-romm-dev'</automated>
</verify>
<acceptance_criteria> - Real Chromium passes the named pending/stability/recovery matrix in both themes, both viewports, and all four supported input paths against the disposable assembled app. - Evidence names a full current commit other than 364bb2764, binds exact UI-path and spec digests, records fresh_run=true, and reports zero owned resources after cleanup. - romm-dev stays exited, normal DB remains healthy, no public port or NAS path is used, and no exact UI_EVIDENCE_PATHS drift exists after the validated commit.
</acceptance_criteria>
<done>The changed manual UI has fresh browser proof that final acceptance can verify without relabeling old evidence.</done>
</task>
</tasks>
<source_coverage>
All GOAL-06, CAT-01..04, MIG-01..05, RESEARCH R-01..09, CONTEXT D-01..23, VERIFICATION MH-01..31, current REVIEW CR-01..04/WR-01 exact source-row semantics, and UI-SPEC UI-01..08 are required. Fresh browser evidence covers the changed manual UI. Nothing is deferred.
</source_coverage>
<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                      |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| test reporters to acceptance JSON | Exact pytest, Vitest, and Playwright identities and outcomes must survive parsing without alias or substitution. |
| changed tests to inventory        | Omitted modules create false acceptance.                                                                         |
| source artifacts to required IDs  | Stale rows can falsely claim current closure.                                                                    |
| harness to Docker/DB              | Self-test must own and clean exact resources.                                                                    |
| old evidence to current parser    | Command/current-set changes must invalidate stale JSON.                                                          |
| browser runner to product commit  | Fresh UI evidence must identify the exact validated UI revision and reject later drift.                          |

## STRIDE Threat Register

| Threat ID       | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                                            |
| --------------- | ---------------------- | ----------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-53-01      | Tampering              | module inventory        | mitigate    | Exact checked-in set and changed-test regression.                                                                                                                          |
| T-06-53-02      | Repudiation            | source audit            | mitigate    | One unique COVERED row for exact current universe.                                                                                                                         |
| T-06-53-03      | Spoofing               | stale evidence          | mitigate    | Bump canonical command identity and digest binding.                                                                                                                        |
| T-06-53-04      | Information disclosure | runner output           | mitigate    | Preserve bounded redaction and no raw inventory.                                                                                                                           |
| T-06-53-09      | Tampering              | Round 4 result set      | mitigate    | Require ordered exact equality with all 33 checked-in IDs and passed outcomes; reject omission, rename, same-module substitution, duplicate, extra, and shortened aliases. |
| T-06-53-05      | Elevation of privilege | cleanup                 | mitigate    | Remove exact created IDs only; discovery is assertion-only.                                                                                                                |
| T-06-53-06      | Repudiation            | prior regression counts | mitigate    | Exact per-module collected/outcome tuples replace aggregate minimum authority.                                                                                             |
| T-06-53-07      | Spoofing               | review rows             | mitigate    | Require exact source type and current semantic label, not ID/status alone.                                                                                                 |
| T-06-53-08      | Tampering              | browser evidence        | mitigate    | Bind full commit plus UI/spec digests and require no drift.                                                                                                                |
| </threat_model> |

<verification>
Run pure harness tests including exact Round 4 omission/rename/substitution mutations, --self-test, --verify-prior-contract, fresh --run-ui-browser, --verify-ui-evidence, scoped Trunk, git diff --check, --audit-only across every Round 4 prefix/label, normal DB SELECT 1, stopped romm-dev, exact resource absence, and baseline restoration. Do not run --run-complete here.
</verification>
<success_criteria>
Current changed tests, the exact 33-entry Round 4 test/result universe, exact per-module prior results, exact source-row semantics, and fresh commit-bound browser evidence are pinned; omission, rename, same-module substitution, stale review/UI evidence, and non-passing outcomes are rejected; self-test and all-prefix cleanup pass without acceptance publication.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-53-SUMMARY.md.
</output>
