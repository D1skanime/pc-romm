---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 08
type: tdd
wave: 7
depends_on: [06-07]
files_modified:
  - backend/handler/database/legacy_migration_handler.py
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/endpoints/test_storage.py
  - backend/tests/integration/test_legacy_migration.py
  - backend/tools/verify_storage_migrations.py
  - backend/tests/tools/test_verify_storage_migrations.py
autonomous: true
requirements: [MIG-02, MIG-04, MIG-05]
must_haves:
  truths:
    - "Authorized administrators can observe bounded rollback eligibility and request rollback with migration ID plus expected version."
    - "Unused rollback restores only prior owned database/configuration state, invalidates mapping revision, and audits atomically."
    - "Used, stale, replayed, or cross-platform rollback fails safely across restart and all three supported dialects."
  artifacts:
    - path: "backend/endpoints/storage.py"
      provides: "Typed rollback status and mutation routes"
    - path: "backend/tools/verify_storage_migrations.py"
      provides: "Three-dialect restart and round-trip proof"
  key_links:
    - from: "backend/endpoints/storage.py"
      to: "backend/handler/database/legacy_migration_handler.py"
      via: "rollback route invokes locked CAS transaction"
      pattern: "rollback|expected_version"
    - from: "backend/tools/verify_storage_migrations.py"
      to: "backend/alembic/versions/0111_safe_lifecycle_legacy_migration.py"
      via: "three-dialect migration proof"
      pattern: "mariadb|mysql|postgresql"
---

<objective>
Typed rollback API and durable persistence
Purpose: Complete D-15/D-16 after Plan 07 wires first-use marking.
Output: bounded admin API, locked rollback transaction, restart tests, and real-dialect evidence.
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/database/storage_handler.py
@backend/endpoints/storage.py
@backend/tools/verify_storage_migrations.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify rollback API and persistence</name>
  <files>backend/tests/endpoints/test_storage.py, backend/tests/integration/test_legacy_migration.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <behavior>
    - Unused rollback succeeds; first use makes rollback ineligible with stable 409.
    - Unknown, stale, replayed, expired, and cross-platform identities reveal no paths or snapshots.
    - Eligibility and mapping/migration/use state survive API and worker restart.
  </behavior>
  <action>Read backend/handler/database/storage_handler.py, backend/endpoints/storage.py, and backend/tools/verify_storage_migrations.py. Write RED endpoint, barrier-race, restart, and dialect cases using admin-before-observation auth, ordered locks, bounded safe errors, and operation-bound authority.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py tests/integration/test_legacy_migration.py tests/tools/test_verify_storage_migrations.py -k "rollback or restart or migration" -x</automated></verify>
  <done>Tests fail only on absent typed rollback and persistence behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement rollback and three-dialect durability</name>
  <files>backend/handler/database/legacy_migration_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/endpoints/test_storage.py, backend/tests/integration/test_legacy_migration.py, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <behavior>
    - GET status exposes bounded eligibility/first-use state; POST rollback requires migration ID and expected version.
    - Rollback locks migration then mapping, restores owned state only, revises mapping, audits, and marks rolled_back atomically.
    - Barrier ordering yields use-first 409 and rollback-first stale read.
  </behavior>
  <action>Follow the storage_handler lock/audit pattern and endpoint auth/safe-error pattern. Implement D-15/D-16 typed admin routes and rollback CAS without any source capability. Extend the verifier for uniquely named disposable MariaDB, MySQL, and PostgreSQL containers, pristine and seeded-0110 upgrade/downgrade/re-upgrade, restart persistence, and exact cleanup. Never reuse an existing database.</action>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py tests/integration/test_legacy_migration.py tests/tools/test_verify_storage_migrations.py -x &amp;&amp; cd .. &amp;&amp; test "$(docker inspect -f '{{range .Mounts}}{{if eq .Destination "/app"}}{{.Source}}{{end}}{{end}}' romm-dev)" = "$PWD" &amp;&amp; cd backend &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --runner-container romm-dev --handler-tests --handler-test-repetitions 1</automated></verify>
  <done>Rollback is typed, authorized, race-safe, restart-durable, source-neutral, and portable.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| admin API to transaction | Replayed rollback identity reaches durable authority. |
| rollback to source | Restored state must remain RomM-owned only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08A | S/T | rollback API | mitigate | Admin auth, expected version, binding, and row locks. |
| T-06-08B | E | rollback handler | mitigate | Restore owned state only; no source path/capability. |
| T-06-08C | I | status | mitigate | Bounded schema omits snapshots and paths. |
</threat_model>
<verification>
Run focused tests and the disposable three-dialect gate. Immutable source manifests cover success, rejection, crash, rollback, and retry.
</verification>
<success_criteria>
Rollback eligibility and mutation are typed, durable, race-safe, portable, and source-neutral.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-08-SUMMARY.md
</output>
