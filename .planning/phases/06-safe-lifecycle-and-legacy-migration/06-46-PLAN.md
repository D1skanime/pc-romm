---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 46
type: execute
wave: 4
depends_on: [06-44]
files_modified:
  - backend/tools/verify_storage_migrations.py
  - backend/tests/tools/test_verify_storage_migrations.py
autonomous: true
requirements: [MIG-02, MIG-04, MIG-05]
gap_closure: true

must_haves:
  truths:
    - "Revision 0114 passes pristine, seeded, downgrade, restart, re-upgrade, and identity-privacy checks on MariaDB, MySQL, and PostgreSQL."
    - "Rows without private source evidence are unselectable after every upgrade/downgrade path, while rows with evidence survive restart and drive exact handler behavior."
    - "Every verifier-created container, DB, user, env/log/PID file, and process is removed by exact identity while normal application DB access remains healthy."
  artifacts:
    - path: "backend/tools/verify_storage_migrations.py"
      provides: "Three-dialect 0114 lifecycle authority"
    - path: "backend/tests/tools/test_verify_storage_migrations.py"
      provides: "Parser, command, seeded-state, invalidation, privacy, and cleanup tests"
  key_links:
    - from: "0113 seeded state"
      to: "0114 head"
      via: "upgrade invalidates evidence-less selectable rows"
      pattern: "0114_legacy_source_identities"
    - from: "0114 evidence state"
      to: "restart and handler probes"
      via: "private rows and exact selection persist"
      pattern: "identity_digest"
    - from: "0114 downgrade/re-upgrade"
      to: "safe unselectable state"
      via: "invalidate before evidence drop"
      pattern: "fingerprint_refresh_required"
---

<objective>
Extend the authoritative migration verifier for the private source-identity revision and exact reconnection behavior.

Purpose: Schema confidence is not sufficient for MIG-02/MIG-04; all three supported database authorities must prove upgrade, downgrade, restart, privacy, and handler semantics.
Output: Verifier and parser tests only; revision/product ownership remains in Plans 42/44.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@backend/tools/verify_storage_migrations.py
@backend/tests/tools/test_verify_storage_migrations.py
@backend/alembic/versions/0114_legacy_source_identities.py
@backend/models/storage.py
@backend/handler/database/legacy_migration_handler.py
@backend/tests/integration/test_legacy_migration.py
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-28-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-32-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-42-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-44-SUMMARY.md

<interfaces>
Keep the standard-library host launcher and exact Docker resource protocol. Add 0114 revision constants and probes for clean head; seeded 0113 selectable result invalidation; seeded 0114 result with sorted unique digests; duplicate/malformed digest rejection; restart persistence; handler selection of present ROM/present child only; downgrade invalidation before table loss; re-upgrade unselectability; and absence of path/filename columns or diagnostic values. Run MariaDB, MySQL, and PostgreSQL independently. Preserve the declared minimal 0107 MySQL handler-test exception only where historical setup requires it; 0114 DDL/data probes still run on MySQL. Expected guarded downgrade failures must be distinguished from infrastructure failures. Never print credentials or raw identity/source values.
</interfaces>
</context>

<test_topology>
Plan 06-46 backend pytest gates never use a fixed or shared database. The exact t1 and pv commands use a 128-bit lowercase-hex nonce: the database is the literal plan/task prefix plus 32 hex characters, while the MariaDB principal is exactly the same 32-character nonce so it remains within the username limit. Before any cleanup trap is registered or any mutation occurs, each command proves normal application SELECT 1, queries authoritative schema and mysql.user metadata, requires exact database and user@% absence, and requires exact basetemp absence; collision or lookup failure exits without cleanup or mutation. Only then does it install EXIT/INT/TERM cleanup with independent db_owned=0 and user_owned=0 flags, create database and principal separately, prove each exact identity present before setting its flag, and grant only after both flags equal 1. Cleanup revokes/drops the principal only when user_owned=1, drops the database only when db_owned=1, removes only the regex-guarded exact basetemp, preserves the primary status while reporting cleanup failures, and proves final exact DB/user/temp absence plus normal application SELECT 1. Every pytest process receives explicit DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWD, Redis, ROMM_BASE_PATH, and auth variables plus -p no:env, -p no:cacheprovider, and the exact basetemp. Plan 06-46 Task 2 additionally delegates three-dialect disposable authority to the current checked-in backend/tools/verify_storage_migrations.py contract. Never clean an identity whose owned flag was not established, print credentials, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify 0114 verifier lifecycle and privacy probes</name>
  <files>backend/tests/tools/test_verify_storage_migrations.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py, backend/alembic/versions/0114_legacy_source_identities.py, backend/models/storage.py, backend/handler/database/legacy_migration_handler.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-42-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-44-SUMMARY.md</read_first>
  <behavior>
    - 0114 lifecycle markers are mandatory.
    - Privacy and invalidation are mandatory.
    - Cleanup remains exact and redacted.
  </behavior>
  <action>Per D-09, D-10, D-12, D-20, and D-23, all three dialects must preserve private bounded evidence, unselectable unsafe results, exact canonical grammars, and retry-safe rollback. Add parser/subprocess fake tests that require the verifier to issue 0114 pristine, seeded-0113 invalidation, seeded-evidence, uniqueness/format, restart, downgrade, re-upgrade, handler, privacy, and cleanup probes for each selected dialect. Add exact test test_verifier_requires_0114_source_identity_round_trip and record its missing-marker behavioral failure as RED. Assert diagnostics redact credentials and identity values and cleanup uses exact created IDs only. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0646_t1_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0646-t1-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0646_t1_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0646-t1-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0646-t1-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; set +e; output=$(docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/tools/test_verify_storage_migrations.py::test_verifier_requires_0114_source_identity_round_trip -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0 items|usage: pytest|unrecognized arguments|INTERNALERROR|OperationalError|Access denied|Can.t connect|Unknown database|docker: Error"; printf "%s\n" "$output" | grep -Eq "FAILED .*test_verifier_requires_0114_source_identity_round_trip"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails because the verifier lacks 0114 lifecycle markers/probes.
    - Every dialect and cleanup branch is represented by fakes.
    - Tests assert diagnostic privacy and exact resource targeting.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>The real three-dialect authority extension is precisely RED-specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN run authoritative 0114 lifecycle on all three dialects</name>
  <files>backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py, backend/alembic/versions/0114_legacy_source_identities.py, backend/models/storage.py, backend/handler/database/legacy_migration_handler.py, backend/tests/integration/test_legacy_migration.py</read_first>
  <behavior>
    - Every supported dialect is authoritative.
    - Evidence loss always removes selectability.
    - Exact selection survives restart without disclosure.
  </behavior>
  <action>Per D-09, D-10, D-12, D-20, and D-23, all three dialects must preserve private bounded evidence, unselectable unsafe results, exact canonical grammars, and retry-safe rollback. Implement the 0114 verifier probes and bounded success markers. Reuse the existing runner, SQL quoting, restart, expected-failure classification, handler repetition, and exact cleanup utilities. Seed no raw path/filename evidence; use synthetic lowercase digests. Verify pre-0114 rows lose selectability, evidence rows survive restart, duplicates/malformed values reject, handler semantics reconnect only exact observed ROM/file rows, downgrade clears authority before drop, and re-upgrade cannot guess evidence. Execute the actual host command cd backend && python3 tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --handler-tests --handler-test-repetitions 1. Capture exact image/dialect/exits and remove every task-owned resource. Preserve normal application SELECT 1 before and after.</action>
  <verify>
    <automated>cd backend &amp;&amp; python3 tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --handler-tests --handler-test-repetitions 1</automated>
  </verify>
  <acceptance_criteria>
    - MariaDB, MySQL, and PostgreSQL each pass all 0114 DDL/data lifecycle probes.
    - MariaDB/PostgreSQL handler tests pass; MySQL historical handler skip does not skip 0114 DDL/data authority.
    - Restart, downgrade/re-upgrade, invalidation, exact selection, privacy, and cleanup all emit bounded success markers.
    - No verifier resource or normal DB mutation remains.
  </acceptance_criteria>
  <done>MIG-02/MIG-04 have real three-dialect authority for the new private source identity contract.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                    |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| pytest runner to MariaDB/root authority | Plan 06-46 tests may create only validated task-nonce resources while normal application storage remains live. |
| host verifier to Docker                 | Temporary database credentials and resources cross process boundaries.                                         |
| dialect schema to handler               | DDL differences must not change authority semantics.                                                           |
| verifier cleanup to host                | Deletion must target only exact created resources.                                                             |

## STRIDE Threat Register

| Threat ID       | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-46-01      | Spoofing               | dialect result            | mitigate    | Record exact dialect/image/revision markers and exits.                                                                                                                                                                                                           |
| T-06-46-02      | Tampering              | seeded evidence           | mitigate    | Synthetic digests plus duplicate/format/restart/downgrade assertions.                                                                                                                                                                                            |
| T-06-46-03      | Information disclosure | credentials/identity      | mitigate    | Redacted diagnostics and no raw path/name/value output.                                                                                                                                                                                                          |
| T-06-46-04      | Denial of service      | containers/tests          | mitigate    | Bounded waits, one repetition, EXIT cleanup.                                                                                                                                                                                                                     |
| T-06-46-05      | Elevation of privilege | cleanup                   | mitigate    | Full exact created IDs only; no broad discovery deletion.                                                                                                                                                                                                        |
| T-06-46-06      | Repudiation            | expected failures         | mitigate    | Separate guarded downgrade success from infrastructure failure.                                                                                                                                                                                                  |
| T-06-46-99      | Elevation of privilege | isolated pytest lifecycle | mitigate    | Authoritative collision lookup precedes trap/mutation; 128-bit exact identities, separate post-presence ownership flags, grant-after-both-owned, ownership-gated exact cleanup, final absence checks, and normal SELECT 1 prevent deletion of unowned resources. |
| </threat_model> |

<verification>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0646_pv_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0646-pv-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0646_pv_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0646-pv-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0646-pv-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/tools/test_verify_storage_migrations.py -x'</automated>
This is an independent p0646_pv 128-bit lifecycle. Its pre-mutation authoritative absence proof, separate ownership flags, ownership-gated cleanup, final absence proof, and before/after application SELECT 1 must complete before the remaining plan gates.
Run unit tests first, then the real host verifier on all three dialects, scoped Trunk, git diff --check, exact resource assertions, baseline status, and normal application DB continuity. Do not restart project services or deploy.
</verification>

<success_criteria>

- Every p0646 pytest lifecycle uses a fresh validated nonce DB/principal/basetemp and proves exact cleanup plus normal DB continuity.
- MIG-02 blocker has real 0114 three-dialect proof.
- MIG-04 restart persistence includes private identity evidence and selection.
- All verifier resources and secrets are cleaned/redacted.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-46-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
