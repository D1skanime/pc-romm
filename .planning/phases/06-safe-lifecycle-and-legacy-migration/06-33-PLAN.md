---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 33
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handler/filesystem/storage_access.py
  - backend/tests/handler/filesystem/test_storage_access.py
  - backend/tests/utils/test_rom_patcher.py
autonomous: true
requirements: [CAT-02]
gap_closure: true

must_haves:
  truths:
    - "OwnedCreate never exposes its final name until complete bytes are fsynced and ready for atomic no-replace publication."
    - "A killed create or subprocess writer, close/file-fsync failure, or collision leaves no final artifact; parent-fsync failure has an explicit complete-but-unacknowledged reconciliation state and never exposes partial bytes."
    - "Cleanup and publication remain descriptor-relative and never widen authority beyond a typed RomM-owned root."
  artifacts:
    - path: "backend/handler/filesystem/storage_access.py"
      provides: "Crash-safe, durable no-replace owned creation"
    - path: "backend/tests/handler/filesystem/test_storage_access.py"
      provides: "Create/subprocess killed-writer, close-error, collision, fsync, publication-state, and cleanup regressions"
    - path: "backend/tests/utils/test_rom_patcher.py"
      provides: "Real patcher-consumer staged-publication and failure regressions"
  key_links:
    - from: "OwnedCreate.create and OwnedCreate.subprocess_file"
      to: "one unique descriptor-relative staging primitive"
      via: "byte write or inherited subprocess descriptor, then file fsync"
      pattern: 'subprocess_file|os\.fsync'
    - from: "staged owned file"
      to: "final owned name"
      via: "atomic no-replace publication plus explicit post-publish parent-fsync state"
      pattern: 'os\.link|RENAME_NOREPLACE'
    - from: "utils.rom_patcher.apply_patch"
      to: "OwnedCreate.subprocess_file"
      via: "inherited staging descriptor, publish only after successful context exit"
      pattern: 'output\.subprocess_file'
---

<objective>
Make OwnedCreate a complete-durable-content-or-no-final-publication primitive.

Purpose: CR-04 remains a CAT-02 blocker because the final name is currently opened before content is complete and Python cleanup cannot cover process death.
Output: A strengthened OwnedCreate contract reusable by primary-manual replacement.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-31-SUMMARY.md
@backend/handler/filesystem/storage_access.py
@backend/tests/handler/filesystem/test_storage_access.py
@backend/utils/rom_patcher/patcher.py
@backend/tests/utils/test_rom_patcher.py

<interfaces>
Preserve open_owned_access and OwnedCreate public authority. Add one internal staged-publication state machine shared by create(), binary-file streaming, and subprocess_file(); subprocess_file yields only the unique staging descriptor and publishes on successful context exit after descriptor close and file fsync. The final target remains atomic no-replace and external descriptors never receive mutation methods. Pre-publication failures remove only the exact staging name. If the post-publication parent fsync fails, attempt exact final-name unlink plus parent fsync; when durable rollback cannot be confirmed, raise a bounded indeterminate-publication error and guarantee any visible final contains complete fsynced bytes, never partial content. This preserves D-05, D-11, D-16, D-22, and the Phase 2 authority boundary.
</interfaces>
</context>

<test_topology>
Plan 06-33 backend pytest gates never use a fixed or shared database. The exact t1, t2, and pv commands use a 128-bit lowercase-hex nonce: the database is the literal plan/task prefix plus 32 hex characters, while the MariaDB principal is exactly the same 32-character nonce so it remains within the username limit. Before any cleanup trap is registered or any mutation occurs, each command proves normal application SELECT 1, queries authoritative schema and mysql.user metadata, requires exact database and user@% absence, and requires exact basetemp absence; collision or lookup failure exits without cleanup or mutation. Only then does it install EXIT/INT/TERM cleanup with independent db_owned=0 and user_owned=0 flags, create database and principal separately, prove each exact identity present before setting its flag, and grant only after both flags equal 1. Cleanup revokes/drops the principal only when user_owned=1, drops the database only when db_owned=1, removes only the regex-guarded exact basetemp, preserves the primary status while reporting cleanup failures, and proves final exact DB/user/temp absence plus normal application SELECT 1. Every pytest process receives explicit DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWD, Redis, ROMM_BASE_PATH, and auth variables plus -p no:env, -p no:cacheprovider, and the exact basetemp. Plan 06-46 Task 2 additionally delegates three-dialect disposable authority to the current checked-in backend/tools/verify_storage_migrations.py contract. Never clean an identity whose owned flag was not established, print credentials, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify crash-safe no-replace owned creation</name>
  <files>backend/tests/handler/filesystem/test_storage_access.py, backend/tests/utils/test_rom_patcher.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py, backend/utils/rom_patcher/patcher.py, backend/tests/utils/test_rom_patcher.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-31-SUMMARY.md</read_first>
  <behavior>
    - Killed create and subprocess writers never publish a partial final name.
    - Close or file-fsync failure cannot bypass exact staged-file cleanup.
    - Collision preserves the existing target; parent-fsync failure has a tested complete-only reconciliation state.
    - The real ROM patcher consumes only the staged descriptor and publishes only after success.
  </behavior>
  <action>Per D-05, D-11, D-16, and D-22, retain source immutability, RomM-owned-only mutation, rollback boundaries, and retained owned history while implementing this task. Add the exact named regressions test_owned_create_process_crash_never_publishes_final and test_owned_create_subprocess_killed_writer_never_publishes_final plus create/subprocess close-error, existing-target collision, file-fsync, parent-fsync, and success-order cases. Each killed-writer subprocess must signal after a partial staging write, be terminated without Python cleanup, and prove the public final name is absent. Inject close failure independently from unlink cleanup and assert cleanup still runs. In test_rom_patcher.py exercise apply_patch through the real OwnedCreate consumer for success, patcher failure, timeout/kill, collision, and context-close/fsync failures; assert no partial final, no raw path, and publication only after successful patch completion. Assert an existing final target remains byte-identical, unique staging names do not collide across concurrent calls, and all normal test artifacts stay under tmp_path. For parent-directory fsync failure, assert successful durable rollback leaves no final; if rollback durability also fails, assert the bounded indeterminate error and that any remaining final is byte-complete and fsynced. Run the named crash test against current code and record only its intended behavioral failure as RED. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0633_t1_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0633-t1-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0633_t1_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0633-t1-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0633-t1-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; set +e; output=$(docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/handler/filesystem/test_storage_access.py::test_owned_create_subprocess_killed_writer_never_publishes_final -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0 items|usage: pytest|unrecognized arguments|INTERNALERROR|OperationalError|Access denied|Can.t connect|Unknown database|docker: Error"; printf "%s\n" "$output" | grep -Eq "FAILED .*test_owned_create_subprocess_killed_writer_never_publishes_final"'</automated>
  </verify>
  <acceptance_criteria>
    - RED exits with pytest status 1 for the exact subprocess killed-writer publication assertion.
    - Collection, usage, infrastructure, timeout, and status 0 do not satisfy RED.
    - Create and subprocess tests prove final-name absence, patcher consumer behavior, exact bounded cleanup, and explicit parent-fsync reconciliation, not only exception types.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>Both public-name create paths, including the live ROM patcher consumer, are deterministically shown unsafe before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN stage, fsync, publish no-replace, and fsync the parent</name>
  <files>backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py, backend/tests/utils/test_rom_patcher.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py, backend/utils/rom_patcher/patcher.py, backend/tests/utils/test_rom_patcher.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-31-SUMMARY.md</read_first>
  <behavior>
    - Every acknowledged create or subprocess output is durable after parent fsync.
    - Every pre-publication failure leaves no final artifact.
    - Parent-fsync failure is never reported as success and never leaves partial final bytes.
    - Cleanup never escapes the trusted owned parent descriptor.
  </behavior>
  <action>Per D-05, D-11, D-16, and D-22, retain source immutability, RomM-owned-only mutation, rollback boundaries, and retained owned history while implementing this task. Implement a cryptographically unique descriptor-relative staging name in the same parent. Write all bytes, fsync the staged descriptor, close it, publish the final name atomically without replacement using a Linux descriptor-relative primitive with portable bounded error translation, unlink only the exact staging name, and fsync the parent directory after the directory-entry changes. Cleanup must be attempted even when close raises, without masking the primary bounded failure. Route create(), binary-file streaming, and subprocess_file() through one mechanism; subprocess_file must yield the staging descriptor, fsync and close it after the child completes, and publish only on successful context exit so the existing patcher cannot expose a partial final. Preserve modes, CLOEXEC, NOFOLLOW, root bounds, _write_all semantics, public signatures, and external-root authority separation. On parent fsync failure follow the explicit rollback/indeterminate behavior in interfaces and never claim success. Do not scan the directory or remove stale names by pattern. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0633_t2_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0633-t2-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0633_t2_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0633-t2-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0633-t2-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/handler/filesystem/test_storage_access.py tests/utils/test_rom_patcher.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - Create and subprocess killed-writer tests leave no final name; ROM patcher success publishes only after the child completes.
    - Success ordering is stage write, file fsync, close, no-replace publish, staging unlink, parent fsync.
    - Close, file-fsync, publish, collision, and patcher failure paths are bounded and preserve existing content.
    - Parent-fsync failure either durably removes the final or raises the bounded indeterminate state with only complete fsynced final bytes possible.
    - All inherited short-write, symlink, traversal, descriptor-closure, and patcher tests remain green.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>OwnedCreate publishes only complete durable content and remains reusable by safe primary-manual streaming.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                                        |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| pytest runner to MariaDB/root authority | Plan 06-33 tests may create only validated task-nonce resources while normal application storage remains live.                     |
| caller to owned capability              | Untrusted bytes enter a root-bounded mutation.                                                                                     |
| staging name to final name              | A directory entry becomes public only after file durability gates; parent durability has explicit rollback/indeterminate handling. |
| ROM patcher child to OwnedCreate        | The child inherits only a staging descriptor and cannot publish the final name directly.                                           |
| kernel close/fsync/link results         | Partial or deceptive completion signals must fail closed.                                                                          |

## STRIDE Threat Register

| Threat ID       | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-33-01      | Tampering              | OwnedCreate publication   | mitigate    | One create/subprocess staging state machine, file fsync, atomic no-replace publication, and parent fsync.                                                                                                                                                        |
| T-06-33-02      | Denial of service      | cleanup/durability        | mitigate    | Exact-name cleanup independent of close errors plus bounded parent-fsync rollback/indeterminate handling.                                                                                                                                                        |
| T-06-33-03      | Elevation of privilege | descriptor authority      | mitigate    | Descriptor-relative NOFOLLOW operations only; no raw path or external mutation capability.                                                                                                                                                                       |
| T-06-33-04      | Information disclosure | errors                    | mitigate    | Retain bounded static storage and indeterminate-publication errors without host paths or staging names.                                                                                                                                                          |
| T-06-33-05      | Elevation of privilege | ROM patcher output        | mitigate    | Patcher inherits only the staging descriptor; final publication remains inside OwnedCreate.                                                                                                                                                                      |
| T-06-33-99      | Elevation of privilege | isolated pytest lifecycle | mitigate    | Authoritative collision lookup precedes trap/mutation; 128-bit exact identities, separate post-presence ownership flags, grant-after-both-owned, ownership-gated exact cleanup, final absence checks, and normal SELECT 1 prevent deletion of unowned resources. |
| </threat_model> |

<verification>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0633_pv_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0633-pv-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0633_pv_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0633-pv-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0633-pv-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/handler/filesystem/test_storage_access.py tests/utils/test_rom_patcher.py tests/handler/filesystem/test_storage_policy.py tests/handler/filesystem/test_storage_inventory.py -x'</automated>
This is an independent p0633_pv 128-bit lifecycle. Its pre-mutation authoritative absence proof, separate ownership flags, ownership-gated cleanup, final absence proof, and before/after application SELECT 1 must complete before the remaining plan gates.
Run the full storage-access and ROM-patcher modules, the storage-policy/inventory regressions, scoped Trunk on all three modified files, git diff --check, exact tmp cleanup, and the application DB continuity check.
</verification>

<success_criteria>

- Every p0633 pytest lifecycle uses a fresh validated nonce DB/principal/basetemp and proves exact cleanup plus normal DB continuity.
- CR-04 is closed by subprocess evidence, not exception-only cleanup.
- The primitive is safe for later manual replacement reuse.
- CAT-02 owned-state integrity and external-root separation remain intact.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
