---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 28
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models/rom.py
  - backend/tests/handler/database/test_storage_lifecycle.py
  - backend/tools/verify_storage_migrations.py
  - backend/tests/tools/test_verify_storage_migrations.py
autonomous: true
requirements: [MIG-02, MIG-04]
gap_closure: true
must_haves:
  truths:
    - "Rom and RomFile incarnation_token is immutable through instance flush, statement values, ORM executemany parameters, Session.bulk_update_mappings, Query.update, and bulk-save update paths supported by the installed SQLAlchemy version."
    - "Every rejected bulk mutation leaves both persisted tokens byte-identical and the session safely recoverable after rollback."
    - "The runtime invariant and unchanged tokens survive restart and execute against MariaDB, MySQL, and PostgreSQL in the authoritative verifier."
    - "Ordinary non-token bulk updates remain permitted so the guard protects lineage without disabling supported maintenance work."
  artifacts:
    - path: "backend/models/rom.py"
      provides: "Execution-boundary immutable token guard for all supported ORM update forms"
    - path: "backend/tests/handler/database/test_storage_lifecycle.py"
      provides: "Complete ORM bulk mutation and non-token positive matrix"
    - path: "backend/tools/verify_storage_migrations.py"
      provides: "All-dialect runtime guard and restart probe"
    - path: "backend/tests/tools/test_verify_storage_migrations.py"
      provides: "Verifier wiring and fail-closed marker assertions"
  key_links:
    - from: "SQLAlchemy ORM update execution"
      to: "backend/models/rom.py incarnation guard"
      via: "statement target plus values and executemany parameter inspection before SQL"
      pattern: "incarnation_token.*immutable"
    - from: "backend/tools/verify_storage_migrations.py"
      to: "MariaDB, MySQL, and PostgreSQL head schemas"
      via: "runtime ORM mutation probe before and after database restart"
      pattern: "bulk_update_mappings|incarnation"
---

<objective>
Close every supported ORM route that can rewrite rollback incarnation authority.

Purpose: Restore the D-14 through D-16 immutable-lineage premise and unblock MIG-02 without introducing a new schema revision.
Output: A fail-closed execution-boundary guard, exhaustive ORM regressions, and three-dialect restart proof.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-21-SUMMARY.md
</context>

<interfaces>
From backend/models/rom.py:
- _assign_fresh_incarnation_token overwrites caller input before insert.
- _reject_incarnation_token_update checks mapped instance history before update.
- _reject_bulk_incarnation_token_update is a Session do_orm_execute listener that checks only statement._values.
- Session.bulk_update_mappings sends token data through an ORM bulk parameter path that bypasses both current listeners.

From backend/handler/database/base_handler.py:

- sync_session is a normal sessionmaker bound to sync_engine; model-level SQLAlchemy events apply across handler sessions and verifier-created engines.

From backend/tools/verify_storage_migrations.py:

- verify_dialect upgrades each disposable MariaDB/MySQL/PostgreSQL database to head, seeds 0113 lineage, restarts the database, and then continues downgrade/re-upgrade checks.
- The MySQL full handler suite is intentionally skipped on the minimal 0107 baseline, so the narrow incarnation guard probe must run separately for all three head schemas.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify every supported ORM token mutation form</name>
  <files>backend/tests/handler/database/test_storage_lifecycle.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <read_first>backend/models/rom.py, backend/tests/handler/database/test_storage_lifecycle.py, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py, backend/handler/database/base_handler.py</read_first>
  <behavior>
    - Rom and RomFile reject changed incarnation_token through loaded-instance flush, update(...).values(...), Query.update, ORM executemany mappings when supported, Session.bulk_update_mappings, and bulk_save_objects update mode when supported.
    - Each rejected operation is rolled back; a new session observes the original token and can perform an ordinary non-token bulk update.
    - The verifier invokes a narrow ORM guard probe against each fully upgraded dialect before restart and repeats token equality after restart.
    - Unsupported SQLAlchemy call shapes are asserted as unsupported without treating their pre-SQL TypeError as immutability evidence.
  </behavior>
  <action>RED: expand test_catalog_incarnation_tokens_overwrite_input_and_reject_updates into a parametrized Rom/RomFile matrix covering the installed SQLAlchemy ORM mutation forms, with the exact named regression test_catalog_incarnation_tokens_reject_bulk_update_mappings for the current Session.bulk_update_mappings bypass. For each form, capture the original token, require ValueError with the stable immutable message before commit, rollback, reopen a session, and assert the token is unchanged; then prove a non-token bulk update succeeds. Add verifier source/unit expectations for a separate all-dialect runtime probe before and after restart. Do not count a framework TypeError before SQL as a passing guard. Commit with hooks as test(06-28): expose ORM incarnation bypasses.</action>
  <verify>
    <automated>bash -lc 'set +e; output=$(docker exec -e DB_NAME=romm_test_0628 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/handler/database/test_storage_lifecycle.py::test_catalog_incarnation_tokens_reject_bulk_update_mappings -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1 &amp;&amp; printf "%s\n" "$output" | grep -Eq "FAILED .*test_storage_lifecycle.py::test_catalog_incarnation_tokens_reject_bulk_update_mappings"'</automated>
  </verify>
  <acceptance_criteria>The RED gate accepts only pytest behavioral-failure status 1 with the exact bulk-update-mappings regression signature, rejects status 0 and collection, usage, interrupt, or infrastructure statuses, and proves bulk_update_mappings can change at least one token while existing instance and statement-value guards remain green.</acceptance_criteria>
  <done>The exact bypass and all installed supported ORM update forms have executable immutable-token contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce immutable tokens at SQLAlchemy execution boundary</name>
  <files>backend/models/rom.py, backend/tests/handler/database/test_storage_lifecycle.py, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <read_first>backend/models/rom.py, backend/handler/database/base_handler.py, backend/tests/handler/database/test_storage_lifecycle.py, backend/tools/verify_storage_migrations.py, backend/tests/tools/test_verify_storage_migrations.py, backend/alembic/versions/0113_legacy_change_lineage.py</read_first>
  <action>GREEN: retain insert freshness and instance history checks, then add one fail-closed SQLAlchemy execution-boundary guard that identifies updates targeting only the roms or rom_files tables and rejects incarnation_token whether it appears in compiled statement values, a single parameter mapping, or any executemany/bulk mapping entry. The guard must run before SQL for Session.execute, Query.update, bulk_update_mappings, and bulk_save_objects paths emitted by the installed SQLAlchemy version, while allowing ordinary updates and 0113 migration execution when application models are not the runtime writer. Avoid global monkey-patching and private method replacement; use documented SQLAlchemy event surfaces and inspect both clause target and parameter sets defensively. Keep the stable ValueError and do not expose tokens. Extend verify_storage_migrations with a narrow exact-checkout ORM probe that runs for MariaDB, MySQL, and PostgreSQL after head upgrade, asserts every supported token mutation is rejected, performs a non-token positive update, records the token, restarts the dialect container, and asserts equality afterward. This enforces D-14 through D-16 and MIG-02/MIG-04. Commit with hooks as fix(06-28): close ORM incarnation mutation paths. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0628 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/handler/database/test_storage_lifecycle.py tests/tools/test_verify_storage_migrations.py -k "incarnation or orm_guard" -x</automated>
  </verify>
  <acceptance_criteria>Every supported ORM token update raises before SQL, persisted values remain exact after rollback/reopen, non-token bulk updates pass, and verifier unit wiring proves all three dialect paths execute the probe.</acceptance_criteria>
  <done>Rollback incarnation authority cannot be rewritten through any supported application ORM bulk path.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Risk                                                             | Disposition | Mitigation                                             |
| ----------------------------- | ---------------------------------------------------------------- | ----------- | ------------------------------------------------------ |
| ORM bulk parameters to SQL    | Token update bypasses mapper and do_orm_execute value checks     | mitigate    | Inspect target plus every parameter mapping before SQL |
| migration lineage to rollback | Mutable token spoofs the original row incarnation                | mitigate    | Runtime immutability plus locked lineage comparison    |
| dialect verifier to database  | One backend behaves differently or loses invariant after restart | mitigate    | Same exact ORM probe on all three head schemas         |

## STRIDE Threat Register

| Threat ID  | Category               | Component                       | Disposition | Mitigation Plan                                                      |
| ---------- | ---------------------- | ------------------------------- | ----------- | -------------------------------------------------------------------- |
| T-06-28-01 | Spoofing               | Rom/RomFile incarnation         | mitigate    | Reject token mutation across all ORM execution forms                 |
| T-06-28-02 | Tampering              | bulk_update_mappings parameters | mitigate    | Scan every mapping entry before SQL                                  |
| T-06-28-03 | Repudiation            | unsupported form classification | mitigate    | Distinguish framework pre-SQL rejection from product guard evidence  |
| T-06-28-04 | Information disclosure | private token                   | mitigate    | No response/OpenAPI/log output                                       |
| T-06-28-05 | Denial of service      | parameter inspection            | accept      | Bounded by the submitted ORM batch already held in memory            |
| T-06-28-06 | Elevation of privilege | stale rollback authority        | mitigate    | Immutable token remains mandatory under deterministic rollback locks |

</threat_model>

<verification>
Use only the task-owned romm_test_0628 database for focused tests and remove its exact grant/database/user afterward. After the fast gate, run backend/tools/verify_storage_migrations.py with --dialects mariadb mysql postgresql --handler-tests --handler-test-repetitions 1 so the new narrow guard probe executes on all three dialects even where the broader MySQL handler suite remains skipped. Assert restart equality, zero dialect containers, normal application SELECT 1 before and after, scoped Trunk on the four changed files, Python AST compilation, git diff --check, and no service restart or deployment.
</verification>

<success_criteria>

- MIG-02 rollback lineage uses an application-immutable token under every supported ORM update form.
- MariaDB, MySQL, and PostgreSQL reject the same mutation forms and preserve token equality across restart.
- Ordinary non-token bulk updates and existing migration lifecycle behavior remain functional.

</success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-28-SUMMARY.md.
</output>
