---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 52
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/handler/filesystem/storage_access.py
  - backend/tests/handler/filesystem/test_storage_access.py
autonomous: true
requirements: [CAT-02]
gap_closure: true
must_haves:
  truths:
    - "A staged-publication descriptor number is passed to os.close at most once, even when close reports an error after consuming it."
    - "A concurrently reused sentinel descriptor remains open through publish failure and abort cleanup."
    - "The original close error remains the bounded reported cause while exact staging-name cleanup and parent fsync still run."
  artifacts:
    - path: "backend/handler/filesystem/storage_access.py"
      provides: "Single-consumption descriptor close semantics for publication and abort"
    - path: "backend/tests/handler/filesystem/test_storage_access.py"
      provides: "Descriptor-reuse sentinel regression for create and subprocess paths"
  key_links:
    - from: "_OwnedStagedPublication._descriptor"
      to: "os.close"
      via: "clear ownership before one close attempt"
      pattern: "_close_descriptor|abort"
    - from: "close failure"
      to: "staging cleanup"
      via: "abort unlinks exact name without retrying consumed integer"
      pattern: "unlink|fsync"
---

<objective>
Remove descriptor-number reuse risk from owned staged-publication cleanup.
Purpose: WR-01 is correctly in Phase 6 scope because primary manuals and other CAT-02 owned assets depend on OwnedCreate, and must-have 31 remains verified only with warning.
Output: One-close ownership semantics plus an adversarial reused-descriptor regression.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md
@backend/utils/rom_patcher/patcher.py
<interfaces>
_OwnedStagedPublication._close_descriptor currently sets _descriptor=None before os.close but retries the same integer after OSError. abort repeats the same pattern. publish catches the original OSError, calls abort, and raises a bounded StorageResolutionError. Ownership must remain cleared before close, and namespace cleanup must continue without re-closing a consumed number.
</interfaces>
</context>
<test_topology>
This module is filesystem-only but still runs in a nonce-owned disposable romm-romm-dev container with canonical backend mounted read-only and task-owned /tmp. Create the exact named container with label io.romm.phase6.round4=06-52, capture and validate its 64-hex Docker ID, and register EXIT/INT/TERM cleanup before creation. Cleanup must preserve the primary verification exit, remove only that exact owned ID, require inspect of that ID to return the exact not-found status, and make cleanup failure fatal after an otherwise successful check. Keep romm-dev stopped. Publish no port, create no DB, and preserve the tracked source manifest and baseline hash 4d4264efbb75049e71230f2417667c867656f6dd5054640e7aa6b8af391c81e1.
</test_topology>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED prove a consumed descriptor number is never retried</name>
  <files>backend/tests/handler/filesystem/test_storage_access.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Simulated close consumes the staging FD, opens/duplicates a sentinel onto the same integer, then raises EIO.
    - Cleanup never closes the reused integer; fstat/read on sentinel still succeeds.
    - Exact staging file is removed, final is absent, and original close error remains chained.
    - Both create and subprocess_file publication paths exercise the behavior; abort with an unclosed descriptor also closes once.
  </behavior>
  <action>Add a deterministic Linux descriptor-reuse sentinel test before production edits. Patch only storage_access.os.close: on the first staging descriptor close, call real close, force a sentinel onto that exact number using dup2 if needed, then raise EIO; record any later close of the number. Assert the current retry closes the sentinel and fails the test. Also assert exact staging cleanup, no final publication, bounded error cause, and no leaked test descriptors. Reject a test that merely counts calls without proving the reused FD remains usable.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); name=p0652-red-$nonce; id=; owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$owned" -eq 1 ]; then docker rm -f "$id" >/dev/null || cleanup_rc=1; if docker inspect "$id" >/dev/null 2>&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; docker info >/dev/null; if docker container inspect "$name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; id=$(docker create --name "$name" --label io.romm.phase6.round4=06-52 --network none --read-only --tmpfs /tmp:rw,nosuid,nodev --user 1000:1000 --mount type=bind,src=/home/d1sk/romm/backend,dst=/app/backend,readonly -w /app/backend --entrypoint /app/.venv/bin/python romm-romm-dev -m pytest -p no:env -p no:cacheprovider tests/handler/filesystem/test_storage_access.py::test_owned_create_close_error_never_closes_reused_descriptor -x); [[ "$id" =~ ^[0-9a-f]{64}$ ]]; owned=1; test "$(docker inspect --format "{{.Name}}" "$id")" = "/$name"; set +e; output=$(docker start --attach "$id" 2>&amp;1); rc=$?; set -e; printf "%s\n" "$output"; test "$rc" -eq 1; printf "%s\n" "$output" | grep -q "test_owned_create_close_error_never_closes_reused_descriptor"; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0|ModuleNotFound|permission denied"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails because the sentinel was closed by a second os.close, not infrastructure.
    - The test proves number reuse, cleanup continuation, original error, and descriptor hygiene.
  </acceptance_criteria>
  <done>The unsafe retry is reproduced with a real reused descriptor number.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN consume descriptor ownership after one close attempt</name>
  <files>backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/handler/filesystem/storage_access.py, backend/tests/handler/filesystem/test_storage_access.py, backend/utils/rom_patcher/patcher.py</read_first>
  <behavior>
    - _close_descriptor clears ownership and calls os.close once.
    - abort clears ownership and calls os.close once when present, swallowing cleanup close errors.
    - publish reports the original bounded error and abort removes exact staging name without touching reused FD.
  </behavior>
  <action>Delete both retry blocks. In _close_descriptor, retain the current clear-before-close order and let the first OSError propagate unchanged. In abort, capture and clear once, attempt one close, swallow only that cleanup error, then continue exact staging unlink and parent fsync. Do not use fcntl probes, retry EINTR, scan /proc, change publication ordering, widen cleanup names, suppress the original publish failure, or alter OwnedReplace and other unrelated capability behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); name=p0652-green-$nonce; id=; owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$owned" -eq 1 ]; then docker rm -f "$id" >/dev/null || cleanup_rc=1; if docker inspect "$id" >/dev/null 2>&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; docker info >/dev/null; if docker container inspect "$name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; id=$(docker create --name "$name" --label io.romm.phase6.round4=06-52 --network none --read-only --tmpfs /tmp:rw,nosuid,nodev --user 1000:1000 --mount type=bind,src=/home/d1sk/romm/backend,dst=/app/backend,readonly -w /app/backend --entrypoint /app/.venv/bin/python romm-romm-dev -m pytest -p no:env -p no:cacheprovider tests/handler/filesystem/test_storage_access.py -x); [[ "$id" =~ ^[0-9a-f]{64}$ ]]; owned=1; test "$(docker inspect --format "{{.Name}}" "$id")" = "/$name"; docker start --attach "$id"'</automated>
  </verify>
  <acceptance_criteria>
    - Full storage-access module passes, including crash, collision, fsync, order, close-error, and reuse tests.
    - The exact p0652 container ID is absent after verification and cleanup failure cannot be suppressed by a passing pytest command.
    - Every staging descriptor has one ownership transfer and at most one close call.
    - No publication order, error taxonomy, or namespace cleanup authority changes.
  </acceptance_criteria>
  <done>Close errors cannot cause cleanup to close an unrelated reused file or socket.</done>
</task>
</tasks>
<source_coverage>
GOAL-06 criterion 1, CAT-02, REVIEW WR-01, and VERIFICATION MH-31 warning are covered. This warning is not deferred because OwnedCreate is an active Phase 6 owned-asset dependency.
</source_coverage>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| capability ownership to kernel FD table | FD numbers can be reused immediately after close. |
| close error to cleanup | Cleanup must continue without replaying a consumed integer. |
| staging name to owned namespace | Only exact task-owned name may be removed. |
| test runner to host | Adversarial FD tests stay in a disposable container. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-52-01 | Tampering | reused FD | mitigate | Clear ownership before one close and never retry its number. |
| T-06-52-02 | Denial of service | unrelated socket/file | mitigate | Sentinel regression proves reuse survives cleanup. |
| T-06-52-03 | Repudiation | original failure | mitigate | Preserve original close error as bounded cause. |
| T-06-52-04 | Elevation of privilege | cleanup namespace | mitigate | Exact descriptor-relative unlink only. |
| T-06-52-05 | Information disclosure | errors | accept | Existing bounded storage error exposes no path or descriptor. |
</threat_model>
<verification>
Run full filesystem module in a network-none disposable runner, scoped Trunk, git diff --check, manifest equality, exact runner/tmp absence, stopped romm-dev, and original baseline.
</verification>
<success_criteria>
Each FD number is closed at most once; reused sentinel remains open; original error and exact cleanup are preserved; all OwnedCreate regressions pass.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-52-SUMMARY.md.
</output>
