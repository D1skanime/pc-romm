---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 16
type: execute
wave: 4
depends_on: [06-11]
files_modified:
  - frontend/src/locales/en_US/rom.json
  - frontend/src/locales/en_GB/rom.json
  - frontend/src/locales/bg_BG/rom.json
  - frontend/src/locales/cs_CZ/rom.json
  - frontend/src/locales/de_DE/rom.json
  - frontend/src/locales/es_ES/rom.json
  - frontend/src/locales/fr_FR/rom.json
  - frontend/src/locales/hu_HU/rom.json
  - frontend/src/locales/it_IT/rom.json
autonomous: true
requirements: [CAT-01, CAT-04]
gap_closure: true
must_haves:
  truths:
    - "The source locale defines unambiguous catalog-only removal and future-scan exclusion copy."
    - "The first locale batch describes source preservation in each locale language without removing frozen-v1 keys."
  artifacts:
    - path: "frontend/src/locales/en_US/rom.json"
      provides: "Canonical catalog-only locale key contract"
  key_links:
    - from: "frontend/src/v2/components/Dialogs/DeleteRomDialog.vue"
      to: "frontend/src/locales/en_US/rom.json"
      via: "exact i18n keys introduced by Plan 11"
      pattern: "remove-from-catalog"
---

<objective>
Establish the source locale contract and first bounded translation batch for catalog-only ROM removal.

Purpose: Make CAT-01/CAT-04 safety language explicit without returning forbidden filesystem-deletion wording to v2.
Output: Nine sorted, valid locale files ready for final parity completion in Plan 17.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-11-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define source keys and translate the first four peers</name>
  <files>frontend/src/locales/en_US/rom.json, frontend/src/locales/en_GB/rom.json, frontend/src/locales/bg_BG/rom.json, frontend/src/locales/cs_CZ/rom.json, frontend/src/locales/de_DE/rom.json</files>
  <read_first>frontend/src/v2/components/Dialogs/DeleteRomDialog.vue, frontend/src/locales/en_US/rom.json, frontend/src/locales/en_GB/rom.json, frontend/src/locales/bg_BG/rom.json, frontend/src/locales/cs_CZ/rom.json, frontend/src/locales/de_DE/rom.json</read_first>
  <action>Add the exact keys consumed by Plan 11 for Remove from catalog title/body/button/success, original-files-unchanged assurance, retained-value assurance, and optional future-scan exclusion explanation. Define concise `en_US` source values first, then actual locale-language values for en_GB, bg_BG, cs_CZ, and de_DE. Preserve all legacy keys still used by frozen v1, avoid generic Delete wording, and sort every object.</action>
  <verify><automated>cd frontend &amp;&amp; python3 src/locales/check_i18n_sorted.py &amp;&amp; for locale in en_US en_GB bg_BG cs_CZ de_DE; do python3 -m json.tool "src/locales/$locale/rom.json" &gt; /dev/null || exit 1; done</automated></verify>
  <acceptance_criteria>The five files are valid/sorted, expose the identical new key set, and their values state catalog-only behavior and unchanged source files.</acceptance_criteria>
  <done>All acceptance criteria above are satisfied by the automated verification.</done>
</task>

<task type="auto">
  <name>Task 2: Translate and validate the remaining first-batch locales</name>
  <files>frontend/src/locales/es_ES/rom.json, frontend/src/locales/fr_FR/rom.json, frontend/src/locales/hu_HU/rom.json, frontend/src/locales/it_IT/rom.json</files>
  <read_first>frontend/src/locales/en_US/rom.json, frontend/src/locales/es_ES/rom.json, frontend/src/locales/fr_FR/rom.json, frontend/src/locales/hu_HU/rom.json, frontend/src/locales/it_IT/rom.json</read_first>
  <action>Copy the exact new `en_US` key set, not its English values, into es_ES, fr_FR, hu_HU, and it_IT with actual locale-language catalog-only/source-preservation/future-scan text. Preserve v1 keys and sorted JSON. Check each file for the complete source-key subset; defer only repository-wide parity until Plan 17 owns the remaining locales.</action>
  <verify><automated>cd frontend &amp;&amp; python3 src/locales/check_i18n_sorted.py &amp;&amp; for locale in es_ES fr_FR hu_HU it_IT; do python3 -m json.tool "src/locales/$locale/rom.json" &gt; /dev/null || exit 1; done</automated></verify>
  <acceptance_criteria>All nine Plan 16 locale files are valid, sorted, retain v1 keys, and contain locale-language values for every new source key.</acceptance_criteria>
  <done>All acceptance criteria above are satisfied by the automated verification.</done>
</task>
</tasks>

<threat_model>

## Attack Surface

Safety-critical destructive-action labels crossing the source-locale to translated-locale boundary.

## Trust Boundaries

| Boundary                         | Risk                                                | Mitigation                                                                               |
| -------------------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Plan 11 i18n key to locale value | Translation reintroduces filesystem deletion claims | Require explicit catalog-only and original-files-unchanged meaning in every owned locale |
| frozen v1 to shared locale files | Removing old keys breaks the frozen interface       | Add new keys without deleting legacy keys; validate JSON and sorting                     |

## Abuse Cases and Security Verification

- No owned value instructs or claims source-file deletion.
- Future-scan exclusion is distinguished from deletion.
- JSON/sorting gates prevent malformed resources from hiding safety copy.
  </threat_model>

<verification>
Run both task gates, compare the new key subset against en_US, and inspect the nine locale-language values for catalog-only/source-preservation parity. Do not edit v1 or deploy.
</verification>

<success_criteria>

- The canonical key set exists in en_US and all eight first-batch peers.
- Every value preserves CAT-01/CAT-04 semantics and all files remain valid/sorted.
- Plan 17 can complete repository-wide parity without modifying Plan 16 files.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-16-SUMMARY.md`.
</output>
