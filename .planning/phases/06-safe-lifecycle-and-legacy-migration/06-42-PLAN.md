---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 42
type: execute
wave: 2
depends_on: [06-37]
files_modified:
  - backend/models/storage.py
  - backend/alembic/versions/0114_legacy_source_identities.py
  - backend/tests/models/test_safe_lifecycle.py
autonomous: true
requirements: [MIG-02, MIG-04, MIG-05]
gap_closure: true

must_haves:
  truths:
    - "Selectable detection results can retain a bounded private set of source-observed identity digests without persisting paths or filenames."
    - "Pre-0114 selectable results and downgraded results become unselectable instead of retaining authority without identity evidence."
    - "The schema is additive, named, portable, restart-persistent, and safely downgradeable on MariaDB, MySQL, and PostgreSQL."
  artifacts:
    - path: "backend/models/storage.py"
      provides: "Private LegacyDetectionSourceIdentity model and relationship"
    - path: "backend/alembic/versions/0114_legacy_source_identities.py"
      provides: "Portable source-identity evidence schema and safe invalidation"
    - path: "backend/tests/models/test_safe_lifecycle.py"
      provides: "Privacy, bounds, constraints, and relationship contract"
  key_links:
    - from: "LegacyDetectionResult"
      to: "LegacyDetectionSourceIdentity"
      via: "cascade-owned private relationship"
      pattern: "source_identities"
    - from: "0113 schema"
      to: "0114 schema"
      via: "create table plus pre-evidence invalidation"
      pattern: "fingerprint_refresh_required"
    - from: "0114 downgrade"
      to: "0113-safe rows"
      via: "invalidate selectable authority before table drop"
      pattern: "selectable"
---

<objective>
Persist only bounded digests of exact source-observed identities and invalidate rows that lack that evidence.

Purpose: CR-01 cannot be closed across preview/restart without durable private membership evidence; public source paths and filenames remain forbidden.
Output: One internal model/table, portable migration, and model-level privacy contract.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@backend/models/storage.py
@backend/alembic/versions/0113_legacy_change_lineage.py
@backend/alembic/versions/0112_phase6_gap_closure.py
@backend/alembic/versions/0114_legacy_source_identities.py
@backend/tests/models/test_safe_lifecycle.py
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-37-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-20-SUMMARY.md

<interfaces>
Create LegacyDetectionSourceIdentity with id, detection_result_id as a non-null foreign key to LegacyDetectionResult with ondelete=CASCADE, and a 64-character lowercase hex identity_digest. Configure the private parent relationship with delete-orphan ownership and passive database deletes. Enforce one digest per result with a named unique constraint, format/length checks, and an ordered supporting index; expose no path, filename, host, raw record, token, or public response relationship. LegacyDetectionResult owns the private rows by cascade and deterministic digest order. Revision 0114_legacy_source_identities revises 0113_legacy_change_lineage. Upgrade invalidates all older selectable detection rows with the established fingerprint_refresh_required marker, clears source_fingerprint, and increments version before they can authorize migration. Downgrade applies the same invalidation to rows with identity evidence before dropping the table, so no 0113 row remains selectable without its exact set. Preserve D-09, D-10, D-12, D-20, D-21, and D-23.
</interfaces>
</context>

<test_topology>
Plan 06-42 backend pytest gates never use a fixed or shared database. The exact t1, t2, and pv commands use a 128-bit lowercase-hex nonce: the database is the literal plan/task prefix plus 32 hex characters, while the MariaDB principal is exactly the same 32-character nonce so it remains within the username limit. Before any cleanup trap is registered or any mutation occurs, each command proves normal application SELECT 1, queries authoritative schema and mysql.user metadata, requires exact database and user@% absence, and requires exact basetemp absence; collision or lookup failure exits without cleanup or mutation. Only then does it install EXIT/INT/TERM cleanup with independent db_owned=0 and user_owned=0 flags, create database and principal separately, prove each exact identity present before setting its flag, and grant only after both flags equal 1. Cleanup revokes/drops the principal only when user_owned=1, drops the database only when db_owned=1, removes only the regex-guarded exact basetemp, preserves the primary status while reporting cleanup failures, and proves final exact DB/user/temp absence plus normal application SELECT 1. Every pytest process receives explicit DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWD, Redis, ROMM_BASE_PATH, and auth variables plus -p no:env, -p no:cacheprovider, and the exact basetemp. Plan 06-46 Task 2 additionally delegates three-dialect disposable authority to the current checked-in backend/tools/verify_storage_migrations.py contract. Never clean an identity whose owned flag was not established, print credentials, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify the private bounded schema and invalidation contract</name>
  <files>backend/tests/models/test_safe_lifecycle.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/models/storage.py, backend/alembic/versions/0112_phase6_gap_closure.py, backend/alembic/versions/0113_legacy_change_lineage.py, backend/alembic/versions/0114_legacy_source_identities.py, backend/tests/models/test_safe_lifecycle.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Only digests can persist.
    - Rows are result-owned and unique.
    - No public schema gains the relationship.
  </behavior>
  <action>Per D-09, D-10, D-12, D-20, D-21, and D-23, persistence must remain bounded and private, keep unsafe results unselectable, survive rollback/retry, and never guess custom layouts. Add exact model tests for LegacyDetectionSourceIdentity columns, length, lowercase digest check, uniqueness, FK/cascade ownership, deterministic relationship order, and forbidden privacy tokens. Add metadata assertions that selectable results own one-or-more evidence rows only at the handler layer and no response schema serializes the relationship. Add the named test test_detection_source_identity_is_private_bounded_and_unique and record its missing-model failure as RED. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0642_t1_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0642-t1-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0642_t1_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0642-t1-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0642-t1-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; set +e; output=$(docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/models/test_safe_lifecycle.py::test_detection_source_identity_is_private_bounded_and_unique -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0 items|usage: pytest|unrecognized arguments|INTERNALERROR|OperationalError|Access denied|Can.t connect|Unknown database|docker: Error"; printf "%s\n" "$output" | grep -Eq "FAILED .*test_detection_source_identity_is_private_bounded_and_unique"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails for the absent internal model/contract.
    - Tests forbid path, filename, host, raw, snapshot, and token columns.
    - Named constraints and relationship ownership are asserted.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>The required private persistence contract is RED-specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN add revision 0114 and the private model</name>
  <files>backend/models/storage.py, backend/alembic/versions/0114_legacy_source_identities.py, backend/tests/models/test_safe_lifecycle.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/models/storage.py, backend/alembic/versions/0112_phase6_gap_closure.py, backend/alembic/versions/0113_legacy_change_lineage.py, backend/alembic/versions/0114_legacy_source_identities.py, backend/tests/models/test_safe_lifecycle.py</read_first>
  <behavior>
    - No raw source inventory is persisted.
    - Old/downgraded rows cannot remain selectable.
    - Dialect verifier extension has a stable revision to target.
  </behavior>
  <action>Per D-09, D-10, D-12, D-20, D-21, and D-23, persistence must remain bounded and private, keep unsafe results unselectable, survive rollback/retry, and never guess custom layouts. Implement the model and relationship exactly as specified. Write the hand-reviewed Alembic migration with portable SQLAlchemy DDL, explicit names, no partial index/advisory lock/upsert, safe pre-0114 invalidation on upgrade, and safe evidence invalidation before downgrade drop. Keep completed LegacyMigration rollback lineage intact; only detection selectability/evidence authority changes. Do not backfill guessed digests from catalog or aggregate fingerprint. Do not expose the table through responses or logs. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0642_t2_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0642-t2-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0642_t2_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0642-t2-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0642-t2-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/models/test_safe_lifecycle.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - Model tests pass with only digest evidence.
    - Upgrade/downgrade code contains explicit invalidation before authority loss.
    - Migration names and types are portable and no public contract changes.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>The durable schema can carry exact private membership evidence without weakening restart or downgrade safety.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                    |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| pytest runner to MariaDB/root authority | Plan 06-42 tests may create only validated task-nonce resources while normal application storage remains live. |
| source observation to database          | Only non-reversible identity digests may cross.                                                                |
| migration revision boundary             | Authority must not survive evidence loss on upgrade/downgrade.                                                 |
| ORM relationship to API                 | Private evidence must never serialize publicly.                                                                |

## STRIDE Threat Register

| Threat ID       | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------------- | ---------------------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-42-01      | Information disclosure | identity table             | mitigate    | Digest-only columns and explicit forbidden-token tests.                                                                                                                                                                                                          |
| T-06-42-02      | Tampering              | duplicate/malformed digest | mitigate    | Length/lowercase checks and per-result uniqueness.                                                                                                                                                                                                               |
| T-06-42-03      | Elevation of privilege | old selectable result      | mitigate    | Upgrade/downgrade invalidation before evidence absence.                                                                                                                                                                                                          |
| T-06-42-04      | Denial of service      | identity volume            | mitigate    | Detection entry budget bounds rows and index supports ordered lookup.                                                                                                                                                                                            |
| T-06-42-99      | Elevation of privilege | isolated pytest lifecycle  | mitigate    | Authoritative collision lookup precedes trap/mutation; 128-bit exact identities, separate post-presence ownership flags, grant-after-both-owned, ownership-gated exact cleanup, final absence checks, and normal SELECT 1 prevent deletion of unowned resources. |
| </threat_model> |

<verification>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0642_pv_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0642-pv-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0642_pv_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0642-pv-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0642-pv-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/models/test_safe_lifecycle.py -x'</automated>
This is an independent p0642_pv 128-bit lifecycle. Its pre-mutation authoritative absence proof, separate ownership flags, ownership-gated cleanup, final absence proof, and before/after application SELECT 1 must complete before the remaining plan gates.
Run model tests and a disposable local upgrade/downgrade smoke on the plan DB; Plan 46 owns real three-dialect authority. Run scoped Trunk, git diff --check, exact cleanup, and normal DB continuity. No generated/public contract change.
</verification>

<success_criteria>

- Every p0642 pytest lifecycle uses a fresh validated nonce DB/principal/basetemp and proves exact cleanup plus normal DB continuity.
- Private bounded identity evidence survives restart.
- No pre-evidence result can authorize migration.
- Revision 0114 is ready for handler and dialect integration.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-42-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
