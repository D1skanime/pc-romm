---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 06
type: tdd
wave: 5
depends_on: [06-03, 06-05]
files_modified:
  - backend/handler/database/legacy_migration_handler.py
  - backend/handler/database/storage_handler.py
  - backend/handler/database/roms_handler.py
  - backend/endpoints/storage.py
  - backend/tests/handler/database/test_storage_lifecycle.py
  - backend/tests/handler/storage/test_legacy_migration.py
autonomous: true
requirements: [CAT-03, MIG-01, MIG-03, MIG-04]
must_haves:
  truths:
    - "Mapping, reconnection, audit, and rollback metadata commit atomically per platform."
    - "Injected failure leaves no partial state."
    - "Ambiguous entries remain visible unreachable."
  artifacts:
    - path: "backend/handler/database/legacy_migration_handler.py"
      provides: "Atomic per-platform migration"
    - path: "backend/tests/handler/storage/test_legacy_migration.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/handler/database/legacy_migration_handler.py"
      to: "backend/models/storage.py"
      via: "atomic mapping/catalog/audit/rollback state"
      pattern: "rollback|audit"
---

<objective>
Atomic per-platform migration
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/database/legacy_migration_handler.py, backend/handler/database/storage_handler.py, backend/handler/database/roms_handler.py, backend/endpoints/storage.py, backend/tests/handler/database/test_storage_lifecycle.py, backend/tests/handler/storage/test_legacy_migration.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/database/storage_handler.py
@backend/handler/database/roms_handler.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Atomic per-platform migration</name>
  <files>backend/tests/handler/database/test_storage_lifecycle.py, backend/tests/handler/storage/test_legacy_migration.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart and source-manifest equality as applicable.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/handler/database/storage_handler.py, backend/handler/database/roms_handler.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_storage_lifecycle.py tests/handler/storage/test_legacy_migration.py -k 'migrate or atomic or race' -x</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Atomic per-platform migration</name>
  <files>backend/handler/database/legacy_migration_handler.py, backend/handler/database/storage_handler.py, backend/handler/database/roms_handler.py, backend/endpoints/storage.py, backend/tests/handler/database/test_storage_lifecycle.py, backend/tests/handler/storage/test_legacy_migration.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Per D-11/D-13/D-14/D-17 through D-20, confirm one platform per transaction with deterministic platform/root/mapping/migration/catalog locks and optimistic versions. Revalidate candidate, expiry and overlap; create/activate mapping, reconnect only unambiguous identities, preserve unmatched/ambiguous visible-unreachable, append audit and rollback metadata, then commit once. Inject failure after every flush and prove no partial state, safe retry and independent earlier platform commits. Barrier-test migrate/migrate, migrate/create and confirm/lifecycle races. Change database/config only, never source; return bounded success/conflict/manual outcomes.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/database/test_storage_lifecycle.py tests/handler/storage/test_legacy_migration.py -k 'migrate or atomic or race' -x</automated></verify>
  <done>Atomic per-platform migration passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-06B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-06C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
| T-06-06D | D | work | mitigate | Fixed budgets, safe boundaries and retryable durable state. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-06-SUMMARY.md
</output>
