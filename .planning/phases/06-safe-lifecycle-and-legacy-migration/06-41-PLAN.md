---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 41
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/v2/components/GameDetails/PdfViewer.vue
  - frontend/src/v2/components/GameDetails/MarkdownViewer.vue
  - frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts
autonomous: true
requirements: [CAT-01, CAT-02]
gap_closure: true

must_haves:
  truths:
    - "Primary-manual delete and re-download controls remain visible with accessible names but become disabled during a pending mutation."
    - "Existing permission-based absence and redownload loading behavior remain unchanged."
  artifacts:
    - path: "frontend/src/v2/components/GameDetails/PdfViewer.vue"
      provides: "Pending-disable contract for PDF manual controls"
    - path: "frontend/src/v2/components/GameDetails/MarkdownViewer.vue"
      provides: "Pending-disable contract for Markdown controls"
    - path: "frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts"
      provides: "Visibility, aria-label, and disabled-state tests"
  key_links:
    - from: "ManualSubtab pending state"
      to: "viewer mutationDisabled prop"
      via: "optional typed prop consumed in Plan 45"
      pattern: "mutationDisabled"
    - from: "viewer prop"
      to: "RBtn controls"
      via: "disabled without hiding or removing accessible name"
      pattern: "disabled|aria-label"
---

<objective>
Add the viewer control contract needed to keep the existing manual visible but non-racing while replacement is pending.

Purpose: The approved UI-SPEC requires stable controls and layout; current props can only hide controls or disable redownload during its own request.
Output: Optional typed pending-disable props and focused viewer-control tests.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@frontend/src/v2/components/GameDetails/PdfViewer.vue
@frontend/src/v2/components/GameDetails/MarkdownViewer.vue
@frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts
@frontend/src/v2/components/GameDetails/ManualSubtab.vue
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md

<interfaces>
Add one optional mutationDisabled boolean prop to each feature viewer. It disables existing delete and redownload RBtn controls while leaving the permission-driven v-if, tooltip, icon, DOM position, aria-label, and native focus behavior intact. Do not add a primitive, key handler, focus layer, color, spacing, typography, or string.
</interfaces>
</context>

<test_topology>
Plan 06-41 frontend tasks run only in node:24-bookworm with one exact task-owned node_modules volume whose name contains a 128-bit lowercase-hex nonce. Each command proves Docker authority and exact volume-name absence before registering cleanup or creating the volume, installs EXIT/INT/TERM handling before creation, records ownership only after docker volume create returns the exact requested name, and validates that exact name by inspect. Cleanup preserves the primary exit status, emits a bounded cleanup failure when needed, removes only the exact owned result artifact and volume without force or discovery, requires exact volume inspect to fail with not-found status, and makes cleanup failure fail an otherwise successful task. Task 1 runs the entire selected Vitest file with JSON reporter output inside the owned volume; a deterministic Node parser requires valid JSON, process status 1, exactly one failed assertion with the exact fully qualified name and file, every other selected assertion passed, exact counters, and zero pending/todo/runtime/unhandled/config/transform/worker/timeout/OOM/infrastructure conditions. npm command echo and console-name grep are never evidence. Task 2 uses the same ownership-safe cleanup contract. Run npm as UID/GID 1000 with HOME=/tmp and NODE_OPTIONS=--max-old-space-size=4096. Never use broad discovery deletion, retain a result artifact, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify visible disabled manual controls</name>
  <files>frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, frontend/src/v2/components/GameDetails/PdfViewer.vue, frontend/src/v2/components/GameDetails/MarkdownViewer.vue, frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts, frontend/src/v2/components/GameDetails/ManualSubtab.vue, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md</read_first>
  <behavior>
    - PDF controls stay visible/disabled.
    - Markdown controls stay visible/disabled.
    - Permission semantics do not change.
  </behavior>
  <action>Per D-05 and D-11, pending viewer controls govern only RomM-owned manual mutation and leave source content outside their authority. Create focused component tests for PDF and Markdown viewers. With permission props true and mutationDisabled true, assert delete and re-download controls remain in the DOM, retain localized accessible names/tooltips, and are disabled/non-activatable. With mutationDisabled false, assert existing emits work. With permission props false, preserve current absence. Record the exact PDF pending test as RED because the prop is absent. Use the planned describe block exactly as describe("ManualViewerControls") and the exact test title "pdf_controls_stay_visible_and_disabled_while_pending" in src/v2/components/GameDetails/ManualViewerControls.test.ts; Vitest 4.1.8 must emit ancestorTitles ["ManualViewerControls"] and fullName "ManualViewerControls pdf_controls_stay_visible_and_disabled_while_pending" with normal spacing. Run the entire file with JSON reporter output and apply the deterministic record, counter-invariant, file, ancestry, title, fullName, failure-kind, and infrastructure parser from verify; console output and requested-name echo cannot satisfy RED.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0641-t1-node-$nonce; result=/app/node_modules/p0641-t1-vitest-$nonce.json; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0641-t1-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; set +e; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/v2/components/GameDetails/ManualViewerControls.test.ts --reporter=json --outputFile=\"$result\""; status=$?; set -e; test "$status" -eq 1; docker run --rm --user 1000:1000 -e RESULT="$result" -e EXPECTED_SUITE="ManualViewerControls" -e EXPECTED_TEST="pdf_controls_stay_visible_and_disabled_while_pending" -e EXPECTED_FILE="src/v2/components/GameDetails/ManualViewerControls.test.ts" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm node -e "const fs=require(\"fs\");let d;try{d=JSON.parse(fs.readFileSync(process.env.RESULT,\"utf8\"));}catch(e){process.exit(41);}const suites=Array.isArray(d.testResults)?d.testResults:[];const assertions=suites.flatMap(r=&gt;Array.isArray(r.assertionResults)?r.assertionResults:[]);const failed=assertions.filter(a=&gt;a.status===\"failed\");const passed=assertions.filter(a=&gt;a.status===\"passed\");const owner=suites.find(r=&gt;(r.assertionResults||[]).includes(failed[0]));const file=String(owner&amp;&amp;owner.name||\"\").replaceAll(\"\\\\\",\"/\");const failureText=failed.flatMap(a=&gt;Array.isArray(a.failureMessages)?a.failureMessages:[]).join(\"\\n\");const infraText=JSON.stringify({messages:suites.map(r=&gt;r&amp;&amp;r.message||\"\"),failures:failureText,errors:Array.isArray(d.errors)?d.errors:[],unhandled:Array.isArray(d.unhandledErrors)?d.unhandledErrors:[]});const forbidden=/(collection|collecting|transform|config|worker|test timed out|timeout|out of memory|\\boom\\b|enomem|no tests?|no test suite|failed to load|cannot find module|module not found|infrastructure)/i;const unhandled=Array.isArray(d.unhandledErrors)?d.unhandledErrors.length:0;const errors=Array.isArray(d.errors)?d.errors.length:0;const expectedFull=process.env.EXPECTED_SUITE+\" \"+process.env.EXPECTED_TEST;const exactAncestors=failed.length===1&amp;&amp;Array.isArray(failed[0].ancestorTitles)&amp;&amp;failed[0].ancestorTitles.length===1&amp;&amp;failed[0].ancestorTitles[0]===process.env.EXPECTED_SUITE;const counters=d.numTotalTests===assertions.length&amp;&amp;d.numFailedTests===failed.length&amp;&amp;d.numPassedTests===passed.length&amp;&amp;(d.numPendingTests||0)===0&amp;&amp;(d.numTodoTests||0)===0;const ok=d.success===false&amp;&amp;suites.length===1&amp;&amp;owner===suites[0]&amp;&amp;assertions.length&gt;0&amp;&amp;assertions.every(a=&gt;a.status===\"passed\"||a.status===\"failed\")&amp;&amp;counters&amp;&amp;failed.length===1&amp;&amp;passed.length+1===assertions.length&amp;&amp;(d.numRuntimeErrorTestSuites||0)===0&amp;&amp;unhandled===0&amp;&amp;errors===0&amp;&amp;exactAncestors&amp;&amp;failed[0].status===\"failed\"&amp;&amp;failed[0].title===process.env.EXPECTED_TEST&amp;&amp;failed[0].fullName===expectedFull&amp;&amp;Array.isArray(failed[0].failureMessages)&amp;&amp;failed[0].failureMessages.length&gt;0&amp;&amp;!forbidden.test(infraText)&amp;&amp;file.endsWith(\"/\"+process.env.EXPECTED_FILE);if(!ok){process.exit(42);}"'</automated>
  </verify>
  <acceptance_criteria>
    - RED uses rendered controls, not static source matching.
    - Accessible names and disabled activation are both asserted.
    - Permission absence remains a distinct test.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
    - RED requires valid Vitest 4.1.8 JSON, status 1, exactly one testResults record for src/v2/components/GameDetails/ManualViewerControls.test.ts, exactly one failed assertion with ancestorTitles ["ManualViewerControls"], title "pdf_controls_stay_visible_and_disabled_while_pending", fullName "ManualViewerControls pdf_controls_stay_visible_and_disabled_while_pending", non-empty failureMessages, every other assertion passed, and zero collection/transform/config/worker/runtime/unhandled/timeout/OOM/no-tests/infrastructure errors; suite-count fields are not hard-coded.
  </acceptance_criteria>
  <done>The missing pending-disable viewer contract is behaviorally specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN add the optional pending-disable prop</name>
  <files>frontend/src/v2/components/GameDetails/PdfViewer.vue, frontend/src/v2/components/GameDetails/MarkdownViewer.vue, frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/v2/components/GameDetails/PdfViewer.vue, frontend/src/v2/components/GameDetails/MarkdownViewer.vue, frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts</read_first>
  <behavior>
    - Existing viewers remain mounted and scrollable.
    - Pending mutation cannot race delete/redownload.
    - Universal input semantics remain native.
  </behavior>
  <action>Per D-05 and D-11, pending viewer controls govern only RomM-owned manual mutation and leave source content outside their authority. Add the optional typed prop and combine it with existing redownloading for the redownload disabled state and with delete for its disabled state. Do not use the prop in v-if, remove aria labels, move DOM order, or change focus styling. Keep native click/keyboard/gamepad behavior through RBtn and existing tooltips. Add no copy, style, raw Vuetify, or dependency. Execute the exact task-owned volume lifecycle in verify; removal and not-found inspection are mandatory, cleanup preserves a failing primary status, and cleanup failure is both reported and fatal when the primary command succeeded.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0641-t2-node-$nonce; result=; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0641-t2-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/v2/components/GameDetails/ManualViewerControls.test.ts &amp;&amp; npm run typecheck"'</automated>
  </verify>
  <acceptance_criteria>
    - Both viewers render visible disabled controls with accessible names.
    - Idle activation and permission behavior remain green.
    - No layout/style/string change is introduced.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
  </acceptance_criteria>
  <done>ManualSubtab can disable mutation controls without hiding them or destabilizing focus/layout.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                               |
| ---------------------------------------- | --------------------------------------------------------- |
| parent pending state to viewer           | A missed prop can leave destructive owned actions active. |
| disabled control to assistive technology | Disabled state must not remove name or stable position.   |

## STRIDE Threat Register

| Threat ID       | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-41-01      | Tampering              | concurrent manual mutations  | mitigate    | Pending prop disables delete/redownload controls.                                                                                                                                                                               |
| T-06-41-02      | Denial of service      | focus/navigation             | mitigate    | Controls remain in DOM order and use native RBtn semantics.                                                                                                                                                                     |
| T-06-41-03      | Information disclosure | tooltips                     | accept      | Only existing localized action names are exposed.                                                                                                                                                                               |
| T-06-41-04      | Elevation of privilege | permissions                  | mitigate    | Existing permission-driven visibility remains authoritative for UI hints.                                                                                                                                                       |
| T-06-41-99      | Elevation of privilege | disposable Node verification | mitigate    | Pre-create exact-name absence, post-create ownership proof, primary-status-preserving exact result/volume cleanup, mandatory volume removal, and not-found inspect prevent deletion or retention outside the task-owned volume. |
| </threat_model> |

<verification>
Run focused disposable Node 24 tests, typecheck, scoped ESLint, git diff --check, and exact volume cleanup. No manual flow, locale, or backend file changes in this plan.
</verification>

<success_criteria>

- Both p0641 Node lifecycles remove and prove absence of their exact owned volume; Task 1 RED is accepted only by the deterministic Vitest JSON contract.
- Viewer controls satisfy the approved stable pending-state contract.
- No focus/input/layout regression is introduced.
- Plan 45 can consume the typed prop without file overlap.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-41-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
