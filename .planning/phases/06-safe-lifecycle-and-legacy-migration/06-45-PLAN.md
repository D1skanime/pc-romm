---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 45
type: execute
wave: 3
depends_on: [06-38, 06-39, 06-40, 06-41, 06-43]
files_modified:
  - frontend/src/v2/components/GameDetails/ManualSubtab.vue
  - frontend/src/v2/components/GameDetails/ManualSubtab.test.ts
  - frontend/src/v2/components/Dialogs/GlobalDialogs.vue
  - frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue
autonomous: true
requirements: [CAT-01, CAT-02]
gap_closure: true

must_haves:
  truths:
    - "One picker/drop gesture produces at most one primary-manual POST and a second local gesture cannot start while pending."
    - "The prior viewer/path remains stable through upload and refresh; success appears only after authoritative same-ROM refresh."
    - "Pending, safe failure, conflict, refresh warning, permissions, accessibility, input, themes, and responsive behavior match the approved UI-SPEC exactly."
  artifacts:
    - path: "frontend/src/v2/components/GameDetails/ManualSubtab.vue"
      provides: "Visible single-operation manual state machine"
    - path: "frontend/src/v2/components/GameDetails/ManualSubtab.test.ts"
      provides: "Pending/failure/success/conflict/refresh/permission regressions"
    - path: "frontend/src/v2/components/Dialogs/GlobalDialogs.vue"
      provides: "Removal of only the active-v2 fan-out coordinator mount"
  key_links:
    - from: "RDropzone one File"
      to: "romApi.uploadManual"
      via: "visible feature-owned pending request"
      pattern: "uploadManual"
    - from: "API success"
      to: "romApi.getRom same ROM"
      via: "refresh before success announcement"
      pattern: "getRom"
    - from: "pending state"
      to: "viewer/drop/delete/redownload controls"
      via: "aria-busy, role=status, disabled stable layout"
      pattern: "aria-busy"
    - from: "GlobalDialogs"
      to: "active-v2 manual flow"
      via: "remove only the v2 ManualUploadTargetDialog mount/import/file"
      pattern: "ManualUploadTargetDialog"
    - from: "frozen v1 manual consumers"
      to: "shared uploadManuals and showManualUploadTargetDialog contracts"
      via: "unchanged service export, emitter type, and v1 component typecheck/control regression"
      pattern: "uploadManuals|showManualUploadTargetDialog"
---

<objective>
Implement the approved single-file primary-manual interaction without redesigning Game Details or Phase 7 storage administration.

Purpose: CR-02 remains user-visible until the active flow owns one request, blocks races, preserves the viewer, and reports truthful post-refresh state.
Output: ManualSubtab state machine, focused tests, removal of only the active-v2 fan-out coordinator, and preserved frozen v1 compatibility.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@frontend/src/v2/components/GameDetails/ManualSubtab.vue
@frontend/src/v2/components/GameDetails/ManualSubtab.test.ts
@frontend/src/v2/components/GameDetails/PdfViewer.vue
@frontend/src/v2/components/GameDetails/MarkdownViewer.vue
@frontend/src/v2/components/Dialogs/GlobalDialogs.vue
@frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue
@frontend/src/types/emitter.d.ts
@frontend/src/services/api/rom.ts
@frontend/src/stores/upload.ts
@frontend/src/v2/components/Notifications/UploadProgressToast.vue
@frontend/src/layouts/Main.vue
@frontend/src/components/Details/MediaTab.vue
@frontend/src/components/common/Game/Dialog/ManualUploadTarget.vue
@frontend/src/components/common/Game/Dialog/EditRom.vue
@frontend/src/v2/sourceMutationControls.test.ts
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-38-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-39-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-40-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-41-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-43-SUMMARY.md

<interfaces>
ManualSubtab directly owns a per-ROM pending phase: idle, uploading/replacing, refresh, and terminal notification. RDropzone is single-select with application/pdf,.md; reject zero/multiple/invalid selections without a request or progress record. Disable drop, upload/replace, delete, and redownload while upload or refresh is pending; viewer remains mounted/readable and Plan 41 props keep icon controls visible/accessibly named. Use the singular Plan 40 service. On API success refresh the same ROM, update currentRom only if its ID still matches, then announce upload/replace success. API failure keeps the exact current viewer/path and announces approved safe copy; 409 uses conflict copy; refresh failure after API success uses only the saved-but-reload warning. Set aria-busy on the region, one localized polite role=status transition, built-in RBtn loading, native DOM order, no focus move, no custom key/grid handler, and existing 70dvh/20rem responsive layout. Add exact approved ownership/helper text with no new typography, color, spacing, token, primitive, modal, panel, source-folder choice, or hard-coded string. Remove only the v2 ManualUploadTargetDialog import/mount from GlobalDialogs and delete only frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue after ManualSubtab owns the singular call. Preserve the shared showManualUploadTargetDialog emitter event/type, romApi.uploadManuals plural export/implementation, frontend/src/layouts/Main.vue, frontend/src/components/Details/MediaTab.vue, frontend/src/components/common/Game/Dialog/ManualUploadTarget.vue, frontend/src/components/common/Game/Dialog/EditRom.vue, and their frozen v1 behavior through Phase 8. Preserve UploadProgressToast. This is the exact UI-01/UI-02/UI-03/UI-04/UI-05/UI-06 active-v2 correction; it does not refactor frozen v1.
</interfaces>
</context>

<test_topology>
Plan 06-45 frontend tasks run only in node:24-bookworm with one exact task-owned node_modules volume whose name contains a 128-bit lowercase-hex nonce. Each command proves Docker authority and exact volume-name absence before registering cleanup or creating the volume, installs EXIT/INT/TERM handling before creation, records ownership only after docker volume create returns the exact requested name, and validates that exact name by inspect. Cleanup preserves the primary exit status, emits a bounded cleanup failure when needed, removes only the exact owned result artifact and volume without force or discovery, requires exact volume inspect to fail with not-found status, and makes cleanup failure fail an otherwise successful task. Task 1 runs the entire selected Vitest file with JSON reporter output inside the owned volume; a deterministic Node parser requires valid JSON, process status 1, exactly one failed assertion with the exact fully qualified name and file, every other selected assertion passed, exact counters, and zero pending/todo/runtime/unhandled/config/transform/worker/timeout/OOM/infrastructure conditions. npm command echo and console-name grep are never evidence. Task 2 uses the same ownership-safe cleanup contract. Run npm as UID/GID 1000 with HOME=/tmp and NODE_OPTIONS=--max-old-space-size=4096. Never use broad discovery deletion, retain a result artifact, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify the complete visible manual state machine</name>
  <files>frontend/src/v2/components/GameDetails/ManualSubtab.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/frontend-i18n/SKILL.md, frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts, frontend/src/v2/components/GameDetails/PdfViewer.vue, frontend/src/v2/components/GameDetails/MarkdownViewer.vue, frontend/src/v2/components/Dialogs/GlobalDialogs.vue, frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue, frontend/src/types/emitter.d.ts, frontend/src/services/api/rom.ts, frontend/src/stores/upload.ts, frontend/src/v2/components/Notifications/UploadProgressToast.vue, frontend/src/layouts/Main.vue, frontend/src/components/Details/MediaTab.vue, frontend/src/components/common/Game/Dialog/ManualUploadTarget.vue, frontend/src/components/common/Game/Dialog/EditRom.vue, frontend/src/v2/sourceMutationControls.test.ts, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md</read_first>
  <behavior>
    - One gesture, one POST.
    - Prior viewer is stable until refresh success.
    - Every error state uses truthful approved copy.
  </behavior>
  <action>Per D-05 and D-11, the visible operation controls only RomM-owned manual state and must truthfully preserve source-library immutability. Create ManualSubtab.test.ts with exact cases for empty upload, existing replace, single picker/drop request, multiple/invalid rejection, slow pending, second local gesture, backend 409, upload failure, retry, API success then refresh, refresh failure, navigation to another ROM, no rom.edit, aria-busy/live status, stable viewer, disabled visible delete/redownload, action labels, native focus, 320px-safe structural classes, and exact UI-01 through UI-06 behavior. Add a compatibility assertion that the active-v2 component calls only uploadManual while the shared uploadManuals export and showManualUploadTargetDialog event remain available to the existing frozen v1 type/control gate. The exact test one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending must fail because current code emits a plural fire-and-forget event. Mock only service/store/viewer boundaries and assert DOM/state order. Record this named behavioral failure as RED. Use the planned describe block exactly as describe("ManualSubtab") and the exact test title "one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending" in src/v2/components/GameDetails/ManualSubtab.test.ts; Vitest 4.1.8 must emit ancestorTitles ["ManualSubtab"] and fullName "ManualSubtab one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending" with normal spacing. Run the entire file with JSON reporter output and apply the deterministic record, counter-invariant, file, ancestry, title, fullName, failure-kind, and infrastructure parser from verify; console output and requested-name echo cannot satisfy RED.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0645-t1-node-$nonce; result=/app/node_modules/p0645-t1-vitest-$nonce.json; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0645-t1-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; set +e; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/v2/components/GameDetails/ManualSubtab.test.ts --reporter=json --outputFile=\"$result\""; status=$?; set -e; test "$status" -eq 1; docker run --rm --user 1000:1000 -e RESULT="$result" -e EXPECTED_SUITE="ManualSubtab" -e EXPECTED_TEST="one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending" -e EXPECTED_FILE="src/v2/components/GameDetails/ManualSubtab.test.ts" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm node -e "const fs=require(\"fs\");let d;try{d=JSON.parse(fs.readFileSync(process.env.RESULT,\"utf8\"));}catch(e){process.exit(41);}const suites=Array.isArray(d.testResults)?d.testResults:[];const assertions=suites.flatMap(r=&gt;Array.isArray(r.assertionResults)?r.assertionResults:[]);const failed=assertions.filter(a=&gt;a.status===\"failed\");const passed=assertions.filter(a=&gt;a.status===\"passed\");const owner=suites.find(r=&gt;(r.assertionResults||[]).includes(failed[0]));const file=String(owner&amp;&amp;owner.name||\"\").replaceAll(\"\\\\\",\"/\");const failureText=failed.flatMap(a=&gt;Array.isArray(a.failureMessages)?a.failureMessages:[]).join(\"\\n\");const infraText=JSON.stringify({messages:suites.map(r=&gt;r&amp;&amp;r.message||\"\"),failures:failureText,errors:Array.isArray(d.errors)?d.errors:[],unhandled:Array.isArray(d.unhandledErrors)?d.unhandledErrors:[]});const forbidden=/(collection|collecting|transform|config|worker|test timed out|timeout|out of memory|\\boom\\b|enomem|no tests?|no test suite|failed to load|cannot find module|module not found|infrastructure)/i;const unhandled=Array.isArray(d.unhandledErrors)?d.unhandledErrors.length:0;const errors=Array.isArray(d.errors)?d.errors.length:0;const expectedFull=process.env.EXPECTED_SUITE+\" \"+process.env.EXPECTED_TEST;const exactAncestors=failed.length===1&amp;&amp;Array.isArray(failed[0].ancestorTitles)&amp;&amp;failed[0].ancestorTitles.length===1&amp;&amp;failed[0].ancestorTitles[0]===process.env.EXPECTED_SUITE;const counters=d.numTotalTests===assertions.length&amp;&amp;d.numFailedTests===failed.length&amp;&amp;d.numPassedTests===passed.length&amp;&amp;(d.numPendingTests||0)===0&amp;&amp;(d.numTodoTests||0)===0;const ok=d.success===false&amp;&amp;suites.length===1&amp;&amp;owner===suites[0]&amp;&amp;assertions.length&gt;0&amp;&amp;assertions.every(a=&gt;a.status===\"passed\"||a.status===\"failed\")&amp;&amp;counters&amp;&amp;failed.length===1&amp;&amp;passed.length+1===assertions.length&amp;&amp;(d.numRuntimeErrorTestSuites||0)===0&amp;&amp;unhandled===0&amp;&amp;errors===0&amp;&amp;exactAncestors&amp;&amp;failed[0].status===\"failed\"&amp;&amp;failed[0].title===process.env.EXPECTED_TEST&amp;&amp;failed[0].fullName===expectedFull&amp;&amp;Array.isArray(failed[0].failureMessages)&amp;&amp;failed[0].failureMessages.length&gt;0&amp;&amp;!forbidden.test(infraText)&amp;&amp;file.endsWith(\"/\"+process.env.EXPECTED_FILE);if(!ok){process.exit(42);}"'</automated>
  </verify>
  <acceptance_criteria>
    - RED fails at the current plural fire-and-forget boundary.
    - Tests distinguish upload failure from post-success refresh failure.
    - Pending assertions cover all entry points, stable viewer, aria, and one progress record.
    - Permissions and same-ROM refresh identity are explicit.
    - RED coverage distinguishes removal of the active-v2 coordinator from preservation of shared frozen v1 service/emitter contracts.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
    - RED requires valid Vitest 4.1.8 JSON, status 1, exactly one testResults record for src/v2/components/GameDetails/ManualSubtab.test.ts, exactly one failed assertion with ancestorTitles ["ManualSubtab"], title "one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending", fullName "ManualSubtab one_manual_gesture_sends_one_post_and_preserves_viewer_while_pending", non-empty failureMessages, every other assertion passed, and zero collection/transform/config/worker/runtime/unhandled/timeout/OOM/no-tests/infrastructure errors; suite-count fields are not hard-coded.
  </acceptance_criteria>
  <done>The approved manual UX contract is fully behaviorally specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN own one request in ManualSubtab and remove fan-out coordination</name>
  <files>frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts, frontend/src/v2/components/Dialogs/GlobalDialogs.vue, frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/frontend-i18n/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts, frontend/src/v2/components/GameDetails/PdfViewer.vue, frontend/src/v2/components/GameDetails/MarkdownViewer.vue, frontend/src/v2/components/Dialogs/GlobalDialogs.vue, frontend/src/v2/components/Dialogs/ManualUploadTargetDialog.vue, frontend/src/types/emitter.d.ts, frontend/src/services/api/rom.ts, frontend/src/stores/upload.ts, frontend/src/v2/components/Notifications/UploadProgressToast.vue, frontend/src/layouts/Main.vue, frontend/src/components/Details/MediaTab.vue, frontend/src/components/common/Game/Dialog/ManualUploadTarget.vue, frontend/src/components/common/Game/Dialog/EditRom.vue, frontend/src/v2/sourceMutationControls.test.ts, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md</read_first>
  <behavior>
    - CR-02 frontend root cause is closed.
    - Approved UI-SPEC is implemented exactly.
    - No broad redesign or new authority exists.
  </behavior>
  <action>Per D-05 and D-11, the visible operation controls only RomM-owned manual state and must truthfully preserve source-library immutability. Implement the exact state machine and approved copy/layout/accessibility contract described in interfaces. Use existing RDropzone/RBtn/viewers/useCan/useSnackbar/store APIs only. Keep existing viewer mounted and do not mutate props.rom/path_manual optimistically. Gate every entry point and pass mutationDisabled to viewers. Refresh the initiating ROM before success and avoid overwriting another current ROM. Classify bounded 409 separately and do not append raw backend details. Remove the active-v2 coordinator import/mount and delete only the v2 coordinator file after the direct singular call compiles. Do not edit or delete the shared emitter event/type, plural uploadManuals service, or any frozen v1 consumer; require the existing sourceMutationControls test and full typecheck to prove those contracts remain live through Phase 8. Preserve UploadProgressToast one-record behavior. Do not add any Phase 7 UI, dialog, target choice, source-folder option, raw controls, media queries, style tokens, typography, colors, or hard-coded strings. Execute the exact task-owned volume lifecycle in verify; removal and not-found inspection are mandatory, cleanup preserves a failing primary status, and cleanup failure is both reported and fatal when the primary command succeeded.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); volume=romm-p0645-t2-node-$nonce; result=; volume_owned=0; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$volume" =~ ^romm-p0645-t2-node-[0-9a-f]{32}$ ]]; cleanup(){ set +e; cleanup_rc=0; if [ "$volume_owned" -eq 1 ]; then if [ -n "$result" ]; then docker run --rm -e RESULT="$result" --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm sh -lc "rm -f -- \"\$RESULT\"; test ! -e \"\$RESULT\"" || cleanup_rc=1; fi; docker volume rm "$volume" &gt;/dev/null || cleanup_rc=1; docker info &gt;/dev/null 2&gt;&amp;1 || cleanup_rc=1; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then cleanup_rc=1; else inspect_rc=$?; test "$inspect_rc" -eq 1 || cleanup_rc=1; fi; fi; if [ "$cleanup_rc" -ne 0 ]; then printf "task-owned Node volume/result cleanup failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; docker info &gt;/dev/null; if docker volume inspect "$volume" &gt;/dev/null 2&gt;&amp;1; then exit 90; else inspect_rc=$?; test "$inspect_rc" -eq 1; fi; trap finish EXIT; trap interrupted INT TERM; created=$(docker volume create "$volume"); test "$created" = "$volume"; volume_owned=1; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; docker run --rm --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules; docker run --rm --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts &gt;/dev/null &amp;&amp; npm run test -- src/services/api/rom.test.ts src/v2/components/GameDetails/ManualViewerControls.test.ts src/v2/components/GameDetails/ManualSubtab.test.ts src/v2/sourceMutationControls.test.ts &amp;&amp; npm run typecheck"'</automated>
  </verify>
  <acceptance_criteria>
    - All approved pending/success/error/conflict/refresh/permission/accessibility tests pass.
    - Active-v2 uses no plural upload call, Promise.allSettled fan-out, or v2 target-dialog coordinator.
    - Shared uploadManuals, showManualUploadTargetDialog, and all frozen v1 consumers remain unchanged, exported, typed, and covered until Phase 8.
    - Viewer and progress records remain stable and one-per-operation.
    - Typecheck and focused controls gate pass with no frozen v1 refactor or Phase 7 UI.
    - The task-owned Node volume and any reporter artifact are absent after execution; cleanup targets only the exact created name, preserves primary failure status, and cannot be ignored.
  </acceptance_criteria>
  <done>The active manual flow is singular, visible, accessible, truthful, and race-resistant end to end.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                        |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------- |
| user gesture to File                     | Only one valid primary manual may be selected.                                                     |
| component pending state to API           | Duplicate local actions must be blocked.                                                           |
| active-v2 cleanup to frozen v1 contracts | Removing the v2 coordinator must not remove shared emitter/service authority still consumed by v1. |
| API success to store refresh             | Success is truthful only after same-ROM authoritative data arrives.                                |
| error to notification                    | Copy must be bounded and distinguish pre/post commit state.                                        |

## STRIDE Threat Register

| Threat ID       | Category               | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                                                 |
| --------------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-45-01      | Tampering              | multiple/concurrent gestures | mitigate    | Singular selection and disabled entry points while pending.                                                                                                                                                                     |
| T-06-45-02      | Repudiation            | success timing               | mitigate    | Success only after same-ROM refresh; distinct refresh warning.                                                                                                                                                                  |
| T-06-45-03      | Information disclosure | snackbar errors              | mitigate    | Approved localized copy only; no raw server/path/filename/token.                                                                                                                                                                |
| T-06-45-04      | Denial of service      | input/focus                  | mitigate    | Native DOM order, no focus move, stable viewer, disabled controls.                                                                                                                                                              |
| T-06-45-05      | Elevation of privilege | rom.edit                     | mitigate    | useCan gates UI while backend remains authoritative.                                                                                                                                                                            |
| T-06-45-06      | Tampering              | frozen v1 compatibility      | mitigate    | Preserve plural service/emitter contracts and prove existing v1 consumers by typecheck/control tests.                                                                                                                           |
| T-06-45-99      | Elevation of privilege | disposable Node verification | mitigate    | Pre-create exact-name absence, post-create ownership proof, primary-status-preserving exact result/volume cleanup, mandatory volume removal, and not-found inspect prevent deletion or retention outside the task-owned volume. |
| </threat_model> |

<verification>
Run focused ManualSubtab/viewer/rom/sourceMutationControls tests and the full frontend suite, typecheck, production build to a trapped mktemp output, locale parity/sorting, scoped ESLint/Trunk, git diff --check, exact Node volume/build cleanup, then manually verify both themes, xs/sm/md/lg/xl including 320px, mouse/touch/keyboard/gamepad, focus-ring modality, slow/failure/retry/conflict/refresh states, and screen-reader announcements. No deployment or service restart.
</verification>

<success_criteria>

- Both p0645 Node lifecycles remove and prove absence of their exact owned volume; Task 1 RED is accepted only by the deterministic Vitest JSON contract.
- CR-02 is closed across backend and active UI.
- UI-01 through UI-06 are executable and tested for active-v2 while frozen v1 contracts remain unchanged through Phase 8.
- CAT-01/CAT-02 copy, integrity, permission, accessibility, and input invariants pass.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-45-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
