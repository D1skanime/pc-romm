---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - backend/handler/storage/legacy_migration.py
  - backend/tasks/manual/detect_legacy_storage.py
  - backend/handler/database/legacy_migration_handler.py
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/handler/storage/test_legacy_migration.py
autonomous: true
requirements: [MIG-01, MIG-03, MIG-05]
must_haves:
  truths:
    - "Only an admin explicitly starts detection."
    - "Only the two literal fs_slug grammars are read within budgets."
    - "Unsafe or custom layouts yield expiring manual status with no fallback."
  artifacts:
    - path: "backend/handler/storage/legacy_migration.py"
      provides: "Exact bounded legacy detection"
    - path: "backend/tests/handler/storage/test_legacy_migration.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/tasks/manual/detect_legacy_storage.py"
      to: "backend/handler/storage/legacy_migration.py"
      via: "explicit job to exact detector"
      pattern: "roms/|/roms"
---

<objective>
Exact bounded legacy detection
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/storage/legacy_migration.py, backend/tasks/manual/detect_legacy_storage.py, backend/handler/database/legacy_migration_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/handler/storage/test_legacy_migration.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/storage/preview.py
@backend/tasks/manual/preview_mapping.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Exact bounded legacy detection</name>
  <files>backend/tests/handler/storage/test_legacy_migration.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart where applicable, and source-manifest equality.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/handler/storage/preview.py, backend/tasks/manual/preview_mapping.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/storage/test_legacy_migration.py tests/endpoints/test_storage.py -k 'legacy or openapi' -x</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Exact bounded legacy detection</name>
  <files>backend/handler/storage/legacy_migration.py, backend/tasks/manual/detect_legacy_storage.py, backend/handler/database/legacy_migration_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/handler/storage/test_legacy_migration.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Per D-06 through D-10/D-23, admin POST explicitly enqueues IDs/versions only, never startup/schedule/heartbeat/watcher. Construct exactly roms/{Platform.fs_slug} and {Platform.fs_slug}/roms from persisted rows. Never enumerate identities or use custom names, aliases, bindings, case/Unicode variants or fuzzy matching. Both present/empty/unreadable/unreachable/unsafe stay visible unselectable. LIST/STAT only with 10000-entry and 5-second budgets, lower-bound counts and capped problems. Results bind live versions, expire after 24 hours, reject stale/expired/replayed/cross-platform use. D-21/MIG-05 status cannot authorize legacy reads.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/storage/test_legacy_migration.py tests/endpoints/test_storage.py -k 'legacy or openapi' -x</automated></verify>
  <done>Exact bounded legacy detection passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-04B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-04C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-04-SUMMARY.md
</output>
