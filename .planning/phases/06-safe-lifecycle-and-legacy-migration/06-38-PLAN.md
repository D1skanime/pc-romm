---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 38
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/locales/en_US/rom.json
  - frontend/src/locales/en_GB/rom.json
  - frontend/src/locales/bg_BG/rom.json
  - frontend/src/locales/cs_CZ/rom.json
  - frontend/src/locales/de_DE/rom.json
  - frontend/src/locales/es_ES/rom.json
  - frontend/src/locales/fr_FR/rom.json
  - frontend/src/locales/hu_HU/rom.json
  - frontend/src/locales/it_IT/rom.json
autonomous: true
requirements: [CAT-01, CAT-02]
gap_closure: true

must_haves:
  truths:
    - "The source locale defines exact single-manual ownership, pending, success, safe failure, conflict, invalid-selection, and refresh-warning copy."
    - "The first eight peer locales convey the same source-immutable meaning in their own languages with exact key parity."
  artifacts:
    - path: "frontend/src/locales/en_US/rom.json"
      provides: "Canonical approved primary-manual copy"
    - path: "frontend/src/locales/en_GB/rom.json"
      provides: "First-batch locale parity and native translations"
  key_links:
    - from: "06-UI-SPEC copy contract"
      to: "en_US rom namespace"
      via: "exact approved strings"
      pattern: "manual-"
    - from: "en_US key set"
      to: "eight peer locale files"
      via: "identical sorted keys and native meaning"
      pattern: "check_i18n_sorted"
---

<objective>
Install the approved primary-manual copy contract in en_US and the first locale batch.

Purpose: The safe visible flow cannot use hard-coded strings or imply that primary manual replacement changes the external game library.
Output: Thirteen exact rom namespace keys across nine sorted locale files.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@frontend/src/locales/en_US/rom.json
@frontend/src/locales/en_GB/rom.json
@frontend/src/locales/bg_BG/rom.json
@frontend/src/locales/cs_CZ/rom.json
@frontend/src/locales/de_DE/rom.json
@frontend/src/locales/es_ES/rom.json
@frontend/src/locales/fr_FR/rom.json
@frontend/src/locales/hu_HU/rom.json
@frontend/src/locales/it_IT/rom.json
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-16-SUMMARY.md

<interfaces>
Create exactly these keys in the rom namespace: replace-manual, manual-primary-helper, manual-empty-upload-hint, manual-uploading, manual-replacing, manual-upload-success, manual-replace-success, manual-upload-failed-safe, manual-replace-failed-safe, manual-upload-conflict, manual-invalid-selection, manual-refresh-warning, manual-primary-label. Exact en_US values in key order are: Replace manual; Primary manuals are stored in RomM resources. Replacing one does not change files in your game library.; Upload one PDF or Markdown file. It will be stored in RomM resources; files in your game library stay unchanged.; Uploading manual...; Replacing manual...; Manual uploaded successfully.; Manual replaced successfully.; Couldn't upload the manual. No manual was added. Try again.; Couldn't replace the manual. The existing manual is unchanged. Try again.; Another manual upload is already in progress. Wait for it to finish, then try again.; Choose one PDF or Markdown file.; Manual saved, but this page couldn't refresh. Reload to view it.; Primary manual. Reuse existing rom.upload-manual and rom.manual-empty; do not rename or delete frozen-interface keys. Every peer value must be a real locale-language translation and must preserve RomM-owned resources versus unchanged game-library files.
</interfaces>
</context>

<test_topology>
Re-run the fail-closed Linux checkout preflight before execution. Backend plans use a plan-specific MariaDB database named romm_test_0638, an exact plan-specific principal, ROMM_BASE_PATH=romm_test, a plan-specific pytest basetemp, -p no:env, and -p no:cacheprovider. Establish an EXIT trap before resource creation; remove only that exact database, principal, basetemp, and task-owned files, then prove normal application SELECT 1 before and after. Frontend plans use node:24-bookworm in a disposable container, initialize one exact task-owned node_modules volume as root, run npm as checkout UID/GID 1000 with NODE_OPTIONS=--max-old-space-size=4096, and trap removal by exact volume/container identity. Never restart a service, deploy, print credentials, or use broad cleanup discovery.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the exact en_US and first four peer contracts</name>
  <files>frontend/src/locales/en_US/rom.json, frontend/src/locales/en_GB/rom.json, frontend/src/locales/bg_BG/rom.json, frontend/src/locales/cs_CZ/rom.json, frontend/src/locales/de_DE/rom.json</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-i18n/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/locales/en_US/rom.json, frontend/src/locales/en_GB/rom.json, frontend/src/locales/bg_BG/rom.json, frontend/src/locales/cs_CZ/rom.json, frontend/src/locales/de_DE/rom.json, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md</read_first>
  <behavior>
    - No user-visible string is hard-coded in a component.
    - RomM resources are distinguished from game-library files.
    - Keys and JSON order are deterministic.
  </behavior>
  <action>Per D-05 and D-11, copy must distinguish RomM-owned resources from immutable source-library content. Add the exact key set replace-manual, manual-primary-helper, manual-empty-upload-hint, manual-uploading, manual-replacing, manual-upload-success, manual-replace-success, manual-upload-failed-safe, manual-replace-failed-safe, manual-upload-conflict, manual-invalid-selection, manual-refresh-warning, manual-primary-label. Use the approved exact en_US copy, native translations in en_GB, bg_BG, cs_CZ, and de_DE, existing terminology for RomM resources/manual/game library, exact punctuation intent, no interpolation variables, and alphabetical JSON order. Preserve every existing key. Validate JSON and sorting; compare only the new key set in this RED-to-content task because global parity completes after Plan 39.</action>
  <verify>
    <automated>cd frontend &amp;&amp; python3 src/locales/check_i18n_sorted.py &amp;&amp; for locale in en_US en_GB bg_BG cs_CZ de_DE; do python3 -m json.tool src/locales/$locale/rom.json &gt;/dev/null; done</automated>
  </verify>
  <acceptance_criteria>
    - en_US text matches the approved UI-SPEC exactly.
    - Each peer uses its own language and preserves unchanged-library meaning.
    - All five files remain valid and sorted.
  </acceptance_criteria>
  <done>The canonical and first peer copy contracts are exact, native, and source-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate the remaining first-batch locales</name>
  <files>frontend/src/locales/es_ES/rom.json, frontend/src/locales/fr_FR/rom.json, frontend/src/locales/hu_HU/rom.json, frontend/src/locales/it_IT/rom.json</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-i18n/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/locales/en_US/rom.json, frontend/src/locales/es_ES/rom.json, frontend/src/locales/fr_FR/rom.json, frontend/src/locales/hu_HU/rom.json, frontend/src/locales/it_IT/rom.json, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md</read_first>
  <behavior>
    - Copy is native and accessible.
    - Failure state claims remain truthful.
    - No frozen key is removed.
  </behavior>
  <action>Per D-05 and D-11, copy must distinguish RomM-owned resources from immutable source-library content. Add the identical replace-manual, manual-primary-helper, manual-empty-upload-hint, manual-uploading, manual-replacing, manual-upload-success, manual-replace-success, manual-upload-failed-safe, manual-replace-failed-safe, manual-upload-conflict, manual-invalid-selection, manual-refresh-warning, manual-primary-label key set with native es_ES, fr_FR, hu_HU, and it_IT translations. Preserve the safe failure distinction: before API success the existing manual is unchanged; after API success plus refresh failure use the saved-but-reload warning. Preserve every existing key and sorted JSON. Do not copy English into peer locales.</action>
  <verify>
    <automated>cd frontend &amp;&amp; python3 src/locales/check_i18n_sorted.py &amp;&amp; for locale in es_ES fr_FR hu_HU it_IT; do python3 -m json.tool src/locales/$locale/rom.json &gt;/dev/null; done</automated>
  </verify>
  <acceptance_criteria>
    - All four files contain the identical new key set.
    - Translations preserve pending/success/failure/conflict semantics.
    - No text suggests source-folder upload or game-library mutation.
  </acceptance_criteria>
  <done>The nine-file first locale batch is ready for repository-wide parity.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                   |
| -------------------------------------- | ------------------------------------------------------------- |
| approved English copy to peer language | A mistranslation can reintroduce unsafe source-write meaning. |
| locale JSON to active UI               | Missing or malformed keys can hide recovery information.      |

## STRIDE Threat Register

| Threat ID       | Category               | Component          | Disposition | Mitigation Plan                                                               |
| --------------- | ---------------------- | ------------------ | ----------- | ----------------------------------------------------------------------------- |
| T-06-38-01      | Information disclosure | error copy         | mitigate    | Only bounded recovery copy; no backend detail, path, filename, or token.      |
| T-06-38-02      | Tampering              | ownership meaning  | mitigate    | Explicit RomM resources and unchanged game-library semantics in every locale. |
| T-06-38-03      | Denial of service      | locale loading     | mitigate    | Valid sorted JSON and exact key set.                                          |
| T-06-38-04      | Repudiation            | translation parity | mitigate    | Exact source-key comparison and native-language review.                       |
| </threat_model> |

<verification>
Run per-file JSON and sorting checks. Global parity is owned by Plan 39 after the complementary locale batch. No component, token, layout, dependency, or runtime service change is allowed.
</verification>

<success_criteria>

- The approved source copy is exact.
- Nine locale files carry identical safe keys.
- CAT-01/CAT-02 ownership meaning is unambiguous.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-38-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
