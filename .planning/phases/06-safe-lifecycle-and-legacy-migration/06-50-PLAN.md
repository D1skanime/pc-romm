---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 50
type: execute
wave: 2
depends_on: [06-49]
files_modified:
  - frontend/src/v2/components/GameDetails/ManualSubtab.vue
  - frontend/src/v2/components/GameDetails/ManualSubtab.test.ts
autonomous: true
requirements: [CAT-02]
gap_closure: true
must_haves:
  truths:
    - "Redownload participates in the same manualMutationPending boundary as upload and refresh."
    - "While redownload is pending, Replace/drop/delete/redownload controls are disabled, the viewer stays mounted, and no second manual mutation starts."
    - "Success is announced only after authoritative same-ROM refresh; failure preserves the visible manual and restores every control."
  artifacts:
    - path: "frontend/src/v2/components/GameDetails/ManualSubtab.vue"
      provides: "One feature-owned pending boundary for every primary-manual mutation"
    - path: "frontend/src/v2/components/GameDetails/ManualSubtab.test.ts"
      provides: "Redownload-versus-replace/delete/drop accessibility and recovery regressions"
  key_links:
    - from: "redownloadingManual"
      to: "manualMutationPending"
      via: "computed state shared by all mutation entry points"
      pattern: "manualMutationPending"
    - from: "pending state"
      to: "RDropzone, viewer controls, and Replace"
      via: "disabled, loading, mutation-disabled, and aria-busy bindings"
      pattern: "aria-busy|mutation-disabled|disabled"
---

<objective>
Close the active-v2 redownload pending gap after the backend adopts shared CAS.
Purpose: CR-03 and failed must-have 29 show Replace stays enabled during redownload and can start a competing request.
Output: One accessible mutation boundary with stable viewer, guarded refresh, and deterministic component tests.
</objective>
<execution_context>
@CLAUDE.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-45-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-49-SUMMARY.md
@frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts
@frontend/src/services/api/rom.ts
<interfaces>
ManualSubtab owns redownloadingManual, uploadingManual, refreshingManual, replacingManual, and manualMutationPending. handleManualFiles, redownloadManual, RDropzone, MarkdownViewer/PdfViewer, and Replace all consume this boundary. Backend Plan 49 returns bounded 409 for a CAS loser and preserves the old path on redownload failure; no frontend API schema change is required.
</interfaces>
</context>
<test_topology>
Use a nonce-owned disposable Node 24 volume named romm-p0650-{red|green}-$nonce and two containers named p0650-{red|green}-{setup|test}-$nonce, matching Plan 53's p0650/romm-p0650 audit prefixes. Label the volume and every setup/test container io.romm.phase6.round4=06-50. Register EXIT/INT/TERM cleanup before the first create; capture each docker create result as a 64-character exact ID, mark it owned immediately, and validate its exact name and label before docker start --attach. Install dependencies only inside the exact owned volume, publish no port, and use no auto-removed or unnamed containers. Cleanup must preserve the primary verification exit, force-remove only the recorded owned container IDs so interruption or client failure cannot leave a runner, assert each exact ID returns Docker's not-found status, and only then remove and assert absence of the exact owned volume. Prefix/label discovery is assertion-only in Plan 53 and must never supply a deletion target; cleanup failure is fatal after an otherwise successful check, without success-forcing fallbacks. Do not start/restart romm-dev or deploy. Preserve both themes, native input order, existing breakpoints, legacy-frontend consumers, and baseline hash 4d4264efbb75049e71230f2417667c867656f6dd5054640e7aa6b8af391c81e1. This plan does not claim fresh browser evidence; Plan 53 must validate the changed UI in a disposable browser environment and bind that evidence before final acceptance.
</test_topology>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED prove redownload blocks every competing manual gesture</name>
  <files>frontend/src/v2/components/GameDetails/ManualSubtab.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts, frontend/src/v2/components/GameDetails/ManualViewerControls.test.ts, frontend/src/services/api/rom.ts, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-45-SUMMARY.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md</read_first>
  <behavior>
    - A deferred redownload makes aria-busy true and disables overlay drop, Replace, viewer delete, and viewer redownload.
    - Drop/Replace/delete/redownload gestures during that promise create no upload, event, or second redownload call.
    - The existing viewer URL stays stable until same-ROM refresh resolves.
    - 409 or transport failure preserves viewer, returns idle state, and permits retry.
  </behavior>
  <action>Add a separate named behavior test using deferred redownload and refresh promises. Inspect rendered primitive stub attributes and emitted events, attempt every competing gesture while pending, and assert exact call counts. Prove viewer stability, refresh-before-success, navigation-safe currentRom update, error recovery, read-only permission behavior, native DOM order, and no new media query/key handler. Make the current component fail only because redownloadingManual is absent from the common pending boundary.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); round4_label=io.romm.phase6.round4=06-50; volume=romm-p0650-red-$nonce; setup_name=p0650-red-setup-$nonce; test_name=p0650-red-test-$nonce; setup_id=; test_id=; volume_owned=0; setup_owned=0; test_owned=0; cleanup(){ set +e; cleanup_rc=0; containers_absent=1; if [ "$test_owned" -eq 1 ]; then if ! docker rm -f "$test_id" >/dev/null; then cleanup_rc=1; fi; if docker inspect "$test_id" >/dev/null 2>&amp;1; then cleanup_rc=1; containers_absent=0; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; containers_absent=0; fi; fi; fi; if [ "$setup_owned" -eq 1 ]; then if ! docker rm -f "$setup_id" >/dev/null; then cleanup_rc=1; fi; if docker inspect "$setup_id" >/dev/null 2>&amp;1; then cleanup_rc=1; containers_absent=0; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; containers_absent=0; fi; fi; fi; if [ "$volume_owned" -eq 1 ]; then if [ "$containers_absent" -ne 1 ]; then cleanup_rc=1; else if ! docker volume rm "$volume" >/dev/null; then cleanup_rc=1; fi; if docker volume inspect "$volume" >/dev/null 2>&amp;1; then cleanup_rc=1; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; fi; fi; fi; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; docker info >/dev/null; if docker volume inspect "$volume" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; if docker container inspect "$setup_name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; if docker container inspect "$test_name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; created=$(docker volume create --label "$round4_label" "$volume"); volume_owned=1; test "$created" = "$volume"; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; test "$(docker volume inspect --format "{{ index .Labels \"io.romm.phase6.round4\" }}" "$volume")" = "06-50"; setup_id=$(docker create --name "$setup_name" --label "$round4_label" --network none --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules); setup_owned=1; [[ "$setup_id" =~ ^[0-9a-f]{64}$ ]]; test "$(docker inspect --format "{{.Name}}" "$setup_id")" = "/$setup_name"; test "$(docker inspect --format "{{ index .Config.Labels \"io.romm.phase6.round4\" }}" "$setup_id")" = "06-50"; docker start --attach "$setup_id"; test_id=$(docker create --name "$test_name" --label "$round4_label" --user 1000:1000 -e HOME=/tmp --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app,readonly --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts >/dev/null &amp;&amp; npm run test -- src/v2/components/GameDetails/ManualSubtab.test.ts -t redownload_blocks_every_competing_manual_gesture"); test_owned=1; [[ "$test_id" =~ ^[0-9a-f]{64}$ ]]; test "$(docker inspect --format "{{.Name}}" "$test_id")" = "/$test_name"; test "$(docker inspect --format "{{ index .Config.Labels \"io.romm.phase6.round4\" }}" "$test_id")" = "06-50"; set +e; output=$(docker start --attach "$test_id" 2>&amp;1); rc=$?; set -e; printf "%s\n" "$output"; test "$rc" -eq 1; printf "%s\n" "$output" | grep -q "redownload_blocks_every_competing_manual_gesture"; ! printf "%s\n" "$output" | grep -Eq "No test|Failed to load|Cannot find module|out of memory"'</automated>
  </verify>
  <acceptance_criteria>
    - RED is the named pending-boundary assertion, not dependency, transform, collection, or runner failure.
    - Every manual entry point and viewer-stability transition is asserted.
    - The p0650-red setup/test containers and romm-p0650-red volume carry io.romm.phase6.round4=06-50; cleanup removes only their recorded exact IDs/name, proves both IDs absent before removing the volume, proves the volume absent, preserves the RED evidence result, and cannot hide cleanup failure.
  </acceptance_criteria>
  <done>The missing redownload membership is captured before component changes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN include redownload in the shared pending state</name>
  <files>frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts</files>
  <read_first>CLAUDE.md, .claude/skills/frontend-v2-components/SKILL.md, .claude/skills/frontend-v2-patterns/SKILL.md, .claude/skills/frontend-v2-input/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, frontend/src/v2/components/GameDetails/ManualSubtab.vue, frontend/src/v2/components/GameDetails/ManualSubtab.test.ts</read_first>
  <behavior>
    - manualMutationPending is true for redownload, upload, or authoritative refresh.
    - Redownload cannot self-deadlock and all local mutations share the same guard.
    - Existing loading and aria semantics remain truthful without new copy or layout.
  </behavior>
  <action>Include redownloadingManual in manualMutationPending and simplify guards so redownload can start from idle but no operation can start while another is active. Bind the existing dropzones, viewer mutation-disabled props, Replace, and region aria-busy to that common state. Keep the redownload control's own loading indicator, existing viewer mounted, and success after refresh. Reuse existing locale keys and primitives; do not add a global store, raw Vuetify, custom key handlers, layout changes, or legacy-frontend edits. Do not reuse or relabel Plan 45 component evidence as fresh browser proof; Plan 53 owns a new disposable Playwright run against this committed UI and the final harness must reject drift after its validated commit.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); round4_label=io.romm.phase6.round4=06-50; volume=romm-p0650-green-$nonce; setup_name=p0650-green-setup-$nonce; test_name=p0650-green-test-$nonce; setup_id=; test_id=; volume_owned=0; setup_owned=0; test_owned=0; cleanup(){ set +e; cleanup_rc=0; containers_absent=1; if [ "$test_owned" -eq 1 ]; then if ! docker rm -f "$test_id" >/dev/null; then cleanup_rc=1; fi; if docker inspect "$test_id" >/dev/null 2>&amp;1; then cleanup_rc=1; containers_absent=0; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; containers_absent=0; fi; fi; fi; if [ "$setup_owned" -eq 1 ]; then if ! docker rm -f "$setup_id" >/dev/null; then cleanup_rc=1; fi; if docker inspect "$setup_id" >/dev/null 2>&amp;1; then cleanup_rc=1; containers_absent=0; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; containers_absent=0; fi; fi; fi; if [ "$volume_owned" -eq 1 ]; then if [ "$containers_absent" -ne 1 ]; then cleanup_rc=1; else if ! docker volume rm "$volume" >/dev/null; then cleanup_rc=1; fi; if docker volume inspect "$volume" >/dev/null 2>&amp;1; then cleanup_rc=1; else inspect_rc=$?; if [ "$inspect_rc" -ne 1 ]; then cleanup_rc=1; fi; fi; fi; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; docker info >/dev/null; if docker volume inspect "$volume" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; if docker container inspect "$setup_name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; if docker container inspect "$test_name" >/dev/null 2>&amp;1; then exit 90; else test "$?" -eq 1; fi; created=$(docker volume create --label "$round4_label" "$volume"); volume_owned=1; test "$created" = "$volume"; test "$(docker volume inspect --format "{{.Name}}" "$volume")" = "$volume"; test "$(docker volume inspect --format "{{ index .Labels \"io.romm.phase6.round4\" }}" "$volume")" = "06-50"; setup_id=$(docker create --name "$setup_name" --label "$round4_label" --network none --mount type=volume,src="$volume",dst=/app/node_modules node:24-bookworm chown -R 1000:1000 /app/node_modules); setup_owned=1; [[ "$setup_id" =~ ^[0-9a-f]{64}$ ]]; test "$(docker inspect --format "{{.Name}}" "$setup_id")" = "/$setup_name"; test "$(docker inspect --format "{{ index .Config.Labels \"io.romm.phase6.round4\" }}" "$setup_id")" = "06-50"; docker start --attach "$setup_id"; test_id=$(docker create --name "$test_name" --label "$round4_label" --user 1000:1000 -e HOME=/tmp -e NODE_OPTIONS=--max-old-space-size=4096 --mount type=bind,src=/home/d1sk/romm/frontend,dst=/app,readonly --mount type=volume,src="$volume",dst=/app/node_modules -w /app node:24-bookworm bash -lc "npm ci --ignore-scripts >/dev/null &amp;&amp; npm run test -- src/v2/components/GameDetails/ManualSubtab.test.ts src/v2/components/GameDetails/ManualViewerControls.test.ts &amp;&amp; npm run typecheck"); test_owned=1; [[ "$test_id" =~ ^[0-9a-f]{64}$ ]]; test "$(docker inspect --format "{{.Name}}" "$test_id")" = "/$test_name"; test "$(docker inspect --format "{{ index .Config.Labels \"io.romm.phase6.round4\" }}" "$test_id")" = "06-50"; docker start --attach "$test_id"'</automated>
  </verify>
  <acceptance_criteria>
    - Focused component/viewer tests and typecheck pass in the disposable Node lifecycle.
    - The p0650-green setup/test containers and romm-p0650-green volume carry io.romm.phase6.round4=06-50; cleanup removes only their recorded exact IDs/name, proves both IDs absent before removing the volume, proves the volume absent, and cannot be suppressed by a passing test command.
    - Redownload blocks upload, drop, Replace, delete, and duplicate redownload until refresh/failure completes.
    - Viewer, permissions, accessibility, responsive structure, and legacy-frontend compatibility remain unchanged.
  </acceptance_criteria>
  <done>Every active-v2 primary-manual mutation shares one visible non-racing pending boundary.</done>
</task>
</tasks>
<source_coverage>
GOAL-06 criterion 1, CAT-02, REVIEW CR-03, and VERIFICATION MH-29 are covered. CAT-01 catalog-removal language, CAT-03, CAT-04 external-source removal, and migration requirements are not claimed.
</source_coverage>
<threat_model>
## Trust Boundaries
| Boundary | Description |
| --- | --- |
| user gestures to API | Multiple controls must not create overlapping mutations. |
| API completion to viewer | Old viewer remains authoritative until same-ROM refresh. |
| permission state to controls | UI hints cannot expose mutation affordances to read-only users. |
| Node runner to workspace | Tests must be disposable and read-only. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-50-01 | Tampering | duplicate gestures | mitigate | One computed pending guard covers all entry points. |
| T-06-50-02 | Spoofing | stale completion | mitigate | Refresh and currentRom update remain bound to initiating ROM id. |
| T-06-50-03 | Repudiation | pending feedback | mitigate | aria-busy and existing loading controls expose the active operation. |
| T-06-50-04 | Elevation of privilege | edit actions | mitigate | Preserve useCan rom.edit gating and backend authority. |
| T-06-50-05 | Denial of service | Node resources | mitigate | Plan 53-auditable p0650/romm-p0650 names and Round 4 labels, pre-execution exact-ID registration, and exact-ID-first trap cleanup. |
</threat_model>
<verification>
Run focused tests and typecheck in disposable Node 24, then scoped Trunk, git diff --check, exact setup/test container-ID absence before exact volume removal, assertion-only absence for Plan 53's p0650/romm-p0650 prefixes and io.romm.phase6.round4 label, stopped romm-dev, and baseline restoration. Plan 54 owns full frontend/build evidence.
</verification>
<success_criteria>
Redownload participates in common pending state; every competing gesture is disabled; viewer and refresh semantics remain stable; focused tests and typecheck pass without UI expansion.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-50-SUMMARY.md.
</output>
