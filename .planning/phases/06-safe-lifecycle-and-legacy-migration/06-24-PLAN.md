---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 24
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/v2/components/Gallery/FirmwareTab.vue
  - frontend/src/v2/components/Gallery/DeleteFirmwareDialog.vue
  - frontend/src/services/api/firmware.ts
  - frontend/src/v2/components/Gallery/FirmwareTab.test.ts
  - backend/endpoints/firmware.py
  - backend/endpoints/screenshots.py
  - backend/endpoints/roms/manual.py
  - backend/tests/endpoints/test_storage_policy_denials.py
autonomous: true
requirements: [CAT-01, CAT-04]
gap_closure: true
must_haves:
  truths:
    - "Active v2 can read/download firmware and remove catalog rows but cannot upload firmware or request source-file deletion."
    - "Crafted UPLOAD, SIDECAR_WRITE, COVER_WRITE, DELETE, EXTRACT, and MKDIR requests against legacy_external_storage fail before filesystem or database mutation."
    - "Personal screenshots and primary manuals retain explicit typed ASSETS/RESOURCES deletion."
    - "Denials preserve external path, type, mode, size, digest, and symlink identity and never disclose resolved sensitive paths."
  artifacts:
    - path: "frontend/src/services/api/firmware.ts"
      provides: "Read/download/catalog-only firmware contract"
    - path: "backend/tests/endpoints/test_storage_policy_denials.py"
      provides: "External mutation-family denial matrix"
    - path: "backend/endpoints/screenshots.py"
      provides: "Explicit typed ASSETS deletion guard"
    - path: "backend/endpoints/roms/manual.py"
      provides: "Explicit typed RESOURCES deletion guard"
  key_links:
    - from: "frontend/src/v2/components/Gallery/FirmwareTab.vue"
      to: "frontend/src/services/api/firmware.ts"
      via: "read/download and catalog-only deletion only"
      pattern: "deleteFirmware"
    - from: "backend/tests/endpoints/test_storage_policy_denials.py"
      to: "legacy_external_storage and typed owned descriptors"
      via: "pre-I/O policy spies and immutable manifests"
      pattern: "UPLOAD|SIDECAR_WRITE|COVER_WRITE|DELETE|EXTRACT|MKDIR"
---

<objective>
Remove firmware external upload/delete authority and prove backend external mutation families fail closed.

Purpose: Close the firmware CAT-04 surface independently of UI absence while preserving useful typed-owned mutation.
Output: Source-safe firmware UI/service, explicit owned guards, and a direct denial matrix.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
</context>

<interfaces>
fs_firmware_handler is legacy_external_storage. POST /firmware authorizes UPLOAD/external and POST /firmware/delete accepts external DELETE intent. Generic RomFile delete, manual/soundtrack SIDECAR_WRITE and DELETE, shared screenshot COVER_WRITE and DELETE, ROM upload UPLOAD, platform MKDIR, and convert-to-folder EXTRACT routes are external. fs_asset_handler and fs_resource_handler are typed ASSETS and RESOURCES. Plan 25 adds the unguarded setup CREATE/MKDIR route to this closed server matrix; Plan 26 adds update-ROM RENAME.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove firmware mutation and close direct external authority</name>
  <files>frontend/src/v2/components/Gallery/FirmwareTab.vue, frontend/src/v2/components/Gallery/DeleteFirmwareDialog.vue, frontend/src/services/api/firmware.ts, frontend/src/v2/components/Gallery/FirmwareTab.test.ts, backend/endpoints/firmware.py, backend/endpoints/screenshots.py, backend/endpoints/roms/manual.py, backend/tests/endpoints/test_storage_policy_denials.py</files>
  <read_first>frontend/src/v2/components/Gallery/FirmwareTab.vue, frontend/src/v2/components/Gallery/DeleteFirmwareDialog.vue, frontend/src/services/api/firmware.ts, frontend/src/v2/components/Gallery/FirmwareTab.test.ts, backend/endpoints/firmware.py, backend/endpoints/screenshots.py, backend/endpoints/roms/manual.py, backend/endpoints/roms/files.py, backend/endpoints/roms/screenshot.py, backend/endpoints/roms/soundtrack.py, backend/endpoints/roms/upload.py, backend/endpoints/roms/__init__.py, backend/endpoints/platform.py, backend/endpoints/storage_policy.py, backend/handler/filesystem/__init__.py, backend/handler/filesystem/storage_policy.py, backend/tests/endpoints/test_storage_policy_denials.py</read_first>
  <behavior>
    - FirmwareTab has no upload dropzone/input/button/call and no filesystem-delete checkbox/state/copy; catalog-row removal carries no delete_from_fs field.
    - firmwareApi has no uploadFirmware export and deleteFirmware has no deleteFromFs parameter or payload.
    - Crafted firmware upload or non-empty delete_from_fs fails before read, write, DB delete, or partial selection; catalog-only deletion stays source-neutral.
    - Direct generic file, manual, soundtrack, shared screenshot, ROM upload start/complete, platform create, and convert-to-folder requests fail at the exact live external operation/descriptor pair before I/O.
    - Personal screenshot deletion authorizes ASSETS and primary manual deletion authorizes RESOURCES immediately before typed handler I/O.
  </behavior>
  <action>RED: extend FirmwareTab tests and the backend policy test first. Assert maximum-grant firmware UI/service cannot express upload or filesystem deletion. Exercise crafted UPLOAD, SIDECAR_WRITE, COVER_WRITE, DELETE, EXTRACT, and MKDIR requests, spying that all filesystem and durable calls remain zero and before/after source manifests match. Add traversal names, symlink/path replacement, invalid content type, oversized body where route validation applies, replay, and path-redaction assertions. Record RED and commit with hooks as test(06-24): close firmware and external authority. GREEN frontend: remove firmware upload UI/plumbing/export and every deleteFromFs value from dialog, caller, signature, and payload while preserving reads/downloads/catalog deletion. GREEN backend: condition external firmware DELETE authorization on non-empty delete_from_fs before the mutation loop; add explicit ASSETS/RESOURCES authorization immediately before personal-screenshot/primary-manual deletion. Do not weaken external guards or create alternate raw clients. Commit with hooks as fix(06-24): remove firmware external mutation. Never use --no-verify.</action>
  <verify>
    <automated>docker exec --workdir /app/frontend romm-dev npm run test -- src/v2/components/Gallery/FirmwareTab.test.ts &amp;&amp; docker exec -e DB_NAME=romm_test_0624 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_storage_policy_denials.py -k "firmware or sidecar or cover or upload or extract or mkdir or owned" -x</automated>
  </verify>
  <acceptance_criteria>RED proves firmware upload/delete and every listed external authority family are reachable today; GREEN removes v2 firmware authority and denies crafted requests before any filesystem/database mutation while typed owned cases pass.</acceptance_criteria>
  <done>Firmware is source-read-only and the backend independently fails closed for the enumerated external mutation families.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Risk                                              | Disposition | Mitigation                                 |
| ---------------------- | ------------------------------------------------- | ----------- | ------------------------------------------ |
| firmware input to API  | Traversal/content/oversized upload reaches source | mitigate    | Remove client path and deny UPLOAD pre-I/O |
| client to routes       | Crafted external mutation bypasses UI             | mitigate    | Direct family matrix                       |
| path lookup to I/O     | Symlink/path replacement wins TOCTOU              | mitigate    | Deny by descriptor before lookup/I/O       |
| owned route to handler | ASSETS/RESOURCES misbound external                | mitigate    | Closed typed descriptor guard              |
| denial to logs/client  | Sensitive path disclosure                         | mitigate    | Stable path-free policy detail             |

## STRIDE Threat Register

| Threat ID       | Category               | Component                  | Disposition | Mitigation Plan                          |
| --------------- | ---------------------- | -------------------------- | ----------- | ---------------------------------------- |
| T-06-24-01      | Tampering              | firmware/external requests | mitigate    | Pre-I/O policy denial                    |
| T-06-24-02      | Elevation of privilege | maximum grants             | mitigate    | Remove upload/delete authority           |
| T-06-24-03      | Spoofing               | storage classification     | mitigate    | Closed enum/descriptors                  |
| T-06-24-04      | Repudiation            | catalog-only deletion      | mitigate    | Assert payload and zero filesystem calls |
| T-06-24-05      | Information disclosure | errors                     | mitigate    | Path-redaction assertions                |
| T-06-24-06      | Denial of service      | upload/bulk IDs            | mitigate    | Existing validation plus pre-I/O denial  |
| </threat_model> |

<verification>
Run immediate focused commands, complete storage-policy denial tests, related endpoint tests, frontend typecheck, scoped Trunk, and git diff --check. Drop only romm_test_0624 and assert absence. Plans 25-26 extend the same denial file sequentially. No restart or deployment.
</verification>

<success_criteria>

- CAT-01/CAT-04 cannot fail through firmware upload/source deletion.
- Listed external mutation families deny before I/O; typed ASSETS/RESOURCES remain usable.
- External source identity/content metadata remain exact and diagnostics are path-free.
  </success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-24-SUMMARY.md.
</output>
