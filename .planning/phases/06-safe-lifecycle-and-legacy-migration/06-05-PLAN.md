---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 05
type: execute
wave: 4
depends_on: [06-01, 06-04]
files_modified:
  - backend/handler/storage/legacy_migration.py
  - backend/handler/database/legacy_migration_handler.py
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/handler/storage/test_legacy_migration.py
  - backend/tests/endpoints/test_storage.py
autonomous: true
requirements: [MIG-01, MIG-03, MIG-05]
must_haves:
  truths:
    - "Impact preview reports proposed mapping, match counts, problems, and owned effects."
    - "Confirmation binds result and live versions."
    - "Active or overlapping mappings block migration."
  artifacts:
    - path: "backend/handler/storage/legacy_migration.py"
      provides: "Typed impact preview and manual outcomes"
    - path: "backend/tests/endpoints/test_storage.py"
      provides: "Acceptance evidence"
  key_links:
    - from: "backend/endpoints/storage.py"
      to: "backend/handler/storage/legacy_migration.py"
      via: "typed impact preview"
      pattern: "impact|unmatched"
---

<objective>
Typed impact preview and manual outcomes
Purpose: Implement assigned Phase 6 requirements without Phase 7 UI, deployment, or source mutation.
Output: backend/handler/storage/legacy_migration.py, backend/handler/database/legacy_migration_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/endpoints/test_storage.py
</objective>
<execution_context>
@CLAUDE.md
@.planning/STATE.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-VERIFICATION.md
@.planning/phases/05-preview-and-read-path-cutover/05-VERIFICATION.md
# Required mapped analogs from 06-PATTERNS.md
@backend/handler/storage/preview.py
@backend/handler/database/storage_handler.py
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify Typed impact preview and manual outcomes</name>
  <files>backend/tests/handler/storage/test_legacy_migration.py, backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - Cover success, rejection, stale/conflict, injected failure, retry, concurrency/restart and source-manifest equality as applicable.
    - Reject path disclosure, raw rows/errors and legacy fallback.
  </behavior>
  <action>Read and follow backend/handler/storage/preview.py, backend/handler/database/storage_handler.py for ordered locks, admin-before-observation safe errors, and storage authority where applicable. Write failing tests for every implementation clause before production changes. Use barriers, not sleeps, for races and isolated fixtures/databases only.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/storage/test_legacy_migration.py tests/endpoints/test_storage.py -k 'impact or conflict or stale or manual' -x</automated></verify>
  <done>Tests fail only because planned behavior is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement Typed impact preview and manual outcomes</name>
  <files>backend/handler/storage/legacy_migration.py, backend/handler/database/legacy_migration_handler.py, backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/handler/storage/test_legacy_migration.py, backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - GREEN then REFACTOR satisfies locked decisions and preserves source manifests.
  </behavior>
  <action>Per D-12, preview each platform with proposed mapping, reconnectable/unmatched counts, bounded problems and planned owned DB effects, never rows or paths. Bind confirmation to result/platform/root/path/live versions/expiry and revalidate. Expose clear custom-name/manual-mapping outcomes. Per D-17/D-18, active mapping and duplicate/equal/ancestor/descendant overlap block without replace/merge/shrink/rewrite. Preview is read-only. Test admin-before-observation, budget exhaustion, changed source/catalog, both grammars, stale/expired/replayed IDs, overlap examples, redaction and zero fallback.</action>
  <verify><automated>cd backend && uv run pytest tests/handler/storage/test_legacy_migration.py tests/endpoints/test_storage.py -k 'impact or conflict or stale or manual' -x</automated></verify>
  <done>Typed impact preview and manual outcomes passes with no source mutation, partial transaction, disclosure or fallback authority.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/job to database | Stale or untrusted identity reaches durable state. |
| database to storage | External reads and owned writes require separate authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05A | T | transaction | mitigate | Ordered locks, optimistic versions, revalidation, atomic commit and audit. |
| T-06-05B | E | storage boundary | mitigate | Operation-bound read and typed owned destination; deny mutation before I/O. |
| T-06-05C | I | result | mitigate | Bounded allowlist, safe codes, expiry, no paths/raw rows. |
| T-06-05D | D | work | mitigate | Fixed budgets, safe boundaries and retryable durable state. |
</threat_model>
<verification>
Run focused tests in isolated Linux fixtures. Compare source path/type/mode/size/SHA-256/symlink identity before and after success, rejection, crash, rollback and retry; exclude atime. Migration work also runs disposable MariaDB, MySQL and PostgreSQL.
</verification>
<success_criteria>
All assigned requirements and decisions pass; no Phase 7 UI, deployment, v1 edit, source mutation or fallback exists.
</success_criteria>
<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-05-SUMMARY.md
</output>
