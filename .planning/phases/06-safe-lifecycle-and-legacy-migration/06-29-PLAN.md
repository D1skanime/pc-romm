---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/v2/sourceMutationInventory.test.ts
autonomous: true
requirements: [CAT-04]
gap_closure: true
must_haves:
  truths:
    - "The live active-v2 extractor discovers property access, string element access, aliases, default/named/namespace clients, literal/template/constant/concatenated routes, and reachable shared-service calls."
    - "Any recognized HTTP client call whose method, receiver, or route cannot be resolved is a bounded unclassifiable failure rather than disappearing from inventory."
    - "Negative fixtures enter through extractRawCalls or inventorySource, never by constructing RawCall manually, and prove forbidden mutation or fail-closed unknown classification."
    - "All current active-v2 modules and safe generic/control-plane/typed-owned calls remain classified with zero forbidden live authorities."
  artifacts:
    - path: "frontend/src/v2/sourceMutationInventory.test.ts"
      provides: "Fail-closed semantic call extractor and repository-wide v2 authority gate"
  key_links:
    - from: "v2Files and reachable service graph"
      to: "finalAuthority"
      via: "extractRawCalls plus usedServiceCalls with no silent syntax drop"
      pattern: "extractRawCalls[\\s\\S]*inventorySource"
---

<objective>
Make the final active-v2 source-mutation inventory fail closed for dynamic and element-access syntax.

Purpose: Close the CAT-04 regression-gate blind spot documented by verification while preserving the already clean live v2 call graph.
Output: One strengthened TypeScript AST inventory with live-path negative fixtures.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-26-SUMMARY.md
</context>

<interfaces>
From frontend/src/v2/sourceMutationInventory.test.ts:
- routeText currently returns only string literals and template expressions; other expressions return null.
- importedClients records default and named imports from axios or services/api, but not namespace imports.
- extractRawCalls currently accepts only CallExpression with PropertyAccessExpression, an identifier receiver, and a resolvable route; every other shape is silently omitted.
- usedServiceCalls has the same property-access and identifier-receiver restriction for the shared ROM service.
- inventorySource combines extracted direct calls and reachable ROM service calls, then classify enforces the reviewed method/route authority table.
- The existing negative test constructs RawCall manually and calls finalAuthority, so it does not exercise extraction.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route every negative syntax fixture through the live extractor</name>
  <files>frontend/src/v2/sourceMutationInventory.test.ts</files>
  <read_first>frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/services/api/index.ts, frontend/src/services/api/rom.ts, frontend/vitest.config.ts, frontend/package.json</read_first>
  <behavior>
    - api["delete"]("/roms/" + romId + "/files/" + fileId) is extracted and classified forbidden.
    - A const route assembled from safe literal and placeholder segments is normalized and classified through inventorySource.
    - Default, named, aliased, and namespace service/client imports reach extraction.
    - Computed method names, mutable/unknown route identifiers, unsupported call receivers, and non-reducible concatenations throw a path-redacted unclassifiable diagnostic.
    - The negative suite does not instantiate RawCall directly.
  </behavior>
  <action>RED: replace the manually constructed renamed and unknown RawCall cases with small source fixtures passed to extractRawCalls or inventorySource. Add the exact named regression "fails closed for string element access before classification" with the assertion message "RED: string element access must reach inventory classification", plus fixtures for dynamic element access, concatenated routes, const route aliases, variable routes, namespace imports, nested namespace clients, and each external mutation family. For extractable cases assert the expected method/normalized route and forbidden authority. For unresolved cases assert a stable diagnostic containing only importer, bounded call text, method if known, route classification, UNKNOWN operation, and unknown storage, with no absolute path or source payload. Keep positive fixtures for generic stats/token/log clients, GET/HEAD, control-plane routes, PATCH external READ plus TEMP, and RESOURCES/ASSETS operations. Commit with hooks as test(06-29): expose inventory syntax blind spots.</action>
  <verify>
    <automated>bash -lc 'set +e; output=$(docker exec --workdir /app/frontend romm-dev npm run test -- src/v2/sourceMutationInventory.test.ts -t "fails closed for string element access before classification" 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1 &amp;&amp; printf "%s\n" "$output" | grep -Fq "RED: string element access must reach inventory classification"'</automated>
  </verify>
  <acceptance_criteria>The RED gate accepts only Vitest behavioral-failure status 1 with the exact assertion message from the named regression in output, rejects status 0 and collection or infrastructure failures that lack that assertion message, and fails because current extraction drops element access before classification; the remaining RED fixtures cover concatenated or variable routes and namespace clients.</acceptance_criteria>
  <done>Every documented blind spot is exercised through the same entry point used for repository production modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve safe syntax and reject every unknown client call</name>
  <files>frontend/src/v2/sourceMutationInventory.test.ts</files>
  <read_first>frontend/src/v2/sourceMutationInventory.test.ts, frontend/src/services/api/index.ts, frontend/src/services/api/rom.ts, frontend/tsconfig.json</read_first>
  <action>GREEN: extend importedClients and alias discovery to default, named, and namespace imports and their safe aliases. Extract HTTP methods from property access and string-literal element access. Resolve route expressions recursively for literals, no-substitution and interpolated templates, immutable const identifiers, and binary string concatenation; normalize identifier expressions to bounded placeholders only when their binding is unambiguous. When a call is rooted in a known API client or reachable service but its method/route/receiver is not statically classifiable, throw the stable unclassifiable diagnostic immediately. Apply the same receiver/property rules to usedServiceCalls. Do not broaden the authority allowlist, silently assume CONTROL_PLANE for an unknown covered call, read outside the checked-in source graph, or expose full source text. Run the negative fixtures through inventorySource and then the complete live v2 traversal. This implements CAT-04 and preserves D-05/D-21 no-hidden-authority behavior. Commit with hooks as test(06-29): fail closed on dynamic v2 calls. Never use --no-verify.</action>
  <verify>
    <automated>docker exec --workdir /app/frontend romm-dev npm run test -- src/v2/sourceMutationInventory.test.ts src/v2/sourceMutationControls.test.ts</automated>
  </verify>
  <acceptance_criteria>Every safe supported syntax is extracted, every unknown known-client call fails closed, all negative fixtures use the live extractor, and the current repository graph still reports zero forbidden authorities.</acceptance_criteria>
  <done>The semantic inventory cannot silently omit an active-v2 API mutation call because of client, member, or route syntax.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Risk                                                       | Disposition | Mitigation                                                          |
| ------------------------------------- | ---------------------------------------------------------- | ----------- | ------------------------------------------------------------------- |
| TypeScript AST to authority inventory | Unsupported syntax disappears before policy classification | mitigate    | Recognize safe forms and fail closed for every known-client unknown |
| shared service import to v2 caller    | Namespace or alias hides a mutation                        | mitigate    | Resolve default, named, namespace, and alias receivers              |
| diagnostic to test output             | Fixture or host path leaks                                 | mitigate    | Bounded importer/call/method/route/authority fields only            |

## STRIDE Threat Register

| Threat ID  | Category               | Component                    | Disposition | Mitigation Plan                                         |
| ---------- | ---------------------- | ---------------------------- | ----------- | ------------------------------------------------------- |
| T-06-29-01 | Tampering              | element-access mutation call | mitigate    | Extract string element access and classify normally     |
| T-06-29-02 | Elevation of privilege | dynamic route/client syntax  | mitigate    | Unknown known-client calls are blocking failures        |
| T-06-29-03 | Spoofing               | namespace or alias receiver  | mitigate    | Trace import and alias identity                         |
| T-06-29-04 | Repudiation            | manual RawCall fixture       | mitigate    | Require fixtures through live extraction entry point    |
| T-06-29-05 | Information disclosure | diagnostics                  | mitigate    | Redacted bounded fields, never full source or host path |
| T-06-29-06 | Denial of service      | recursive AST resolution     | accept      | Checked-in source only with cycle-safe const resolution |

</threat_model>

<verification>
Run the focused test after RED and GREEN, then the complete sourceMutationInventory and sourceMutationControls suites, frontend typecheck, direct repository-config ESLint for the changed test, and git diff --check. Record current production module, reachable service, operation, descriptor, and route-family counts without treating exact counts as stable requirements. No browser action, generated contract, service restart, deployment, database, or temporary external resource is required.
</verification>

<success_criteria>

- CAT-04 inventory coverage includes element access, safe concatenation, constant routes, namespace imports, aliases, and unknown syntax.
- Negative fixtures exercise the production extraction path and fail with bounded diagnostics.
- The live active-v2 graph contains zero forbidden external mutation authorities and retains safe generic/owned/control-plane calls.

</success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-29-SUMMARY.md.
</output>
