---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 43
type: execute
wave: 2
depends_on: [06-33]
files_modified:
  - backend/endpoints/roms/manual.py
  - backend/handler/database/manual_handler.py
  - backend/handler/database/__init__.py
  - backend/tests/endpoints/roms/test_manual.py
autonomous: true
requirements: [CAT-02]
gap_closure: true

must_haves:
  truths:
    - "A failed or stale primary-manual request leaves the prior path and bytes authoritative and removes only its own new artifact."
    - "A successful request publishes a complete unique owned file, compare-and-swaps path_manual, then removes the superseded owned file."
    - "Concurrent requests for one ROM have one winner; stale requests receive bounded conflict without truncating or deleting the winner or prior manual."
  artifacts:
    - path: "backend/endpoints/roms/manual.py"
      provides: "Single streamed staged primary-manual endpoint"
    - path: "backend/handler/database/manual_handler.py"
      provides: "Locked expected-path compare-and-swap transaction"
    - path: "backend/tests/endpoints/roms/test_manual.py"
      provides: "Disconnect, retry, DB failure, extension replacement, and concurrency regressions"
  key_links:
    - from: "request stream"
      to: "OwnedCreate.binary_file"
      via: "unique staged write, fsync, no-replace publish"
      pattern: "binary_file"
    - from: "published unique manual"
      to: "path_manual"
      via: "locked expected-prior-path CAS"
      pattern: "expected.*path_manual"
    - from: "committed path_manual"
      to: "superseded owned file"
      via: "delete only after commit"
      pattern: 'StorageOperation\.DELETE'
---

<objective>
Make primary-manual replacement single-file, serialized by compare-and-swap, and failure-atomic.

Purpose: CR-02 deletes or truncates the prior manual before success and races concurrent uploads; the approved UI-SPEC requires the old viewer/path until complete success.
Output: A thin endpoint, dedicated transactional handler, and adversarial backend tests reusing Plan 33 OwnedCreate.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/frontend-i18n/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-RESEARCH.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-PATTERNS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@backend/endpoints/roms/manual.py
@backend/handler/database/manual_handler.py
@backend/handler/database/__init__.py
@backend/tests/endpoints/roms/test_manual.py
@backend/handler/filesystem/storage_access.py
@backend/handler/database/roms_handler.py
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md

<interfaces>
DBPrimaryManualHandler.compare_and_swap_path locks the exact Rom row, requires its current path_manual to equal the pre-request expected value, writes the new unique path, and commits through @begin_session. The endpoint keeps existing ROM visibility and ROMS_WRITE checks, accepts one request file through the existing multipart/header contract, streams it into Plan 33's typed RESOURCES OwnedCreate binary-file context under a cryptographically unique server name, and never writes the public final name during receive. Publish completes before DB CAS; CAS failure or DB error deletes only the request's new owned artifact. After CAS commit, delete only the exact superseded owned path if nonempty and different. Never glob, truncate, overwrite, or derive authority from the client filename. A normal deletion failure may retain a harmless superseded owned file but cannot roll back or misreport the new authoritative path; log only a bounded static message. No external descriptor mutation or public schema change.
</interfaces>
</context>

<test_topology>
Plan 06-43 backend pytest gates never use a fixed or shared database. The exact t1, t2, and pv commands use a 128-bit lowercase-hex nonce: the database is the literal plan/task prefix plus 32 hex characters, while the MariaDB principal is exactly the same 32-character nonce so it remains within the username limit. Before any cleanup trap is registered or any mutation occurs, each command proves normal application SELECT 1, queries authoritative schema and mysql.user metadata, requires exact database and user@% absence, and requires exact basetemp absence; collision or lookup failure exits without cleanup or mutation. Only then does it install EXIT/INT/TERM cleanup with independent db_owned=0 and user_owned=0 flags, create database and principal separately, prove each exact identity present before setting its flag, and grant only after both flags equal 1. Cleanup revokes/drops the principal only when user_owned=1, drops the database only when db_owned=1, removes only the regex-guarded exact basetemp, preserves the primary status while reporting cleanup failures, and proves final exact DB/user/temp absence plus normal application SELECT 1. Every pytest process receives explicit DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWD, Redis, ROMM_BASE_PATH, and auth variables plus -p no:env, -p no:cacheprovider, and the exact basetemp. Plan 06-46 Task 2 additionally delegates three-dialect disposable authority to the current checked-in backend/tools/verify_storage_migrations.py contract. Never clean an identity whose owned flag was not established, print credentials, restart services, deploy, or mutate source content.
</test_topology>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED specify failure-atomic and concurrent primary-manual replacement</name>
  <files>backend/tests/endpoints/roms/test_manual.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/database/__init__.py, backend/tests/endpoints/roms/test_manual.py, backend/handler/filesystem/storage_access.py, backend/handler/database/roms_handler.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-UI-SPEC.md, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-VERIFICATION.md</read_first>
  <behavior>
    - Failure preserves prior manual.
    - Concurrency has one winner and bounded conflict.
    - No request touches external source content.
  </behavior>
  <action>Per D-05 and D-11, replacement may mutate only RomM-owned manual state and must never open source-library mutation authority. Add exact tests for client disconnect, parser/mid-write error, file fsync failure, DB CAS failure, same-extension retry, different-extension replacement, concurrent two-request barrier, stale expected path, cleanup failure after commit, and normal empty/existing upload. The named RED test test_primary_manual_concurrent_replacement_has_one_winner must drive two real endpoint calls through barriers, assert one 201 and one bounded 409, prove prior bytes remain until the winner's commit seam, prove the winner bytes/path afterward, and prove the loser artifact is absent. Source and unrelated resources manifests must be byte-identical. Record only this named behavioral failure as RED. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0643_t1_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0643-t1-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0643_t1_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0643-t1-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0643-t1-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; set +e; output=$(docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/endpoints/roms/test_manual.py::test_primary_manual_concurrent_replacement_has_one_winner -x 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; test "$status" -eq 1; ! printf "%s\n" "$output" | grep -Eq "ERROR collecting|collected 0 items|usage: pytest|unrecognized arguments|INTERNALERROR|OperationalError|Access denied|Can.t connect|Unknown database|docker: Error"; printf "%s\n" "$output" | grep -Eq "FAILED .*test_primary_manual_concurrent_replacement_has_one_winner"'</automated>
  </verify>
  <acceptance_criteria>
    - RED proves current concurrent requests race destructively.
    - Every failure asserts prior DB path and bytes, plus exact loser cleanup.
    - Barriers replace sleeps; external source manifests are complete and unchanged.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>The backend manual integrity and concurrency contract is fully RED-specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN publish unique owned content, CAS the path, then retire the prior file</name>
  <files>backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/database/__init__.py, backend/tests/endpoints/roms/test_manual.py</files>
  <read_first>CLAUDE.md, .claude/skills/backend-development/SKILL.md, .claude/skills/pre-pr-verification/SKILL.md, backend/endpoints/roms/manual.py, backend/handler/database/manual_handler.py, backend/handler/database/__init__.py, backend/tests/endpoints/roms/test_manual.py, backend/handler/filesystem/storage_access.py, backend/handler/filesystem/storage_policy.py, backend/handler/database/roms_handler.py, .planning/phases/06-safe-lifecycle-and-legacy-migration/06-33-SUMMARY.md</read_first>
  <behavior>
    - Prior manual remains until complete publish and DB commit.
    - Concurrent stale requests cannot overwrite a winner.
    - Only typed RESOURCES authority is used.
  </behavior>
  <action>Per D-05 and D-11, replacement may mutate only RomM-owned manual state and must never open source-library mutation authority. Implement the dedicated handler and register its singleton. In the endpoint snapshot the current path after visibility, validate one PDF/Markdown filename without using it as storage authority, generate one opaque final basename, ensure the owned manual directory, open typed RESOURCES CREATE, and adapt the streaming parser to the capability-provided binary file so capability exit performs full fsync/no-replace publication. Call the handler CAS only after publication. On disconnect, parser error, publication error, stale CAS, or DB failure, close safely and delete only this request's exact new owned path. Translate stale CAS to a bounded 409 detail suitable for the approved conflict copy. After successful commit, authorize typed RESOURCES DELETE and remove only the exact prior path; never glob or delete before commit. Keep logs path-free and static on errors. Execute the inline verify lifecycle exactly: 128-bit identities, authoritative pre-mutation absence checks before trap registration, independent post-proof db_owned/user_owned flags, separate creation, grant-after-both-owned, explicit DB/Redis/auth environment, exact basetemp/plugins, ownership-gated cleanup, final absence proof, and before/after normal SELECT 1 are mandatory task behavior.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0643_t2_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0643-t2-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0643_t2_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0643-t2-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0643-t2-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/endpoints/roms/test_manual.py tests/handler/filesystem/test_storage_access.py -x'</automated>
  </verify>
  <acceptance_criteria>
    - All failure seams preserve prior path/bytes and remove request-owned artifacts.
    - Concurrent barrier test yields one winner and one bounded conflict.
    - Success commits new path before exact old deletion and never exposes partial bytes.
    - Existing visibility, permissions, extension validation, deletion, and redownload tests remain green.
    - Collision exits without mutation; owned DB/principal/basetemp identities are absent after the command, no unowned identity is cleaned, and normal application SELECT 1 succeeds before and after.
  </acceptance_criteria>
  <done>Primary-manual replacement is backend-authoritative, one-file, crash-safe at publication, and non-destructive on failure.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                    |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| pytest runner to MariaDB/root authority  | Plan 06-43 tests may create only validated task-nonce resources while normal application storage remains live. |
| client stream to RESOURCES capability    | Untrusted multipart bytes enter owned storage.                                                                 |
| published file to DB CAS                 | A new immutable path becomes authoritative only after locked comparison.                                       |
| committed DB path to prior file deletion | Cleanup occurs after authority changes and must target only the old owned path.                                |

## STRIDE Threat Register

| Threat ID       | Category               | Component                 | Disposition | Mitigation Plan                                                                                                                                                                                                                                                  |
| --------------- | ---------------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-43-01      | Tampering              | concurrent replacement    | mitigate    | Unique names plus locked expected-path CAS yield one winner.                                                                                                                                                                                                     |
| T-06-43-02      | Denial of service      | disconnect/close          | mitigate    | Capability cleanup and exact request-artifact deletion on every failure.                                                                                                                                                                                         |
| T-06-43-03      | Elevation of privilege | storage target            | mitigate    | Typed RESOURCES descriptor only; client filename never selects authority.                                                                                                                                                                                        |
| T-06-43-04      | Information disclosure | errors/logs               | mitigate    | Static bounded 400/409/500 details with no paths, names, tokens, or row data.                                                                                                                                                                                    |
| T-06-43-05      | Repudiation            | commit ordering           | mitigate    | Barrier and seam tests prove publish, CAS commit, then old deletion.                                                                                                                                                                                             |
| T-06-43-99      | Elevation of privilege | isolated pytest lifecycle | mitigate    | Authoritative collision lookup precedes trap/mutation; 128-bit exact identities, separate post-presence ownership flags, grant-after-both-owned, ownership-gated exact cleanup, final absence checks, and normal SELECT 1 prevent deletion of unowned resources. |
| </threat_model> |

<verification>
<automated>bash -lc 'set -euo pipefail; nonce=$(openssl rand -hex 16); db=romm_p0643_pv_$nonce; principal=$nonce; secret=p$nonce; basetemp=/tmp/romm-p0643-pv-$nonce; root_sql(){ docker exec -i romm-db-dev sh -lc "exec mariadb -uroot -p\"\$MARIADB_ROOT_PASSWORD\" --batch --skip-column-names"; }; app_select(){ docker exec romm-db-dev sh -lc "exec mariadb -h127.0.0.1 -u\"\$MARIADB_USER\" -p\"\$MARIADB_PASSWORD\" \"\$MARIADB_DATABASE\" -Nse \"SELECT 1\""; }; [[ "$nonce" =~ ^[0-9a-f]{32}$ ]]; [[ "$db" =~ ^romm_p0643_pv_[0-9a-f]{32}$ ]]; [[ "$principal" =~ ^[0-9a-f]{32}$ ]]; [[ "$basetemp" =~ ^/tmp/romm-p0643-pv-[0-9a-f]{32}$ ]]; test "$(app_select)" = 1; initial=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql); test "$initial" = "0:0"; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\""; db_owned=0; user_owned=0; cleanup(){ set +e; cleanup_rc=0; if [ "$user_owned" -eq 1 ]; then printf "REVOKE ALL PRIVILEGES, GRANT OPTION FROM \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; printf "DROP USER \x27%s\x27@\x27%%\x27;\n" "$principal" | root_sql &gt;/dev/null || cleanup_rc=1; fi; if [ "$db_owned" -eq 1 ]; then printf "DROP DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null || cleanup_rc=1; fi; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "case \"\$PLAN_BASETEMP\" in /tmp/romm-p0643-pv-????????????????????????????????) rm -rf -- \"\$PLAN_BASETEMP\";; *) exit 97;; esac" || cleanup_rc=1; final=$(printf "SELECT CONCAT((SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27),\x27:\x27,(SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27));\n" "$db" "$principal" | root_sql) || cleanup_rc=1; test "$final" = "0:0" || cleanup_rc=1; docker exec -e PLAN_BASETEMP="$basetemp" romm-dev sh -lc "test ! -e \"\$PLAN_BASETEMP\"" || cleanup_rc=1; test "$(app_select)" = 1 || cleanup_rc=1; if [ "$cleanup_rc" -ne 0 ]; then printf "isolated lifecycle cleanup or absence verification failed\n" &gt;&amp;2; fi; return "$cleanup_rc"; }; finish(){ run_rc=$?; trap - EXIT INT TERM; cleanup; cleanup_rc=$?; if [ "$run_rc" -ne 0 ]; then exit "$run_rc"; fi; exit "$cleanup_rc"; }; interrupted(){ exit 130; }; trap finish EXIT; trap interrupted INT TERM; printf "CREATE DATABASE \`%s\`;\n" "$db" | root_sql &gt;/dev/null; db_present=$(printf "SELECT COUNT(*) FROM INFORMATION_SCHEMA.SCHEMATA WHERE SCHEMA_NAME=\x27%s\x27;\n" "$db" | root_sql); test "$db_present" = "1"; db_owned=1; printf "CREATE USER \x27%s\x27@\x27%%\x27 IDENTIFIED BY \x27%s\x27;\n" "$principal" "$secret" | root_sql &gt;/dev/null; user_present=$(printf "SELECT COUNT(*) FROM mysql.user WHERE User=\x27%s\x27 AND Host=\x27%%\x27;\n" "$principal" | root_sql); test "$user_present" = "1"; user_owned=1; test "$db_owned" -eq 1; test "$user_owned" -eq 1; printf "GRANT ALL PRIVILEGES ON \`%s\`.* TO \x27%s\x27@\x27%%\x27; FLUSH PRIVILEGES;\n" "$db" "$principal" | root_sql &gt;/dev/null; docker exec -e DB_HOST=romm-db-dev -e DB_PORT=3306 -e DB_NAME="$db" -e DB_USER="$principal" -e DB_PASSWD="$secret" -e REDIS_HOST=romm-valkey-dev -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -e ROMM_BASE_PATH=romm_test -e ROMM_AUTH_SECRET_KEY=phase6-test-only --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider --basetemp "$basetemp" tests/endpoints/roms/test_manual.py tests/handler/filesystem/test_storage_access.py -x'</automated>
This is an independent p0643_pv 128-bit lifecycle. Its pre-mutation authoritative absence proof, separate ownership flags, ownership-gated cleanup, final absence proof, and before/after application SELECT 1 must complete before the remaining plan gates.
Run full manual and storage-access tests in the isolated plan DB, compare complete source and owned-manual manifests at each seam, scoped Trunk, git diff --check, cleanup, and normal DB continuity. Assert OpenAPI/generated hashes remain unchanged because the route contract is preserved. No frontend or Phase 7 UI in this plan.
</verification>

<success_criteria>

- Every p0643 pytest lifecycle uses a fresh validated nonce DB/principal/basetemp and proves exact cleanup plus normal DB continuity.
- CR-02 backend root cause is closed.
- Manual replacement reuses Plan 33 rather than duplicating atomic publication.
- D-05/D-11 source immutability and CAT-02 owned integrity remain explicit.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-safe-lifecycle-and-legacy-migration/06-43-SUMMARY.md` with RED/GREEN evidence, exact commands and exits, resource cleanup, and preserved 28-path baseline status.
</output>
