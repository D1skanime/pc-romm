---
phase: 06-safe-lifecycle-and-legacy-migration
plan: 25
type: execute
wave: 3
depends_on: [06-18, 06-24]
files_modified:
  - frontend/src/v2/components/GameDetails/PatcherTab.vue
  - frontend/src/v2/views/Upload.vue
  - frontend/src/v2/views/Upload.test.ts
  - frontend/src/v2/components/AppShell/UserMenu.vue
  - frontend/src/v2/views/Gallery/Platform.vue
  - frontend/src/v2/components/Settings/SettingsSidebar.vue
  - frontend/src/v2/views/Home.vue
  - frontend/src/v2/views/Auth/Setup.vue
  - frontend/src/v2/components/Auth/SetupStepPlatforms.vue
  - frontend/src/services/api/rom.ts
  - frontend/src/services/api/platform.ts
  - frontend/src/services/api/setup.ts
  - frontend/src/v2/sourceMutationControls.test.ts
  - backend/endpoints/heartbeat.py
  - backend/tests/endpoints/test_storage_policy_denials.py
autonomous: true
requirements: [CAT-01, CAT-04]
gap_closure: true
must_haves:
  truths:
    - "Active v2 cannot upload ROMs, re-upload patched output, create platform/source directories, or create a first-run library structure."
    - "Patch generation remains usable only as external READ plus typed TEMP PATCH with local browser download; it cannot persist output into the source library."
    - "Every maximum-grant upload navigation and dropzone/file control is absent from UserMenu, Platform, SettingsSidebar, Home empty state, Upload, setup, and patcher surfaces, while ordinary platform reads and initial user creation remain usable."
    - "Crafted setup, platform, and ROM upload requests deny before create, mkdir, temp assembly, move, overwrite, or durable mutation."
  artifacts:
    - path: "frontend/src/services/api/rom.ts"
      provides: "ROM service without uploadRoms"
    - path: "frontend/src/services/api/platform.ts"
      provides: "Platform service without uploadPlatform/MKDIR"
    - path: "frontend/src/services/api/setup.ts"
      provides: "Setup service without createPlatforms"
    - path: "backend/endpoints/heartbeat.py"
      provides: "Fail-closed setup CREATE/MKDIR authorization"
    - path: "frontend/src/v2/components/Settings/SettingsSidebar.vue"
      provides: "Settings navigation without ROUTES.UPLOAD"
    - path: "frontend/src/v2/views/Home.vue"
      provides: "Empty-library choices without ROUTES.UPLOAD"
    - path: "frontend/src/v2/sourceMutationControls.test.ts"
      provides: "Rendered upload/setup/patch/navigation negative regression"
  key_links:
    - from: "frontend/src/v2/components/GameDetails/PatcherTab.vue"
      to: "backend/endpoints/roms/patch.py"
      via: "POST patch classified as external READ plus typed TEMP PATCH"
      pattern: "/patch"
    - from: "frontend/src/v2/sourceMutationControls.test.ts"
      to: "UserMenu, Platform, SettingsSidebar, Home, Upload, setup, and PatcherTab"
      via: "maximum-grant render and ROUTES.UPLOAD negative assertions"
      pattern: "ROUTES.UPLOAD"
    - from: "backend/endpoints/heartbeat.py"
      to: "legacy_external_storage"
      via: "CREATE and MKDIR authorization before setup writes"
      pattern: "StorageOperation.CREATE|StorageOperation.MKDIR"
---

<objective>
Remove active-v2 external upload and directory-creation authority while preserving typed TEMP patch output.

Purpose: Close CAT-04 UPLOAD/CREATE/MKDIR flows, including first-run setup and patch re-upload, without banning unrelated raw POST clients.
Output: Source-read-only upload/setup/patch surfaces and direct server denial proof.
</objective>

<execution_context>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@.claude/skills/frontend-v2-components/SKILL.md
@.claude/skills/frontend-v2-patterns/SKILL.md
@.claude/skills/frontend-v2-input/SKILL.md
@.claude/skills/pre-pr-verification/SKILL.md
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-CONTEXT.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-REVIEW.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-18-SUMMARY.md
@.planning/phases/06-safe-lifecycle-and-legacy-migration/06-24-SUMMARY.md
</context>

<interfaces>
uploadRoms reaches POST /roms/upload/start, PUT chunk storage, and POST /roms/upload/complete; start/complete authorize UPLOAD against legacy_external_storage and completion moves assembled TEMP bytes into source. platformApi.uploadPlatform reaches POST /platforms with MKDIR/external. setupApi.createPlatforms reaches POST /setup/platforms, whose live handler creates the library structure and platform directories without a storage-policy guard. PatcherTab raw POST /roms/{file_id}/patch is allowed because the backend authorizes source READ plus typed TEMP PATCH; its optional uploadRoms continuation is forbidden. SettingsSidebar contains a maximum-grant ROMS_WRITE-gated ROUTES.UPLOAD entry and Home exposes ROUTES.UPLOAD in the empty-library choice; both are forbidden navigation capabilities even if Upload.vue is inert. Database-only platform catalog deletion and metadata update remain allowed.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Remove active-v2 upload, source creation, and patch persistence controls</name>
  <files>frontend/src/v2/components/GameDetails/PatcherTab.vue, frontend/src/v2/views/Upload.vue, frontend/src/v2/views/Upload.test.ts, frontend/src/v2/components/AppShell/UserMenu.vue, frontend/src/v2/views/Gallery/Platform.vue, frontend/src/v2/components/Settings/SettingsSidebar.vue, frontend/src/v2/views/Home.vue, frontend/src/v2/views/Auth/Setup.vue, frontend/src/v2/components/Auth/SetupStepPlatforms.vue, frontend/src/services/api/rom.ts, frontend/src/services/api/platform.ts, frontend/src/services/api/setup.ts, frontend/src/v2/sourceMutationControls.test.ts</files>
  <read_first>frontend/src/v2/components/GameDetails/PatcherTab.vue, frontend/src/v2/views/Upload.vue, frontend/src/v2/views/Upload.test.ts, frontend/src/v2/components/AppShell/UserMenu.vue, frontend/src/v2/views/Gallery/Platform.vue, frontend/src/v2/components/Settings/SettingsSidebar.vue, frontend/src/v2/views/Home.vue, frontend/src/v2/views/Auth/Setup.vue, frontend/src/v2/components/Auth/SetupStepPlatforms.vue, frontend/src/services/api/rom.ts, frontend/src/services/api/platform.ts, frontend/src/services/api/setup.ts, frontend/src/v2/sourceMutationControls.test.ts, frontend/src/plugins/router.ts, backend/endpoints/roms/upload.py, backend/endpoints/roms/patch.py, backend/endpoints/platform.py, backend/endpoints/heartbeat.py, backend/handler/filesystem/storage_policy.py, frontend/package.json</read_first>
  <behavior>
    - Maximum-grant v2 has no ROUTES.UPLOAD entry or link in UserMenu, Platform, SettingsSidebar, or Home empty state; no upload action/dropzone/file input; no patch save-into-RomM toggle/platform picker/re-upload call; and no setup new-platform selection/create call.
    - uploadRoms, uploadPlatform, and createPlatforms are absent from shared service signatures/exports and cannot be reached through aliases.
    - Patcher raw POST remains and is explicitly classified by normalized route as source READ plus typed TEMP PATCH; local browser download still works.
    - Setup still reads existing library/platform data and creates the admin user; platform catalog reads/update/delete and ordinary generic-client/control-plane calls remain allowed.
  </behavior>
  <action>RED: extend rendered controls tests and Upload tests first. Render SettingsSidebar with ROMS_WRITE and Home in its empty-library state, proving both live ROUTES.UPLOAD controls alongside every other upload/setup/platform/patch persistence control and service call, and add a positive raw api.post patch fixture whose backend pair is READ/external plus PATCH/TEMP. Record RED and commit with hooks as test(06-25): expose external upload controls. GREEN: remove every ROUTES.UPLOAD link/entry/action from UserMenu, Platform, SettingsSidebar, and the Home empty-library choices, make Upload.vue an inert source-read-only surface with no file/dropzone/platform-create/upload code, and update its test to assert no mutation services. In PatcherTab retain patch-to-TEMP and local download, but remove saveIntoRomM, uploadPatchedFile, scan side effects, and write-grant plumbing. In first-run Setup remove new-platform selection/submission while preserving library reads and admin creation; make SetupStepPlatforms display existing/detected state without create selection. Remove uploadRoms, uploadPlatform, and createPlatforms implementations/exports. Use existing locale text or remove obsolete copy; do not add a replacement raw client. Commit with hooks as fix(06-25): remove external upload authority. Never use --no-verify.</action>
  <verify>
    <automated>docker exec --workdir /app/frontend romm-dev npm run test -- src/v2/views/Upload.test.ts src/v2/sourceMutationControls.test.ts</automated>
  </verify>
  <acceptance_criteria>Focused tests render SettingsSidebar and Home under maximum grants/empty-library state and pass with every ROUTES.UPLOAD navigation, upload/create control, and service export absent, while patch-to-TEMP/local download, existing-platform reads, and user creation remain functional.</acceptance_criteria>
  <done>No maximum-grant active-v2 navigation or control can reach ROM upload, platform/setup source creation, or patched-output persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on crafted setup source creation</name>
  <files>backend/endpoints/heartbeat.py, backend/tests/endpoints/test_storage_policy_denials.py</files>
  <read_first>backend/endpoints/heartbeat.py, backend/tests/endpoints/test_storage_policy_denials.py, backend/endpoints/storage_policy.py, backend/endpoints/platform.py, backend/endpoints/roms/upload.py, backend/handler/filesystem/__init__.py, backend/handler/filesystem/storage_policy.py</read_first>
  <behavior>
    - Empty setup platform input remains a source-neutral no-op.
    - Non-empty setup input authorizes CREATE and MKDIR against legacy_external_storage before structure detection or filesystem/database mutation and is denied in Phase 6.
    - Crafted platform POST and ROM upload start/complete remain denied at MKDIR/UPLOAD; no temp assembly, move, overwrite, or cleanup mutates source.
    - Failure output reports stable operation/storage metadata without fs_slug-derived or resolved sensitive paths.
  </behavior>
  <action>RED: add direct setup tests with filesystem/database spies and immutable manifests; show non-empty input reaches unguarded create_library_structure/add_platform today while empty input is neutral. Include replay and symlink/path-replacement fixtures and retain Plan 24 family coverage. Commit with hooks as test(06-25): deny setup source creation. GREEN: authorize CREATE and MKDIR against legacy_external_storage for non-empty input before detection or any write. Phase 6 denial must leave all calls/manifests unchanged; keep empty input neutral and do not catch/translate StoragePolicyDenied into a permissive fallback. Commit with hooks as fix(06-25): guard setup source creation. Never use --no-verify.</action>
  <verify>
    <automated>docker exec -e DB_NAME=romm_test_0625 --workdir /app/backend romm-dev /app/.venv/bin/pytest -p no:env -p no:cacheprovider tests/endpoints/test_storage_policy_denials.py -k "setup or mkdir or upload" -x</automated>
  </verify>
  <acceptance_criteria>Focused backend tests prove all setup/platform/ROM upload requests fail before I/O or durable change, empty setup is neutral, and errors are path-free.</acceptance_criteria>
  <done>Direct clients cannot create or upload into external source storage.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Risk                                                                        | Disposition | Mitigation                                                              |
| ---------------------------------- | --------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------- |
| maximum-grant navigation to upload | Hidden Settings/Home link preserves reachability after controls are removed | mitigate    | Render and remove ROUTES.UPLOAD from every active-v2 navigation surface |
| browser file/dropzone to upload    | Traversal/content/size exhausts or writes source                            | mitigate    | Remove upload surface/export and retain server pre-I/O denial           |
| patch input/output                 | Uploaded patch or output escapes TEMP                                       | mitigate    | Validated patch route only, typed TEMP, local download                  |
| setup input to external root       | Unauthenticated first-run request creates directories                       | mitigate    | CREATE/MKDIR denial before detection/write                              |
| route replay                       | Stale/replayed request writes after UI removal                              | mitigate    | Backend denial on every request                                         |
| diagnostics                        | Platform/path names disclose layout                                         | mitigate    | Stable path-free denial                                                 |

## STRIDE Threat Register

| Threat ID       | Category               | Component                                    | Disposition | Mitigation Plan                                                                    |
| --------------- | ---------------------- | -------------------------------------------- | ----------- | ---------------------------------------------------------------------------------- |
| T-06-25-01      | Tampering              | upload/setup                                 | mitigate    | Remove controls and deny pre-I/O                                                   |
| T-06-25-02      | Elevation of privilege | maximum-grant navigation and first-run setup | mitigate    | Remove every ROUTES.UPLOAD control and keep policy guard independent of auth state |
| T-06-25-03      | Spoofing               | patch classification                         | mitigate    | Method/route/live descriptor pair                                                  |
| T-06-25-04      | Repudiation            | replay                                       | mitigate    | Deterministic direct-request tests                                                 |
| T-06-25-05      | Information disclosure | errors                                       | mitigate    | Path-free output                                                                   |
| T-06-25-06      | Denial of service      | upload content                               | mitigate    | No client path and early server denial                                             |
| </threat_model> |

<verification>
Run each fast focused command first. Then run full storage-policy denials, frontend typecheck, scoped Trunk, git diff --check, and drop/assert absence of romm_test_0625. Plan 26 runs the all-v2 semantic inventory. No restart/deploy.
</verification>

<success_criteria>

- CAT-04 denies UPLOAD/CREATE/MKDIR from active v2 and crafted clients.
- Typed TEMP patch output and local download remain usable.
- No create, move, copy, overwrite, rename, or delete occurs under external source.
  </success_criteria>

<output>
After completion, create .planning/phases/06-safe-lifecycle-and-legacy-migration/06-25-SUMMARY.md.
</output>
