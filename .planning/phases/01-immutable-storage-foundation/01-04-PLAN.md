---
phase: 01-immutable-storage-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified: [backend/handler/database/storage_handler.py, backend/handler/database/__init__.py, backend/tests/handler/database/test_storage_handler.py, backend/tests/conftest.py]
autonomous: true
requirements: [ROOT-01]
must_haves:
  truths: ["Existing deployment roots register without creation", "Mappings validate and save atomically", "Overlap and duplicate races fail closed"]
  artifacts:
    - {path: backend/handler/database/storage_handler.py, provides: "root and mapping persistence"}
    - {path: backend/tests/handler/database/test_storage_handler.py, provides: "transaction and overlap evidence"}
  key_links:
    - {from: backend/handler/database/storage_handler.py, to: backend/handler/filesystem/storage_resolver.py, via: "health and target validation"}
---
<objective>Persist roots and non-overlapping mappings atomically. Purpose: connect schema to resolver without API or consumer cutover. Output: handler, registry, tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-immutable-storage-foundation/01-01-SUMMARY.md
@.planning/phases/01-immutable-storage-foundation/01-03-SUMMARY.md
@.planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Specify registration, overlap, and race behavior</name><files>backend/tests/handler/database/test_storage_handler.py, backend/tests/conftest.py</files><read_first>backend/tests/handler/database/test_music_playlists_handler.py, backend/tests/conftest.py, backend/models/storage.py, backend/handler/filesystem/storage_resolver.py</read_first><action>Write tests per D-01 through D-17, D-29, D-31 for existing-root-only registration, normalized non-empty mapping persistence, second-platform rejection, and overlap based on canonical resolved targets across every active mapping regardless of root identity. Cover equal, ancestor, and descendant physical targets within one root; distinct roots resolving to the same directory; nested roots whose mappings resolve to ancestor/descendant directories; unrelated canonical targets with common textual prefixes; rollback; and concurrent sessions attempting exact and cross-root overlap. Add a real two-session concurrency test in which both transactions begin before either mapping exists and attempt first inserts through distinct or nested active roots whose canonical targets are equal or ancestor/descendant. Coordinate the sessions so they contend on the root lock set, then assert exactly one commit succeeds and the loser rechecks after acquiring the locks and raises the bounded overlap error. Assert exact `(root_id, relative_path)` uniqueness is only a race backstop. Wrap registration and save with manifests/tripwires.</action><acceptance_criteria>Tests prove canonical cross-root overlap rejection, including two concurrent first inserts with an initially empty mapping table where only one commits and the loser returns the bounded overlap error after recheck; deterministic root-lock ordering avoids deadlock; exact uniqueness backstop, rollback, and unchanged manifests also pass.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/database/test_storage_handler.py -k "register or overlap or concurrent" -x</automated></verify><done>ROOT-01 workflow is executable before implementation.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement DBStorageHandler</name><files>backend/handler/database/storage_handler.py, backend/handler/database/__init__.py</files><read_first>backend/models/storage.py, backend/handler/database/platforms_handler.py, backend/handler/database/base_handler.py, backend/handler/database/__init__.py, backend/handler/filesystem/storage_resolver.py, backend/tests/handler/database/test_storage_handler.py</read_first><action>Implement @begin_session registration and mapping save per D-01 through D-17. Accept absolute root only at the trusted deployment-owned internal boundary, force external_read_only, health-inspect before persistence, and fail missing, unreadable, or symlink roots. Within the mapping-save transaction, acquire a portable shared serialization set by selecting every active StorageRoot row ordered by `StorageRoot.id` with SQLAlchemy `select(StorageRoot).where(StorageRoot.active.is_(True)).order_by(StorageRoot.id).with_for_update()` before resolving or comparing mappings. This stable row set exists even when the mapping table is empty and serializes conflicting first inserts; deterministic ID ordering prevents deadlocks. After acquiring it, load all active mappings with their roots, normalize and canonically resolve the candidate and every stored target, reject equality or either-direction ancestry across root identities and within one root, then recheck immediately before insert/flush. Root registration is bounded in Phase 1 to deployment initialization before mapping writes; no concurrent active-root creation is supported in this phase. Keep unique `(root_id, relative_path)` and unique platform constraints as database race backstops for exact identity collisions; translate IntegrityError and post-lock overlap rejection to bounded storage errors. Do not use advisory locks, dialect-specific primitives, or add any later-phase surface.</action><acceptance_criteria>Handler tests pass on the supported SQLAlchemy dialect contract; two concurrent first inserts through distinct or nested active roots serialize on all active StorageRoot rows in deterministic ID order, exactly one commits, and the loser raises the bounded overlap error after recheck; fixtures remain unchanged.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/database/test_storage_handler.py -k "register or overlap or concurrent" -x &amp;&amp; uv run pytest tests/models/test_storage.py -x</automated></verify><done>ROOT-01 registration and safe mapping persistence work atomically.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deployment registration to filesystem/database | Absolute root accepted only from trusted configuration. |
| Mapping input to transaction | Relative path and platform are untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-ROOT-CREATE | Tampering | register_root | mitigate | existing-root inspection plus manifests/tripwires. |
| T-01-MODE-BYPASS | Elevation of Privilege | handler | mitigate | force/recheck external_read_only. |
| T-01-OVERLAP | Tampering | save_mapping | mitigate | Canonical target comparison across every active mapping and root identity. |
| T-01-DUPLICATE-RACE | Tampering | transaction/flush | mitigate | Lock every active StorageRoot row by ascending ID with `SELECT FOR UPDATE`, then compare, recheck, and insert; unique constraints backstop exact collisions. |
| T-01-PATH-DISCLOSURE | Information Disclosure | errors | mitigate | IDs and logical relative values only. |
</threat_model>
<verification>Run handler, resolver and model suites together.</verification>
<success_criteria>ROOT-01 registers an existing safe root and persists validated mappings without source mutation.</success_criteria>
<output>After completion, create .planning/phases/01-immutable-storage-foundation/01-04-SUMMARY.md</output>
