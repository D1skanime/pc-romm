---
phase: 01-immutable-storage-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [backend/handler/filesystem/storage_resolver.py, backend/tests/handler/filesystem/test_storage_resolver.py]
autonomous: true
requirements: [ROOT-03, PATH-02, PATH-03, PATH-05]
must_haves:
  truths: ["Health is bounded and write-free", "Only active existing readable directories resolve", "All symlinks and canonical escapes fail closed"]
  artifacts:
    - {path: backend/handler/filesystem/storage_resolver.py, provides: "health and point-in-time resolution"}
    - {path: backend/tests/handler/filesystem/test_storage_resolver.py, provides: "real filesystem security tests"}
  key_links:
    - {from: backend/handler/filesystem/storage_resolver.py, to: backend/models/storage.py, via: "root mode and active state"}
---
<objective>Complete metadata-only health and canonical directory resolution. Purpose: enforce the immutable root boundary. Output: resolver and real-filesystem tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-immutable-storage-foundation/01-01-SUMMARY.md
@.planning/phases/01-immutable-storage-foundation/01-02-SUMMARY.md
@.planning/phases/01-immutable-storage-foundation/01-CONTEXT.md
@.planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Specify health, containment, target, and symlink behavior</name><files>backend/tests/handler/filesystem/test_storage_resolver.py</files><read_first>backend/handler/filesystem/storage_resolver.py, backend/models/storage.py, backend/tests/handler/filesystem/test_base_handler.py, .planning/phases/01-immutable-storage-foundation/01-VALIDATION.md</read_first><action>Add real-tree tests per D-05, D-09, D-10, D-17, D-20 through D-24, D-29 through D-31. Cover missing/inactive/unreadable/writable roots; missing/file/unreadable targets; sibling-prefix escape; root, first, middle and leaf symlinks with absolute/relative, in-root/escaping, broken and loop targets. Build recursive before/after manifests recording relative name, kind, size, mode, mtime and link target, excluding atime. Tripwire mkdir, write-open, tempfile, rename/move/copy, unlink/remove. Mock only permission observation when root-run.</action><acceptance_criteria>All target/link variants and writable-fixture immutability are represented by failing tests.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -k "health or containment or target or symlink or immutable" -x</automated></verify><done>ROOT-03 and PATH-02/03/05 contracts are executable.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement health and canonical resolution</name><files>backend/handler/filesystem/storage_resolver.py</files><read_first>backend/handler/filesystem/storage_resolver.py, backend/models/storage.py, backend/exceptions/storage_exceptions.py, backend/tests/handler/filesystem/test_storage_resolver.py, .planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</read_first><action>Implement per D-05, D-09, D-10, D-17, D-18, D-20 through D-24: require active external_read_only root, reject a configured-root symlink and lstat every target component to reject links, strict-resolve root/target, enforce target.relative_to(root), require existing readable directory, translate OS errors to bounded types, and treat writable as unsafe. Empty relative path is accepted only by the explicit internal root health/resolution entry point, never mapping resolution. Persist nullable bounded health fields directly on StorageRoot without a write probe. Retain PATTERNS.md rules that FSHandler is an anti-analog, errors cannot disclose unrelated paths, and resolver work cannot mutate storage or wire later-phase consumers. Health may stat, enumerate, and os.access only. Never probe-write, mkdir, lock, tempfile, rename or delete.</action><acceptance_criteria>Focused tests pass; source manifests remain identical; configured-root and component symlinks and escapes fail closed.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -x</automated></verify><done>Central resolution and health are canonical, bounded, and non-mutating.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stored root and logical path to filesystem | Mutable host metadata is untrusted. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-ESCAPE | Tampering | resolve_directory | mitigate | strict resolve plus relative_to. |
| T-01-SYMLINK | Tampering | component walk | mitigate | lstat every component and test all positions. |
| T-01-TOCTOU | Tampering | post-check use | accept | Point-in-time only; Phase 1 opens no file and Phase 2 owns enforcement. |
| T-01-HEALTH-WRITE | Tampering | health | mitigate | metadata-only operations, manifests, tripwires. |
| T-01-PATH-LEAK | Information Disclosure | errors | mitigate | bounded translation. |
| T-01-MODE-BYPASS | Elevation of Privilege | root validation | mitigate | require active external_read_only and fail writable safety. |
</threat_model>
<verification>Run the full resolver suite and unchanged-manifest assertions.</verification>
<success_criteria>ROOT-03 and PATH-02/03/05 pass with no mutation.</success_criteria>
<output>After completion, create .planning/phases/01-immutable-storage-foundation/01-03-SUMMARY.md</output>
