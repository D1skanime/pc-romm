---
phase: 01-immutable-storage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/models/storage.py, backend/models/platform.py, backend/alembic/env.py, backend/alembic/versions/0108_immutable_storage_foundation.py, backend/tools/verify_storage_migrations.py, backend/tests/models/test_storage.py]
autonomous: true
requirements: [ROOT-02, ROOT-04]
must_haves:
  truths: ["Only external_read_only persists", "Mappings store root identity plus normalized relative path", "0108 is portable and filesystem-free"]
  artifacts:
    - {path: backend/models/storage.py, provides: "StorageRoot and PlatformStorageMapping"}
    - {path: backend/alembic/versions/0108_immutable_storage_foundation.py, provides: "portable schema"}
  key_links:
    - {from: backend/models/platform.py, to: backend/models/storage.py, via: "scalar back_populates relationship"}
---
<objective>Create the immutable persistence contract and portable 0108 migration. Purpose: establish root identity and relative mappings before consumers. Output: models, migration, tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-immutable-storage-foundation/01-CONTEXT.md
@.planning/phases/01-immutable-storage-foundation/01-RESEARCH.md
@.planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Define and implement storage ORM contracts</name><files>backend/tests/models/test_storage.py, backend/models/storage.py, backend/models/platform.py, backend/alembic/env.py</files><read_first>backend/models/base.py, backend/models/music.py, backend/models/platform.py, backend/tests/handler/database/test_music_playlists_handler.py, backend/alembic/env.py</read_first><action>Write failing tests, then implement typed StorageRoot and PlatformStorageMapping per D-03, D-06 through D-15, D-25, D-28. Use integer IDs, inherited timestamps, stable string mode with a named check allowing only external_read_only, unique container_path, unique platform_id, unique root_id plus relative_path, root_id index, restrictive root deletion, and explicit back_populates. Store reachable, readable, non_writable, last_checked_at, bounded safe_error on the root. Mapping has no absolute/composed/NAS path. Preserve Platform.fs_slug. Import models into Alembic metadata. No API, backfill, consumer, or writable toggle per D-27 and D-33.</action><acceptance_criteria>Tests reject every other mode, second platform mapping, duplicate root-relative mapping, and root deletion with mappings; mappings expose only relative_path plus identities.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_storage.py -x</automated></verify><done>ROOT-02 and ROOT-04 constraints pass at ORM and database levels.</done></task>
<task type="auto"><name>Task 2: Create and exercise revision 0108</name><files>backend/alembic/versions/0108_immutable_storage_foundation.py, backend/tools/verify_storage_migrations.py</files><read_first>backend/alembic/versions/0102_music_playlists.py, backend/alembic/versions/0107_roms_dedup_cover_index.py, backend/models/storage.py, .github/workflows/migrations.yml, docker-compose.yml, .claude/skills/backend-development/SKILL.md</read_first><action>Create revision 0108 with down_revision 0107_roms_dedup_cover_index per D-25, D-26, D-28. Use explicit portable columns and named constraints, roots before mappings, reverse-order downgrade, no native enum, filesystem import, backfill, or data transform. Add `backend/tools/verify_storage_migrations.py` using the existing Docker CLI pattern to start uniquely named ephemeral MariaDB 10.11, MySQL 8.4, and PostgreSQL 15 containers on dynamic host ports, wait for their health commands, apply the existing RomM database environment contract, run upgrade head, downgrade to 0107, and re-upgrade, then remove containers in `finally`. The MySQL server is tested explicitly with the project's MySQL-compatible SQLAlchemy driver. Support `--dialects mariadb mysql postgresql`, fail nonzero on any cycle, and never mount a source-library fixture.</action><acceptance_criteria>All three independent containerized dialect cycles pass, cleanup runs on failure, and no library path is mounted.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql &amp;&amp; uv run pytest tests/models/test_storage.py -x</automated></verify><done>0108 is reversible against MariaDB, MySQL, and PostgreSQL.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| ORM caller to database | Mode and mapping values are untrusted. |
| Migration to database | Schema code must never access source storage. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-MODE-BYPASS | Elevation of Privilege | mode constraint | mitigate | Named check permits external_read_only only. |
| T-01-DUPLICATE-RACE | Tampering | mapping constraints | mitigate | Unique platform and root-relative constraints. |
| T-01-PATH-DISCLOSURE | Information Disclosure | mapping schema | mitigate | No composed absolute mapping path. |
| T-01-SOURCE-CREATE | Tampering | migration | mitigate | No filesystem imports or backfill. |
</threat_model>
<verification>Run model tests and MariaDB/PostgreSQL upgrade-downgrade-upgrade.</verification>
<success_criteria>Immutable mode and relative mapping constraints are portable and enforced.</success_criteria>
<output>After completion, create .planning/phases/01-immutable-storage-foundation/01-01-SUMMARY.md</output>
