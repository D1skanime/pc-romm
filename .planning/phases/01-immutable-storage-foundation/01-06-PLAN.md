---
phase: 01-immutable-storage-foundation
plan: 06
type: execute
wave: 5
depends_on: [01-04, 01-05]
files_modified:
  [
    backend/handler/database/storage_handler.py,
    backend/exceptions/storage_exceptions.py,
    backend/tests/handler/database/test_storage_handler.py,
    backend/tests/models/test_storage.py,
    backend/tools/verify_storage_migrations.py,
  ]
autonomous: true
gap_closure: true
requirements: [ROOT-01, ROOT-04, TEST-01]
must_haves:
  truths:
    - "PostgreSQL can save a mapping without locking an outer-joined relationship"
    - "Ordered serialization still yields one commit and one bounded two-session conflict"
    - "Only named mapping uniqueness constraints become DuplicateStorageMappingError"
    - "Missing identities and unrelated integrity failures remain bounded and distinct"
    - "Model tests cannot drop migrated storage tables needed by later tests"
  artifacts:
    - {
        path: backend/handler/database/storage_handler.py,
        provides: "portable locks and precise persistence errors",
      }
    - {
        path: backend/tests/handler/database/test_storage_handler.py,
        provides: "PostgreSQL, concurrency, and classification regressions",
      }
    - {
        path: backend/tools/verify_storage_migrations.py,
        provides: "isolated PostgreSQL handler execution",
      }
    - {
        path: backend/tests/models/test_storage.py,
        provides: "order-independent fixtures",
      }
  key_links:
    - {
        from: backend/handler/database/storage_handler.py,
        to: models.storage.PlatformStorageMapping,
        via: "separate relationship load and entity-qualified row lock",
      }
    - {
        from: backend/handler/database/storage_handler.py,
        to: backend/exceptions/storage_exceptions.py,
        via: "identity validation and named-constraint classification",
      }
---

<objective>Close both Phase 1 persistence blockers without weakening deterministic portable serialization. Purpose: make mapping saves valid on every supported database and errors bounded and truthful. Output: portable lock query, precise error boundary, PostgreSQL regressions, and order-independent fixtures.</objective>

<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>

<context>@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-immutable-storage-foundation/01-CONTEXT.md
@.planning/phases/01-immutable-storage-foundation/01-RESEARCH.md
@.planning/phases/01-immutable-storage-foundation/01-PATTERNS.md
@.planning/phases/01-immutable-storage-foundation/01-VERIFICATION.md
@.planning/phases/01-immutable-storage-foundation/01-04-SUMMARY.md
@.planning/phases/01-immutable-storage-foundation/01-05-SUMMARY.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify PostgreSQL locking and bounded failures</name>
  <files>backend/tests/handler/database/test_storage_handler.py, backend/tests/models/test_storage.py, backend/tools/verify_storage_migrations.py</files>
  <read_first>backend/handler/database/storage_handler.py, backend/models/storage.py, backend/models/platform.py, backend/tests/conftest.py, backend/tests/handler/database/test_storage_handler.py, backend/tests/models/test_storage.py, backend/tools/verify_storage_migrations.py, .planning/phases/01-immutable-storage-foundation/01-VERIFICATION.md</read_first>
  <behavior>
    - PostgreSQL executes save_mapping and commits without a FOR UPDATE outer-join error; captured SQL locks PlatformStorageMapping only and loads StorageRoot separately.
    - Ordered active-root serialization still produces one commit and one StorageMappingOverlapError in a coordinated two-session conflict.
    - Each named mapping unique constraint becomes DuplicateStorageMappingError; nonexistent platform, missing/raced root, and synthetic unrelated IntegrityError never do and leak no SQL or path.
    - Running model tests before handler tests leaves migrated storage tables available.
  </behavior>
  <action>Write failing regressions first. Extend the existing ephemeral verifier with --handler-tests to run focused handler behavior against PostgreSQL after upgrade, reusing its unique container, dynamic port, cleanup, and no-library-mount isolation. Retain MariaDB concurrency evidence and add PostgreSQL behavior, not DDL-only coverage. Repair test_storage.py so its module fixture never drops Alembic-owned tables; isolate or delete only test-created rows. Include this confirmed fixture-order warning solely because reliable evidence depends on it. Do not expand into CI trigger cleanup or the register_root mode warning.</action>
  <acceptance_criteria>Tests reproduce both blockers and prove PostgreSQL behavior, deterministic conflict, bounded classification, and order independence without touching application DBs, services, Team4s, external storage, or NAS mounts.</acceptance_criteria>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_storage.py tests/handler/database/test_storage_handler.py -x &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects postgresql --handler-tests</automated></verify>
  <done>Regression evidence guards both verified gaps and the fixture poisoning that could invalidate it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement portable locks and precise classification</name>
  <files>backend/handler/database/storage_handler.py, backend/exceptions/storage_exceptions.py</files>
  <read_first>backend/tests/handler/database/test_storage_handler.py, backend/models/storage.py, backend/models/platform.py, backend/exceptions/storage_exceptions.py, backend/handler/database/storage_handler.py, backend/handler/database/platforms_handler.py, backend/decorators/database.py</read_first>
  <behavior>
    - Mapping rows use selectinload(storage_root) with with_for_update(of=PlatformStorageMapping), or an equivalent separate load and entity-qualified PostgreSQL-valid lock.
    - Active roots remain locked in ascending ID order before the current mapping read per D-13 through D-15.
    - Platform and root identities are validated in the same transaction and missing references raise bounded storage-domain errors.
    - Only uq_platform_storage_mappings_platform_id and uq_platform_storage_mappings_root_relative_path translate to DuplicateStorageMappingError across supported DBAPI diagnostics; unknown failures remain unmislabelled.
  </behavior>
  <action>Replace joined eager loading on the locking mapping query with separate relationship loading and qualify the mapping lock per D-25. Preserve ordered root locks, current-read recheck, canonical overlap comparison, and two-session behavior from 01-04. Validate Platform and StorageRoot references transactionally, adding narrowly named bounded storage exceptions if needed. Recognize only the two explicit unique constraint names using constraint_name where available and bounded vendor-specific diagnostics otherwise. Never classify generic IntegrityError, echo DB errors, or weaken locking. Preserve D-11 through D-17, D-24, D-25, D-28, and D-16 non-mutation.</action>
  <acceptance_criteria>Valid saves and conflicts pass on PostgreSQL and MariaDB; named duplicates map correctly while FK and unrelated failures do not.</acceptance_criteria>
  <verify><automated>cd backend &amp;&amp; uv run pytest tests/models/test_storage.py tests/handler/database/test_storage_handler.py tests/handler/filesystem/test_storage_resolver.py -x &amp;&amp; uv run python tools/verify_storage_migrations.py --dialects mariadb mysql postgresql --handler-tests &amp;&amp; uvx ruff check handler/database/storage_handler.py exceptions/storage_exceptions.py tests/handler/database/test_storage_handler.py tests/models/test_storage.py tools/verify_storage_migrations.py</automated></verify>
  <done>Both failed truths close with portable execution, deterministic concurrency, and bounded semantic errors.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                     |
| ---------------------- | --------------------------------------------------------------- |
| Caller to transaction  | Untrusted identities and paths enter locked persistence.        |
| DBAPI to domain errors | Diagnostics may contain SQL, parameters, or deployment data.    |
| Tests to PostgreSQL    | Verification must stay isolated from application and NAS state. |

## STRIDE Threat Register

| Threat ID       | Category               | Component               | Disposition | Mitigation Plan                                                                   |
| --------------- | ---------------------- | ----------------------- | ----------- | --------------------------------------------------------------------------------- |
| T-01-06-LOCK    | Tampering              | mapping query           | mitigate    | Ordered root locks and entity-qualified mapping lock.                             |
| T-01-06-RACE    | Tampering              | save_mapping            | mitigate    | Validate and recheck in one transaction.                                          |
| T-01-06-CLASS   | Repudiation            | IntegrityError boundary | mitigate    | Translate only two named unique constraints.                                      |
| T-01-06-LEAK    | Information Disclosure | DBAPI diagnostics       | mitigate    | Fixed bounded messages; diagnostics remain internal.                              |
| T-01-06-TEST    | Denial of Service      | verifier                | mitigate    | Ephemeral containers, dynamic ports, finally cleanup, no restart or source mount. |
| </threat_model> |

<source_audit>

| Source          | ID                       | Item                                       | Plan  | Status   | Notes                                                 |
| --------------- | ------------------------ | ------------------------------------------ | ----- | -------- | ----------------------------------------------------- |
| GOAL            | -                        | Portable mapping persistence               | 01-06 | COVERED  | PostgreSQL handler execution closes portability.      |
| REQ             | ROOT-01, ROOT-04         | Root identity and relative mapping         | 01-06 | COVERED  | Transaction and schema contracts retained.            |
| REQ             | TEST-01                  | Adversarial bounded evidence               | 01-06 | COVERED  | Dialect, race, classification, order tests.           |
| RESEARCH        | -                        | Portable locking and serialization         | 01-06 | COVERED  | Qualified lock plus separate load.                    |
| CONTEXT         | D-11 to D-17, D-24, D-25 | Atomic mappings, typed errors, portability | 01-06 | COVERED  | All blocker-relevant decisions traced in tasks.       |
| CONTEXT         | D-33 to D-35             | Deferred work                              | NONE  | EXCLUDED | API/UI/consumers, Team4s, NAS, and later work absent. |
| </source_audit> |

<verification>Run model then handler tests, ephemeral PostgreSQL behavior, supported dialect checks, scoped Ruff, then Phase 1 verification.</verification>
<success_criteria>Verification reaches 14/14: PostgreSQL saves succeed, conflicts stay deterministic, and only genuine uniqueness failures receive the duplicate error.</success_criteria>
<output>After completion, create .planning/phases/01-immutable-storage-foundation/01-06-SUMMARY.md</output>
