---
phase: 01-immutable-storage-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/exceptions/storage_exceptions.py, backend/handler/filesystem/storage_resolver.py, backend/tests/handler/filesystem/test_storage_resolver.py]
autonomous: true
requirements: [PATH-01, PATH-04]
must_haves:
  truths: ["Unsafe lexical forms fail before filesystem composition", "Valid archive names are preserved exactly", "Errors are typed and bounded"]
  artifacts:
    - {path: backend/handler/filesystem/storage_resolver.py, provides: "pure path normalization"}
    - {path: backend/exceptions/storage_exceptions.py, provides: "safe domain errors"}
  key_links:
    - {from: backend/handler/filesystem/storage_resolver.py, to: backend/exceptions/storage_exceptions.py, via: "typed rejection"}
---
<objective>Create the pure lexical boundary for central resolution. Purpose: reject hostile path text before I/O while preserving legitimate names. Output: normalization, errors, tests.</objective>
<execution_context>@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/01-immutable-storage-foundation/01-CONTEXT.md
@.planning/phases/01-immutable-storage-foundation/01-RESEARCH.md
@.planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 1: Specify the lexical matrix</name><files>backend/tests/handler/filesystem/test_storage_resolver.py</files><read_first>backend/tests/handler/filesystem/test_base_handler.py, backend/handler/filesystem/base_handler.py, .planning/phases/01-immutable-storage-foundation/01-VALIDATION.md</read_first><action>Create parameterized and Hypothesis tests per D-18, D-19, D-23, D-24, D-30. Reject empty mapping path, leading/trailing/repeated separators, dot/empty segments, traversal variants, POSIX absolute, drive, UNC, device, backslash, NUL, C0 and DEL. Prove spaces, dots, hyphens, umlauts, composed/decomposed Unicode, nested and bounded-long components preserve exact text. Assert safe errors omit unrelated absolute roots.</action><acceptance_criteria>Every lexical category has a named failing test and valid names are not case-folded or Unicode-normalized.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -k "normalize or valid_names or safe_error" -x</automated></verify><done>PATH-01 and PATH-04 are executable before implementation.</done></task>
<task type="auto" tdd="true"><name>Task 2: Implement normalization and exception contracts</name><files>backend/exceptions/storage_exceptions.py, backend/handler/filesystem/storage_resolver.py</files><read_first>backend/exceptions/fs_exceptions.py, backend/tests/handler/filesystem/test_storage_resolver.py, backend/handler/filesystem/base_handler.py, .planning/phases/01-immutable-storage-foundation/01-PATTERNS.md</read_first><action>Create StorageError subclasses distinguishing invalid input, missing/inactive root, missing/non-directory/unreadable target, symlink, escape, writable-root safety per D-24. Implement pure normalize_relative_path validating raw text before Path construction. Mapping empty path always fails; an explicit internal root-resolution contract may allow empty. Retain PATTERNS.md rules: FSHandler is an anti-analog, all errors stay bounded, and this cross-cutting utility must neither mutate storage nor wire later-phase consumers. Never inherit/instantiate mutating FSHandler or call resolve, mkdir, open, tempfile, or mutation helpers here.</action><acceptance_criteria>Focused tests pass with exact valid-name preservation and bounded typed errors.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -k "normalize or valid_names or safe_error" -x</automated></verify><done>Lexical resolution is deterministic and non-mutating.</done></task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Logical path to resolver | Raw text is attacker-controlled. |
| Resolver error to caller | OS paths must not leak. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-TRAVERSAL | Tampering | normalization | mitigate | Reject unsafe forms before Path construction. |
| T-01-PATH-LEAK | Information Disclosure | errors | mitigate | Bounded typed messages omit absolute roots. |
| T-01-ACCIDENTAL-CREATE | Tampering | resolver | mitigate | Independent from FSHandler; pure tests. |
</threat_model>
<verification>Run the lexical test selection and inspect imports for no mutating handler dependency.</verification>
<success_criteria>PATH-01 and PATH-04 pass without I/O.</success_criteria>
<output>After completion, create .planning/phases/01-immutable-storage-foundation/01-02-SUMMARY.md</output>
