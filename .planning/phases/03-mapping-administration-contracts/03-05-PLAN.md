---
phase: 03-mapping-administration-contracts
plan: 05
type: tdd
wave: 4
depends_on: [03-04]
files_modified:
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/endpoints/test_storage.py
autonomous: true
requirements:
  [
    MAP-01,
    MAP-02,
    MAP-03,
    MAP-04,
    MAP-05,
    MAP-06,
    API-03,
    API-04,
    AUD-01,
    AUD-02,
    TEST-02,
  ]
must_haves:
  truths:
    - Admins can inspect, test, create, change, deactivate/remove, reactivate, and audit mappings through typed routes.
    - Test is non-mutating; mutations require version preconditions and return safe 409 conflicts.
    - Audit history is admin-only, newest-first, filter-bound cursor paginated, and path-safe.
    - Mapping removal preserves catalog/source and makes missing mapping explicit.
  artifacts:
    - path: backend/endpoints/responses/storage.py
      provides: Mapping lifecycle, test, audit, and conflict schemas
    - path: backend/endpoints/storage.py
      provides: Thin authorized lifecycle and audit routes
    - path: backend/tests/endpoints/test_storage.py
      provides: Complete lifecycle/auth/conflict/audit HTTP matrix
  key_links:
    - from: backend/endpoints/storage.py
      to: backend/handler/database/storage_handler.py
      via: Actor-aware lifecycle delegation and typed exception translation
---

<objective>
Expose the audited optimistic mapping lifecycle and non-mutating test operation through typed admin APIs.

Purpose: Make the complete mapping administration contract usable without scanner or UI coupling.
Output: Lifecycle/audit schemas, routes, safe conflict mapping, and comprehensive endpoint tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-RESEARCH.md
@.planning/phases/03-mapping-administration-contracts/03-03-SUMMARY.md
@.planning/phases/03-mapping-administration-contracts/03-04-SUMMARY.md
<read_first>
@CLAUDE.md
@backend/endpoints/responses/storage.py
@backend/endpoints/storage.py
@backend/handler/database/storage_handler.py
@backend/handler/auth/dependencies.py
@backend/models/user.py
@backend/tests/endpoints/test_storage.py
</read_first>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED mapping lifecycle and audit API matrix</name>
  <files>backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - Typed mapping read/test/create/update/deactivate/remove/reactivate routes enforce the 401/403/admin matrix.
    - Test validates containment/readability/active conflicts but changes no mapping, audit, catalog, or source state.
    - Duplicate, overlap, and stale-version cases return 409 with stable code, allowlisted IDs, and current version where applicable.
    - Missing required active mapping returns `platform_mapping_missing`, never fallback or empty success.
    - Audit history is newest-first, cursor-paginated, filterable by platform/mapping/action, and contains only D-14/D-15 fields.
  </behavior>
  <action>Extend endpoint tests first. Cover distinct sibling success, equal/ancestor/descendant conflict, active-only reuse, concurrent one-winner behavior, stale update/state changes, immutable actor snapshots, audit rollback, and no-audit reads/tests. Capture source and catalog manifests around every operation. Commit RED as `test(03-05): define mapping administration API matrix`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py -k "mapping or audit or conflict or non_mutating" -x -q'</automated></verify>
  <done>Lifecycle endpoint tests fail only for missing public contracts.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN typed lifecycle and audit endpoints</name>
  <files>backend/endpoints/responses/storage.py, backend/endpoints/storage.py</files>
  <behavior>
    - Request schemas require bounded relative path and positive expected_version where state can change.
    - Actor ID/display snapshot comes from the authenticated request and is passed to the handler mutation.
    - Error translation never serializes exception strings or conflicting paths.
  </behavior>
  <action>Add explicit mapping/test/audit request and response models. Implement separate read, test, create, update, deactivate/remove, reactivate, and audit-list endpoints, all protected and admin-asserted before lookup. Keep testing strictly separate from persistence per D-05. Require expected_version for every update/state change per D-09. Map typed missing/duplicate/overlap/stale exceptions to HTTP 409 with stable allowlisted detail per D-08/D-10/D-12. Serialize only relative path belonging to the requested mapping; audit snapshots follow D-14. Commit GREEN as `feat(03-05): expose audited mapping lifecycle APIs`; refactor only with the full endpoint selection green.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py tests/handler/database/test_storage_handler.py -k "mapping or audit or conflict or non_mutating or version" -x -q'</automated></verify>
  <done>All lifecycle, auth, 409, audit, catalog/source preservation, and no-leak tests pass.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                             |
| ---------------------------------------------- | --------------------------------------------------------------------------------------- |
| Administrator command to transactional handler | Untrusted identifiers, paths, filters, versions, and actor display enter durable state. |

## STRIDE Threat Register

| Threat ID       | Category               | Component               | Disposition | Mitigation Plan                                                                  |
| --------------- | ---------------------- | ----------------------- | ----------- | -------------------------------------------------------------------------------- |
| T-03-13         | Spoofing               | audit actor capture     | mitigate    | Derive immutable actor ID/display only from authenticated request state.         |
| T-03-14         | Tampering              | lifecycle routes        | mitigate    | Require version preconditions and delegate lock/validation atomically.           |
| T-03-15         | Information Disclosure | 409 and audit responses | mitigate    | Stable envelope with allowlisted IDs/current version and bounded snapshots only. |
| </threat_model> |

<verification>Endpoint, handler, source-manifest, and authorization matrices pass; `git diff --check` is clean.</verification>
<success_criteria>All mapping lifecycle and audit operations are available as typed, authorized, conflict-safe APIs.</success_criteria>
<output>After completion, create `.planning/phases/03-mapping-administration-contracts/03-05-SUMMARY.md`.</output>
