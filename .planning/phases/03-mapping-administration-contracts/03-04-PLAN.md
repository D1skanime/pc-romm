---
phase: 03-mapping-administration-contracts
plan: 04
type: tdd
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/main.py
  - backend/tests/endpoints/test_storage.py
autonomous: true
requirements: [API-01, API-02, API-04, AUD-02, TEST-02]
must_haves:
  truths:
    - Anonymous callers receive 401 and authenticated non-admins receive 403 before root lookup or filesystem observation.
    - Administrators receive current path-safe root health and minimal bounded browse pages.
    - Public root/browse schemas cannot serialize container paths, raw errors, or unrelated mapping data.
  artifacts:
    - path: backend/endpoints/responses/storage.py
      provides: Explicit safe root, browse, cursor, and error schemas
    - path: backend/endpoints/storage.py
      provides: Admin-only root and directory-read routes
    - path: backend/main.py
      provides: Single /api/storage router registration
    - path: backend/tests/endpoints/test_storage.py
      provides: Authorization and safe read-contract evidence
  key_links:
    - from: backend/endpoints/storage.py
      to: backend/handler/filesystem/storage_resolver.py
      via: Authorized live health and contained browse delegation
    - from: backend/main.py
      to: backend/endpoints/storage.py
      via: /api router inclusion
---

<objective>
Expose safe administrator-only root inspection and contained directory browsing contracts.

Purpose: Deliver API-01/API-02 without allowing unauthenticated enumeration or leaking deployment paths.
Output: Storage response schemas, registered routes, and endpoint authorization/adversarial tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-RESEARCH.md
@.planning/phases/03-mapping-administration-contracts/03-02-SUMMARY.md
@.planning/phases/03-mapping-administration-contracts/03-03-SUMMARY.md
<read_first>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@backend/endpoints/permissions.py
@backend/endpoints/responses/base.py
@backend/endpoints/responses/permission.py
@backend/decorators/auth.py
@backend/handler/auth/constants.py
@backend/handler/auth/dependencies.py
@backend/main.py
@backend/tests/endpoints/test_permissions_admin.py
@backend/tests/endpoints/test_permissions_me.py
</read_first>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED authorization and root-browser HTTP contracts</name>
  <files>backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - Every route yields anonymous 401 and non-admin 403 before mocked database/filesystem calls execute.
    - Admin root list/detail performs a live probe but emits no UPDATE and returns stored timestamps separately from current health.
    - Admin browse returns bounded minimal entries and next cursor; invalid path/cursor/root failures have stable code and bounded safe message.
    - Sentinel absolute, NAS, unrelated mapping, raw OS, and SQL strings never occur in JSON or captured logs.
  </behavior>
  <action>Create reusable anonymous/viewer/admin fixtures by following the exact 401/403 permission endpoint analogs named in 03-PATTERNS, then write failing endpoint tests. Include route registration, exact status behavior, SQL-update tripwire, cursor replay across roots/parents, traversal/symlink and typed scan-ceiling failures, globally sorted pagination independent of enumeration order, OpenAPI field exclusion, and safe-error sentinels. Commit RED as `test(03-04): define storage read API contracts`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /app/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py -k "root or browse or auth or leak" -x -q'</automated></verify>
  <done>Tests fail only because schemas/routes are not implemented.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN safe root and browse endpoints</name>
  <files>backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/main.py</files>
  <behavior>
    - `protected_route` and `assert_admin` run before lookup/I/O per API-04.
    - Root output uses the pure live-health result per D-01 and excludes container_path.
    - Browse delegates to the resolver and serializes only D-03 fields plus bounded pagination metadata.
  </behavior>
  <action>Create explicit Pydantic models with field/cursor/message bounds and Literal/enum codes, using the `responses/base.py`, permission schema, protected-route, and router-registration analogs in 03-PATTERNS while filling its typed-storage-error-envelope gap. Add one `/storage` router, authorize first, keep endpoints thin, delegate database retrieval and filesystem observation, translate typed storage and `directory_scan_limit_exceeded` exceptions into the safe envelope, and include the router once under `/api`. Never serialize ORM objects generically. Commit GREEN as `feat(03-04): expose safe storage read APIs`, then refactor only with focused tests green.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /app/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py -k "root or browse or auth or leak" -x -q'</automated></verify>
  <done>Admin root/browse APIs pass authorization, pagination, no-write, and path-leak tests.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                    |
| ----------------------------------------------- | ------------------------------------------------------------------------------ |
| Network caller to storage administration router | Caller identity and untrusted path/cursor reach sensitive library enumeration. |

## STRIDE Threat Register

| Threat ID       | Category               | Component              | Disposition | Mitigation Plan                                                    |
| --------------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------ |
| T-03-10         | Elevation of Privilege | storage router         | mitigate    | Apply protected scope and assert_admin before all lookups and I/O. |
| T-03-11         | Information Disclosure | response serialization | mitigate    | Explicit allowlisted schemas and sentinel leak assertions.         |
| T-03-12         | Denial of Service      | browse route           | mitigate    | Enforce hard page/cursor bounds before enumeration.                |
| </threat_model> |

<verification>Focused endpoint and resolver suites pass; route/OpenAPI schema exists; `git diff --check` is clean.</verification>
<success_criteria>Authorized administrators can inspect roots and browse safely while all unauthorized callers learn nothing.</success_criteria>
<output>After completion, create `.planning/phases/03-mapping-administration-contracts/03-04-SUMMARY.md`.</output>
