---
phase: 03-mapping-administration-contracts
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/models/storage.py
  - backend/exceptions/storage_exceptions.py
  - backend/alembic/versions/0109_mapping_administration_contracts.py
  - backend/tests/models/test_storage.py
  - backend/tools/verify_storage_migrations.py
  - backend/tests/tools/test_verify_storage_migrations.py
autonomous: true
requirements: [MAP-02, AUD-01, AUD-02]
must_haves:
  truths:
    - Existing mappings upgrade as active version 1 without changing catalog or source data.
    - Inactive mapping rows retain traceability but no longer reserve a platform or relative path.
    - Audit rows retain immutable actor and mapping snapshots without host-path fields.
  artifacts:
    - path: backend/alembic/versions/0109_mapping_administration_contracts.py
      provides: Portable lifecycle and audit schema migration
    - path: backend/models/storage.py
      provides: Versioned mapping and append-only audit ORM contracts
    - path: backend/exceptions/storage_exceptions.py
      provides: Bounded typed lifecycle, cursor, and conflict exceptions
  key_links:
    - from: backend/models/storage.py
      to: backend/alembic/versions/0109_mapping_administration_contracts.py
      via: Matching columns, lengths, indexes, and constraints
    - from: backend/tools/verify_storage_migrations.py
      to: backend/alembic/versions/0109_mapping_administration_contracts.py
      via: 0108 to 0109 upgrade, downgrade, and re-upgrade assertions
---

<objective>
Establish the portable versioned mapping lifecycle and immutable audit storage required by every Phase 3 contract.

Purpose: Make active-only conflicts, optimistic concurrency, and atomic attribution representable across MariaDB, MySQL, and PostgreSQL.
Output: Migration 0109, ORM contracts, bounded domain exceptions, and cross-dialect migration tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-RESEARCH.md
@.planning/phases/03-mapping-administration-contracts/03-PATTERNS.md
@.planning/phases/03-mapping-administration-contracts/03-VALIDATION.md

<read_first>
@CLAUDE.md
@.claude/skills/backend-development/SKILL.md
@backend/models/base.py
@backend/models/storage.py
@backend/models/platform.py
@backend/models/user.py
@backend/exceptions/storage_exceptions.py
@backend/alembic/versions/0108_immutable_storage_foundation.py
@backend/tools/verify_storage_migrations.py
@backend/tests/models/test_storage.py
@backend/tests/tools/test_verify_storage_migrations.py
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED model and migration lifecycle contract</name>
  <files>backend/tests/models/test_storage.py, backend/tests/tools/test_verify_storage_migrations.py</files>
  <behavior>
    - Existing mapping rows become active with integer version 1 after 0109.
    - Mapping active/version columns and portable supporting indexes exist while unconditional platform and root/path unique constraints do not.
    - Audit rows contain actor ID, bounded actor display snapshot, platform ID, mapping ID snapshot, action, timestamp, and allowlisted old/new root/path/version/active fields only.
    - A pristine 0109 state downgrades to 0108 and re-upgrades on all three dialects; any inactive/versioned/audited lifecycle history aborts downgrade before DDL and remains intact.
  </behavior>
  <action>Write failing model-inspection and migration-verifier tests first, following the exact model/migration analogs in 03-PATTERNS (`backend/models/storage.py` and `0108_immutable_storage_foundation.py`) and filling its append-only-audit gap. Seed an existing 0108 mapping and assert the exact backfill/schema. On every dialect also create a real lifecycle dataset containing an inactive mapping, reused active platform/path state, version greater than 1, and audit history; assert downgrade aborts before any DDL and leaves all 0109 rows/schema intact. Separately prove a pristine upgraded dataset downgrades and re-upgrades. Prove audit history has no cascade-dependent actor or mapping relationship. Commit the failing RED tests as `test(03-01): define mapping lifecycle schema contract`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /app/backend &amp;&amp; uv run pytest tests/models/test_storage.py tests/tools/test_verify_storage_migrations.py -x -q'</automated></verify>
  <done>Tests fail only because 0109, lifecycle fields, and audit storage are not implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN portable 0109 models and bounded exceptions</name>
  <files>backend/models/storage.py, backend/exceptions/storage_exceptions.py, backend/alembic/versions/0109_mapping_administration_contracts.py, backend/tools/verify_storage_migrations.py</files>
  <behavior>
    - Mapping defaults are active and version 1, and inactive rows remain stored.
    - Audit snapshots are scalar, bounded, append-only records and never contain container or NAS paths per D-14 and D-15.
    - Typed errors carry allowlisted IDs/current version with stable bounded messages and never another mapping path per D-04, D-10, and D-12.
  </behavior>
  <action>Implement the ORM and hand-reviewed 0109 migration using the 0108 `_timestamps`, named-constraint, and dependency-order analogs from 03-PATTERNS. Backfill before making fields non-null, remove both unconditional unique constraints, add portable indexes rather than partial-index assumptions, and create audit storage in dependency-safe order. Define action values for create/update/remove/activate/deactivate. Before downgrade DDL, fail closed if any audit exists, any mapping is inactive, any version differs from 1, or restored 0108 uniqueness would fail; never silently discard history or partially downgrade. Add typed missing-mapping, stale-version, duplicate-active, overlap, invalid-cursor, scan-limit, and safe filesystem errors whose public attributes contain only allowlisted identifiers. Update the verifier for pristine round trips and lifecycle preflight rejection on all three dialects, and add `--handler-test-repetitions` as a positive integer used with `--handler-tests` so final MariaDB/PostgreSQL concurrency evidence has an explicit repeat count. Do not alter platform/catalog/source rows. Commit GREEN as `feat(03-01): add versioned mapping audit schema`, then refactor names/helpers without behavior changes and commit only if needed.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /app/backend &amp;&amp; uv run pytest tests/models/test_storage.py tests/tools/test_verify_storage_migrations.py -x -q' &amp;&amp; python3 backend/tools/verify_storage_migrations.py --dialects mariadb mysql postgresql</automated></verify>
  <done>0109 is reversible on all supported dialects, existing mappings are active version 1, audit fields are path-safe, and all RED tests are green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                    |
| --------------------------------------- | -------------------------------------------------------------- |
| Migration input to persistent schema    | Existing database rows cross into new lifecycle semantics.     |
| Authenticated identity to audit storage | Mutable user data is reduced to immutable bounded attribution. |

## STRIDE Threat Register

| Threat ID       | Category               | Component                  | Disposition | Mitigation Plan                                                                        |
| --------------- | ---------------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------- |
| T-03-01         | Tampering              | 0109 migration             | mitigate    | Backfill active/version before non-null enforcement and verify three-dialect cycles.   |
| T-03-02         | Repudiation            | StorageMappingAudit        | mitigate    | Persist immutable actor and before/after scalar snapshots in the mutation transaction. |
| T-03-03         | Information Disclosure | exceptions and audit model | mitigate    | Allowlist bounded IDs/fields and prohibit container paths, raw SQL, and raw OS errors. |
| </threat_model> |

<verification>
Model and migration tests pass, the three-dialect verifier completes, and `git diff --check` is clean.
</verification>

<success_criteria>
All downstream plans can rely on explicit active/version fields, immutable safe audit records, and stable typed domain errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mapping-administration-contracts/03-01-SUMMARY.md`.
</output>
