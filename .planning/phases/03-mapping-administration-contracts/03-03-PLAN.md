---
phase: 03-mapping-administration-contracts
plan: 03
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - backend/handler/database/storage_handler.py
  - backend/handler/database/__init__.py
  - backend/tests/handler/database/test_storage_handler.py
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, MAP-05, MAP-06, AUD-01, AUD-02]
must_haves:
  truths:
    - One active mapping per platform is enforced under concurrent requests while inactive history releases identities.
    - Distinct sibling mappings succeed; equal and ancestor/descendant active overlaps fail safely.
    - Test is non-mutating, lifecycle writes use version preconditions, and every mutation appends one atomic audit row.
    - Removal deactivates configuration only and missing mapping is explicit.
  artifacts:
    - path: backend/handler/database/storage_handler.py
      provides: Transactional lifecycle, test, audit, query, and conflict semantics
    - path: backend/tests/handler/database/test_storage_handler.py
      provides: Concurrency, rollback, audit, catalog, and source immutability evidence
  key_links:
    - from: backend/handler/database/storage_handler.py
      to: backend/models/storage.py
      via: Row locks, version checks, active-only queries, and same-session audit inserts
---

<objective>
Implement the complete transactional mapping lifecycle and audit behavior below HTTP.

Purpose: Centralize all invariants in DBStorageHandler so every API and later consumer receives identical fail-closed behavior.
Output: Tested create/read/test/update/deactivate/reactivate/remove and audit-query services.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-RESEARCH.md
@.planning/phases/03-mapping-administration-contracts/03-PATTERNS.md
@.planning/phases/03-mapping-administration-contracts/03-01-SUMMARY.md
<read_first>
@CLAUDE.md
@backend/decorators/database.py
@backend/handler/database/storage_handler.py
@backend/handler/database/__init__.py
@backend/models/storage.py
@backend/models/platform.py
@backend/models/rom.py
@backend/tests/handler/database/test_storage_handler.py
</read_first>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED lifecycle, concurrency, and atomic-audit matrix</name>
  <files>backend/tests/handler/database/test_storage_handler.py</files>
  <behavior>
    - Create permits contained directories and distinct siblings, but exactly one concurrent active create wins for a platform or overlapping location.
    - Only active rows block duplicates/overlaps; reactivation revalidates conflicts.
    - Update/deactivate/reactivate/remove reject stale expected_version with current version and do not retry or merge.
    - Test performs validation but creates no mapping/audit/catalog/source change.
    - Each successful mutation writes exactly one same-transaction audit; rollback removes both mutation and audit.
    - Removal preserves ROM/catalog rows and source manifest and leaves the platform explicitly unmapped.
  </behavior>
  <action>Write failing handler tests first, including two-session MariaDB/PostgreSQL concurrency fixtures with deterministic completion and repeated execution. Assert IDs, codes, current version, audit allowlist, immutable actor snapshot, active-only conflicts, no raw vendor diagnostics, catalog preservation, and filesystem mutation tripwires. Commit RED as `test(03-03): define mapping lifecycle transaction contract`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/handler/database/test_storage_handler.py -k "lifecycle or version or audit or concurrent or active_only or non_mutating" -x -q'</automated></verify>
  <done>Tests fail only because the Phase 3 handler lifecycle is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN transactional lifecycle and audit query</name>
  <files>backend/handler/database/storage_handler.py, backend/handler/database/__init__.py</files>
  <behavior>
    - Per D-09/D-12, expected integer version is compared under lock and stale commands return typed conflicts without retry.
    - Per D-10/D-11, conflict checks consider active mappings only and reveal IDs rather than paths.
    - Per D-13 through D-16, mutations append allowlisted audit rows atomically while test/read operations do not audit.
  </behavior>
  <action>Extend DBStorageHandler rather than creating parallel persistence. Preserve ordered platform/root/mapping locks, filter conflicts to active rows, validate immediately before flush, increment version on every state change, and translate only recognized constraint diagnostics. Implement explicit create, get-active-for-platform, test, update, deactivate/remove, reactivate, and cursor-paginated newest-first audit query methods with platform/mapping/action filter binding. Test and read methods must not add, flush, commit, audit, touch catalog, or write source. Removal is soft deactivation per D-07 so audit/history remain available and the platform is unmapped. Commit GREEN as `feat(03-03): implement audited mapping lifecycle`; refactor only if all focused tests remain green.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/handler/database/test_storage_handler.py tests/models/test_storage.py -x -q'</automated></verify>
  <done>Lifecycle, concurrent conflict, rollback, audit, catalog-preservation, and source-manifest tests all pass.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                          |
| --------------------------------------------- | -------------------------------------------------------------------- |
| Concurrent administrator commands to database | Independently authenticated writes compete for shared mapping state. |
| Mutation transaction to audit history         | State and attribution must commit or roll back together.             |

## STRIDE Threat Register

| Threat ID       | Category               | Component            | Disposition | Mitigation Plan                                                                                     |
| --------------- | ---------------------- | -------------------- | ----------- | --------------------------------------------------------------------------------------------------- |
| T-03-07         | Tampering              | lifecycle writes     | mitigate    | Deterministic locks, final active-only validation, expected-version comparison, no automatic retry. |
| T-03-08         | Repudiation            | audit append         | mitigate    | Append exactly one actor snapshot and old/new allowlist in the same session.                        |
| T-03-09         | Information Disclosure | conflict translation | mitigate    | Return identifiers/current version only, never relative paths or vendor diagnostics.                |
| </threat_model> |

<verification>Focused handler/model suite passes repeatedly, including real supported-dialect concurrency, and `git diff --check` is clean.</verification>
<success_criteria>All mapping invariants and audits are enforced atomically below the endpoint layer.</success_criteria>
<output>After completion, create `.planning/phases/03-mapping-administration-contracts/03-03-SUMMARY.md`.</output>
