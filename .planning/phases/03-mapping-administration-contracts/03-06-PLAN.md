---
phase: 03-mapping-administration-contracts
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - backend/endpoints/responses/storage.py
  - backend/endpoints/storage.py
  - backend/tests/endpoints/test_storage.py
  - frontend/src/__generated__/
autonomous: true
requirements: [MAP-03, MAP-06, API-01, API-02, API-03, API-04, AUD-02, TEST-02]
must_haves:
  truths:
    - Admin preview is bounded and non-mutating and requires an explicit active mapping.
    - Public OpenAPI describes every storage contract and contains no forbidden deployment-path fields.
    - Focused, cross-dialect, generated-client, and source-manifest gates jointly cover all Phase 3 requirements.
  artifacts:
    - path: backend/endpoints/storage.py
      provides: Bounded non-scanning preview contract
    - path: backend/tests/endpoints/test_storage.py
      provides: Preview and deterministic OpenAPI safety assertions
    - path: frontend/src/__generated__/
      provides: Generated TypeScript representation of backend contracts
  key_links:
    - from: backend/endpoints/storage.py
      to: backend/handler/database/storage_handler.py
      via: Explicit active mapping lookup with no legacy fallback
    - from: backend/endpoints/responses/storage.py
      to: frontend/src/__generated__/
      via: OpenAPI generation pipeline
---

<objective>
Close the Phase 3 contract with bounded non-mutating preview and generated/cross-dialect evidence.

Purpose: Prove every public schema, supported database, and source-safety invariant before Phase 3 verification.
Output: Preview endpoint, OpenAPI assertions/generated types, and complete automated phase gates.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-VALIDATION.md
@.planning/phases/03-mapping-administration-contracts/03-05-SUMMARY.md
<read_first>
@CLAUDE.md
@backend/endpoints/responses/storage.py
@backend/endpoints/storage.py
@backend/tests/endpoints/test_storage.py
@backend/tools/verify_storage_migrations.py
@frontend/package.json
</read_first>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN bounded mapping preview</name>
  <files>backend/endpoints/responses/storage.py, backend/endpoints/storage.py, backend/tests/endpoints/test_storage.py</files>
  <behavior>
    - Preview requires admin and an active mapping; absence returns typed HTTP 409 `platform_mapping_missing`.
    - Preview reports only a capped candidate summary and explicit partial/continuation state without invoking scanner/catalog persistence.
    - Preview performs no source, mapping, audit, or catalog mutation and exposes no absolute paths.
  </behavior>
  <action>Write the failing preview/auth/missing-mapping/mutation-tripwire tests and commit RED as `test(03-06): define bounded mapping preview contract`. Then implement a thin preview route using existing mapping lookup and read-only resolver enumeration. Keep this an administration contract only: do not call scanner jobs, create catalog records, derive legacy paths, or introduce Phase 5 cutover behavior. Bound all counts/work and make partial state explicit. Commit GREEN as `feat(03-06): add non-mutating mapping preview`, then refactor only while tests remain green.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py -k "preview or missing or source_manifest" -x -q'</automated></verify>
  <done>Preview is typed, bounded, admin-only, mapping-required, and proven non-mutating.</done>
</task>
<task type="auto">
  <name>Task 2: Regenerate and verify the authoritative OpenAPI contract</name>
  <files>backend/tests/endpoints/test_storage.py, frontend/src/__generated__/</files>
  <action>Add deterministic OpenAPI assertions for every public root, browse, mapping, test, preview, lifecycle, audit, and safe-error model. Assert forbidden names such as `container_path` and sentinel host/NAS/raw-error text are absent from public storage schemas. Start the backend in the established container environment, run the repository generation command, and never hand-edit generated output. Run frontend typecheck and commit generated changes as `chore(03-06): regenerate storage API contracts`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py -k "openapi or schema or leak" -x -q' &amp;&amp; docker run --rm --network host -v "$PWD/frontend:/app" -w /app node:24-bookworm sh -lc 'npm ci &amp;&amp; npm run generate &amp;&amp; npm run typecheck'</automated></verify>
  <done>Generated clients match backend OpenAPI, typecheck passes, and no forbidden field is public.</done>
</task>
<task type="auto">
  <name>Task 3: Run Phase 3 final automated evidence gates</name>
  <files>backend/tests/endpoints/test_storage.py</files>
  <action>Run the full focused Phase 3 suite, repeat the concurrency selection on MariaDB and PostgreSQL enough to exercise both orderings, run the 0109 migration cycle for MariaDB/MySQL/PostgreSQL, run full backend pytest, and run Trunk plus generated-client typecheck. Confirm source manifests remain identical around every lifecycle/preview operation and no xfail/skip hides a Phase 3 requirement. Fix only Phase 3 regressions in owned files; do not touch UI, scanner cutover, Team4s, or unrelated code. Commit a test-only correction only if required; otherwise record commands/evidence in the summary.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/endpoints/test_storage.py tests/handler/database/test_storage_handler.py tests/handler/filesystem/test_storage_resolver.py tests/models/test_storage.py tests/tools/test_verify_storage_migrations.py -x &amp;&amp; uv run pytest -vv' &amp;&amp; python3 backend/tools/verify_storage_migrations.py --dialects mariadb mysql postgresql &amp;&amp; trunk fmt &amp;&amp; trunk check</automated></verify>
  <done>Focused/full backend, concurrency, three-dialect migration, Trunk, OpenAPI generation, typecheck, leak, and source-mutation gates pass.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                 |
| ---------------------------------------------- | --------------------------------------------------------------------------- |
| Preview request to filesystem/catalog boundary | Read-only administrative inspection approaches later scanner functionality. |
| Backend OpenAPI to generated frontend client   | Public schema crosses language/runtime boundary.                            |

## STRIDE Threat Register

| Threat ID       | Category               | Component               | Disposition | Mitigation Plan                                                        |
| --------------- | ---------------------- | ----------------------- | ----------- | ---------------------------------------------------------------------- |
| T-03-16         | Tampering              | preview route           | mitigate    | Read-only resolver only, mutation tripwires, no scanner/catalog calls. |
| T-03-17         | Denial of Service      | preview enumeration     | mitigate    | Hard work/result caps and explicit partial state.                      |
| T-03-18         | Information Disclosure | OpenAPI/generated types | mitigate    | Deterministic forbidden-field assertions before generation.            |
| </threat_model> |

<verification>Every command in 03-VALIDATION.md phase gates passes and all thirteen requirement IDs map to automated evidence.</verification>
<success_criteria>Phase 3 backend/admin contracts are complete, portable, generated, and independently verifiable without UI or scanner cutover.</success_criteria>
<output>After completion, create `.planning/phases/03-mapping-administration-contracts/03-06-SUMMARY.md`.</output>
