---
phase: 03-mapping-administration-contracts
plan: 02
type: tdd
wave: 2
depends_on: [03-01]
files_modified:
  - backend/handler/filesystem/storage_resolver.py
  - backend/tests/handler/filesystem/test_storage_resolver.py
autonomous: true
requirements: [MAP-03, API-01, API-02, TEST-02]
must_haves:
  truths:
    - Root retrieval can report live health without dirtying or persisting an ORM row.
    - Browsing exposes only immediate contained directories in deterministic bounded pages.
    - Traversal, symlinks, malformed/replayed cursors, and filesystem failures fail with safe bounded errors.
  artifacts:
    - path: backend/handler/filesystem/storage_resolver.py
      provides: Pure health snapshot and contained cursor directory browser
    - path: backend/tests/handler/filesystem/test_storage_resolver.py
      provides: Property, pagination, containment, and no-mutation evidence
  key_links:
    - from: backend/handler/filesystem/storage_resolver.py
      to: backend/exceptions/storage_exceptions.py
      via: Typed bounded resolution and cursor failures
---

<objective>
Build the read-only filesystem primitives for live root status and safe paginated directory browsing.

Purpose: Ensure administration reads observe the external library without mutating database or source state.
Output: Pure health snapshots, query-bound directory cursors, and adversarial resolver tests.
</objective>

<execution_context>
@C:/Users/admin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/admin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-mapping-administration-contracts/03-CONTEXT.md
@.planning/phases/03-mapping-administration-contracts/03-RESEARCH.md
@.planning/phases/03-mapping-administration-contracts/03-PATTERNS.md
@.planning/phases/03-mapping-administration-contracts/03-01-SUMMARY.md
<read_first>
@CLAUDE.md
@backend/handler/filesystem/storage_resolver.py
@backend/exceptions/storage_exceptions.py
@backend/tests/handler/filesystem/test_storage_resolver.py
</read_first>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED live-health and browse contract</name>
  <files>backend/tests/handler/filesystem/test_storage_resolver.py</files>
  <behavior>
    - A health probe returns current reachable/readable/non-writable/checked/error values without changing the supplied StorageRoot.
    - Browse returns only name, normalized relative path, and navigable for immediate directories, sorted deterministically with a hard page cap.
    - Cursor state is versioned and bound to root ID plus normalized parent; malformed, oversized, wrong-root, and wrong-parent cursors are rejected.
    - Source manifest and mutation tripwires remain identical across health and browse operations.
  </behavior>
  <action>Add failing tests before implementation. Cover empty root parent, nested Unicode names, pagination continuation, early-stop enumeration, traversal, absolute/Windows syntax, symlink entries, permission errors, cursor replay, and bounded safe messages. The manifest excludes atime but includes relative name, kind, size, mode, mtime, link target, and content hash. Commit RED as `test(03-02): define safe storage browse contract`.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -k "health_snapshot or browse or cursor or source_manifest" -x -q'</automated></verify>
  <done>New tests fail only for absent pure-health and browse behavior.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN pure health and bounded browsing</name>
  <files>backend/handler/filesystem/storage_resolver.py</files>
  <behavior>
    - Per D-01, every call performs a live metadata/permission observation and returns a value object rather than mutating persisted state.
    - Per D-02 and D-03, browse uses bounded deterministic continuation and minimal entries.
    - Per D-04, no raw filesystem or absolute path reaches the result or error.
  </behavior>
  <action>Extract a pure health-snapshot API while retaining registration compatibility deliberately. Add storage-local cursor encode/decode helpers with strict type/length/version/root/parent validation. Resolve the selected root/parent through existing normalization, no-follow symlink, and containment checks, enumerate immediate directories with `os.scandir`, avoid metadata not required for navigability, sort by a stable normalized key with a deterministic tie-break, and return an explicit next cursor rather than silent truncation. Never write, create, or persist. Commit GREEN as `feat(03-02): add safe live storage browsing`, then refactor only with the full narrow suite green.</action>
  <verify><automated>docker exec romm-dev sh -lc 'cd /romm/backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_resolver.py -x -q'</automated></verify>
  <done>All resolver tests pass and source manifests prove no mutation.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                    |
| --------------------------------------- | -------------------------------------------------------------- |
| API-derived relative path to filesystem | Untrusted path and cursor select content under a trusted root. |

## STRIDE Threat Register

| Threat ID       | Category               | Component             | Disposition | Mitigation Plan                                                                          |
| --------------- | ---------------------- | --------------------- | ----------- | ---------------------------------------------------------------------------------------- |
| T-03-04         | Tampering              | directory resolution  | mitigate    | Normalize, reject symlinks, strictly resolve, and re-check containment on every request. |
| T-03-05         | Information Disclosure | browse errors/results | mitigate    | Minimal allowlisted entry model and bounded typed errors without raw paths.              |
| T-03-06         | Denial of Service      | directory pagination  | mitigate    | Hard limit, bounded cursor, immediate children only, deterministic continuation.         |
| </threat_model> |

<verification>Resolver tests and mutation tripwires pass; `git diff --check` is clean.</verification>
<success_criteria>Filesystem administration primitives satisfy D-01 through D-04 without database or source writes.</success_criteria>
<output>After completion, create `.planning/phases/03-mapping-administration-contracts/03-02-SUMMARY.md`.</output>
