---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/models/rom.py
  - backend/alembic/versions/20260831_add_pc_rom_components.py
  - backend/handler/filesystem/roms_handler.py
  - backend/handler/scan_handler.py
  - backend/tests/models/test_rom.py
  - backend/tests/handler/filesystem/test_roms_handler.py
  - backend/tests/handler/test_scan_handler.py
autonomous: true
requirements: [PCMOD-01, PCMOD-02, PCMOD-03, PCMOD-04, PCSAFE-01, PCTEST-01]
---

<objective>
Add a read-only PC component model and reproducible component manifests to the existing
logical `Rom` / `RomFile` scan path.
</objective>

<threat_model>

- Assets: external PC library bytes, component identity, manifest digests.
- Threats: path traversal, accidental writes, weak-name misclassification, digest
  reuse after a file changes.
- Controls: reuse rooted filesystem validation; never call mutation helpers from the
  PC scan path; reject paths outside the registered root; classify only explicit
  layouts; recompute a strong digest for each persisted manifest member.
- Blocking proof: a before/after fixture tree manifest is byte-identical after the
  complete scan and ambiguous folders persist no guessed component type.
  </threat_model>

<tasks>
<task type="tdd">
<name>Task 1: Define component and manifest persistence</name>
<files>backend/models/rom.py, backend/alembic/versions/20260831_add_pc_rom_components.py, backend/tests/models/test_rom.py</files>
<read_first>backend/models/rom.py; backend/models/base.py; backend/alembic/versions/; backend/tests/models/test_rom.py</read_first>
<action>Add a component aggregate associated with one logical `Rom`, with a component
kind enum covering `base`, `update`, `dlc`, `hotfix`, `language_pack`, `extra`, and
`unresolved`. Add manifest-member persistence with `relative_path`, `size_bytes`, and
`sha256`. Enforce a unique component-relative path per ROM and a unique manifest path
per component. Generate the matching Alembic migration; do not alter existing library
file fields or create a write-back path.</action>
<acceptance_criteria>Model tests prove all seven kinds serialize; duplicate component
paths fail validation; a manifest member retains its exact relative path, byte size,
and SHA-256; `alembic upgrade head` creates the new schema without modifying fixtures.</acceptance_criteria>
</task>

<task type="tdd">
<name>Task 2: Discover only explicit PC component layouts and build manifests</name>
<files>backend/handler/filesystem/roms_handler.py, backend/handler/scan_handler.py, backend/tests/handler/filesystem/test_roms_handler.py, backend/tests/handler/test_scan_handler.py</files>
<read_first>backend/handler/filesystem/roms_handler.py; backend/handler/scan_handler.py; backend/models/rom.py; backend/handler/filesystem/platforms_handler.py</read_first>
<action>Introduce a pure PC component-layout parser used by the scanner. Accept only
configured explicit top-level component folders or marker names and return
`unresolved` for all other nested layouts. Enumerate every regular file beneath each
component through the existing rooted handler, store a root-relative manifest path,
byte size, and SHA-256, and pass the result to persistence. Do not call mkdir, rename,
unlink, extract, upload, or resource-writing code.</action>
<acceptance_criteria>Fixture tests classify one base, update, DLC, hotfix,
language-pack, and extra folder; every expected member has exact path/size/SHA-256;
an ambiguous folder returns `unresolved`; path traversal is rejected; the source-tree
before/after digest fixture is identical.</acceptance_criteria>
</task>

<task type="execute">
<name>Task 3: Reconcile repeated scans without source mutation</name>
<files>backend/handler/scan_handler.py, backend/tests/handler/test_scan_handler.py</files>
<read_first>backend/handler/scan_handler.py; backend/handler/database/roms_handler.py; backend/tests/handler/test_scan_handler.py</read_first>
<action>Reconcile PC components by their normalized component-relative path and
manifest members by their relative path. Replace only database rows for a changed
manifest in the scan transaction; preserve unchanged rows. Emit an explicit scan log
message for unresolved layouts and do not queue metadata application from this task.</action>
<acceptance_criteria>Two scans of an unchanged fixture produce the same component and
manifest identities; changing one fixture file changes only that member's digest after
rescan; scan logs contain `unresolved PC component layout` for ambiguity; focused
filesystem and scanner test commands exit 0.</acceptance_criteria>
</task>
</tasks>

<verification>
Run focused backend model, filesystem, and scan tests; run the generated migration
against the test database; run `trunk check backend/models/rom.py backend/handler/filesystem/roms_handler.py backend/handler/scan_handler.py`.
</verification>

<success_criteria>

- A single PC `Rom` has explicit component rows and per-component SHA-256 manifests.
- No uncertain layout is silently categorized.
- The read-only fixture tree is identical before and after scans.
  </success_criteria>
