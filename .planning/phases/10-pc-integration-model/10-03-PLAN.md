---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - frontend/src/services/api/rom.ts
  - frontend/src/v2/components/GameDetails/PcComponents.vue
  - frontend/src/v2/components/GameDetails/PcMetadataReview.vue
  - frontend/src/v2/views/GameDetails.vue
  - frontend/src/v2/components/GameDetails/PcComponents.test.ts
  - frontend/src/v2/components/GameDetails/PcMetadataReview.test.ts
  - frontend/e2e/pc-integration-model.spec.ts
autonomous: true
requirements:
  [PCMOD-01, PCMOD-04, PCMETA-01, PCMETA-02, PCLB-02, PCSAFE-01, PCTEST-01]
---

<objective>
Make PC components, immutable manifests, review-first metadata selection, and selected
media visible and testable in the existing v2 game-details flow.
</objective>

<threat_model>

- Assets: selected metadata correctness and user trust in read-only source status.
- Threats: a deceptive auto-apply UI, unsafe rendering of provider media, and a UI
  action that implies file repair or mutation.
- Controls: apply button remains disabled until one candidate is selected; display
  provider-attributed media through existing safe media rendering; offer no filesystem
  mutation controls; errors retain current ROM details.
  </threat_model>

<tasks>
<task type="tdd">
<name>Task 1: Add typed PC metadata API client and component details presentation</name>
<files>frontend/src/services/api/rom.ts, frontend/src/v2/components/GameDetails/PcComponents.vue, frontend/src/v2/components/GameDetails/PcComponents.test.ts, frontend/src/v2/views/GameDetails.vue</files>
<read_first>frontend/src/services/api/rom.ts; frontend/src/v2/views/GameDetails.vue; frontend/src/v2/components/GameDetails/FilesTab/; frontend/src/__generated__/</read_first>
<action>Add typed client functions for `pc-metadata-candidates` and
`pc-metadata-selection` using generated request/response types. Render a PC component
section only for PC-ROM responses, grouped in this exact order: Base game, Updates,
DLC, Hotfixes, Language packs, Extras, Needs classification. Expandable manifest rows
show relative path, byte size, and SHA-256 and contain no edit/delete/repair action.</action>
<acceptance_criteria>Vitest renders every group in the required order; an unresolved
component displays `Needs classification`; expanded rows show all three manifest
values; source inspection finds no frontend API method that writes a library path.</acceptance_criteria>
</task>

<task type="tdd">
<name>Task 2: Build the review-first metadata interaction</name>
<files>frontend/src/v2/components/GameDetails/PcMetadataReview.vue, frontend/src/v2/components/GameDetails/PcMetadataReview.test.ts, frontend/src/v2/views/GameDetails.vue</files>
<read_first>.planning/phases/10-pc-integration-model/10-UI-SPEC.md; frontend/src/v2/views/Scan.vue; frontend/src/v2/components/Scan/; frontend/src/v2/lib/index.ts</read_first>
<action>Add the `Find metadata` action and a v2 dialog/surface that lists
provider-attributed candidates. Keep `Apply selected metadata` disabled until exactly
one candidate is selected. Show `No metadata matches found` for an empty response and
`This metadata source is unavailable. Try again or choose another source.` per
provider failure. After successful apply, refresh the game details and show selected
eligible LaunchBox media with its source label.</action>
<acceptance_criteria>Vitest proves no POST occurs before selection; selecting one row
enables apply; apply calls the typed client with candidate ID and expected version;
empty and provider-error copy matches the UI-SPEC exactly; selected LaunchBox media
shows a `LaunchBox` source label.</acceptance_criteria>
</task>

<task type="execute">
<name>Task 3: Cover the end-to-end read-only operator path</name>
<files>frontend/e2e/pc-integration-model.spec.ts</files>
<read_first>frontend/e2e/operational-immutability.spec.ts; frontend/e2e/fixtures/; .planning/phases/10-pc-integration-model/10-VALIDATION.md</read_first>
<action>Create an isolated PC-library fixture and E2E flow that scans it, opens the
PC game details, expands a component manifest, opens `Find metadata`, selects a
candidate, applies it, and verifies title/description/media source. Capture the
fixture-tree digest before and after the flow and fail when it differs. Do not use a
real NAS, Team4s, or a live production stack.</action>
<acceptance_criteria>The E2E test exits 0 against the isolated fixture; it observes
the review-before-apply control sequence; it asserts base/update/DLC data and selected
metadata visibility; before/after fixture digests are equal.</acceptance_criteria>
</task>
</tasks>

<verification>
Run focused Vitest tests, the isolated Playwright test, generated-type validation after
API changes, and `trunk check` on all changed frontend files.
</verification>

<success_criteria>

- An operator can inspect all PC component groups and immutable manifests in v2.
- A metadata candidate is visibly selected before application.
- Provider failure and unresolved layouts are clear and non-destructive.
  </success_criteria>
