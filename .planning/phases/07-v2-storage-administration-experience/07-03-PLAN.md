---
phase: 07-v2-storage-administration-experience
plan: 03
wave: 3
depends_on: [07-02]
files_modified:
  - frontend/src/v2/components/Storage
  - frontend/src/v2/locales
requirements: [UI-04, UI-06]
autonomous: true
---

# Plan 07-03: Recovery, Removal, and Accessibility

<objective>
Finish resilient recovery states, confirmed mapping removal, and accessible responsive interaction for storage administration.

Purpose: failure states remain understandable and safe, while destructive actions and mobile/keyboard operation are trustworthy.
Output: recovery UI, confirmation flow, translations, and focused verification.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add safe recovery and preview state handling</name>
  <files>frontend/src/v2/components/Storage</files>
  <action>Render clear local states for loading, empty roots, unreachable roots, forbidden actions, failed tests, pending preview, partial preview, complete preview, and stale preview. For unreachable roots expose Check again and a safe explanation only; never offer infrastructure changes. Retain failed-test drafts. While refresh occurs, keep the last preview visibly available but label it stale until fresh data arrives.</action>
  <verify>Run focused component tests for each recovery state and stale-preview retention.</verify>
  <done>Failure and transitional states guide administrators to safe recovery without losing useful context.</done>
</task>

<task type="auto">
  <name>Task 2: Require confirmation for removal and finish accessible responsive controls</name>
  <files>frontend/src/v2/components/Storage, frontend/src/v2/locales</files>
  <action>Implement a distinct confirmation step before removing a platform mapping. State clearly that original files are unchanged, keep the action reversible only through a new mapping, and refresh the overview after completion. Apply existing v2 accessibility and localization conventions: keyboard-operable browsing and actions, visible focus, translated status text, responsive layout, and touch targets of at least 44px where interactive controls are used.</action>
  <verify>Run focused interaction tests for removal confirmation/cancel, keyboard flow, and responsive/accessibility checks; run frontend typecheck and git diff --check.</verify>
  <done>Mapping removal is explicit and safe, and the workflow works with keyboard, localization, and narrow viewports.</done>
</task>
</tasks>

<verification>
- UI-04 recovery feedback and UI-06 administrator/accessibility safeguards are covered.
- Removing a mapping never promises or performs deletion of original files.
- Full end-to-end environment validation remains a Phase 9 activity by approved project decision.
</verification>
