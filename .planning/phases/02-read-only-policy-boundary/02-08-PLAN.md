---
phase: 02-read-only-policy-boundary
plan: 08
type: execute
wave: 6
depends_on: [02-05, 02-06, 02-07]
files_modified:
  [
    backend/handler/filesystem/storage_inventory.py,
    backend/tests/handler/filesystem/test_storage_inventory.py,
  ]
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-04, SAFE-06, TEST-03]
must_haves:
  truths:
    [
      "A closed post-enforcement inventory maps every read and mutation symbol to exact capability, enforcement, and runnable evidence",
      "All denials precede I/O",
      "Every claim has runnable evidence",
    ]
  artifacts:
    - {
        path: backend/handler/filesystem/storage_inventory.py,
        provides: "closed registry",
      }
    - {
        path: backend/tests/handler/filesystem/test_storage_inventory.py,
        provides: "AST and evidence gate",
      }
  key_links:
    - {
        from: backend/tests/handler/filesystem/test_storage_inventory.py,
        to: backend/handler/filesystem/storage_inventory.py,
        via: "discovery comparison",
      }
    - {
        from: backend/handler/filesystem/storage_inventory.py,
        to: backend/handler/filesystem/storage_access.py,
        via: "exact read capability",
      }
    - {
        from: backend/handler/filesystem/storage_inventory.py,
        to: backend/handler/filesystem/storage_policy.py,
        via: "mutation enforcement",
      }
---

<objective>A closed post-enforcement inventory maps every read and mutation symbol to exact capability, enforcement, and runnable evidence</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/02-read-only-policy-boundary/02-CONTEXT.md
@.planning/phases/02-read-only-policy-boundary/02-RESEARCH.md
@.planning/phases/02-read-only-policy-boundary/02-VALIDATION.md</context>
<tasks><task type="auto" tdd="true"><name>Task 02-08-01: Register complete enforcement evidence</name><files>backend/handler/filesystem/storage_inventory.py, backend/tests/handler/filesystem/test_storage_inventory.py</files><behavior>Per D-15, every discovered mutation seam is classified exactly as policy-governed or structurally RomM-owned-only, with runnable evidence. Rows name module/symbol, source/destination class, exact operations and pytest evidence. Reads include the storage_composition legacy descriptor provider plus firmware/platform/heartbeat/config/scan/socket/watcher/ROM/hash/stream/direct/ZIP. Mutations include endpoints/archive/ZIP/patch/export/audio/sync/watcher/bootstrap/cleanup/tasks. Migrations are explicit non-runtime exclusions.</behavior><action>Build the complete D-15 mutation inventory from discovery, not a selected sample. Require every runtime seam to declare exactly one disposition: policy-governed with its enforcement link, or structurally owned-only with a test proving it cannot address an external root. Rows name module/symbol, source/destination class, exact operations and pytest evidence. Reads include the storage_composition legacy descriptor provider plus firmware/platform/heartbeat/config/scan/socket/watcher/ROM/hash/stream/direct/ZIP. Mutations include endpoints/archive/ZIP/patch/export/audio/sync/watcher/bootstrap/cleanup/tasks. Migrations are explicit non-runtime exclusions. Implement D-04 through D-16 as applicable, with exact absent-call evidence and no Phase 3 or Phase 5 scope drift.</action><acceptance_criteria>Every discovered mutation seam has exactly one governed or structurally owned-only row with runnable evidence.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_inventory.py -x</automated></verify><done>D-15 is covered. Rows name module/symbol, source/destination class, exact operations and pytest evidence. Reads include the storage_composition legacy descriptor provider plus firmware/platform/heartbeat/config/scan/socket/watcher/ROM/hash/stream/direct/ZIP. Mutations include endpoints/archive/ZIP/patch/export/audio/sync/watcher/bootstrap/cleanup/tasks. Migrations are explicit non-runtime exclusions.</done></task><task type="auto" tdd="true"><name>Task 02-08-02: Fail closed on unknown seams and adapters</name><files>backend/handler/filesystem/storage_inventory.py, backend/tests/handler/filesystem/test_storage_inventory.py</files><behavior>Per D-15, any missing or unclassified mutation seam blocks completion. AST discovery fails on unlisted filesystem/subprocess/LIBRARY_BASE_PATH consumers, any consumer not linked to the immutable storage_composition descriptor, missing evidence, raw external Paths, FileResponse/FileRedirectResponse/X-Accel conversion, or unexplained exclusion; comments do not count.</behavior><action>Enforce exact equality between discovered mutation seams and inventory rows. Fail on omissions, duplicates, unclassified rows, rows without exactly one policy-governed or structurally owned-only disposition, or owned-only claims without runnable structural evidence. AST discovery fails on unlisted filesystem/subprocess/LIBRARY_BASE_PATH consumers, any consumer not linked to the immutable storage_composition descriptor, missing evidence, raw external Paths, FileResponse/FileRedirectResponse/X-Accel conversion, or unexplained exclusion; comments do not count. Implement D-04 through D-16 as applicable, with exact absent-call evidence and no Phase 3 or Phase 5 scope drift.</action><verify><automated>cd backend &amp;&amp; uv run pytest tests/handler/filesystem/test_storage_inventory.py -x</automated></verify><done>AST discovery fails on unlisted filesystem/subprocess/LIBRARY_BASE_PATH consumers, any consumer not linked to the immutable storage_composition descriptor, missing evidence, raw external Paths, FileResponse/FileRedirectResponse/X-Accel conversion, or unexplained exclusion; comments do not count.</done></task></tasks>
<threat_model><trust_boundaries>Runtime caller to trusted storage authority.</trust_boundaries><threats>Inventory gaps, fallback success, destination confusion, and mount reliance are HIGH and block completion; mitigate with closed evidence and pre-I/O tests.</threats></threat_model>
<verification>The suite passes only when discovered mutation seams exactly equal classified rows and each row has runnable governed or structurally owned-only evidence; every D-15 unclassified entry fails the gate.</verification>
<success_criteria>A closed post-enforcement inventory maps every read and mutation symbol to exact capability, enforcement, and runnable evidence; all mapped decisions, requirements, and threats pass.</success_criteria>
<output>After completion create .planning/phases/02-read-only-policy-boundary/02-08-SUMMARY.md</output>
