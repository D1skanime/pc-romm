---
phase: 02-read-only-policy-boundary
plan: 11
type: tdd
wave: 9
depends_on: [02-10]
files_modified:
  - backend/tests/handler/filesystem/test_storage_inventory.py
autonomous: true
gap_closure: true
requirements: [SAFE-01, SAFE-04, TEST-03]
must_haves:
  truths:
    - "Independent AST discovery resolves ImportFrom aliases and calls through those aliases to _create_external_descriptor"
    - "Any private-factory import or call outside trusted composition is rejected, including aliased forms"
    - "Every typed StorageRoot parameter that is branched on or whose container_path is accessed is discovered regardless of function name"
    - "Focused inventory, full Phase 2, dual-mount, and scoped Trunk evidence remain green"
  artifacts:
    - path: backend/tests/handler/filesystem/test_storage_inventory.py
      provides: "alias-aware and type-driven independent authority-seam discovery with adversarial mutants"
  key_links:
    - from: backend/tests/handler/filesystem/test_storage_inventory.py
      to: backend/handler/filesystem/storage_policy.py
      via: "ImportFrom alias resolution and aliased call inspection"
      pattern: "ImportFrom|asname|_create_external_descriptor"
    - from: backend/tests/handler/filesystem/test_storage_inventory.py
      to: backend/models/storage.py
      via: "typed StorageRoot parameter analysis independent of function names"
      pattern: "StorageRoot|container_path|isinstance"
---

<objective>Close the single remaining Phase 2 verification gap by making independent AST authority discovery alias-aware and exhaustive for typed raw-root use. Purpose: restore the fail-closed inventory proof required by D-04, D-13, D-15, SAFE-04, and the Phase 2 goal without changing the already-sound production policy. Output: adversarial RED/GREEN tests and refreshed complete Phase 2 evidence.</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-read-only-policy-boundary/02-CONTEXT.md
@.planning/phases/02-read-only-policy-boundary/02-VALIDATION.md
@.planning/phases/02-read-only-policy-boundary/02-VERIFICATION.md
@.planning/phases/02-read-only-policy-boundary/02-08-SUMMARY.md
@.planning/phases/02-read-only-policy-boundary/02-09-SUMMARY.md
@.planning/phases/02-read-only-policy-boundary/02-10-PLAN.md
@.planning/phases/02-read-only-policy-boundary/02-10-SUMMARY.md
@backend/handler/filesystem/storage_inventory.py
@backend/tests/handler/filesystem/test_storage_inventory.py

<interfaces>
`AUTHORITY_FACTORY` is `_create_external_descriptor`. `AUTHORITY_PROVIDER` is `EXTERNAL_AUTHORITY_PROVIDER`, currently `(handler.filesystem.storage_composition, build_storage_composition)`. `_authority_seams(root: Path) -> set[tuple[str, str, str]]` scans runtime Python files and the equality gate permits exactly `(*AUTHORITY_PROVIDER, "descriptor_factory")`. Runtime discovery excludes tests, tools, alembic, and caches. Production policy and composition are already sound after 02-10; modify them only if a newly failing non-mutant production test demonstrates a real defect.
</interfaces>
</context>

<feature>
  <name>Alias-aware, function-name-independent authority seam discovery</name>
  <files>backend/tests/handler/filesystem/test_storage_inventory.py</files>
  <behavior>
    - Mutant A contains an aliased or literal `ImportFrom` of `_create_external_descriptor` and no call; its import seam tuple alone is discovered for its enclosing function.
    - Mutant B lives in a separate module and enclosing function, imports `_create_external_descriptor as make_descriptor`, and calls `make_descriptor(...)`; its call seam tuple is independently discovered.
    - A neutrally named function such as `select(root: StorageRoot)` that returns or otherwise accesses `root.container_path` is discovered as `raw_storage_root`.
    - Any neutrally named function with a typed `StorageRoot` parameter that branches on that parameter, including an `isinstance` branch, is discovered as `raw_storage_root`.
    - The current trusted `build_storage_composition` provider remains the sole allowed runtime descriptor-factory seam and current production source produces no raw-storage-root seam.
  </behavior>
  <implementation>Use a RED commit first. Replace or extend the existing seeded mutant test with separate fixtures that prove the exact verifier evasions: an `ImportFrom` alias plus call through the alias, and neutrally named typed `StorageRoot` functions for both `container_path` access and branching. Run the focused inventory test and require assertion failures caused by missing seams, not syntax/import/setup errors. In GREEN, build a per-module alias map from `ast.ImportFrom` nodes for `_create_external_descriptor`, inspect imports and calls using every resolved local alias, and associate each forbidden occurrence with its enclosing function. Analyze each function's typed parameters, including supported annotation forms already present in the repository, identify the actual StorageRoot parameter names, then detect branching on those names or attribute access to their `container_path` without consulting the function name. Preserve the exact trusted-provider equality contract and runtime exclusions. Do not weaken the allowlist, add filename/function-name heuristics, or implement Phase 3 mapping contracts or Phase 5 database identity lookup/consumer cutover. Keep production files unchanged unless the strengthened test exposes a demonstrable production violation; if that happens, stop and document the evidence before expanding files_modified.</implementation>
</feature>

<tasks>
<task type="auto" tdd="true">
  <name>Task 02-11-01: RED - seed aliased factory and neutral raw-root evasions</name>
  <files>backend/tests/handler/filesystem/test_storage_inventory.py</files>
  <read_first>backend/tests/handler/filesystem/test_storage_inventory.py, backend/handler/filesystem/storage_inventory.py, backend/handler/filesystem/storage_policy.py, backend/handler/filesystem/storage_composition.py, backend/models/storage.py</read_first>
  <behavior>Mutant A's unused private-factory ImportFrom tuple, mutant B's alias-call tuple in a separate module/function, and the neutrally named typed StorageRoot container-path/branch tuples must each appear independently in the discovered seam set.</behavior>
  <action>Per D-04, D-13, and D-15, add four explicit adversarial fixtures. Factory mutant A must define a function containing an aliased or literal `ImportFrom` of `_create_external_descriptor` and must never call the imported binding. Factory mutant B must be in a different module and different enclosing function, import `_create_external_descriptor as make_descriptor`, and call `make_descriptor(...)`. Keep the neutral-name typed `StorageRoot` container_path and branch mutants. Assert mutant A's exact `(module, function, "descriptor_factory")` import seam tuple separately from mutant B's exact `(different_module, different_function, "descriptor_factory")` call seam tuple, then assert both exact raw-root tuples. Do not use one combined import-plus-call fixture as proof of import discovery. Run the focused inventory test through the proven MariaDB-network and temporary-Valkey command. Require RED on current code for both factory assertions and both neutral-name assertions, caused by absent tuples rather than syntax/import/setup errors. Commit as `test(02-11): expose authority discovery alias gaps`.</action>
  <verify>
    <automated>redis_name=romm-phase02-11-red-$$; docker run -d --rm --name "$redis_name" --network container:romm-db-dev valkey/valkey:8 >/dev/null || exit 1; trap 'docker stop "$redis_name" >/dev/null 2>&amp;1 || true' EXIT; docker run --rm --entrypoint pytest --network container:romm-db-dev -e DB_HOST=127.0.0.1 -e DB_PORT=3306 -e DB_USER=romm -e DB_PASSWD=romm -e DB_NAME=romm -e REDIS_HOST=127.0.0.1 -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -v "$PWD/backend:/app/backend" -w /app/backend romm-romm-dev tests/handler/filesystem/test_storage_inventory.py -x</automated>
  </verify>
  <done>The focused test has well-formed seeded alias-import/call and neutral raw-root mutants and fails only because current discovery returns incomplete seam tuples.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-11-02: GREEN - resolve aliases and typed raw-root use structurally</name>
  <files>backend/tests/handler/filesystem/test_storage_inventory.py</files>
  <read_first>backend/tests/handler/filesystem/test_storage_inventory.py, backend/handler/filesystem/storage_inventory.py</read_first>
  <behavior>Discovery structurally resolves aliased factory imports/calls and every typed StorageRoot parameter branch or container_path access, while the production equality gate still permits only trusted composition.</behavior>
  <action>Implement the feature's AST rules in `_authority_seams()` without function-name substring gates. Resolve the local binding created by each relevant `ImportFrom`, emit its seam even when the binding is never called, and independently inspect calls through the binding in the separate mutant module/function. Reject both forms outside `AUTHORITY_PROVIDER`. Derive typed StorageRoot parameter names from the function signature, then inspect branch tests and `container_path` attributes rooted in those names. Keep discovery independent from `INVENTORY` and retain all existing runtime exclusions. Require GREEN for the exact unused-import tuple, exact separate alias-call tuple, both neutral-name tuples, and the unchanged production equality `_authority_seams(BACKEND) == {(*AUTHORITY_PROVIDER, "descriptor_factory")}`. Run the focused suite until GREEN, then commit as `feat(02-11): close authority AST discovery`.</action>
  <verify>
    <automated>redis_name=romm-phase02-11-green-$$; docker run -d --rm --name "$redis_name" --network container:romm-db-dev valkey/valkey:8 >/dev/null || exit 1; trap 'docker stop "$redis_name" >/dev/null 2>&amp;1 || true' EXIT; docker run --rm --entrypoint pytest --network container:romm-db-dev -e DB_HOST=127.0.0.1 -e DB_PORT=3306 -e DB_USER=romm -e DB_PASSWD=romm -e DB_NAME=romm -e REDIS_HOST=127.0.0.1 -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -v "$PWD/backend:/app/backend" -w /app/backend romm-romm-dev tests/handler/filesystem/test_storage_inventory.py -x</automated>
  </verify>
  <done>All seeded evasions are discovered, current runtime equality remains exactly composition-only, and the focused inventory suite passes.</done>
</task>

<task type="auto">
  <name>Task 02-11-03: Re-run complete Phase 2 and dual-mount evidence</name>
  <files>backend/tests/handler/filesystem/test_storage_inventory.py</files>
  <read_first>.planning/phases/02-read-only-policy-boundary/02-10-PLAN.md, .planning/phases/02-read-only-policy-boundary/02-VALIDATION.md, backend/tools/verify_read_only_policy.py, backend/docker-compose.policy-test.yml</read_first>
  <action>Run the focused inventory gate first, then the exact complete Phase 2 suite proven by 02-10, including `tests/utils/test_rom_patcher.py`, in the disposable repository image sharing the `romm-db-dev` MariaDB network namespace with a temporary Valkey sidecar. Run the dual-mount verifier on the host. Run scoped Trunk format and check only on the modified inventory test via the proven cached launcher, then rerun the inventory test after formatting. Any mutant escape, runtime equality mismatch, full-suite failure, mount-manifest mismatch, touched tripwire, or Trunk issue blocks completion. Do not change production policy merely to satisfy test implementation details.</action>
  <verify>
    <automated>redis_name=romm-phase02-11-final-$$; docker run -d --rm --name "$redis_name" --network container:romm-db-dev valkey/valkey:8 >/dev/null || exit 1; trap 'docker stop "$redis_name" >/dev/null 2>&amp;1 || true' EXIT; phase2_pytest() { docker run --rm --entrypoint pytest --network container:romm-db-dev -e DB_HOST=127.0.0.1 -e DB_PORT=3306 -e DB_USER=romm -e DB_PASSWD=romm -e DB_NAME=romm -e REDIS_HOST=127.0.0.1 -e REDIS_PORT=6379 -e REDIS_DB=0 -e REDIS_SSL=false -v "$PWD/backend:/app/backend" -w /app/backend romm-romm-dev "$@"; }; phase2_pytest tests/handler/filesystem/test_storage_inventory.py -x &amp;&amp; phase2_pytest tests/handler/filesystem/test_storage_policy.py tests/handler/filesystem/test_storage_access.py tests/handler/filesystem/test_owned_storage.py tests/handler/filesystem/test_external_read_consumers.py tests/handler/filesystem/test_firmware_handler.py tests/handler/filesystem/test_platforms_handler.py tests/endpoints/test_heartbeat.py tests/config/test_config_loader.py tests/endpoints/sockets/test_scan.py tests/handler/filesystem/test_roms_handler.py tests/endpoints/test_streaming.py tests/endpoints/roms/test_files.py tests/endpoints/roms/test_rom.py tests/endpoints/test_storage_policy_denials.py tests/endpoints/roms/test_upload.py tests/endpoints/roms/test_manual.py tests/endpoints/roms/test_screenshot.py tests/endpoints/roms/test_soundtrack.py tests/endpoints/test_platform.py tests/tasks/test_storage_policy.py tests/handler/filesystem/test_storage_inventory.py tests/utils/test_archives.py tests/utils/test_zip_cache.py tests/utils/test_gamelist_exporter.py tests/utils/test_pegasus_exporter.py tests/utils/test_audio_tags.py tests/utils/test_rom_patcher.py tests/handler/filesystem/test_sync_handler.py tests/test_sync_watcher.py tests/tasks/test_cleanup_orphaned_resources.py tests/tasks/test_cleanup_upload_tmp.py tests/tasks/test_cleanup_zip_cache.py tests/tasks/test_sync_push_pull.py -x &amp;&amp; python3 backend/tools/verify_read_only_policy.py &amp;&amp; /home/d1sk/.cache/trunk/cli/1.25.0-linux-x86_64/trunk fmt backend/tests/handler/filesystem/test_storage_inventory.py &amp;&amp; /home/d1sk/.cache/trunk/cli/1.25.0-linux-x86_64/trunk check backend/tests/handler/filesystem/test_storage_inventory.py &amp;&amp; phase2_pytest tests/handler/filesystem/test_storage_inventory.py -x</automated>
  </verify>
  <done>Focused inventory, full Phase 2 including ROM patcher, dual-mount parity, scoped Trunk, and post-format inventory all pass with both verifier-seeded mutant families rejected.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                                            |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| runtime module to descriptor authority    | Any runtime import/call of the private factory must be discovered and limited to trusted composition.                  |
| typed StorageRoot to filesystem authority | Any function branching on a typed raw root or reading its container path must be discovered independently of its name. |

## STRIDE Threat Register

| Threat ID        | Category               | Component               | Disposition | Mitigation Plan                                                                                       |
| ---------------- | ---------------------- | ----------------------- | ----------- | ----------------------------------------------------------------------------------------------------- |
| T-02-11-ALIAS    | Elevation of Privilege | `_authority_seams()`    | mitigate    | Resolve `ImportFrom` bindings and calls through aliases, then enforce the exact composition-only set. |
| T-02-11-NEUTRAL  | Elevation of Privilege | `_authority_seams()`    | mitigate    | Analyze typed parameter use structurally, without function-name heuristics.                           |
| T-02-11-EVIDENCE | Repudiation            | final verification gate | mitigate    | Preserve RED/GREEN commits and rerun focused, full, dual-mount, Trunk, and post-format evidence.      |
| </threat_model>  |

<verification>RED must separately fail the exact unused-import assertion and the exact alias-call assertion from different modules/functions on current code, plus both typed StorageRoot neutral-name assertions. GREEN must pass all four mutant assertions and the unchanged composition-only production equality gate. Final verification must pass the exact 02-10 full suite, dual-mount verifier, scoped Trunk, and post-format inventory rerun.</verification>

<success_criteria>The independent AST gate detects a forbidden unused private-factory import and, separately, an alias call in another module/function, plus every typed StorageRoot branch/container_path access regardless of function name; trusted composition remains the only runtime authority provider; all Phase 2 evidence passes; no Phase 3 or Phase 5 scope is introduced.</success_criteria>

<source_audit>
GOAL Phase 2 truth 5 is COVERED by Tasks 02-11-01 through 03. REQ SAFE-04 is COVERED directly; SAFE-01 and TEST-03 are regression-gated. CONTEXT D-04, D-13, D-15, and D-16 are COVERED and referenced in actions/evidence. RESEARCH contributes no new gap-closure item because research is explicitly skipped and 02-10 established the trusted composition pattern. All other Phase 2 goal, requirement, research, and D-01 through D-16 items remain covered by 02-01 through 02-10 and are protected by the full regression gate. Deferred/later Phase 3 mapping administration and Phase 5 database lookup/cutover are explicitly excluded. No source item is missing.
</source_audit>

<output>After completion create `.planning/phases/02-read-only-policy-boundary/02-11-SUMMARY.md` with RED/GREEN gate evidence and exact verification results.</output>
