---
phase: 02-read-only-policy-boundary
plan: 05
type: execute
wave: 5
depends_on: [02-03, 02-04]
files_modified:
  [
    backend/endpoints/roms/upload.py,
    backend/endpoints/roms/patch.py,
    backend/endpoints/roms/files.py,
    backend/endpoints/roms/__init__.py,
    backend/endpoints/roms/manual.py,
    backend/endpoints/roms/screenshot.py,
    backend/endpoints/roms/soundtrack.py,
    backend/endpoints/saves.py,
    backend/endpoints/states.py,
    backend/endpoints/forms/identity.py,
    backend/endpoints/collections.py,
    backend/endpoints/firmware.py,
    backend/endpoints/platform.py,
    backend/tests/endpoints/test_storage_policy_denials.py,
  ]
autonomous: true
requirements: [SAFE-02, SAFE-03, SAFE-04, SAFE-06, TEST-03]
must_haves:
  truths:
    [
      "All real existing mutation endpoints obtain external identity only from the composition-owned legacy descriptor and return bounded 403 or use typed owned-only output",
      "Caller path text cannot forge or alter storage classification",
      "Denial occurs before filesystem I/O",
      "No Phase 3 mapping API or Phase 5 mapping lookup is introduced",
    ]
  artifacts:
    - {
        path: backend/tests/endpoints/test_storage_policy_denials.py,
        provides: "real-route 403 matrix",
      }
    - {
        path: backend/endpoints/roms/patch.py,
        provides: "READ input and owned PATCH output",
      }
    - {
        path: backend/endpoints/firmware.py,
        provides: "real firmware route enforcement",
      }
    - {
        path: backend/endpoints/platform.py,
        provides: "real platform route enforcement",
      }
  key_links:
    - {
        from: backend/handler/filesystem/storage_composition.py,
        to: backend/endpoints/roms/upload.py,
        via: "immutable legacy external descriptor; same provider is used by every mutation route in this plan",
      }
    - {
        from: backend/endpoints/roms/upload.py,
        to: backend/handler/filesystem/storage_policy.py,
        via: "provider descriptor plus UPLOAD before I/O",
      }
    - {
        from: backend/endpoints/roms/patch.py,
        to: backend/handler/filesystem/storage_policy.py,
        via: "READ plus owned PATCH",
      }
    - {
        from: backend/endpoints/firmware.py,
        to: backend/exceptions/storage_exceptions.py,
        via: "bounded 403",
      }
    - {
        from: backend/endpoints/platform.py,
        to: backend/handler/filesystem/storage_policy.py,
        via: "bootstrap target authorization",
      }
---

<objective>All real existing mutation endpoints return bounded 403 or use typed owned-only output</objective>
<execution_context>@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md</execution_context>
<context>@.planning/phases/02-read-only-policy-boundary/02-CONTEXT.md
@.planning/phases/02-read-only-policy-boundary/02-RESEARCH.md
@.planning/phases/02-read-only-policy-boundary/02-PATTERNS.md
@.planning/phases/02-read-only-policy-boundary/02-VALIDATION.md</context>
<tasks>
<task type="auto" tdd="true"><name>Task 02-05-01: Enforce ROM mutation and mixed workflows</name><files>backend/endpoints/roms/upload.py, backend/endpoints/roms/patch.py, backend/endpoints/roms/files.py, backend/endpoints/roms/**init**.py, backend/endpoints/roms/manual.py, backend/endpoints/roms/screenshot.py, backend/endpoints/roms/soundtrack.py, backend/tests/endpoints/test_storage_policy_denials.py</files><behavior>Existing upload/overwrite/rename/move/copy/delete/extract/patch/mkdir/sidecar/cover routes deny external targets before lookup, body/temp or I/O.</behavior><action>Apply D-05 through D-14 on existing registered routes only. Each ROM route obtains the immutable legacy external descriptor from storage_composition and authorizes its operation against that identity; request, database, and form path text may select only a descriptor-relative target and can never construct or reclassify the root. Preserve auth, scopes, schemas. Split external READ inputs and owned output. Add TEST-03 absent-call and descriptor-non-forgeability evidence. No Phase 5 lookup.</action><verify><automated>cd backend &amp;&amp; uv run pytest tests/endpoints/test_storage_policy_denials.py tests/endpoints/roms/test_upload.py tests/endpoints/roms/test_files.py tests/endpoints/roms/test_manual.py tests/endpoints/roms/test_screenshot.py tests/endpoints/roms/test_soundtrack.py -x</automated></verify><done>Existing upload/overwrite/rename/move/copy/delete/extract/patch/mkdir/sidecar/cover routes deny external targets before lookup, body/temp or I/O.</done></task><task type="auto" tdd="true"><name>Task 02-05-02: Enforce remaining mutation endpoints</name><files>backend/endpoints/saves.py, backend/endpoints/states.py, backend/endpoints/forms/identity.py, backend/endpoints/collections.py, backend/endpoints/firmware.py, backend/endpoints/platform.py, backend/tests/endpoints/test_storage_policy_denials.py</files><behavior>Per D-10, every direct API policy denial returns exactly HTTP 403 with machine-readable code external_storage_operation_denied; owned targets proceed and denial occurs before I/O.</behavior><action>Inject or import the composition-owned legacy external descriptor into every remaining real route, use it for trusted classification and D-09 through D-12 translation, and reject caller attempts to supply or alter root class. Assert D-10 across the real registered endpoint matrix: status is exactly 403, code is external_storage_operation_denied, response fields are bounded per D-12, and denial precedes I/O. No synthetic route, API change, or Phase 5 mapping lookup. Add route, operation, provider-identity, non-forgeability, and pre-I/O table evidence.</action><acceptance_criteria>Every real direct endpoint denial returns exactly HTTP 403 plus external_storage_operation_denied, leaks no path data, and reaches no filesystem primitive.</acceptance_criteria><verify><automated>cd backend &amp;&amp; uv run pytest tests/endpoints/test_storage_policy_denials.py tests/endpoints/test_platform.py -x</automated></verify><done>D-10 is proven on every real direct endpoint before I/O.</done></task>
</tasks>
<threat_model><trust_boundaries>Untrusted caller or runtime path crosses into trusted storage classification and filesystem authority.</trust_boundaries><threats>T-02-API-BYPASS is mitigated with real-route tests. T-02-PARTIAL-MUTATION uses split grants. T-02-DISCLOSURE uses bounded response fields. Every HIGH finding blocks completion.</threats></threat_model>
<verification>All commands pass; the real-route matrix asserts D-10 HTTP 403 and external_storage_operation_denied for every denial.</verification>
<success_criteria>All real existing mutation endpoints return bounded 403 or use typed owned-only output; applicable ROOT-05, SAFE-01 through SAFE-06, TEST-03, and D-01 through D-16 evidence passes.</success_criteria>
<output>After completion create .planning/phases/02-read-only-policy-boundary/02-05-SUMMARY.md</output>
