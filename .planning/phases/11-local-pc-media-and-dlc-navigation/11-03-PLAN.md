---
phase: 11
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - tests/fixtures/pc-integration-model/source-library/Cyberpunk2077/
  - backend/tests/endpoints/roms/test_pc_metadata.py
  - backend/tests/handler/filesystem/test_roms_handler.py
  - frontend/e2e/pc-local-media-and-dlc-navigation.spec.ts
  - frontend/e2e/fixtures/
  - .planning/phases/11-local-pc-media-and-dlc-navigation/11-UAT.md
autonomous: false
requirements: [PCLM-01, PCLM-02, PCDLC-01, PCDLC-02, PCSAFE-01, PCTEST-01]
---

<objective>
Prove the complete local-media and DLC-navigation flow against the isolated PC
fixture, then capture concise user acceptance evidence without contacting the real
NAS or altering Team4s services.
</objective>

<threat_model>

- Assets: source-fixture integrity, selected owned artwork, metadata separation,
  and internal navigation behavior.
- Threats: accidental fixture/source write, a false-positive only-UI pass,
  archive extraction, missing migration/API generation, and external IGDB escape.
- Controls: isolated disposable stack only; before/after tree digest sentinel;
  explicit valid image fixture alongside PDF/ZIP negatives; direct API and browser
  assertions; browser test intercepts external windows; all cleanup restricted to
  phase-owned test resources.
- Blocking proof: source digest is identical after the full flow and only the
  deliberately changed disposable image clears its corresponding selection.
  </threat_model>

<tasks>
<task type="tdd">
<name>Task 1: Extend the isolated fixture and browser journey</name>
<files>tests/fixtures/pc-integration-model/source-library/Cyberpunk2077/, frontend/e2e/pc-local-media-and-dlc-navigation.spec.ts, frontend/e2e/fixtures/</files>
<read_first>frontend/e2e/pc-integration-model.spec.ts; frontend/e2e/operational-immutability.spec.ts; tests/fixtures/pc-integration-model/source-library/Cyberpunk2077/; backend/tools/seed_phase10_pc_fixture.py</read_first>
<action>Create or generate a valid disposable PNG fixture without changing the
operator-supplied archive semantics. Add an isolated browser flow that scans the
fixture, explicitly selects the PNG as cover/background/gallery, matches Phantom
Liberty as a DLC component, activates it from Overview, and asserts the current URL
and manifest filter. Snapshot source paths/sizes/digests before and after each flow;
intercept new windows and fail on an IGDB URL.</action>
<acceptance_criteria>The Playwright flow demonstrates review before every apply,
observes all three selected roles in owned UI surfaces, opens the filtered DLC files
inside the same game, opens no external window, and produces equal before/after source
digests.</acceptance_criteria>
</task>

<task type="tdd">
<name>Task 2: Exercise unchanged and changed-image rescan boundaries</name>
<files>backend/tests/endpoints/roms/test_pc_metadata.py, backend/tests/handler/filesystem/test_roms_handler.py, frontend/e2e/pc-local-media-and-dlc-navigation.spec.ts</files>
<read_first>.planning/phases/11-local-pc-media-and-dlc-navigation/11-RESEARCH.md; backend/tests/handler/filesystem/test_roms_handler.py; backend/tests/endpoints/roms/test_pc_metadata.py</read_first>
<action>Add focused regression cases and a controlled disposable-fixture step. Confirm
an unchanged image selection survives rescan byte-for-byte. Then change or remove only
the disposable image, rescan, and confirm only the affected selected role is cleared,
its owned resource is removed, and the external tree changes solely by the explicit
test-fixture setup step. Include PDF, ZIP, and invalid renamed-image negatives.</action>
<acceptance_criteria>All unchanged selections survive; changed/missing evidence clears
only the matching selection; PDF/ZIP/invalid images never appear as art; cleanup does
not remove unrelated owned media; focused regression suites exit 0.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify">
<name>Task 3: Capture isolated-stack UAT and final quality evidence</name>
<files>.planning/phases/11-local-pc-media-and-dlc-navigation/11-UAT.md</files>
<read_first>.planning/phases/11-local-pc-media-and-dlc-navigation/11-VALIDATION.md; frontend/e2e/pc-local-media-and-dlc-navigation.spec.ts; .planning/phases/10-pc-integration-model/10-UAT.md</read_first>
<action>Run focused backend and frontend checks, migration/type generation, browser
flow, and Trunk. Start only the existing isolated test stack, never the real NAS or
Team4s. Record commands, result summaries, source-tree digest evidence, and the
operator's UI confirmation in `11-UAT.md`. Pause for the user only after automated
checks pass, asking them to confirm the three local-art roles and internal Phantom
Liberty files destination in the browser.</action>
<acceptance_criteria>All automated commands are green; UAT documents the isolated
environment and source digest equality; the user confirms cover/background/gallery
visibility plus internal DLC navigation, or records actionable discrepancies.</acceptance_criteria>
</task>
</tasks>

<verification>
Run all commands in `11-VALIDATION.md`, then verify `git diff --check` and the
phase-specific isolated source-tree evidence before handoff.
</verification>

<success_criteria>

- The supplied Cyberpunk-style fixture proves local art and DLC behavior end to end.
- Archive safety is evidenced, including after rescan reconciliation.
- Final browser acceptance is isolated, reproducible, and does not touch production.
  </success_criteria>
