# Operational Immutability Fixture

Repository-controlled fixture contract for Phase 9.

- Two mapping roots are represented under `source-library/arcade` and `source-library/pc`.
- Unicode, nested directories, zero-byte files, and multi-file content are committed here.
- The harness may synthesize rejection-only symlink or traversal cases into task-owned output, not into the source tree.
